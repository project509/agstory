# AUDIO — ship report (waves 6-10 input)

_Reporter: AUDIO. Read-only recon, 2026-09-15, during wave 5. 20 findings: 14 CONFIRMED, 4 REFUTED, 2 BLOCKED-designer. Line numbers are as read at ~11:30; wave 5 is editing RaidView/RaidPrep/Widgets/Settings._

## Executive summary

1. **The brief's premise is wrong: the tree HAS an audio subsystem.** Commit `756081a` (2026-09-10) shipped `default_bus_layout.tres` (Master/Music/UI/Voice), the `Audio` autoload (`game/core/Audio.gd`, 575 lines, pooled players, duck, mixer), the four Settings rows moving four buses, a deterministic numpy generator (`tools/audio/gen_sfx.py`) and 17 samples for ten of the eleven docs/13 §12.4 hooks, with 39 headless tests. Audit M6-AUD-01/02 are done; M6-AUD-03 is done bar its gate.
2. **Shippable today:** the mixer and the settings (the sheet shows four live rows at 100/80/80/100); `ui.tab` on screen change, `ui.coin` on a real gold change, `ui.silence` at wipe t=0; headless safety; originality (nothing downloaded).
3. **Not shippable today:** EIGHT of the eleven hooks are samples nothing plays — including `ui.stamp`, "the game's signature sound". One game call site exists outside Audio.gd (`RaidView.gd:1713`, the duck). No ambience: six stages mount in silence. No music, and no numbered docs/15 ruling for any of it.
4. **Gap 1 — the bindings (AUDIO-03/04/08/15/16/18):** every trigger site is located (`_append_line`'s mistake block, `Widgets.cta`'s `button_down`, `RaidPrep._toggle`, `RaidPlan.analyse`'s validity transition, three `_selected` transitions, `_on_depart`, `GameState.day_advanced`, `tab_row`, `_wipe_stamp`, `_drop_seal`). One M unit, wave 6, plus a tape test that proves every hook without ears.
5. **Gap 2 — the gate (AUDIO-05):** the generator reproduces the tree byte-for-byte (verified 17/17 by hand) but `gen_sfx.py` has no `--check` and `build_art.sh` never calls it; a hand-edited sample passes verify.sh today. S, wave 6, and the shape `gen_amb.py` reuses.
6. **Gap 3 — ambience (AUDIO-07):** `gen_amb.py` (fire, tavern murmur, market chatter + anvil, cave drip, dungeon rumble, aerial wind/gulls), rank layers on the camp bed, one door in `SceneStage.load` so no screen learns about audio. L, wave 7, on the Music bus by default (Q-A).
7. **Blocked on the designer (AUDIO-09/13):** music (cut / generated lute behind a switch / composed stems), what "voice of the scribe" is, whether the fight makes sound. Defaults are stated; all three ship as "cut, stated as a switch" if unanswered by wave 8.
8. **Record-keeping (AUDIO-06):** `q-audio.md`'s proposed Q-96/Q-97 were never applied and Q-96 is now the arena question; renumber and land them after W5-DOCS closes, with this report's defaults folded in.
9. **Two waves suffice:** W6-AUD-BIND (M) + W6-AUD-GATE (S) + three handoff lines + a docs unit; W7-AUD-AMB (L) + W7-AUD-POLISH (S). Wave 8 only if the designer answers (music L, world/voice S-M). Nothing in waves 9-10 unless a listen-through sends back re-tunes (every number is in `PARAMS`).
10. **The one thing no test can tick:** the designer listening to `ui.stamp` through one real wipe at 1x — docs/15 §8's own warning that "a signature sound that grates is worse than no sound".

## Findings

### AUDIO-01 — The brief's premise "there is no audio subsystem" is REFUTED: an Audio autoload, a generator and 17 samples are in the tree
**Status** REFUTED (the brief and audit M6-AUD-01's `not-started` are stale; M6-AUD-03's 2026-09-11 reconciliation note is the accurate record).
**Evidence** `ls game/core/` → `Audio.gd` (24,133 bytes, 2026-09-10 15:42). `ls tools/audio/` → `gen_sfx.py` (19,735 bytes), `README.md` (8,007 bytes). `ls game/assets/audio/sfx/` → 17 `.wav` + 17 `.wav.import` (ui_tab, ui_row_select_1/2, ui_chalk_1/2/3, ui_chalk_bad, ui_stamp_1/2/3, ui_seal, ui_coin_1/2, ui_page_turn, ui_ledger_close, ui_blot_1/2) — ten of the eleven docs/13 §12.4 hooks; `ui.silence` is a duck, not a sample. `find` for `*.wav|*.ogg|*.tres` → only these 17 wavs and `./default_bus_layout.tres` (no bus layout under `game/assets/audio/`).
**Doc** M6-AUD-01 evidence ("ZERO hits", "no .wav") is false as of 2026-09-10; M6-AUD-03's verification block (audit.json) is correct. BUILD_STATE HANDOFF's "What is playable end to end today" lists "audio" — see AUDIO-02..05 for how much of that is true.
**Fix** Reconcile the audit: M6-AUD-01 → `partial` (autoload exists; bus layout/project.godot state checked below), M6-AUD-03 → `partial` (build wiring), and make every later finding here start from what the file shows, not the entry.
**Owns** build/plan/audit.json (orchestrator), BUILD_STATE.md.
**Size** S. **Wave** 6 (first — every audio unit's brief must be written against the real tree). **Audit** M6-AUD-01, M6-AUD-03.

### AUDIO-02 — The bus layout, the Audio autoload and the settings mixer are DONE; M6-AUD-01 and M6-AUD-02 are closable
**Status** REFUTED (as open items). Landed in commit `756081a` "audio, generated rather than sourced" (2026-09-10, 44 files, +2,524).
**Evidence** `default_bus_layout.tres:19-42` declares Master / Music / UI / Voice in docs/13 §15.1's order, Music/UI/Voice `send = &"Master"`, all at 0 dB. `project.godot:33` `Audio="*res://game/core/Audio.gd"` (fourth autoload) and `:41` `buses/default_bus_layout="res://default_bus_layout.tres"`. `game/core/Audio.gd:47` `BUSES`, `:52-57` `BUS_FOR_KEY`, `:267-288` `apply_levels()/_apply_key()` (0 → `set_bus_mute`, else `linear_to_db(pct/100)`), `:312-317` `_on_setting_changed` (empty key = all), `:252-261` `_ensure_buses()` recreates a missing bus. `game/screens/Settings.gd:103-110` four live rows in §15.1 order with `reason: ""`; `:533` `VOLUME_STEPS [0,20,40,60,80,100]`; `:542-570` `_volume_control` (pips + a stepping chip — no HSlider, honouring the screen-test contract). Tests: `tests/unit/test_audio.gd` (675 lines; registered autoload, four buses live, unknown hook is an error, duck restore, `sim/` scan for audio), `tests/unit/test_settings.gd:52,100,337,347`, `tests/unit/test_options_layout.gd:407-423`.
**Doc** docs/13 §15.1 (100/80/80/100); the layout's own comment says "no SFX bus" because §15.1 names voice-of-the-scribe, not SFX. Note: the layout is at the repo ROOT (`default_bus_layout.tres`), not `game/assets/audio/bus_layout.tres` as M6-AUD-01 proposed — acceptable (it is Godot's default path and `project.godot:41` names it explicitly) but a unit that moves assets should know.
**Fix** None to the code. Audit: mark M6-AUD-01 and M6-AUD-02 `done` with `756081a` as evidence.
**Owns** build/plan/audit.json. **Size** S. **Wave** 6. **Audit** M6-AUD-01, M6-AUD-02 (close).

### AUDIO-03 — Only THREE of the eleven §12.4 hooks are bound at a trigger site; eight are samples that nothing plays
**Status** CONFIRMED — the single biggest audio gap. The game today makes a sound on screen change, on a gold change and at wipe t=0, and on nothing else.
**Evidence** `grep -rn '"Audio"|play("ui.|Audio.play|duck(|play_bed' game/ tools/ tests/` outside `Audio.gd` returns exactly ONE game call site: `game/screens/RaidView.gd:1713-1715` (`_wipe_silence` → `audio.duck(-60.0, WIPE_BEAT_LINE_MS/1000)`), i.e. `ui.silence`. Inside `Audio.gd` two hooks self-bind by signal: `ui.tab` ← `ScreenRouter.screen_changed` (`Audio.gd:217-221`, `:474-475`; the router emits it at `ScreenRouter.gd:147,156,172`), `ui.coin` ← `GameState.gold_changed` (`Audio.gd:235-243`, `:503-510`, emits at `GameState.gd:680,769,778,2853`). No call anywhere for `ui.stamp`, `ui.blot`, `ui.seal`, `ui.row_select`, `ui.chalk`, `ui.chalk_bad`, `ui.page_turn`, `ui.ledger_close`. `Audio.gd:376-411` `play()` is complete and pooled (`POOL_PER_BUS := 8`, `:142`), so every missing bind is one line at the right site, not infrastructure. `test_audio.gd` asserts the autoload's own behaviour but no test drives a raid and asserts a stamp on the tape (M6-AUD-04's acceptance test does not exist; `debug_tape`/`tape` at `Audio.gd:146-147` is ready for it).
**Doc** docs/13 §12.4 (trigger + sync per hook); §12.4's last line makes `ui.stamp` "the game's signature sound" — the one hook that most needs binding is one of the eight unbound. No docs/15 row. M6-AUD-04 remains fully open (`not-started` is correct).
**Fix** See AUDIO-06..13 for the per-hook site, sync rule and owner. Bind by signal where a signal exists (`Audio.gd` subscribes), by a one-line `Services.find(self,"Audio").play("<hook>")` where none does; add `tests/unit/test_audio_binds.gd` driving `LogPlayer` through a scripted mistake log with `debug_tape = true` and asserting one `ui.stamp` + one `ui.blot` per mistake row and one `ui.silence` per wipe.
**Owns** game/core/Audio.gd (signal subscriptions), game/ui/Widgets.gd (`ui.seal`), game/screens/RaidView.gd (`ui.stamp`, `ui.blot`), game/screens/RaidPrep.gd (`ui.chalk`, `ui.chalk_bad`, `ui.row_select`), tests/unit/test_audio_binds.gd. **Size** M (all eight, one unit). **Wave** 6 — it is the cheapest, highest-value audio change in the tree and every later audio judgement ("does the stamp fatigue?") is unanswerable until it fires. **Audit** M6-AUD-04.

### AUDIO-04 — Per-hook trigger sites for the eight unbound hooks (the binding map a wave-6 unit works from)
**Status** CONFIRMED (each site read in the tree 2026-09-15; wave 5 is editing RaidView/RaidPrep/Widgets so line numbers may drift by the time a unit opens them — the function names will not).
**Evidence / Fix**, one row per hook — sync rule from docs/13 §12.4, site from the tree:

| Hook | §12.4 trigger / sync | Site in the tree | Bind |
|---|---|---|---|
| `ui.stamp` | StampBadge land — the mistake sound; "exactly on frame 1 of the press, never late" | `RaidView._append_line`, the `if is_mistake:` block that builds `Widgets.stamp_box("MISTAKE","danger")` and calls `Widgets.stamp_press(stamp)` under `if _live:` (`RaidView.gd:1983-2000`) | `audio.play("ui.stamp")` in the same `if _live:` guard as `stamp_press`, BEFORE the row is added (`build/plan/handoff-audio.md` §1a has the exact edit). Also gate on `_player.speed != LogPlayer.Speed.INSTANT`: Instant "has no reveal" (docs/13 §11.2) and `_live` stays true there (`LogPlayer.gd:114-116`: `reveal_all()`), so without the gate a wall of stamps lands in one frame. The skip path already flips `_live=false` (`RaidView.gd:1917`). Mistake holds are >= 300 ms at every speed (`LogPlayer.gd:34,146-148`) so the stamp never exceeds ~3/s — §12.4's fatigue bound is structurally met. |
| `ui.blot` | "With the stamp, one layer under it" | same block, the `blot` TextureRect (`RaidView.gd:1985-1993`) | `audio.play("ui.blot")` immediately after the stamp call; the sample is rendered 12 dB under the stamp (-26 vs -14 dBFS, `gen_sfx.py` PARAMS) so no mix trim is needed. |
| `ui.seal` | Any WaxButton commit — "on press, not release" | `Widgets.cta()` (`Widgets.gd:699`), `cta_priced` (`:724` -> cta), `wax_button` (`:789`) — every commit control goes through these three | in `_guard_cta(b)` (`Widgets.gd:728`) and `wax_button`: connect `b.button_down` to `Services.find(b,"Audio").play("ui.seal")`. `button_down`, never `pressed` (release). Disabled buttons do not emit `button_down`, so a reasoned CTA stays silent for free. Kit-level: no screen learns about audio. |
| `ui.chalk` | ChalkSlot fill / clear — leading edge of the stroke | `RaidPrep._toggle(raider_id)` (`RaidPrep.gd:638-650`), both branches | `play("ui.chalk")` once in `_toggle` after the cap check passes (a refused chalk at cap, `:645`, is NOT a fill — see chalk_bad). Three round-robin variants already exist. |
| `ui.chalk_bad` | Comp check TURNS invalid — 60 ms after the slot redraw | `RaidPrep._refresh()` -> `_build_readout()` (`:744`), which calls `RaidPlan.analyse` (`:746`); validity = `slots_filled >= slots_required` and `tank_weight >= tanks_required` (the two HARD rows; healers are "recommended") | keep `_comp_ok: bool`; after `_refresh()` computes the new analysis, if `was_ok and not now_ok`: `get_tree().create_timer(0.06).timeout` -> `play("ui.chalk_bad")`. Transition only (handoff-audio §3 has the edit). Also fire it on the refused-chalk-at-cap early return at `:645-646` — the only other "bad chalk" gesture; no docs/15 row, recommended default: yes, it is the chalk squeak the player expects when the slot will not take. |
| `ui.row_select` | Row select — on the brass bar wipe | three screens keep their own `_selected`: `AdventureBoard._select` (`:908-923`, the notice/paper select), `Market` (`:705`, the sell-row pick), `Tavern` (`:424,:607`, the candidate pick) | one `play("ui.row_select")` at each of the three transitions (the guard `if encounter_id == _selected: return` at `AdventureBoard.gd:909` already makes it fire on CHANGE only; mirror that guard in Market/Tavern). handoff-audio §4 left it unbound because "no brass bar wipe exists" — the bar is docs/13 §12.2 motion the kit never built (M6-JUICE-02); the SELECT gesture exists in three places and that is the trigger, the bar is only the sync. Recommended default: bind to the gesture. |
| `ui.tab` | Tab / screen change — first pixel of the dissolve | bound for SCREEN change via `ScreenRouter.screen_changed` (`Audio.gd:474`); NOT bound for in-screen TAB presses: `Widgets.tab_row` (`Widgets.gd:1020-1057`) builds the Guildhall/Market/Records tabs and connects `pressed` only to `_tab_refocus` | add `play("ui.tab")` in tab_row's `pressed` lambda (`:1030-1032`) — kit-level, one line. Note the router has NO dissolve ("the desk does not move", `ScreenRouter.gd:4-7`), so §12.4's sync is moot; the hook fires after `_load_into_host` (90-270 ms after the press per `tools/perf_probe.gd`). Recommended: leave until the MOUNT budget lands (BUILD_STATE next-task (1)), then re-check the felt latency; if still late, emit `ui.tab` before `_load_into_host` in `goto/push/pop`. |
| `ui.page_turn` | Departure — start of the turn | `RaidPrep._on_depart` (`:867-889`), before `_router.goto(RAID_VIEW)` (`:886`) | one line before the goto. See AUDIO-08 for the JUICE-02 dependency. |
| `ui.ledger_close` | Day advance — start of the close | `GameState.advance_day()` emits `day_advanced(day)` (`GameState.gd:101,782-785`); called from `:1670` (rest) | `Audio._connect_state` subscribes `day_advanced` -> `play("ui.ledger_close")`, the same shape as `gold_changed`. No screen touched. See AUDIO-08. |

**Doc** docs/13 §12.4 for every row; §11.2 (Instant = no reveal) for the stamp gate. No docs/15 row (AUDIO-11).
**Owns** split so no two units collide with wave-5/6 screen owners: (a) `game/core/Audio.gd` (signal subscriptions: `day_advanced`; the tape test) + `game/ui/Widgets.gd` (`ui.seal`, in-screen `ui.tab`) + `tests/unit/test_audio_binds.gd`; (b) `game/screens/RaidView.gd` (`ui.stamp`, `ui.blot` — ~6 lines); (c) `game/screens/RaidPrep.gd` (`ui.chalk`, `ui.chalk_bad`, `ui.page_turn`); (d) `AdventureBoard.gd`, `Market.gd`, `Tavern.gd` (`ui.row_select`, one line each — hand these as handoff lines to whichever unit owns those screens that wave rather than a fourth audio owner).
**Size** M total (a: S, b: S, c: S, d: S). **Wave** 6. **Audit** M6-AUD-04.

### AUDIO-05 — The SFX generator is deterministic and the tree matches it byte-for-byte, but NO gate checks that: gen_sfx.py has no `--check`, build_art.sh never calls it
**Status** CONFIRMED. (Verified 2026-09-15: `python tools/audio/gen_sfx.py --out <scratch>` then `cmp` against `game/assets/audio/sfx/` -> 17/17 AGREE, 204.2 kB. No Godot involved.)
**Evidence** `tools/audio/gen_sfx.py:404-410` `main()` parses only `--out` and `--list` — no `--check`. `tools/build_art.sh:36-43` parses `--gen|--check|--tags` only; its `--check` branch (`:94-118`) runs `python tools/art/gen_clouds.py --check`, `gen_actors.py --check`, `patch_plate.py --check` — the exact pattern for a Python generator's byte gate — and never `tools/audio/gen_sfx.py`. `grep -n audio tools/*.sh` -> nothing. `verify.sh:128-139` stage 2b calls `build_art.sh --check`, so a hand-edited or stale sample would pass the ten-stage gate today. Determinism is real: `gen_sfx.py:283` seeds `np.random.Generator` from `sha256("<hook>:<index>")` (docstring `:26-30`).
**Doc** M6-AUD-03 "only the build wiring" is exactly right; `tools/audio/README.md:9-10` documents the invocation as prose. The brief's "byte-gated like build_art.sh --check" is the ask.
**Fix** (1) add `--check` to `gen_sfx.py`: render every file to a `tempfile.mkdtemp()`, `filecmp` against `game/assets/audio/sfx/`, print AGREE/DIFFERS/MISSING per file and exit 1 on any miss — the same contract `gen_clouds.py --check` gives build_art.sh; also fail on an ORPHAN (a .wav in the tree the manifest does not name). (2) `build_art.sh --check`: a fourth `if [ -f tools/audio/gen_sfx.py ]` block beside the three PY generators (`:94-118`). (3) `build_art.sh --gen`: run `python tools/audio/gen_sfx.py` too (the samples are committed and deterministic; a re-render only churns bytes when PARAMS change, which is exactly when it should). handoff-audio §6's separate `--audio` flag is unnecessary once the gate is a byte-compare — one door, `--check`, for every generated asset. (4) The `.import` records: add a `test_audio.gd` assertion that every `.wav` has a sibling `.wav.import` (a new sample without one is invisible to `ResourceLoader.exists` on a fresh clone — the `--import` pass is the fix and the README says so at `:10`).
**Owns** tools/audio/gen_sfx.py, tools/build_art.sh (the --check/--gen blocks only), tools/audio/README.md, tests/unit/test_audio.gd. **Size** S. **Wave** 6 (it is the guard every later audio unit's output is gated by; gen_amb.py in wave 7 reuses the same `--check` shape). **Audit** M6-AUD-03 (closes).

### AUDIO-06 — docs/15 still has no numbered audio decision; the audio pass's proposed Q-96/Q-97 were never applied AND their numbers now collide with the arena question
**Status** CONFIRMED (M6-AUD-06 is correctly `not-started`).
**Evidence** `grep -o "Q-[0-9]*" docs/15-open-questions.md | sort -uV | tail` -> Q-96 is the last number and `docs/15:586` Q-96 is "**Which arena is each encounter fought in?**" (added 2026-09-11). Audio appears only at `docs/15:637`, the UNNUMBERED "Audio and music" row in §8 "Ownership gaps", still reading "**Assign an owner.**". `build/plan/q-audio.md` (173 lines, 2026-09-10) proposes "Q-96 — What is the audio direction..." and "Q-97 — Are the per-rank music beds ... composed, licensed, or cut?" — both numbers are now taken or off by one. `docs/13 §12.4` (`:702-720`) still hands eleven names to "audio's call" with no anchor; `docs/14 §4` (`:249`) still lists `game/autoload/audio.gd` (the file is `game/core/Audio.gd`); BUILD_STATE.md's HANDOFF says "audio" is playable but never says a fourth autoload exists. W5-DOCS owns docs/15 and "build/plan/q-*.md (read; mark consumed)" RIGHT NOW — its report (`build/plan/report-W5-DOCS.md`) has no audio mention at the time of reading, so q-audio.md is probably still unconsumed.
**Doc** docs/15 §8 gap row; the project rule (an ambiguity goes behind a switch and into docs/15). Canon: `docs/_source/*` contain no audio word at all (the only hit is the Bard's "Random song effects TBD", a class kit) — the gap row's "nowhere in the doc set" is accurate for canon.
**Fix** Apply q-audio.md's two entries as the NEXT free numbers (Q-97, Q-98 at the time of reading — renumber on landing) with three amendments: (a) Q-97's ruling gains the two house choices the repair pass disclosed (the 140 ms ceiling applied to every non-indulgent one-shot; a save-restore is not a gold change); (b) Q-98 gains the AMBIENCE half's bus ruling (AUDIO-07) and the combat-sound question (AUDIO-13) so the designer answers the whole soundscape once; (c) the §8 gap row gets "-> Q-97, Q-98" instead of "Assign an owner". Cross-refs: docs/13 §12.4 one line "bound in `game/core/Audio.gd`; ruling Q-97"; docs/14 §4 tree `game/autoload/audio.gd` -> `game/core/Audio.gd`; BUILD_STATE HANDOFF "four autoloads". If W5-DOCS consumes q-audio.md first, this finding collapses to the three cross-refs.
**Owns** docs/15-open-questions.md, docs/13-ui-ux.md (§12.4 one line), docs/14-technical-architecture.md (§4 tree, one line), BUILD_STATE.md, build/plan/q-audio.md (mark consumed). **Size** S. **Wave** 6 (docs unit; must not overlap W5-DOCS — schedule after wave 5 closes). **Audit** M6-AUD-06 (closes), M6-AUD-05 (its designer half gets a number).

### AUDIO-07 — No ambience exists: six stages mount in silence; a procedural `gen_amb.py` can ship five loops autonomously, and the bed needs ONE door so no screen learns about audio
**Status** CONFIRMED.
**Evidence** `ls game/assets/audio/` -> only `sfx/`; no `amb/`. `Audio.play_bed()` is a deliberate no-op (`Audio.gd:567-575`). Stages in the tree: `game/assets/scenes/` -> stage_arena_cave, stage_arena_dungeon, stage_camp, stage_market, stage_tavern, stage_town (six). Who mounts what: `Town.gd:184`, `AdventureBoard.gd:180`, `Guildhall.gd:124`, `LoadSave.gd:64`, `Completion.gd:52` -> `stage_camp`; `Tavern.gd:156` -> `stage_tavern`; `Market.gd:195` -> `stage_market`; `MainMenu.gd:39` -> `stage_town`; RaidPrep/RaidView/Results -> `SceneStage.DEFAULT_ARENA = "stage_arena_cave"` (`SceneStage.gd:140`; docs/15 Q-96 keeps one arena until authored). `Settings.gd:154-165` already holds a screen->stage table for its preview. `SceneStage.WARM_SCENES` (`:350`) shows the stage id is the kit's own key for per-scene treatment — ambience keys off the same id.
**Doc** docs/02 §9.1 Audio column (`docs/02:349-354`): Unknown "Wind, one dog, sparse lute" / Known "+drum" / Respected "Anvil ring loop, market chatter" / Established "Full ensemble; crowd murmur bed" / Renowned "Bell toll on entry" / Legendary "Cheer stinger; leitmotif" — all PROPOSED, and the rank ladder is for the TOWN scene. docs/12 owns the world layer and says nothing about sound. docs/13 §12.2: "Nothing in the UI loops... ambient motion belongs to the world layer" — an ambience LOOP is world-layer by the doc's own split. Canon silent. Bus: docs/13 §15.1 has master/music/UI/voice only — ambience fits none literally; the Settings row for `audio_music` already says "The bus is here and waiting" (`Settings.gd:106`).
**Fix** (1) `tools/audio/gen_amb.py`, same shape as gen_sfx.py (numpy + wave, seeded per `sha256("<bed>:<layer>")`, `--check`/`--list`/`--out`), rendering SEAMLESS loops (render N+crossfade samples, fold the tail into the head with an equal-power crossfade so the loop point is inaudible) at 44.1 kHz mono 16-bit, 20-30 s each: `amb_camp` (fire crackle = sparse random bursts of band-passed noise 1-4 kHz with a low rumble bed + occasional pop; night wind = slow-modulated 100-600 Hz noise), `amb_tavern` (murmur = 4-6 detuned formant-filtered noise voices at 200-800 Hz with slow amplitude walks; a clink = damped inharmonic partials every 6-14 s; hearth bed shared with camp), `amb_market` (chatter as tavern but brighter and busier; canvas flap = gated 300-900 Hz noise; a distant anvil ring every 8-20 s at Respected+ = the docs/02 row), `amb_cave` (drip = pitched resonant tick 1.2-2.5 kHz with a 40 ms decay every 2-7 s on a low hollow rumble 40-120 Hz), `amb_dungeon` (the cave rumble with a slower chain-creak = stick-slip at 2-4 Hz and no drip), `amb_town` (aerial: wind + gulls as glide_band chirps — `gen_sfx.py:140` already has `glide_band`). ~6 x 25 s x 88 kB/s = ~13 MB of WAV; acceptable, or save as OGG if `--check` compares decoded PCM (Godot imports WAV loops natively via `loop_mode` in the `.import`, so WAV is the zero-dependency choice; set `edit/loop_mode=1` in each `.import`). (2) `Audio.play_bed(id)`: two `AudioStreamPlayer`s on the Music bus (see question Q-A below) crossfading over 600 ms (`Tween` on `volume_db`, honouring reduced_motion is NOT required — a fade is not motion), `stop_bed()` fades out; `BEDS := {stage_id: path}`; unknown id is a `push_error` like `play()`. Rank-keyed variants for `stage_camp` (the hub = docs/02's town): layer the anvil at Respected+, the murmur at Established+ — render as separate layer files (`amb_camp_base`, `amb_camp_anvil`, `amb_camp_crowd`) mixed live by rank, so the docs/02 ladder is a table in Audio.gd, not six re-renders. (3) THE DOOR: `SceneStage.load(id)` (the one function every stage goes through) calls `Services.find(node, "Audio").play_bed(id)` from the returned node's `tree_entered`, guarded null-safe (tools/shot.gd runs headless and the autoload's players are outside the tree there, `Audio.gd:409` already skips `play()` off-tree). No screen changes. Screens without a stage (Settings pushed over a screen) keep the underlying bed because nothing re-mounts. (4) `tests/unit/test_audio_beds.gd`: every stage id in `game/assets/scenes/*.json` has a bed; every bed file exists and its `.import` says loop; `play_bed` on an unknown id errors; headless `play_bed` does not crash; `--check` byte gate in build_art.sh as AUDIO-05. (5) Update `test_play_bed_is_a_no_op_because_no_one_owns_the_music_yet` (`test_audio.gd:496`) — it is the tripwire q-audio.md said would change; it becomes "play_bed plays AMBIENCE and never a melodic bed".
**Owns** tools/audio/gen_amb.py (new), tools/audio/README.md (a second table), game/assets/audio/amb/ (new), game/core/Audio.gd (`play_bed/stop_bed/BEDS`), game/ui/SceneStage.gd (ONE call in `load()` — coordinate with W5-MOUNT's cache edits; wave 7 after it lands), tests/unit/test_audio_beds.gd (new), tests/unit/test_audio.gd (the one tripwire), tools/build_art.sh (one --check block). **Size** L (the generator is the work: five beds that do not sound like a hairdryer is an afternoon per two beds; the door and the crossfade are S). **Wave** 7 (needs AUDIO-05's --check shape and W5-MOUNT's SceneStage to be still). **Audit** M6-AUD-05 (its generable half).

### AUDIO-08 — `ui.page_turn` and `ui.ledger_close` are held hostage to animations that docs/13 §2 M5 says will never exist; bind them to the events now
**Status** CONFIRMED (a judgement in the tree, not a doc's, is what keeps them silent).
**Evidence** `Audio.gd:71-74` and `handoff-audio.md:411-418`: "Neither animation exists... Firing them on a hard cut would be worse than silence" — that sentence is the audio pass's own, not a doc's. `ScreenRouter.gd:4-7`: docs/13 §2 M5 "**The desk does not move.**"... "this router has no transition animation and never will". M6-JUICE-02 records the §12.2 motion table (700 ms page turn, 900 ms ledger close) vs M5 as an unresolved contradiction. The samples exist and pass the length bound (`ui_page_turn.wav` 380 ms, `ui_ledger_close.wav` 430 ms, both under their §12.2 motion; `test_audio.gd` "stays inside"). Trigger sites exist: `RaidPrep._on_depart` (`:867`) and `GameState.day_advanced` (`:101,:785`).
**Doc** docs/13 §12.4 (Departure / Day advance); §12.2 (the two indulgent motions); §13 reduced motion "disables ... the page turn, the ledger close" — the doc itself contemplates the events happening WITHOUT their motion. No docs/15 row (M6-JUICE-02 is the audit id; the register needs a Q).
**Fix** Bind both now (AUDIO-04's last two rows): the sound IS the transition when the desk does not move — a page-turn sample over a cut reads as a page turn (the reader's ear supplies the motion), and it is exactly what reduced-motion players get by the doc's own §13 row. If M6-JUICE-02 is later ruled "build the motion", the bind moves from the event to the tween's first frame — one line moves. Recommended default in the docs/15 row: "sounds bind to the event; motion, if ever built, syncs to the sound, not the other way".
**Owns** game/core/Audio.gd (`day_advanced` subscription), game/screens/RaidPrep.gd (one line in `_on_depart`), docs/15 (the Q row, via AUDIO-06's docs unit). **Size** S. **Wave** 6 (with AUDIO-04). **Audit** M6-AUD-04, M6-JUICE-02 (relates).

### AUDIO-09 — Music: canon says nothing, docs/02 §9.1 proposes five melodic beds and a leitmotif, and the tree ships none — BLOCKED on the designer, with a procedural fallback the designer must explicitly accept or refuse
**Status** BLOCKED-designer (M6-AUD-05's melodic half; the audit's `blocked-needs-human` is right).
**Evidence** `Audio.play_bed()` no-op with the reason on it (`Audio.gd:551-575`); `test_audio.gd:496` pins it. docs/02 §9.1 rows (`docs/02:349-354`): "sparse lute", "lute gains a drum", "full ensemble", "bell toll on entry", "cheer stinger on entry; leitmotif" — every one 🔷 PROPOSED, and the Established/Legendary rows are "pure proposal" (`docs/02:356`). `docs/_source/*`: no music word. BACKLOG.md:167 "Generate procedurally or source CC0" — sourcing is impossible (no network, no licence file; M5-END-4 says the credits/attribution list does not exist either). Settings has a live `audio_music` row whose note admits "no music is written yet" (`Settings.gd:106`).
**Doc** docs/02 §9.1; docs/15 §8 gap row; docs/00 §4.4 checklist item (5) "all audio" must be original.
**Fix** The designer answers ONE question (below, Q-B) with three legal answers, each sized: (i) **Cut for 1.0** — Settings' `audio_music` row keeps its "no music is written yet" note, `play_bed` plays ambience only (AUDIO-07); S, no work beyond the docs/15 row. (ii) **Procedural, accepted as a switch** — `tools/audio/gen_music.py`: a deterministic generative "sparse lute" (Karplus-Strong plucked-string on a pentatonic mode the designer names, sparse random-walk phrases at 60-70 BPM, seeded; Known adds a frame-drum = low-passed noise thump on beats 1 and 3; Established adds a second voice) rendered as 60-90 s loops per rank, on the Music bus under the ambience; L (an afternoon for the pluck model that does not sound like a Casio, an afternoon for the phrase walker, an hour per rank). The leitmotif, bell toll and cheer stinger stay OUT of this answer — a generator cannot author a theme (Audio.gd's own reasoning, and correct). (iii) **Composed by a person** — the tree needs five seamless stems + a stinger at 44.1 kHz, dropped into `game/assets/audio/music/` with a `BEDS_MUSIC` table; S to wire, XL and not a build-loop task to make. Whatever the answer, the Legendary "cheer stinger on entry" and "bell toll on entry" are ONE-SHOTS, not beds — the bell is a `struck()` inharmonic bank gen_sfx.py can already render (S) and could ship under answer (ii) or (iii); the cheer is crowd material gen_amb.py can render (S). Default if unanswered by wave 8: (i), stated as a switch in the docs/15 row.
**Owns** (ii) tools/audio/gen_music.py (new), game/assets/audio/music/, game/core/Audio.gd (`BEDS_MUSIC`), tests/unit/test_audio_music.gd; (i)/(iii) docs/15 only. **Size** (i) S / (ii) L / (iii) S-wire. **Wave** 8 if (ii) (after ambience proves the bed door), else 6 (a docs row). **Audit** M6-AUD-05 (melodic half).

### AUDIO-10 — `ui.coin` is honest by a four-fact heuristic; the one-flag fix in GameState the handoff wrote out never landed
**Status** CONFIRMED (working, wordy; low priority).
**Evidence** `Audio.gd:478-548`: `_coin_note` compares seed / day / roster-head object id to tell a transaction from a campaign arriving; `handoff-audio.md:34-126` §0 writes out an `announcing` flag around the two arrival emits (`GameState.new_game`, `from_dict`); `grep -n announcing game/core/GameState.gd` -> nothing. `GameState.gd:680-681` and `:2853-2854` still emit `gold_changed` then `roster_changed` with no flag; the heuristic depends on that ORDER (`Audio.gd:500-502`), which nothing pins.
**Doc** docs/13 §12.4 "Gold changes / On the value write"; q-audio.md addendum (b) records the "restore is not a change" default.
**Fix** Land handoff §0 (5 lines in GameState, one early-return in `_on_gold_changed`, delete `_coin_note`'s seed/day/head half, retarget `test_a_campaign_arriving_does_not_ring_the_coin` at the real `new_game`/`from_dict`). Also `add_gold(0)`/`spend_gold` of a zero stays silent (`moved` check) — keep that.
**Owns** game/core/GameState.gd (W5-TESTS holds it for comments only this wave — wait), game/core/Audio.gd, tests/unit/test_audio.gd. **Size** S. **Wave** 7. **Audit** M6-AUD-04 (relates).

### AUDIO-11 — Reduced motion, reduced effects, comedy brake and Instant speed: the interplay with sound is undefined in the tree and mostly undefined in the doc; three defaults to record
**Status** CONFIRMED (no code reads `reduced_motion` in Audio.gd; nothing needs to, but the rule must be written).
**Evidence** `grep -n reduced Audio.gd` -> nothing. `GameSettings.gd:42,44,37` keys; `:231` `MOTION_OFF if reduced_motion`; `Widgets.stamp_press` (`Widgets.gd:637-652`) shrinks the stamp to the 60 ms floor under reduced motion but still lands it. docs/13 §13 Reduced motion row: "Stamps still land (they carry state)"; §11.4 t=0 "Silence for 400ms — the only silence in the game"; §11.2 Instant "Full log + post-mortem, no reveal". `_effects_allowed` (`RaidView.gd:1390-1405`) gates per-line EFFECTS by speed and `reduced_effects`, not sound. Nothing in docs/13 §15.1's closed inventory is a "quiet mode" or "mute SFX" toggle — the four bus levels are the whole audio inventory, and `audio_ui` at 0 IS the quiet mode.
**Doc** docs/13 §13, §15.1, §11.2, §11.4. No docs/15 row.
**Fix** Record three defaults in the Q row (AUDIO-06) and assert them in `test_audio_binds.gd`: (1) `reduced_motion` never silences a hook — sound is not motion, and the doc's own carve-out ("stamps still land") is the precedent; the page-turn/ledger-close sounds play under reduced motion because the doc disables the MOTION, and the sound is what remains. (2) Sim speed: hooks fire at 1x/2x/4x (the >= 300 ms mistake hold bounds the stamp rate); at **Instant and during a skip no per-line hook fires** (`_live` / speed gate, AUDIO-04) and only `ui.silence` + the wipe/clear stamp fire once at the end. (3) `reduced_effects` and `comedy_brake` do not touch audio (the brake changes PACING, and pacing is what the hooks follow). Also: the duck's restore runs in `_process` (`Audio.gd:339-345`) on a node whose `process_mode` is INHERIT — the tree is never paused in this project (`grep "tree().paused"` -> nothing), so it is fine today; set the autoload `process_mode = PROCESS_MODE_ALWAYS` in `_ready` anyway (its players already are, `:440`) so a future pause menu cannot freeze a -60 dB duck.
**Owns** game/core/Audio.gd (one line), docs/15 (row text), tests/unit/test_audio_binds.gd. **Size** S. **Wave** 6 (with AUDIO-04). **Audit** M6-JUICE-06 (relates: it lists reduced_motion's consumers).

### AUDIO-12 — A11y captions for sound: docs/13 does not ask for them, and the game needs none — every hook is redundant with a visible state
**Status** REFUTED (as a requirement). No work.
**Evidence** docs/13 §13 table (`:727-741`) rows: colour, CVD, contrast, text size, text scaling, keyboard, gamepad, reduced motion, reduced flashing, log dwell, font, emoji-free — no captions/subtitles row; §15.1 inventory has none. Every §12.4 hook is the sound OF a visible event (a stamp that is drawn, a chalk slot that fills, a coin total that changes, a wipe that stops the page); `ui.silence` is the absence of sound and is paired with the visible halt. No voice/dialogue audio exists (AUDIO-13), so there is nothing to caption.
**Doc** docs/13 §13. **Fix** none; note in the Q row that captions are not owed BECAUSE no sound carries information a sighted-or-not reader does not already get from the ledger — and that this becomes false the day a voice line ships. **Owns** —. **Size** —. **Wave** —. **Audit** —.

### AUDIO-13 — The Voice bus has no consumer and "voice of the scribe" is defined nowhere; combat/world sound beyond the eleven UI hooks is unspecified — one designer question, two recommended defaults
**Status** BLOCKED-designer (a genuine gap in the docs, not a build item).
**Evidence** "voice-of-the-scribe" occurs in the doc set exactly once, `docs/13:843` (the §15.1 bus row), and in no other doc, no canon source, no BACKLOG item; nothing in the tree routes to the `Voice` bus (`grep -rn '"Voice"' game/` -> only `Audio.gd:47,56` and the layout). `Settings.gd:110` note: "The bus is here and waiting; nothing is voiced yet." Combat: docs/13 §12.4 governs UI sound only ("All UI sound is material"); docs/12 (the world layer, which owns the stage's flashes, numbers and bubbles — `RaidView._line_effects`, `SceneStage`) has no sound row; docs/07 has none. The stage today shows attacks, heals, mechanic cells, a boss flash and damage numbers in silence between stamps.
**Doc** docs/13 §15.1 (Voice bus); docs/12 (silent); docs/13 §11.1 "the log is a document" — the design's own answer to "why is the fight quiet" is that the player is reading an account, not watching a fight.
**Fix** Two questions for the designer (Q-C, Q-D below). Recommended defaults: (a) **Voice** — the bus stays, unused, with its Settings note; do not invent a scribe. If the designer wants the scribe to be the raid narrator's "voice" (e.g. a quill-scratch texture under every log line arrival, `ui.quill`, on the Voice bus — material, docs/13-consistent, S to generate with `stick_slip`), that is a NEW hook and needs a §12.4 row; recommended as the one addition that makes the Voice slider do something. (b) **Combat** — ship 1.0 with the stage silent apart from the eleven hooks and the ambience bed (the document reading); offer as a switch a small material set on the world layer — `world.hit` (low thump + cloth), `world.spell` (glide_band whoosh), `world.heal` (a soft struck partial) — keyed to `Enums.Verb.ATTACK/HEAL/MECHANIC` inside `RaidView._line_effects` under the SAME `_effects_allowed` gate (so 4x/Instant/reduced_effects silence them, docs/13 §13's three-changes rule), on the... Music bus is wrong and UI is wrong: this is why a fifth bus was refused (`default_bus_layout.tres:8-10`). If (b) is wanted, §15.1's inventory must grow a "world" level — designer's call, and it changes a closed list. Size: M for the three sounds + bind + test.
**Owns** (a) tools/audio/gen_sfx.py (one PARAMS entry), game/core/Audio.gd (one HOOKS row), game/screens/RaidView.gd (one line in `_append_line`), docs/13 §12.4 (a row — designer). (b) same plus default_bus_layout.tres, GameSettings.gd (a fifth key), Settings.gd (a fifth row), docs/13 §15.1. **Size** (a) S, (b) M. **Wave** 8 (after the designer's answer; both are additive and touch nothing the binding unit owns). **Audit** M6-AUD-05 (relates), no id of its own — file one.

### AUDIO-14 — The Settings screen shows four live audio sliders and two of them move buses that carry nothing; at ship that is an enabled control wired to silence
**Status** CONFIRMED (a polish/mental-model gap, visible on the sheet).
**Evidence** `build/shots/all/fixture/Settings.png` (2026-09-15 10:53), the Options panel rows at y~620-743: "Audio — master 100", "Audio — music 80 — The bus is here and waiting; no music is written yet.", "Audio — interface 80", "Audio — voice of the scribe 100 — The bus is here and waiting; nothing is voiced yet." All four render with the pip ladder and a stepping chip, enabled (no padlock, unlike Prose font swap / V-sync / Language / Controller glyphs which are dimmed with a CAUTION reason). The header counts them among "13 of 17 live in this build". `Settings.gd:103-110` has `reason: ""` on all four. docs/13 §12.3: a disabled control needs "a reason string adjacent"; nothing in the doc says an ENABLED control may control nothing — `Audio.gd:23-26` already flags that this judgement is the tree's own.
**Doc** docs/13 §15.1 (the inventory is closed: four levels, no fewer), §12.3.
**Fix** Tie the two notes to the truth at ship time: once AUDIO-07 lands, the music row's note becomes "Ambience — the camp fire, the tavern, the cave — and music when it is written" (the Music bus then carries the beds, so the slider works). The Voice row: if AUDIO-13 (a) lands a `ui.quill` hook, the note names it; if the designer refuses (a), ship the Voice row DISABLED with the reason "Nothing is voiced in this build." (`reason` non-empty -> `Widgets.reasoned` padlock, the same treatment as V-sync) — an honest padlock beats a slider that moves nothing. Both are one-string edits plus `test_settings.gd:347` (`test_no_audio_row_still_claims_there_is_no_audio`) adjusted to allow the Voice reason.
**Owns** game/screens/Settings.gd (ROWS strings only — W5-SCALE owns the file this wave; wave 7), tests/unit/test_settings.gd. **Size** S. **Wave** 7 (after AUDIO-07 and the Q-C answer). **Audit** M6-AUD-02 (relates).

### AUDIO-15 — `ui.tab` rings on boot, before the player has touched anything
**Status** CONFIRMED (minor; the coin's own rule applies).
**Evidence** `Audio.gd:474-475` plays `ui.tab` on EVERY `screen_changed`; `game/ui/Boot.gd:151` `router.goto(MAIN_MENU)` fires it on the first frame of the game. (Continue/Load — `MainMenu.gd:361,383`, `LoadSave.gd:498` `goto(TOWN)` — are presses the player made, so their tab sound is correct.) q-audio.md addendum (b) established for `ui.coin` that "a sound the player did not cause is worse than a missing one"; the same reasoning is unapplied to the tab. Also the boot emit lands while `Audio._ready` may not yet have applied levels (`_levels_applied` is set in `ensure_wired`, `Audio.gd:198-204`, and `play()` calls `ensure_wired()` first, so levels ARE applied — no bug there, just the unbidden sound).
**Doc** docs/13 §12.4 "Tab / screen change" — silent on boot. No row.
**Fix** In `_on_screen_changed`, skip when the previous path was empty (the boot transition) — `ScreenRouter.current_path()` is "" before the first goto, or keep a `_first_change` flag; recommended default recorded in the Q row: "the first screen of a session is silent; every change the player causes sounds". S, one condition. Assert in `test_audio_binds.gd`.
**Owns** game/core/Audio.gd, tests/unit/test_audio_binds.gd. **Size** S. **Wave** 6 (with AUDIO-04). **Audit** M6-AUD-04 (relates).

### AUDIO-16 — The WIPE stamp at t=900 has its own press and no sound; the eleven-hook table covers it (StampBadge land) and the bind is missing from the wipe sequence
**Status** CONFIRMED.
**Evidence** `RaidView._wipe_stamp` (`RaidView.gd:1756-1811`) builds `Widgets.stamp_box("WIPE.","danger")`, scales from `WIPE_STAMP_FROM` 1.15 and tweens `scale`/`rotation` over `WIPE_STAMP_MS` 180 — a StampBadge land by docs/13 §11.4's own words ("A large WIPE stamp presses diagonally across the page: 180ms"). No `play(` in the function. The t=0 duck (`_wipe_silence`, `:1713`) restores at t=400 (`WIPE_BEAT_LINE_MS`), so at t=900 the mixer is back at the player's level and the stamp would be heard. `ui.seal` at t=1400 ("a wax seal drops onto the lower-right corner", `_wipe_dim_page`/the seal beat) is likewise a seal landing with no sound — but §12.4 binds `ui.seal` to a WaxButton COMMIT, not to a seal graphic; recommended default: play `ui.seal` on the seal drop too (it is the same wax, and the sequence is once per wipe), stated in the Q row. The "Try again"/"Return to town" CTAs at t=2400 get `ui.seal` for free from AUDIO-04's kit bind.
**Doc** docs/13 §11.4 beats; §12.4 `ui.stamp` "StampBadge land". No row for "does the WIPE stamp use the mistake sound" — recommended default YES with `pitch_scale` 1.0 (no jitter override, no invented number); the designer may want it heavier (a lower pitch is a one-number switch in `opts`).
**Fix** `audio.play("ui.stamp", {"pitch_scale": 1.0})` as the first line of `_wipe_stamp` (frame 1 of the press), `audio.play("ui.seal")` at the seal-drop beat; both under the existing `_wipe_beats` once-only guard, so a skip (`_on_skip`) that lands every beat in one frame fires each once. Assert in the tape test: a wipe = one `ui.silence`, one `ui.stamp`, one `ui.seal`, in that order.
**Owns** game/screens/RaidView.gd (three lines, the same unit as AUDIO-04 (b)), tests/unit/test_audio_binds.gd. **Size** S. **Wave** 6. **Audit** M6-AUD-04, M6-JUICE-03 (relates: the wipe beats).

### AUDIO-17 — Originality: every shipped sound is generated in-tree, so the docs/00 §4.4 checklist item (5) "all audio" is satisfied and the credits item needs no audio attribution — REFUTED as a blocker
**Status** REFUTED (the M5-END-4 entry's worry about "sourcing CC0" audio is moot).
**Evidence** `tools/audio/gen_sfx.py:19-22` "nothing downloaded"; `tools/audio/README.md:3-6`; every `.wav` in the tree is reproduced byte-for-byte by the generator (AUDIO-05's cmp, 17/17). `export_presets.cfg:44-46` `export_filter="all_resources"` with an exclude list of `*.aseprite,*.md,*.py,*.lua,*.sh,*.ps1,*.log` — the `.wav`s export, the generator does not. M5-END-4 (`audit.json`) reads "docs/16 W6 audio explicitly contemplates sourcing CC0" — nothing was sourced.
**Doc** docs/00 §4.4 checklist row (5); docs/00:189 "All ... audio ... original to this project".
**Fix** When M5-END-4's `data/credits.json` is authored, the audio line is "Sound: generated from physical models, tools/audio (this project)"; no third-party audio notice. Keep the rule for AUDIO-07/09: gen_amb.py and any gen_music.py follow the same no-download rule, so this stays true.
**Owns** data/credits.json (M5-END-4's owner). **Size** —. **Wave** —. **Audit** M5-END-4 (relates), M6-AUD-03.

### AUDIO-18 — No test drives a raid and listens: the acceptance test M6-AUD-04 named (a tape of hooks per scripted log) does not exist, and it is the only way an autonomous wave can prove the bindings without ears
**Status** CONFIRMED.
**Evidence** `tests/unit/test_audio.gd` has 39 tests (`:80-632`), all against the autoload in isolation (levels, buses, pool, duck, coin decision function, the generator's files). None instantiates RaidView or RaidPrep with `debug_tape = true`. `Audio.debug_tape`/`tape` (`Audio.gd:146-147`, `:384-385`) exist for exactly this and have no consumer outside `test_audio.gd`'s own unit tests. `tests/unit/test_full_loop.gd` walks a whole campaign headless (W5-TESTS owns it) and never reads the tape.
**Doc** M6-AUD-04's acceptance sentence.
**Fix** `tests/unit/test_audio_binds.gd`: (1) mount RaidView on a `--fixture=raid` log through the screen-test harness (`test_screens.gd`'s pattern), set `debug_tape`, `_player.speed = ONE`, advance until finished, assert `count("ui.stamp") == mistakes in the log` and `== count("ui.blot")`, and for a wipe fixture `["ui.silence","ui.stamp","ui.seal"]` in order at the end; (2) at `Speed.INSTANT` the tape holds only the wipe/clear tail; (3) RaidPrep: toggle a raider -> one `ui.chalk`; toggle at cap -> one `ui.chalk_bad`, no `ui.chalk`; drop the only tank when `tanks_required > 0` -> `ui.chalk_bad` once after 60 ms (`SceneTreeTimer` — advance with `await` is forbidden in sim/ only; the test harness may `await get_tree().create_timer`); (4) `Widgets.cta("x").button_down.emit()` -> one `ui.seal`; a `reasoned` (disabled) CTA -> none; (5) the boot rule (AUDIO-15). Every assertion reads the tape, never the driver.
**Owns** tests/unit/test_audio_binds.gd (new). **Size** S-M. **Wave** 6 (lands WITH the bindings — a unit's "its own test"). **Audit** M6-AUD-04.

### AUDIO-19 — A clear makes no sound of its own: the account just stops, Results prints the outcome as a Label, and the only audio is the tab and (later) the coin
**Status** CONFIRMED (a design consequence, not a defect; one question and a default).
**Evidence** `RaidView.gd:1667-1673`: on finish `_release_exits()`; only `_is_wipe()` runs a beat sequence. `Results.gd:395-397`: `if result.cleared(): box.add_child(Widgets.label_as(word, "LabelSection"))` — a Label, while a wipe gets `Widgets.stamp_box(word,"danger")` (`:402`). docs/13 §7's StampBadge word list (`docs/13:247`) includes CLEARED, so the doc contemplates a CLEARED stamp the tree does not press. docs/13 §12.4 has no clear hook; §11 "No victory framing" (`docs/13:540`) is the design's stated reason.
**Doc** docs/13 §7 (CLEARED in the stamp list), §11 (no victory framing), §12.4 (closed list). No docs/15 row.
**Fix** Recommended default: nothing new — the `ui.coin` on the loot/gold write (`GameState.add_gold`, bound today) is the clear's sound, and it is the doc-consistent one (the reward is the news). If the Results/JUICE unit ever presses a CLEARED StampBadge (the doc's own list), it inherits `ui.stamp` through AUDIO-04's `stamp_press` site for free. Put the choice in the Q row so it is a decision and not an omission.
**Owns** docs/15 (row text). **Size** S (text). **Wave** 6 (docs). **Audit** M6-JUICE-03 (relates).

### AUDIO-20 — The two-wave unit split, with ownership that never touches a screen another unit owns
**Status** CONFIRMED plan (derived from AUDIO-01..19; sizes are agent-afternoons, M = one).

**Wave 6 — "every hook makes a sound" (two units + three handoff lines):**
| Unit | Owns (absolute) | Delivers | Size |
|---|---|---|---|
| **W6-AUD-BIND** | `game/core/Audio.gd`, `game/ui/Widgets.gd` (three lambdas: `_guard_cta`/`wax_button` `button_down` -> `ui.seal`; `tab_row` pressed -> `ui.tab`; nothing else in the file), `game/screens/RaidView.gd` (`_append_line`'s mistake block, `_wipe_stamp`, `_drop_seal` — ~10 lines), `game/screens/RaidPrep.gd` (`_toggle`, `_refresh`'s comp transition, `_on_depart` — ~15 lines), `tests/unit/test_audio_binds.gd` (new), `tests/unit/test_audio.gd` | AUDIO-03/04/08/11/15/16/18: `ui.stamp`, `ui.blot`, `ui.seal`, `ui.chalk`, `ui.chalk_bad`, `ui.page_turn` (event), `ui.ledger_close` (`day_advanced` subscription), in-screen `ui.tab`, the boot-silence rule, the Instant/skip gate, `process_mode` ALWAYS, the tape test | M |
| **W6-AUD-GATE** | `tools/audio/gen_sfx.py` (`--check`), `tools/build_art.sh` (the two PY blocks), `tools/audio/README.md`, `tests/unit/test_audio.gd` (the `.import` sibling assertion only — coordinate: BIND owns the rest of that file; GATE appends one test at the END) | AUDIO-05: the byte gate in stage 2b; M6-AUD-03 closes | S |
| handoff lines (to the wave's owners of `AdventureBoard.gd`, `Market.gd`, `Tavern.gd`) | one `play("ui.row_select")` each at the `_selected` transition | AUDIO-04 row_select | S each |
| docs unit (after W5-DOCS closes) | `docs/15` (Q-97/Q-98 from q-audio.md, renumbered, plus AUDIO-08/11/13/16/19's defaults as sub-rulings), `docs/13 §12.4` one line, `docs/14 §4` one line, `BUILD_STATE.md`, `build/plan/audit.json` rows M6-AUD-01..06 | AUDIO-01/02/06 | S |

**Wave 7 — "ambience per scene, and the sliders tell the truth":**
| Unit | Owns | Delivers | Size |
|---|---|---|---|
| **W7-AUD-AMB** | `tools/audio/gen_amb.py` (new), `game/assets/audio/amb/` (new), `game/core/Audio.gd` (`play_bed`/`stop_bed`/`BEDS`, the crossfade), `game/ui/SceneStage.gd` (ONE call in `load()`'s returned node — after W5-MOUNT's cache lands), `tests/unit/test_audio_beds.gd` (new), `tests/unit/test_audio.gd` (`test_play_bed_is_a_no_op...` only), `tools/build_art.sh` (one block), `tools/audio/README.md` (second table) | AUDIO-07: six beds (camp, tavern, market, cave, dungeon, town aerial), rank layers on the camp bed, the door | L |
| **W7-AUD-POLISH** | `game/core/GameState.gd` (the `announcing` flag, 5 lines), `game/core/Audio.gd` (`_on_gold_changed` early return; delete the heuristic half), `game/screens/Settings.gd` (the two ROWS note strings, the Voice reason if Q-C is "no"), `tests/unit/test_audio.gd`, `tests/unit/test_settings.gd` | AUDIO-10/14 | S |

**Wave 8 (only if the designer answers):** W8-AUD-MUSIC (AUDIO-09 (ii): `gen_music.py`, L) and/or W8-AUD-WORLD (AUDIO-13 (a) `ui.quill` S; (b) the world set + fifth bus, M). Waves 9-10: nothing audio unless a listen-through by the designer sends back re-tunes (every number is in `PARAMS`; a re-tune is S and re-gated by `--check`).

**Rule for every unit:** no `await`, no `Audio` global identifier (`Services.find`), nothing under `sim/` (`test_nothing_under_sim_reaches_for_audio` enforces), every generated byte reproduced by `--check`, every hook proven on the tape.

## Shippable bar

A reviewer ticks these for AUDIO before 1.0. Each names the proof, not the promise.

- [ ] **Every one of docs/13 §12.4's eleven hooks fires at its trigger** — `tests/unit/test_audio_binds.gd` green: a scripted raid's tape shows one `ui.stamp` + one `ui.blot` per mistake row, `ui.silence` → `ui.stamp` → `ui.seal` at a wipe, `ui.chalk`/`ui.chalk_bad` from RaidPrep, `ui.seal` from any `Widgets.cta`, `ui.row_select` from Board/Market/Tavern, `ui.tab` from the router AND `tab_row`, `ui.coin` from `add_gold`/`spend_gold` only, `ui.page_turn` on Depart, `ui.ledger_close` on `day_advanced`. Today: 3 of 11.
- [ ] **The stamp is on frame 1 and never a wall** — the bind precedes the row build; no per-line hook at Instant or during a skip; the tape at `Speed.INSTANT` holds only the tail.
- [ ] **Nothing sounds that the player did not cause** — boot is silent (AUDIO-15); a save restore rings no coin (`test_a_campaign_arriving_does_not_ring_the_coin`).
- [ ] **Ambience per scene** — six beds (`stage_camp`, `stage_tavern`, `stage_market`, `stage_arena_cave`, `stage_arena_dungeon`, `stage_town`) mount through `SceneStage.load` with a crossfade; `test_audio_beds.gd` maps every `game/assets/scenes/*.json` to a bed; the loops are seamless (loop point asserted by a wrap-around RMS check in `gen_amb.py --check`). Today: none.
- [ ] **The four settings move four buses and each bus carries something** — `test_audio.gd`'s mixer tests (green today); Music carries the beds; Voice either carries `ui.quill` or its row is padlocked with a reason (AUDIO-14).
- [ ] **Every shipped sample is reproduced byte-for-byte by its generator under `verify.sh` stage 2b** — `build_art.sh --check` runs `gen_sfx.py --check` and `gen_amb.py --check`; an orphan `.wav` or a missing `.import` fails the gate. Today: deterministic (17/17 verified by hand) but ungated.
- [ ] **No download, no licence** — `tools/audio/*` import only numpy + stdlib; `data/credits.json`'s audio line names the generator (AUDIO-17).
- [ ] **docs/15 carries the audio ruling** — a numbered Q for the direction (buses, hooks, generated SFX, the 140 ms house rule, the restore-is-not-a-change default, the event-not-motion bind, the Instant gate, the boot silence, the clear's silence) and a numbered Q for music with the designer's answer applied or "cut for 1.0" stated as the switch. docs/13 §12.4 and docs/14 §4 point at the file that binds. Today: an unnumbered gap row.
- [ ] **`sim/` is silent** — `test_nothing_under_sim_reaches_for_audio` (green today).
- [ ] **Headless is safe** — the suite, `tools/shot.gd` and `tools/playtest.gd` boot the autoload with no device and log no `Audio:` error (the 2026-09-15 sheet logs show none).
- [ ] **The designer has listened once** — the one thing no test can tick: `ui.stamp` at 1x through a real wipe does not grate (docs/15 §8's own warning). Recorded as a review line, not an acceptance audit.

## Questions for the designer

Only the ones a build loop may not answer. Each has the default the loop will ship if unanswered by wave 8.

- **Q-A — Which slider owns ambience?** docs/13 §15.1's inventory has master / music / UI / voice and no "ambience". Default: the beds ride the **Music** bus and the row's note says so ("Ambience, and music when it is written"). Alternative: a fifth bus and a fifth §15.1 row — which reopens a closed list.
- **Q-B — Music: cut, procedural, or composed?** docs/02 §9.1's sparse lute → full ensemble → leitmotif is all 🔷 PROPOSED and canon has no music word. (i) Cut for 1.0 (default; the Settings note stays honest). (ii) A deterministic generated lute/drum bed per rank behind a switch — accepted knowing it will be "technically music" (L). (iii) Composed stems from a person (not a loop task). The bell toll and the cheer stinger are one-shots the generator CAN make under (ii) or (iii); the leitmotif never.
- **Q-C — What is "the voice of the scribe"?** The phrase exists once in the whole doc set (§15.1's bus row). Default: nothing is voiced at 1.0 and the Voice row is padlocked with that reason. Offered: a `ui.quill` material texture under every log-line arrival on the Voice bus (S) — the only cheap thing that makes the slider real.
- **Q-D — Does the fight make sound?** The stage animates attacks, heals, flashes and damage numbers in silence between stamps; docs/12 has no sound row and docs/13 §11.1 calls the log a document. Default: silent world layer at 1.0. Offered as a switch: three material world sounds (hit / spell / heal) under `_effects_allowed`'s gate — but they need a bus, which is Q-A's question again.
- **Q-E — Does the WIPE stamp use the mistake sound?** §12.4 binds `ui.stamp` to "StampBadge land"; the WIPE stamp is one. Default: yes, unjittered. If it should be heavier, name the pitch (one number in `opts`).
- **Q-F — Is the arena bed per encounter or per raid?** Same shape as docs/15 Q-96 (which arena): the cave bed plays under every fight until `backdrop` is authored. Default: follow `SceneStage.DEFAULT_ARENA`, i.e. whatever Q-96 decides, automatically.

## Coverage

**Read (evidence in the findings):** `BUILD_STATE.md` (Current focus, HANDOFF, invariants); `build/plan/audit.json` rows M6-AUD-01..06, M5-END-4, M6-JUICE-02/03/06, M6-A11Y-05 (titles of the rest); `build/plan/q-audio.md`, `handoff-audio.md` (§0-§7), `report-audio.md` (outline); `build/shots/_w5_args.json` (all seven units' ownership); `docs/13-ui-ux.md` §11.4, §12.2-12.4, §13, §15.1, §7 (the stamp list); `docs/02` §9.1; `docs/14` §3.2, §4; `docs/12`, `docs/07`, `docs/16`, `docs/00` (every audio/sound/music mention by grep); `docs/_source/*` (grep: no audio word); `docs/15` (Q numbering, the §8 gap row, Q-96); `game/core/Audio.gd` (all 575 lines); `default_bus_layout.tres`; `project.godot`; `game/core/GameSettings.gd` (the keys, `motion_duration`); `game/core/ScreenRouter.gd` (signals, goto/push/pop, the M5 header); `game/core/GameState.gd` (signal sites, `advance_day`); `game/core/LogPlayer.gd` (cadence, holds, Instant); `game/screens/Settings.gd` (ROWS, `_volume_control`, `VOLUME_STEPS`); `game/screens/RaidView.gd` (`_append_line`, the wipe beats, `_effects_allowed`, `_on_skip`, `_live`); `game/screens/RaidPrep.gd` (`_toggle`, `_build_readout`, `_depart_reason`, `_on_depart`); `game/screens/Results.gd` (the headline); `game/screens/AdventureBoard.gd`, `Market.gd`, `Tavern.gd` (the `_selected` transitions); `game/ui/Widgets.gd` (`stamp_press`, `cta`, `_guard_cta`, `wax_button`, `tab_row`); `game/ui/SceneStage.gd` (scene ids, `DEFAULT_ARENA`, `WARM_SCENES`); `game/ui/Boot.gd:151`; `tools/audio/gen_sfx.py` (header, CLI, manifest) and `README.md` (in full); `tools/build_art.sh` (in full), `tools/verify.sh` (stage 2b); `export_presets.cfg` (filters); `tests/unit/test_audio.gd` (all 39 test names; the orphan/loop/bed tests' bodies); `build/shots/all/fixture/Settings.png` (the four rows) and every sheet `.log` for `Audio:` errors (none).

**Measured without Godot:** `gen_sfx.py --out <scratch>` re-render vs the tree — 17/17 byte-identical; the manifest's per-hook ms/dBFS table.

**Not done:** no Godot run (mutex held by wave 5) — the 39 audio tests are trusted GREEN on BUILD_STATE's 1,959 at the wave-4 close, not re-run; no listening (no ears in a report — every "material, not blip" claim is the README's, and the designer's listen is the last line of the bar); `report-audio.md` §"Honest risks" skimmed by outline only; the `.godot/imported/*.sample` state on a fresh clone not verified (the `.import` assertion in AUDIO-05 exists because of that); `tools/playtest.gd`/`test_full_loop.gd` not read for a tape hook (W5-TESTS owns them this wave); line numbers in RaidView/RaidPrep/Widgets/Settings are as of 2026-09-15 ~11:30 and wave 5 (W5-SCALE, W5-KIT3) is editing those files — a wave-6 unit re-greps by function name.
