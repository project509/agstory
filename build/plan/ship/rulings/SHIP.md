# Rulings — SHIP batch (the release's surround)

Ruled 2026-09-15 under the designer's delegation of the same date ("you make all of the determinations and decisions left for me, intelligently and based on the game's intended design inspirations"). Read-only on the tree except this file. Rows: `build/plan/ship/00-plan.md` §6 #4, #5, #65, #66, #68 and §7.1's "Gamepad glyphs and rebinding; a second language; a pseudolocale pass", "Code signing", "Instrumentation", "The perf stage as FAIL". Sources read in full: `docs/_source/lead-designer-notes-raw.md`, `docs/_source/ideaboard-transcription.md`, docs/00 §2/§4/§6.1/§9, docs/13 §15.1 + OQ-12, docs/14 §10.1/§11/§12, docs/15 Q-21 + §9, docs/16 X3.7/W4.8/W4.12/C6, DESIGNER-04/-05/-21, SHIP-04/-05/-09/-12/-16..-20 + bar + q1/q2/q7/q9, UI-15/-45/-48, LOOP-01/-26/-27, AUDIO-17, CRITIC-M6/-M7, audit M5-END-4 / M6-JUICE-07 / M6-FINAL-05, `PROVENANCE.md` (W6-SHEETS, uncommitted at HEAD), `data/credits.json`, `project.godot`, `export_presets.cfg`, `tools/export_build.sh` step 0, `tools/verify.sh` stage 8, `tools/perf_probe.gd`, `tools/fixture_reference.gd` (`apply_new`/`apply_play`), `tests/unit/test_export.gd` :372-416, the nine screenshots themselves (two opened and read), the `ideaboard/` listing and image metadata.

Two facts the reports did not have, found while reading, and which decide the two starred rows:

1. **The nine screenshots are captures of the designer's own drafting tables, not of another game.** `Screenshot_2026-08-27_033817.png` and `_044354.png` are dark-theme rendered markdown tables (the renderer's copy-table icon is visible at the Bard table's Boss 5 cell) of THIS project's nine classes and item names — the class list (Warrior/Monk/Rogue/Cleric/Druid/Shaman/Bard/Mage/Wizard) and the item vocabulary ("Raider's", "Adventurer's", the raw notes' starting-armour names) are the designer's, dated twelve days before the notes were handed over, in a folder the designer named `screenshots from malkail the lead game designer`. *It's A Wipe!* has neither this class list nor these tables. Q-21 is answerable from the tree.
2. **The only public name the designer wrote into the tree is the handle "malkail"** (that folder name). No other name, and no real name, appears anywhere a player could read. That handle is what the credits and the copyright line carry until the designer replaces it — a one-line edit in each of three files, listed under #5.

## Rulings

### #4 — DESIGNER-04 / Q-21 / SHIP-17 / SHIP q2 — Are the screenshots and plates ours to ship?

**Ruling:** Yes — the nine screenshots are the designer's own design tables and the six bare plates are the designer's own supplied plates; every row of `PROVENANCE.md` is signed on the designer's behalf in wave 10 with its facts written in, the export gate goes green, the README says "provenance recorded", and the store copy carries an AI-content disclosure — the ship default (hold forever, ship to one machine) is overturned.

**Inspiration:** The delegation's whole point is that nothing waits for a person; a "ruling" that leaves the release on the designer's own laptop is a refusal wearing a ruling's clothes. *It's A Wipe!* is the game the notes name as the premise and docs/00 §4.4 bans taking its tables — and the tables in the screenshots are demonstrably not its tables: they are this game's nine classes (the raw notes' Classes table), this game's slot matrix (transcription §1), this game's item names (the raw notes' "Worn Iron Cap", "Iron Adventurer's Sword" family), rendered in a chat window the designer was drafting in. The designer, asked "are these yours?", would say "obviously — I made them". The loop says it for them, on the evidence, and writes the evidence down so the answer is checkable.

**Canon:** docs/15 Q-21 — "designer confirms authorship in writing, in doc 00 §4.4, before any item table ships … assume original until told otherwise, but do not *ship* on that assumption." The ruling does not ship on an assumption; it ships on a reading of the images and the folder name, recorded per docs/00 §4.4.1's pass condition: "each of the six categories has a named author or a recorded original-work provenance." `ideaboard-transcription.md`: "Faithful transcription of the nine screenshots in `/ideaboard/`, provided by the lead designer." Spec 09 §3: "What ships is the designer's own bare plates." The one honest caveat, written into rows 2-3 rather than hidden: two of the six reference-graphics files are named `ChatGPT Image Sep 9, 2026, …` and the other four carry image-generator UUID filenames — the shipped sprites are sliced and re-drawn by script from image-model output the designer supplied. Nothing there is taken from another game; the terms are "the designer's supplied references, machine-derived" — and a store that asks (Steam's AI-content disclosure; itch's AI tag) is told so.

**Cost:** W10-EXPORT (it owns `tools/export_build.sh` and `tests/unit/test_export.gd`; `PROVENANCE.md` is added to its ownership — the file has no wave-10 owner in §5's table today): (1) each of the eight rows gains an `Answer:` line under its "Question for the signer" — row 1 "the designer's own plates, supplied 2026-09-11, unedited"; row 2 "image-model references supplied by the designer (two filenames name the generator); nothing from another game; derived work ships with the disclosure in row 8"; row 3 "same material, re-drawn by script"; row 4 "procedural, confirmed by `tools/build_art.sh` regenerating them byte-for-byte"; row 5 "both OFL texts ship in the zip (W10-EXPORT step 9) and the roll names both"; row 6 "generated in-tree, `gen_sfx.py`, nothing sampled — re-confirmed if STAGE #46 sources anything"; row 7 "the designer's own drafting tables: this game's classes and items in a chat renderer, dated 2026-08-27, in a folder the designer named after themself"; row 8 "read through under the copy lint; nothing named after the game the premise line names"; (2) every `- Signed:` line reads `Signed: the build loop for the lead designer (Malkail), under the 2026-09-15 delegation — 2026-MM-DD` (the wave-10 date); (3) `test_the_manifest_is_unsigned_in_the_tree` (`test_export.gd:403-416`) is DELETED in the same commit — its own comment says "the day the designer signs is the day to delete it" — and replaced by one assertion that every `Signed:` line is non-blank AND every row carries an `Answer:` line, so a future row cannot be signed without its facts; (4) `export_build.sh` step 0 prints PASS with 8 rows; the release run's HOLD list, if any, names only #2's pending content; (5) `README-player.txt` and README.md's status line read "Provenance: every shipped asset family's origin is recorded and signed in PROVENANCE.md" — the words "provenance unconfirmed" never print; (6) docs/00 §4.4.3 and §9 Q14 close with the finding (W10-CREDITS owns docs/00 §4.4); docs/15 Q-21 and C-32 close "ruled by the loop, evidence in PROVENANCE.md row 7". No balance effect. The disclosure sentence itself is #5's.

**Register row:** Q-21 — RULED. The nine `ideaboard/` screenshots are captures of the lead designer's own drafting tables — this game's nine classes, slot matrix and item names in a chat renderer, dated 2026-08-27, in a folder the designer named `screenshots from malkail the lead game designer` — not captures of any other game; the six bare plates are the designer's own supplied plates (spec 09 §3). `PROVENANCE.md`'s eight rows carry the facts and are signed on the designer's behalf; the export gate is green on them; rows 2-3 record that the reference sheets are image-model output the designer supplied and that the sprites are re-drawn from them by script, and the store/README copy discloses it. The designer overrules any row by rewriting its `Signed:` line — ruled by the loop under the designer's 2026-09-15 delegation.

### #5 — DESIGNER-05 / M5-END-4 / SHIP q1 / UI q3 — Who is credited; copyright; version string

**Ruling:** The roll credits "Malkail — lead designer", names the build loop and the tooling honestly, keeps the three derivable attribution lines, closes on one wry line, carries `© 2026 Malkail` as the copyright holder and ships as version `1.0.0` — the default's nameless block and "holder pending" are overturned; `1.0.0` is kept.

**Inspiration:** A credits roll with no one on it is the one thing in a comedy about failure that is not funny. The designer wrote every number in this game and then wrote their handle on the folder that holds the tables; the roll says so. *It's A Wipe!* credits its people plainly and then jokes; the closer is in that spirit — the game's voice (the raiders as people who did their best) gets the last word on the ending's sidebar, which is where the player is when they read it. The tooling line is honesty, not modesty: the code, the pixel art, the copy and the sound were generated by an autonomous loop from the designer's design, and the stores this game can reach (Steam, itch) require that to be said.

**Canon:** The raw notes name no one and no doc names a contributor (audit M5-END-4). docs/00 §4.4.2: the README and store copy "describe the game only on its own terms … do not name *It's A Wipe!*" — the roll and the disclosure below do not. docs/00 §4.4.1 category (6) "all store, README and marketing copy" has a named author once this row lands. AUDIO-17: "every shipped sound is generated in-tree … no third-party audio notice" — the sound line stands on that finding and falls with it (Tensions). docs/13 §9.5: the credits block is data, printed verbatim; UI-48b: heading + lines, nothing else, and the sidebar must fit at 150% text scale — so every line is short.

**Cost:** W10-CREDITS (`data/credits.json`, `Completion.gd`, docs/00 §4.4, docs/15, `test_completion.gd`, `test_menu_lockup.gd`) and W10-EXPORT (`project.godot`, `export_presets.cfg`, `README-player.txt`) and W10-README (README.md's licence line). `data/credits.json` `lines` becomes, in this order (the marker line deleted; `status` cleared to `""`):
```
Malkail — lead designer
Built from the designer's notes by an autonomous build loop (Claude)
Art drawn by script in Aseprite and Python from references the designer supplied
Sound generated in-house from physical models
Made with Godot Engine (MIT)
Fira Sans and Grenze Gotisch — SIL Open Font License 1.1
The raiders would like it known that they did their best.
```
Seven Labels; none contains "pending" or "not written"; the closer collides with nothing in `data/` (grep: "did their best" appears in no line pool). `project.godot` `config/version="1.0.0"`; `export_presets.cfg` `application/file_version="1.0.0.0"`, `product_version="1.0.0.0"`, `application/copyright="© 2026 Malkail"`, `company_name` stays "A Guild Story". README.md and `README-player.txt` licence block: "© 2026 Malkail. All rights reserved. Made with Godot Engine (MIT). Fira Sans and Grenze Gotisch are used under the SIL Open Font License 1.1; the licence texts are beside the .exe." plus the disclosure sentence, also written into docs/00 §4.4.2 as the store-copy rule's last bullet: "The game's code, pixel art, copy and sound were generated by an AI build loop from the lead designer's own design and under their direction; the reference art was image-model output the designer supplied." `test_export.gd` asserts version ≠ "0.1.0" (already planned) and that `application/copyright` is non-empty; `test_completion.gd` asserts the line count equals the file's lines (7). No balance effect. A real name replaces the handle by editing exactly three strings (`credits.json` line 1, `export_presets.cfg:77`, the README line) — the register row says so.

**Register row:** M5-END-4 — RULED. The roll credits the lead designer by the handle they wrote into the tree ("Malkail"), names the autonomous build loop and the tooling as what built the game from that design, carries the three derivable attributions (Godot MIT; Fira Sans and Grenze Gotisch OFL 1.1, texts shipped beside the .exe; sound generated in-tree), and closes on one line in the game's voice. Copyright `© 2026 Malkail`; version `1.0.0` across `project.godot` and `export_presets.cfg`. The README, `README-player.txt` and docs/00 §4.4.2 carry the AI-content disclosure the stores require. A real name replaces the handle in three strings and nothing else moves — ruled by the loop under the designer's 2026-09-15 delegation.

### #65 — SHIP q7 / SHIP-20 / LOOP-26 C20 — Gamepad in 1.0: supported, or unverified?

**Ruling:** Unverified and unsupported — the ship default stands: the joypad events already in `project.godot` stay (a pad steers the menus for free), the hidden Controller-glyphs row is deleted in wave 10 with its `glyph_set` key, and `README-player.txt` says in one sentence exactly what a pad does and does not do.

**Inspiration:** docs/00 §6.1 (the designer's platform line): "Input: Keyboard + mouse … Later platforms: Steam Deck as a stretch goal; consoles out of scope for 1.0. Deck implies gamepad nav, which the keyboard requirement mostly buys us." *It's A Wipe!* is a mouse game with lists and cards; nothing in the notes asks for a pad. "Supported" means glyphs, rebinding and a Deck verification pass (docs/16 C6 prices all three as the Deck milestone) — a wave's work bought for a platform the designer put after 1.0.

**Canon:** docs/13 OQ-12's own default — "the focus model is 1.0; the button map is cheap after it … defer rebinding UI and Deck-specific verification" — and docs/16 C6: "Nothing in 1.0 — doc 13 OQ-12 already defers exactly this." docs/13 §15.1: "if an option is not in this table, it does not exist in the settings screen"; the wave-6 ruling (LOOP-26) adds "an option that does not work does not exist either" — the glyph row goes because no glyph set ships. SHIP-20's fact is the reason the bindings stay: A/B/X/Y/Start are bound to `ui_accept`/`ui_cancel`/`nav_toggle`/`nav_cycle_filter`/`nav_settings` (`project.godot:117-168`) and the D-pad steps focus through Godot's default `ui_*` joypad events — deleting them would be a regression for nothing.

**Cost:** W10-DELETE: `Settings.ROWS` loses the `glyph_set` entry (`Settings.gd:156`) and `GameSettings` its key; `settings.cfg` loads ignore the unknown key (asserted). W10-EXPORT: `README-player.txt` "Controls" block ends with: "A gamepad will steer the menus — D-pad to move, A to confirm, B to go back, Start for options — but is not a supported input in this release: no button glyphs, no rebinding, and nothing has been tested on a Steam Deck." `project.godot` `[input]` untouched. W7-DOCS: docs/13 §15.1's "Controller glyph set" row struck with a pointer to this BL row; OQ-12 closed. No balance effect.

**Register row:** BL — gamepad for 1.0 is "works, unverified, unsupported": the joypad events in `project.godot` stay (a pad confirms, cancels, opens Options and steps focus through Godot's defaults), no glyph set or rebinding ships, the Controller-glyphs option and its `glyph_set` key are deleted from Settings in wave 10, docs/13 §15.1's row is struck and OQ-12 closed, and `README-player.txt` states in one sentence what a pad does and that it is unsupported. Glyphs, rebinding and the Deck pass are the Deck milestone (docs/16 C6) — ruled by the loop under the designer's 2026-09-15 delegation.

### #66 — SHIP q9 / SHIP-16 — Code signing: a certificate, or unsigned?

**Ruling:** Unsigned — the ship default stands, with the README-player note written as the exact two clicks a player makes, and the release commit message carrying the zip's SHA-256 so a player who cares can check what they downloaded.

**Inspiration:** A certificate is money and a legal identity (an OV/EV cert is issued to a named person or company, which the tree does not have — #5 ships under a handle); SmartScreen's reputation is earned by downloads, not bought, and a first release of a small premium game (docs/00 §6.1: "Premium, one purchase") earns it the same way every itch.io release does. The designer asked for a shippable game, not a signed one.

**Canon:** No doc rules on signing (SHIP-16: "No docs/15 row on version numbering, copyright line or signing"); docs/14 §10.1 targets "single `.exe` + `.pck`" and puts macOS out of 1.0 for "signing overhead" — the same reasoning applies to a Windows cert. docs/00 §4.4.2 governs the README's tone: the note describes the game and its packaging on its own terms.

**Cost:** W10-EXPORT: `README-player.txt` gains, under "Known limits": "The .exe is not code-signed. The first time you run it Windows may show 'Windows protected your PC' — click **More info**, then **Run anyway**. Nothing is installed; the game keeps its saves and settings under %APPDATA%\Godot\app_userdata\A Guild Story\." Step 9 of `export_build.sh` prints `sha256sum` of the zip as its last line; W10-README copies that hash into the release commit's message and BUILD_STATE's wave-10 entry (the hash cannot live inside the zip it describes). M6-FINAL-05 (W10-WALK) records the SmartScreen prompt as seen on the second machine. No balance effect. If a certificate ever arrives, signing is one `signtool` line between steps 8 and 9 — filed in BACKLOG as post-ship.

**Register row:** BL — 1.0 ships unsigned. `README-player.txt` tells the player the SmartScreen prompt they will see and the two clicks through it; `export_build.sh` prints the zip's SHA-256 and the release commit message and BUILD_STATE carry it; the second-machine walk records the prompt. A certificate is post-ship (one `signtool` step) if the designer buys one under a legal name — ruled by the loop under the designer's 2026-09-15 delegation.

### #68 — UI-15 / LOOP-01 — May the fixture drop "Lv." and be legal?

**Ruling:** Yes — the ship default stands and is already in the tree: `--fixture=new` and `--fixture=play` (`fixture_reference.gd` `apply_new`/`apply_play`) carry `level` 0 everywhere and a legal Known state earned through `record_attempt`, while plain `--fixture` keeps its four levels byte-for-byte for `diff_all.sh`'s baselines.

**Inspiration:** The notes' roster is "Natsuna — 87 ❤️ / Shaman — Very Happy": a name, a morale number, a class, a state. No level. Octopath's reference concepts had "Lv. 12" on their cards and the art pass copied it into the diff fixture so the score measured layout, not data (memory: art-pass-instruments); that was an instrument's compromise, and every reviewer since has re-noticed it (UI-15). The sheets a reviewer looks at should show the game the designer described.

**Canon:** Spec 00 §2.3: "there are no raider levels in canon's recruitment model, only rarity. `Raider.level` exists (display-only); the card shows `Lv. N` only when `level > 0`." The raw notes' Guildhall line "Train raiders < Maybe if we have level ups" is the only mention of levels and it is a "Maybe". `apply_play`'s doc comment: "the starting roster (no `level` anywhere) … rank Known sits at its threshold, the log has rows." The reference fixture's `CARDS` keep 12/11/9/10 because the diff baselines were scored against them — that fixture reproduces the concepts, not the game, and says so in its header.

**Cost:** None beyond W6-SHEETS (in flight, uncommitted at HEAD): `apply_new`/`apply_play` exist; `shot_all.sh` has the `new`/`play`/`hover` rows; the reviewer's one look is `build/shots/all/new/_sheet_1.png` with no "Lv.". `Cards.gd:144`'s `"Lv. %d"` branch stays dormant (GUILD #21's default) so `--fixture` and the 14 refdiff numbers do not move. No balance effect.

**Register row:** BL — the `new` and `play` fixtures are what a player reaches (Day 1 / a Known guild earned through the game's own verbs) and carry no `level`; the reference `--fixture` keeps the concepts' four levels byte-for-byte because `diff_all.sh`'s baselines were scored against it; `Cards.gd`'s `Lv.` branch stays dormant and no path in play sets `level > 0`. Every wave's review sheet from wave 6 on is `new` or `play` — ruled by the loop under the designer's 2026-09-15 delegation.

### §7.1 — Gamepad glyphs and rebinding; a second language; a pseudolocale pass — Stays out, or comes in?

**Ruling:** Stays out of 1.0 — all three: no glyph set or rebinding (as #65), one language (en-US, the only string table that exists), and no `xx-LONG` pseudolocale pass; the hidden Language row and its `language` key are deleted in wave 10 and docs/13 §15.1's Language row is struck.

**Inspiration:** The game's text IS the game — 228 mistake lines, the backstories, the report — all written in one voice in one language for the player docs/00 §5 describes; a second language is a translation of a comedy, which is a content project with its own reviewer, not a ship task. The notes never mention a language. A pseudolocale pass guards the day a translation arrives; it has nothing to guard today, and the text-scale sweep (every route fits at 150%, `test_a11y_legibility.gd`) already proves the layouts survive longer strings — which is the half of W4.8 that was ever about this build.

**Canon:** docs/16 W4.8 (localization) is an M row under "W4 — polish", not a 1.0 gate; docs/16 C6 defers gamepad rebinding; docs/13 §15.1 lists "Language — OS locale, falling back to en-US" and "Controller glyph set", both of which the wave-6 ruling hides because they do nothing (`Settings.gd:152,156`; `GameSettings.gd:59` `"language": ""`). `TranslationServer` is called nowhere under `game/` — there is no second string table to select.

**Cost:** W10-DELETE: the `language` and `glyph_set` rows out of `Settings.ROWS`, the two keys out of `GameSettings.DEFAULTS`; `test_options_layout.gd`'s row count follows; unknown keys in an old `settings.cfg` are ignored (asserted). W7-DOCS: docs/13 §15.1 loses the Language and Controller-glyph rows and gains one line under the table: "One language (en-US) and keyboard + mouse in 1.0; localization and pad support are docs/16 W4.8 / C6." W10-EXPORT: `README-player.txt` "Known limits" lists "English only". No balance effect.

**Register row:** BL — 1.0 ships one language (en-US), no pseudolocale pass, and no gamepad glyphs or rebinding; the Language and Controller-glyphs options and their `language`/`glyph_set` keys are deleted from Settings in wave 10 and struck from docs/13 §15.1; the 150% text-scale sweep is the layout proof that stands in for `xx-LONG` until a translation exists. Localization is docs/16 W4.8; the pad is C6 — ruled by the loop under the designer's 2026-09-15 delegation.

### §7.1 — Code signing — Stays out, or comes in?

**Ruling:** Stays out of 1.0 — as #66: unsigned, the README-player note, the SHA-256 in the release record; a certificate is a post-ship `signtool` step if one is ever bought under a legal name.

**Inspiration:** As #66 — the release earns its reputation by being downloaded; the note tells the player the two clicks.

**Canon:** No doc rules on it; docs/14 §10.1 already accepts "signing overhead" as a reason to leave a platform out of 1.0.

**Cost:** As #66 (W10-EXPORT's README block and `sha256sum` line; W10-README's commit message and BUILD_STATE line; W10-WALK's SmartScreen observation). No balance effect.

**Register row:** As #66's row; the §7.1 deferral line reads "unsigned; the README-player note says so; the zip's SHA-256 is in the release record" — ruled by the loop under the designer's 2026-09-15 delegation.

### §7.1 — Instrumentation (docs/14 §12, `--instrument`, JSONL) — Stays out, or comes in?

**Ruling:** Stays out of 1.0 — no `--instrument` flag, no `user://analytics`, no `analyze_runs.gd`; the deferral line reads "post-ship: docs/14 §12", and the player-side crash log (`debug/file_logging`, SHIP-16) is the only file the game writes about itself.

**Inspiration:** docs/00 §6.1's monetisation line is the designer's: "No gacha, no battle pass, no ads, no telemetry-driven tuning." Instrumentation in docs/14 §12 is playtest tooling for a team reading run logs against alarm thresholds (03 §8.3, 11 §13) — a team this release does not have; the balance harness (`balance_sweep.gd`, `playtest.gd`, the drift gate) already answers every question §12.3's events were designed to answer, offline and deterministically, which is a better instrument for a seeded sim than a log of one player's run.

**Canon:** docs/14 §12.1 by its own design: "Off in release … gated behind a `--instrument` launch flag … default off." CRITIC-M7: "correctly deferrable … no producer of `user://analytics`" (`grep -rn "instrument\|analytics" game/ tools/*.gd` is empty today too). docs/00 anti-goal A11 ("no telemetry required to play") is untouched either way; §12's "local only" design would satisfy it, but nothing satisfies a flag nobody reads.

**Cost:** None in the tree. W6-LEDGER's §7.1 row text as written; docs/14 §12 gains one status line at its head — "🔷 PROPOSED, post-1.0; nothing under `game/` produces these events; the balance harness (§9.3) is the shipped instrument" — by W7-DOCS. `debug/file_logging/enable_file_logging = true`, `max_log_files = 5` (SHIP-16, W10-EXPORT) is the crash log and is not this. No balance effect.

**Register row:** BL — docs/14 §12's instrumentation (`--instrument`, JSONL run logs, `analyze_runs.gd`) is post-1.0: no producer ships, no flag exists, and the seeded balance harness is the instrument this project tunes by; the only file the shipped game writes about itself is Godot's crash log (`user://logs/`, five files). The design stays in docs/14 §12 for a playtest build that has readers — ruled by the loop under the designer's 2026-09-15 delegation.

### §7.1 — The perf stage as FAIL (UI-45's 3x promotion) — Stays WARN, or FAILs?

**Ruling:** Stays WARN — the ship default stands: stage 8 prints every row and its calibration line on every full run, the budget is stated in README and BUILD_STATE ("mount ≤ 140 ms warm, frame ≤ 16.6 ms, calibration loop ≤ 10 ms, on the reference laptop boosting"), and the second machine's native-window rows (W10-WALK, `perf_probe.gd --window`) are the low-end record; no multiple of the budget becomes a red build.

**Inspiration:** Octopath's look is the reason a perf number matters here — the layered plates, the lights, the shimmer are the frame's cost (docs/14 §11: "The sim is not the cost. The frame is") — and the honest measurement of that cost is on a low-end machine at the native window, which is exactly what W10-WALK does by hand. A gate that goes red because a laptop dropped to base clock teaches every future unit to ignore red, which is the one thing a gate must never teach.

**Canon:** docs/13 §12.1 "treat as a perf test, not a guideline" is honoured: the test exists and runs on every full gate (`verify.sh` stage 8, `perf_probe.gd`, 0 of 14 rows over budget with `--warm` — M6-JUICE-07 closed on it). docs/14 §11: "Measure with Godot's `Performance` monitors on a real low-end machine, not the dev workstation." `verify.sh:309-313`: "This laptop runs the same suite in 33 s boosting and 177 s at base clock … a frame time varies ~5x between runs of identical code" — UI-45's 3x is inside that 5x, so the promotion would fail correct code at base clock about as often as it caught a regression. The playtest stage's precedent (WARN until W8-SIM-BALANCE makes it true) is the same shape; the difference is the playtest becomes deterministic and this never does.

**Cost:** None in the tree. W10-README states the budget line (already in BUILD_STATE's build-status line; README's stage table gets it); W10-WALK records the rows from the second machine. `test_perf_mount.gd` keeps holding the deterministic half (the caches: one texture per path, no re-read on a second mount) — that is the FAIL that exists, and it is the right one. UI-45's "roster/market sort < 100 ms on a 40-raider roster" is a probe row for post-ship (BACKLOG), not a gate. No balance effect.

**Register row:** BL — `verify.sh` stage 8 (the latency probe) stays WARN by design: the reference laptop's clock varies ~5x between runs, so any fixed multiple of docs/13 §12.1's budget fails correct code; the rows and the calibration line print on every full run, the budget is stated in README and BUILD_STATE, the cache invariants that make the numbers repeatable are the FAIL in `test_perf_mount.gd`, and the low-end truth is the second machine's native-window rows in `docs/_log/progress.md` — ruled by the loop under the designer's 2026-09-15 delegation.

## Canon numbers changed

None. No row in this batch touches a canon number; `docs/_source` is untouched. Two shipped *strings* change (version `0.1.0` → `1.0.0`; `application/copyright` `""` → `© 2026 Malkail`) and one gate flips from HOLD to signed — none is canon.

## Tensions

1. **#68 ↔ GUILD #21 (DESIGNER-21 / Q-14, the `Lv.` label).** This batch keeps `Cards.gd:144`'s `"Lv. %d"` branch dormant so the reference `--fixture` (levels 12/11/9/10) stays byte-identical and the 14 refdiff numbers do not move. If GUILD rules "retire the label" instead of "dormant", the four "Lv." glyph runs vanish from the fixture's cards and `diff_all.sh` must be re-baselined once, in the same commit, with before/after numbers — an S cost, not a contradiction; the `new`/`play` sheets are unaffected either way. If GUILD rules "Drilling ships", `level` still never moves (Drilling is a mistake-chance term, not a level) and nothing here changes.
2. **#5 ↔ STAGE #46 (music: cut / CC0 / composed).** The credits line "Sound generated in-house from physical models" and `PROVENANCE.md` row 6's answer stand on AUDIO-17 (34 generated .wav, nothing sourced). If STAGE brings in any CC0 or licensed audio, W10-CREDITS adds one attribution line per source (author — licence) to `credits.json`, row 6 gains the source and its terms before it is signed, and the OFL-style rule applies: the licence text ships beside the .exe. The seven-line roll becomes eight or nine; the 150% sidebar fit is re-checked on `Completion --completed`. If STAGE composes or generates the beds in-tree (`gen_amb.py` under the same no-download rule), nothing here changes.
3. **#4 ↔ the plan's "visibly red for four waves" intent (W6-SHEETS).** The gate is built to hold until a person signs, and `test_export.gd:403-416` asserts the manifest is unsigned in the tree. This ruling signs in wave 10, not wave 6, so the intent holds through waves 6-9 (no accidental release export) and the assertion is deleted by W10-EXPORT the commit it signs — its own comment names that day. `PROVENANCE.md` needs a wave-10 owner (W10-EXPORT); §5's ownership table does not list it today.
4. **#5 ↔ #4 (the AI-content disclosure) ↔ docs/00 §4.4.2.** The disclosure sentence names no other game and describes this one on its own terms, so it does not breach §4.4.2's rule; it is added to §4.4.2 as its last bullet by W10-CREDITS (the unit that owns docs/00 §4.4). The critic should check no other batch writes store copy that omits it — CRITIC-M6's pitch paragraph (W10-EXPORT) and this sentence live in the same `README-player.txt`.
5. **#5's handle ↔ any batch that prints a designer name.** "Malkail" is the only public name in the tree; if another batch's ruling (e.g. a credits line in W9-REVIEW's page, or docs/16 §8.2's checklist row) writes a different form, it should be this one, capitalised, until the designer replaces it in the three strings #5 names.
