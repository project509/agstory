# CRITIC — coherence review of the eight ruling batches (2026-09-15)

Scope: build/plan/ship/rulings/{BALANCE,GUILD,COMBAT,CONTENT,STAGE,SCREENS,UX,SHIP}.md against docs/_source (canon), docs/15 §9 (binding Q-01..Q-15), and build/plan/ship/00-plan.md §1-§7.
Read-only except this file. Written as I go.

**Summary:** 5 contradictions (all resolved), 0 silent canon violations (one canon number changed on the record — the daggers; two departures from §9's optional "next three" lines, both already the plan's defaults), 14 amendments (none reverses a ruling's intent; most re-home work to the unit that owns the file that wave). The M6-BAL-04 lever recomputes exactly from docs/05 §8 and docs/08 §9.3; Tier 1 is completable and the first wipe survives (P(no wipe before the raid door) ≈ 13 %), on two conditions: the attrition grace stays 5 (A11) and the raid bands are checked at morale 55 (A3). Waves 7-10 fit at 7 / 7 / 7 / 5 + buffer.

## Contradictions

Batch-against-batch, where the tree cannot hold both. Each is resolved in ## Amendments; the number here is the count of the ones that needed a pick, not of every seam (the seams that are only a wrong unit name are in ## Plan impact).

**C1 — GUILD #21 vs SHIP #68: the "Lv." branches.** GUILD retires `Cards.gd:150-153` and `RaidView.gd:1023-1024` in W10-DELETE; SHIP keeps them dormant so the reference `--fixture` stays byte-identical for `diff_all.sh`. Pick: **GUILD** — a branch no shipped path reaches is exactly what W10-DELETE's own contract deletes, and the design wins over the reference's UI (memory: reference-concepts-are-a-standard). The cost SHIP feared is one `diff_all.sh` re-record at the wave-10 close, which the orchestrator runs anyway and which W10-README records with before/after; four "Lv. 12" glyph runs on a 1536-wide sheet sit inside the 0.07 jitter. The fixture DATA keeps its four levels (SHIP's point stands: `--fixture` reproduces the concepts); they stop rendering. Spec 00 §2.3's sentence "the card shows `Lv. N` only when `level > 0`" becomes "no level is shown". → A1.

**C2 — STAGE #42c / plan W8-FACILITY vs SCREENS #22: the rank props.** Three collisions in one table. (i) The statue: STAGE and the plan put it at Renowned; SCREENS at Legendary. (ii) SCREENS' Known row puts "one cloth pennant on the big guild tent" — STAGE #42c's facility L2 state is "+ a cloth banner" on the same tent; two tables would dress one prop by two keys. (iii) SCREENS' Renowned/Legendary rows (second tent, two guards, braziers) are new props the plan never sized, and SCREENS lands them in "W9-TIERS's long branch", which does not own `stage_camp.json` in wave 9 (STAGE #29 gives that file to W9-ART). Pick: **the plan's four props at the plan's ranks** (pennant Known, notice post Respected, cobble band Established, statue Renowned — W8-FACILITY as sized), **SCREENS' walkers/lantern counts kept** (they are what makes Unknown → Known felt, docs/02 §2.1 R2), the Known pennant moved off the tent to a pole beside the Board callout, and SCREENS' extras re-ranked: Renowned = statue + 2 brazier flames; Legendary = the second guild tent, the two `knight_unlabelled` guards, crest pennants on all four tents, 4 braziers — ranks 4-5 in **W9-ART**, only if tiers mount (they do, CONTENT #2). → A2.

**C3 — BALANCE #9 vs BALANCE #1's own farm argument, docs/08 §9.2's costing and COMBAT #55: the morale the raid bands are measured at.** BALANCE writes every band "all-Common, morale 45", and in #1 argues that the harness farms the Adventure before the raid so "a farming guild enters the raid above 45 — the town loop working". docs/08 §9.2 costs E1-E5 at Content (the ×0.78 column), `balance_sweep.gd`'s `REFERENCE_MORALE` is 55, and COMBAT #55 sizes E4's teeth against Common/raid_entry/55. A curve for E1-E5 at 45 describes a state the game's own loop does not deliver to the raid door; today's baseline at 55 already reads E1-E4 100/100/100/100 and E5 80.5, so the "too easy → `target_rounds` +2" arm would fire on E3 and E4 in wave 9 to chase a band measured at the wrong morale. Pick: **the on-ramp bands at 45 (the fresh guild), the raid bands at 55 (the guild the farm delivers, and §9.2's own costing)**; the morale-45 raid cells print as the "after a wipe" reading beside the band, never as the gate. The band VALUES stand. → A3.

**C4 — UX #39 vs BALANCE #9 + UX #36: one punchline twice on one page.** BALANCE's new TR notice ends "Read the Wipe Report; you will be seeing a lot of it."; UX #36 puts the notice on every Results page as the epigraph; UX #39's TR `lesson_report` — printed on the same wipe page — ends "You will be seeing a lot of it." Pick: the notice keeps the line (it is read first, on the Board, and it is the designer-register joke); `lesson_report` drops its last sentence. → A4.

**C5 — SHIP #5 vs STAGE #46: the credits' sound line.** SHIP's line 4 is "Sound generated in-house from physical models"; STAGE brings the generated lute into 1.0 and asks for "Sound and music". SHIP's own tension #2 anticipated this. Pick: "Sound and music generated in-house from physical models" — and if the lute sheds (A13), "Sound" alone; W10-CREDITS reads `Audio.MUSIC_BED` before it writes the line. → A5.

Not contradictions, checked and clear: STAGE #42j and UX §7.1 both retire the Raid Group tab (same word); STAGE #23/#67 and the plan's W7-STAGE/W7-AUD-AMB give one arena rule; GUILD #12 (accept the collection endgame) is consistent with CONTENT #2/#53 (five tiers mount, Renowned pays); CONTENT #25's titles and SCREENS #45's "no boss names from the references" are the same ruling read from two ends (SCREENS' wording is folded into A6); BALANCE #63's 15 G Common is the number UX #60's walk-in and CONTENT #19's 80-G shelf both read; COMBAT #61's −4 and BALANCE #62's "no exemption" stack as both batches expect; COMBAT #52's Guard AC 21 = docs/08 §9.3's Boss 4 tank AC; UX #6/#51's commit rule and COMBAT #58's break roll join only if the roll's result is stored (A10).

## Canon

Read against both docs/_source files in full and docs/15 §9. The test is two-part: does a ruling contradict a canon line WITHOUT saying so, and does any ruling reverse a §9 binding row.

**§9's six signatures — none reversed.** Q-01 (AC curve): every BALANCE/COMBAT number is computed under R3, `AC_K = 60` (stage-0 mitigation 18.9 %, Boss 4 tank AC 21 — both batches reproduce docs/08 §9.1a/§9.3). Q-02 (Mana magnitude; class-fixed Focus off gear): COMBAT #54 keeps Focus in the design with its numbers decided and defers only the SHIP of it, as the plan's §7.1 already did; the Bard's +20 Instrument feeds song magnitude (Q-46), not a pool. Q-04/05 (one mistake table, per mechanic event): BALANCE #57's `TUTORIAL_MISTAKE_MULT` table and #49's `DIFFICULTY_MULT` are multipliers on the one equation before the clamp — the table stays one table. Q-09 (no mid-raid input): UX #6/#51 restate it as the commit rule; COMBAT #58's break roll fires at Depart, before the attempt, and is not an input. Q-06/07 (half-open bands, Quite Happy): untouched; BALANCE's 29.8 % at 45 is band 4 under `Enums.morale_band`. Q-15/18: untouched.

**§9's "next three, if there is time" — two departures, both stated, both already the plan's §6 default before the delegation.** Q-13 ("Blacksmith yes at upgrades-only"): CONTENT #10 rules the building out of 1.0. Q-14 ("no level-ups; ship Drilling"): GUILD #21 rules no Drilling. Neither is a signature row — §9 lists them after the six as optional — and the plan's own §6 rows #10 and #21 took "(b) out" and "Drilling post-1.0" as ship defaults when it was synthesised. Both rulers quote the canon line that outranks the recommendation ("Blacksmith (Maybe)" three times; "Train raiders < Maybe if we have level ups"). They stand; docs/15's Q-13 and Q-14 rows must say "§9's recommendation not taken; reason" rather than closing silently (W7-DOCS). Q-11 (Master Looter + Suggested) and Q-12 (15 → 20 by rank) are kept by GUILD #15/#59 and UX.

**Canon numbers changed, and said so (allowed under the delegation):** COMBAT #17 — Basic Raid Dagger +4 → +5, Strong +6 → +7 (ideaboard §3.4); the row names the table, the reason and the pin, and docs/_source is not edited. This is the only canon NUMBER any batch moves. Every other moved number is 🔷 (A0/TR/A1-A3 rows, docs/09 §10.2's Chipped Sword, the palette samples, docs/08 §9.2's target rounds under the correction rule).

**Canon strings changed:** none. CONTENT #18 keeps the healer's 1-AC "Worn Leggings" byte-identical; CONTENT #47 keeps "Adventure's Board" with canon's apostrophe; the ladder words and enemy log names are canon's placeholders (CONTENT #25); Natsuna is final (CONTENT #3).

**Canon tensions the ruler acknowledged (accepted, on the record):**
- UX #60's walk-in is a Common at 0 G at any rank, against "@Respected you no longer find common". The ruler's reading — the Tavern's find-rule describes who the guild finds; the walk-in finds the guild — is stated on the card ("A walk-in. Will raid for a bed."). Accepted: the condition (roster short of a party, purse below the cheapest seat, nothing to sell) is a guild that has already lost the thing the rank describes.
- BALANCE #1 sizes the whole on-ramp at canon starting armour against docs/10 §8's own sentence and BL-30's staged reading; BL-30 is superseded in writing, BL-28 stands.
- COMBAT #52 reads canon's "Position-dependent burst DPS" as a Front penalty with Behind = 1.0 (docs/08 §8.2's default) rather than docs/06's 1.3 bonus. Checked: a Behind bonus of even 1.05 puts the Rogue over the Wizard at Boss 5 (49.9 × 1.05 = 52.4 > 50.2) and canon's "Very high single-target DPS" is the Wizard's; the "burst" is the two swings. Forced by canon's numbers, not convenience.
- CONTENT #25's titles are roles, not creatures, because one boss set serves five tiers — canon says "they will need names later"; a role name is a name.

**Canon lines every batch honoured that the critic checked by hand:** raid size 12 and "most fights normally requiring 2 tanks" (TR at one tank is the DECIDED tutorial exception; E3 is the tier's first swap); the five-encounter raid shape and star ramp; the morale table's ten bands and the −5 Common offset (kept by BALANCE #1); "one Legendary per class, named" (nine names, one each); "Really awesome frontend design is a priority" (STAGE #27, SCREENS #33, SCREENS #40 all argue from it); "2D-HD … Octopath" (STAGE #26/#27 — one pixel pitch, integer scale, nothing blurry; SCREENS #32 no DoF on 1:1 plates).

Count of silent canon violations: **0**.

## Spirit

Would the designer recognise it as theirs? Read for the three inspirations (It's A Wipe!'s guild-leader fantasy and comedy of failure; the Octopath look; the notes' own tone) and for the three failure modes (generic, timid, engineer's convenience).

**Recognisably the designer's — no note needed:** BALANCE #1/#9 (the Adventure is beatable on arrival, the raid door is the wall, the tutorial raid's one trick is the fire); BALANCE #57 (A0 shows exactly one idiot); BALANCE #48 (a 96 G round that a Raid 1 clear cannot pay for is the economy's worst joke); GUILD #13 (the town hears the board); GUILD #20 (Established changes the town, not just the Tavern); GUILD #21/#50/#59 (the person is the backstory; no third stat, no sixth axis); COMBAT #56 (the enrage is the DPS check that sends a leader to the Market); COMBAT #58 (the break-phase ninja pull is the raid joke every raider has a story about); COMBAT #52 (kits built to the nine one-line roles and no further; the Wizard Ramp refused as a borrowing); COMBAT #14 (nine "never makes THAT mistake" gifts, one line each; "Lets Sleeping Adds Lie"); CONTENT #2 (straight MMO tier words — the joke is Steve wearing them); CONTENT #3 (eight names in Natsuna's register, each fitted to a file); CONTENT #25 (the Doorman, the Landlord, the Auditor, the Last Word — jobs the guild is failing an interview for); CONTENT #37 (Pauline_4 is funnier in the log than explained on a card); CONTENT #19 ("Borrowed Sword · Damage 2"); STAGE #27 (one pixel pitch; a boss bigger than four raiders); STAGE #29 (the ❤️🙂😒😡 ladder read for one mistake); STAGE #42b (the tank's badge is the shield); STAGE #46 (the sparse lute docs/02 wrote, as a plucked-string model, not a tune; the quill under the log); SCREENS #22 (the town visibly changes at every rank); SCREENS #31 (a gem beside a number is a currency); SCREENS #33 (a page settles in place); SCREENS #40 (three states, lightness-ordered); UX #6/#51 (the wipe is the joke and the retry is the loop); UX #36 ("The notice said:" — the brief-versus-outcome gag); UX #39 (the two lesson lines); UX #60 (the walk-in); SHIP #5 (a credits roll with someone on it; the raiders get the last word).

**Rulings that lean toward an engineer's convenience — with the better reading:**

1. **BALANCE #9's correction rule, applied to the raid rungs.** "Too easy → `target_rounds` +2, two steps at most" is a good rule for the on-ramp rows this batch re-derived; applied to E3/E4 it would inflate docs/08 §9.2's costed boss HP by up to 27 % (E3 3,200 → 4,050) to hit a statistic, on a sweep that measures the benchmark comp at one morale. The designer's escalation across the raid is "HP totals, mechanic density and the AoE channel" (§9.3's own sentence), and COMBAT #55 has just given E4 its mechanic density. Better: for E1-E5 the too-easy arm takes at most ONE step in 1.0 (+2 rounds, HP re-derived, the row prints the HP moved), the mechanic lever having gone first; the residual is a 1.0.x row after real play, not a second inflation. → A8-spirit (folded into A3).

2. **COMBAT #17's +1 on the daggers.** Defensible and stated, but it is a numbers patch that turns a downgrade into a sidegrade (docs/08 §9.1's Rogue column reads 24.2 / 24.2 at Adventure / Boss 1 after the fix). The designer's visible intent — the dagger hits lighter — survives; what the player will still notice is that the Rogue's Boss 1 upgrade is the SHARED Basic Raid Sword in the main hand, not the dagger. Not amended; CONTENT-03's item note for the two daggers (W8-ITEMS) should carry that reading in the register ("Lighter than the sword. The Rogue will tell you that is the point.") so the sidegrade reads as a choice, not a mistake.

3. **SHIP §7.1 perf stays WARN.** Argued from the 5× clock variance and honest about it; the FAIL that exists (`test_perf_mount.gd`'s cache invariants) is the right one. Not convenience — but the README's budget line must say "measured on the second machine" so a reader does not take WARN for "not measured".

4. **UX #38's cycle chips for everything** (no slider for the six-step audio ladder). Right for the kit and the reference; the one place a player will feel it is volume, which is six presses to cross. Accepted for 1.0 — the pips make the ladder legible; a slider is a 1.1 kit unit if players ask.

**Timid?** None. Every batch overturned at least one default where the default was a refusal-in-waiting (the provenance gate, the nameless credits, the unfindable Legendaries, Tier-1-only, "no music", "no bail-out", "inert quirks", "1x boss"), and each overturn quotes the canon line or the inspiration that pays for it.

**Generic?** One near-miss, caught by the ruler: CONTENT #2 dropped "Mithril" for "Silvered" on originality grounds. The critic grepped every creative word in the eight files against `data/`, `game/`, `sim/` (Gunnar, Ottilie, Alder, Solenne, Casimir, Tallis, Isaura, Lorcan; the 21 titles; Pocket Change; Borrowed; the twenty words; "Will raid for a bed"): no collision. "Vanquisher / Runegold / Immortal" already sit in `tier_words.json`'s `candidates` block, as CONTENT says.

**One line to re-read at W9-REVIEW:** BALANCE's A3 notice "…no Mana on any piece of armour they own" is true for the fresh six and false for the farming guild that re-reads it in Adventure healer armour (+13 Mana). Under UX #36 the notice is a prophecy, not a report, so it is allowed to be wrong later — but the reviewing agent should decide whether the joke survives its third reading.

## Numbers

Every lever, curve, price and rate in the eight files was checked for a value and a cited derivation. All have both. The M6-BAL-04 lever is recomputed below from the tree's constants (which are docs/08 §8.8's equation as W7-DOCS will publish it, and docs/05 §8's baseline as `Morale.gd` holds it).

### The lever (BALANCE #1), recomputed

**docs/05 §8 / `Morale.gd`:** `BASE_BASELINE 50 + RARITY_OFFSET[Common] −5 = 45` — a Common rests at 45 at facility tier 0 and `rest_until_recovered()` never lifts it. Band: `min(9, floor(45/10)) = 4`, Slightly Annoyed (half-open, Q-06).

**docs/08 §8.8 as shipped (`Formulas.mistake_chance_bp`):** `2400 × (1 + 0.20 × 1.20) = 2976 bp = 29.8 %` at 45; `2400 bp = 24.0 %` at 55 (Content). So `TAX = 0.702`, `TAX_CONTENT = 0.760`, ratio `0.924` — BALANCE's three figures reproduce exactly.

**docs/08 §9.3's clock at stage 0:** mitigation(7 AC) = 14 / (14 + 60) = **18.9 %**; the unhealed 3.5-round invariant gives `boss_auto_raw = 120 / (3.5 × 0.811) = 42.3` — the "42 raw" of §9.1a's own table. Against one healer at `HEAL_FLOOR` 6 the tank nets 34.1 − 6 = 28.1 a round and dies in 4.3 rounds; a boss that needs 15+ rounds to kill at stage 0 is therefore unwinnable by construction — the 0/24 and 0/20 pins were arithmetic, not tuning. §9.4 says the 3.5-round clock was designed to be unhealable solo ("solo-healing the tank must not work") — for the twelve's three healers. BALANCE's finding that the six was handed a twelve's clock is correct.

**BALANCE's §9.3a (healed clock), recomputed row by row:**

| Rung | target | fight_len = target / 0.702 | tank_hp / fight_len | + 6 | ÷ 0.811 → raw | ruled | HP = nominal × target × 0.924 | ruled |
|---|---|---|---|---|---|---|---|---|
| A0 (4, nominal 11.0) | 6 | 8.55 | 14.04 | 20.04 | 24.7 | 20 (kept under budget) | 61.0 | 61 ✓ |
| A1 | 8 | 11.40 | 10.53 | 16.53 | 20.4 | 21 = 7 × 3 ✓ | 121.9 | 3 × 41 = 123 ✓ |
| A2 | 10 | 14.25 | 8.42 | 14.42 | 17.8 | 18 = 9 × 2 ✓ | 152.4 | 2 × 76 = 152 ✓ |
| A3 / TR | 12 | 17.09 | 7.02 | 13.02 | 16.1 | 16 = 8 × 2 ✓ | 182.9 | 183 ✓ |

Every ruled number is the formula's output rounded to the swing count. The M04 add HPs are docs/10 §8's ~8-9 % of the pull: 11/123 = 8.9 %, 12/152 = 7.9 %, 15/183 = 8.2 % ✓.

**Does it clear, and does the first wipe survive?** Kill clock at 45 = HP / (nominal × 0.702): A1 10.5 rounds, A2 13.1, A3/TR 15.8; tank clock 11.4 / 14.25 / 17.1 — margins of 0.9 / 1.1 / 1.3 rounds, i.e. a fight decided by whether the mistakes fall on the DPS or the healer. Enrage `ceil(1.4 × target)` = 12 / 14 / 17; with COMBAT #56's grace the attrition line is 17 / 19 / 22, comfortably past the kill clocks; WITHOUT the grace (the plan's "+5 gone") A2's line is 14 against a 13.1 mean kill — the tails would fail. **BALANCE's derivation therefore depends on COMBAT #56's grace of 5, and the plan's SIM-12 line ("attrition only at the round cap") must go.** The curve: A0 ≈ 100 (organic rate 0; the boss cannot kill four raiders in nine rounds at 20 raw), TR 55, A1 75, A2 65, A3 50 → P(a fresh guild reaches the raid door without a wipe) = 0.55 × 0.75 × 0.65 × 0.50 = **13 %**; the median guild wipes first on TR or A1 and reads its first Wipe Report before Raid 1. The first wipe does not disappear. The raid door: E1 at ≥ 90 in full Adventure gear (docs/08 §9.2's E1 is 2,200 HP against 182 nominal — 12 rounds at Content, a stage-0 twelve needs 48) is the farm's reward; at E5 40 % (30-55) the Q-90 arithmetic holds: P(≤ 3 attempts) = 78 %, P(≤ 5) = 92 %. **Tier 1 is completable and the first wipe is intact — with two conditions: the grace stays 5 (C4/A11), and the raid bands are checked at 55 (C3/A3).**

**One caveat on the derivation's tax.** docs/08 §8.8's published tax (`effective_dps = Σ nominal × (1 − chance)`) is what BALANCE used, and it is the doc's own formula (rule 6 satisfied). The SIM rolls per site with BL-21's `ROLL_SITE_WEIGHT` 0.55 / 0.55 / 0.15 and a mistake costs the round's output only at the action site, so the measured DPS tax at 45 is nearer 0.55 × 29.8 ≈ 16 % than 29.8 %. The ruled rows therefore err generous — HP about 4 % lower and swings about 10 % lower than a site-weighted derivation would give — and the first 200-seed pass should be expected to land A1 and A2 ABOVE their bands; the rule's own "too easy → target_rounds +2" step then fires once. That is the rule working, not a flaw; the row should say it so nobody reads the first sweep as a mistake.

### The other numbers, checked

- **BALANCE #1b** — 36 clears at one roll per slot vs 18 at two (12 feet + 12 weapons over 2 rolls; 12 legs over 1; 24 head/chest over 2): reproduces. With CONTENT #19's off-hand slot on A2, A2 becomes 2 slots × 2 = 4 rolls; the constant applies, the table's "A2 2" becomes 4 — state it in the row.
- **BALANCE #7** — Epic 420 × (1 + 0.15) = 483 → 480 ✓; Legendary 1,000 × 1.30 / 1.45 = 1,300 / 1,450 ✓.
- **BALANCE #11** — 15 Commons × 1 G × ~4 paydays per tier ≈ 60 G ≈ 11 % of docs/11 §4.3's 520 G; the "per week" in the row means per payday and the 11 % is per tier — say so.
- **BALANCE #49** — 29.8 % × 0.75 = 22.4 % ✓ (above the 12 % floor, so the floor never clamps a Common).
- **GUILD #13** — 2 × 15 = 30; 2815 + 30 + 325 = 3170 < 3200 ✓; cap at Known ⌊0.15 × 120⌋ = 18 ≥ 15 ✓.
- **GUILD #20** — "one in twenty" = `find_weights` 50 / 1000 ✓.
- **COMBAT #55** — the every-6 cycle over 17 rounds: M09 live 3-5 / 9-11 / 15-17, M08 6-7 / 12-13, M05 checks 4 / 9 / 14 → zero fixate-under-debuff rounds ✓ (M05's round-9 check falls inside a debuff window, which is a pulse the healers must eat while debuffed — intended teeth).
- **COMBAT #17** — Rogue column 24.2 / 24.2 / 28.0 / 35.6 / 49.9 vs Wizard 36.0 / 38.2 / 40.5 / 50.2: monotone, Wizard on top at every rung ✓ (by 0.3 at Boss 5 — the margin that forbids any Behind bonus, see Canon).
- **COMBAT #52** — Monk 14 AC + 7 = 21 = docs/08 §9.3's Boss 4 tank AC ✓; Bard S = 1 + floor(10/10) = 2 with the canon charm, 3 with the Instrument, 4 with both ✓.
- **COMBAT #54** — Cleric 100 + 7 × 22 = 254 ≥ 22 × 9 = 198 ✓; Druid dry at round 100 / (14 − 7) ≈ 14-15 ✓.
- **CONTENT #19** — 5 × 5 + 10 + 3 × 10 + 3 × 5 = 80 G to arm twelve against 60 G ✓; Chipped +3 vs Iron +5 = 60 % ("40 % of the value, rounded to 5" is the PRICE, 5 G of ~12 G — say which).
- **STAGE #27** — 131 × 2 = 262 px main boss ✓; 105 × 2 = 210 mini ✓.
- **STAGE #46** — 24 bars at 66 BPM in 4/4 = 24 × 4 × 60/66 = 87.27 s ✓.
- **SCREENS #40** — the L* table is `tools/art/cvd.py`'s output; the minimum adjacent step is 6.2 (tritanope) ≥ 5 ✓; contrast 4.81 on `SURFACE_INSET` is AA at 13 px ✓.
- **UX #60** — the cheapest seat is `Recruitment.cost_of` at `PRICE_SCALE`: 15 G (doc 11) ✓; the predicate needs `party_size(next_open_mission)`, which is 4 for A0 and 6 for TR-A3 — a guild of three with 5 G on A0's rung is insolvent, one with four is not; stated correctly.
- **SHIP** — no numbers beyond version strings.

Every price is a price, every curve a list, every lever a constant with a file. Nothing reads "tune later"; the one "measure then move" (BALANCE #9's correction rule) is a written procedure with a step size and a cap.

## Plan impact

Which unit contracts in 00-plan.md §1-§5 change, wave by wave, and whether waves 7-10 still fit 5-7 units. Rule kept throughout: one owner per file per wave (§0.1/§0.3); where a ruling named a unit that does not own the file that wave, the work is re-homed here and the ruling amended (## Amendments).

### §0.5 — the switch register, as the rulings leave it
Struck: `Formulas.SWING_PRICES_MECHANICS` (BALANCE #1/#9 — no one-tank M01 carrier exists); `Mistakes.NINJAPULL_REACHABLE` (COMBAT #58 — built, not flagged). Changed default: `Frame.REP_ICON = "sigil"` (SCREENS #31); `ScreenRouter.TRANSITION_MS = 110` (SCREENS #33); `Quirks.SPECS` = nine rows and `FLAG_DEFAULTS.legendary_quirks = true` (COMBAT #14); `Results.COMEDY_LINE = "epigraph"` (UX #36); `marks.boss.scale = 2` (STAGE #27); `Icons.WARRIOR_GLYPH = "shield"` (STAGE #42b); `GameState.COMPLETED_AT = "last_named_tier"` now resolves to tier 5 (CONTENT #2/#53). New: `Mistakes.TUTORIAL_MISTAKE_MULT = {"A0": 0.0, "TR": 0.5}` (BALANCE #57); `Loot.ADVENTURE_ROLLS_PER_SLOT = 2` (BALANCE #1b); `RaidSim.ATTRITION_GRACE_ROUNDS = 5` + per-record `attrition_grace` (COMBAT #56); `Formulas.DIFFICULTY_MULT` 1.0 / 0.75 (BALANCE #49, as planned); `TierScaling.DAGGER_OFFSET = -1` (COMBAT #17); `Audio.MUSIC_BED = true` (STAGE #46); `FLAG_DEFAULTS.achievement_rp = true` (GUILD #13); `Palette.CAUTION #E8A302` / `POSITIVE #AFEBA2` / `POSITIVE_CVD #C6DDF1` (SCREENS #40); `GameState.is_insolvent()` + the walk-in (UX #60); `Encounter.title` (CONTENT #25). Unchanged as planned: `PRICE_SCALE "doc11"`, `TRY_AGAIN`, `TUTORIAL_BAND`, `EXITS_GATED false`, `FOCUS_ENABLED false`, `MANA_BURN_DRAIN_ENABLED false`, `GEAR_SURCHARGE_BP 1500`, `StartingRoster.ARMED true`, `LogPlayer.COLLAPSE`, `arena_for()`, `colourblind_safe false`, `BARD_S_DIVISOR 10`.

### Wave 7 (7 units — fits)
- **W7-PREP** — unchanged.
- **W7-STAGE** — `marks.boss.scale` = 2 on both arenas for every rank (STAGE #27) replaces "stays 1 — Q02"; `test_scene_stage.gd:602` → `Vector2(2, 2)` read from the mark; the dungeon marks clear the front rank by ≥ 24 px with a boss up to 400 px wide. Still L; if it sheds, UI-04/UI-07 go as planned.
- **W7-REPORT** — gains: `Encounter.title` (loader, beside `lesson`), TR's title "The Doorman" in `encounters_tutorial_t1.json`, the boss-plate title line in RaidView (CONTENT #25, S — A6); `Results.COMEDY_LINE = "epigraph"` with the eyebrow "The notice said:" (UX #36, S); the three lesson strings as UX #39 words them, `lesson_report` on TR's wipe branch only, its last sentence dropped (A4). Size M → M+ (its own shed line — UI-39's rally line, UI-24 — stands).
- **W7-SIM-EFFECTS** — CONTENT-12's line becomes the BUILD branch: no `NINJAPULL_REACHABLE`, the type never flagged unreachable, `test_mistake_lines.gd`'s skip not written (COMBAT #58). `run()`'s `ninja_pulled` honouring stays W8-SIM-BALANCE's. Unchanged otherwise; L.
- **W7-SAVE** — `active_run` gains `difficulty_mult` (default 1.0) and `ninja_pulled` (default "") inside the dict (BALANCE #49, COMBAT #58, UX tension 7) — no top-level key, `FROZEN_SHAPE` holds (A10). Gains ownership of `data/achievements.json` and `sim/core/Achievements.gd` for GUILD #13's reputation branch + `FLAG_DEFAULTS.achievement_rp = true` + `GameState.gd:2920`'s flag read, and CONTENT #10's "Pocket Change" record replacing `econ_upgrade_one` (A9); `test_achievements.gd` is already its file. M → M+.
- **W7-AUD-AMB** — a fifth bed `amb_dungeon` (STAGE §7.1); `BEDS`: dungeon → its own, `stage_town` → `amb_camp` by design; `--check` 5 AGREE. M.
- **W7-DOCS** — gains ownership of `docs/12` and `docs/00` VS7 (SCREENS #32; nobody else owns docs/12 in waves 7-10; docs/00 §4.4 stays W10-CREDITS's). Rows it now carries beyond the plan's list: BL-103 amended (STAGE #27), Q-98 (i)/(iii) amended and docs/13 §12.4's twelfth hook (STAGE #46), docs/02 §9.1 as "one plate, six dressings" (A2's table), docs/13 §8.3/§13/§15.1/§14 (SCREENS #40), §12.2 row 1 and §11.4 (SCREENS #33), §13.2 (UX #34), §5 S11/S13/S16 + §13.1 (UX #51/#64), docs/03 §6.1 row + §7's Established/Legendary cells + §5.4 signed (GUILD #13/#20), docs/02 §4.3 column struck + §8.1 post-1.0 + §11 note (GUILD #15/#21), docs/04 §10 and docs/14 §5 (GUILD #50), docs/09 §14.3 / docs/11 §6.3 tags (GUILD #59), docs/11 §3 "gold (G)" + §8.3/§4.2 S3 struck (CONTENT #47, BALANCE #48), docs/02 §7-8 + docs/11 §9-10 "post-1.0 (Q-13)" (CONTENT #10), docs/08 §5.3 DECIDED-for-1.1 (COMBAT #54), docs/07 §7.3 grace row + docs/10 §6 (COMBAT #56), docs/07 §3/§7.3 Retreat (UX #51), docs/01 OQ-6 + §6.1 fee row (UX #6), docs/14 §12 status line (SHIP §7.1), docs/13 §15.1 Language/glyph rows (SHIP #65/§7.1), Q-13/Q-14 "§9's line not taken" (Canon), BL-22/BL-40/BL-42/BL-96/BL-98 states, the `#q-31` duplicate anchor (BALANCE #11). docs/08 §9.3a and docs/10 §8/§9.1's tables are NOT drafted here — they are W8-SIM-BALANCE's in wave 8 with the rows (BALANCE's own ordering: docs commit, then data commit, inside one unit). Size M → **L**.

### Wave 8 (7 units — fits; three L)
- **W8-SIM-BALANCE** — the lever is BALANCE #1's §9.3a rule and the five rows (not "each rung's own stage with the mechanic term"); `SWING_PRICES_MECHANICS` not built; `TUTORIAL_MISTAKE_MULT` as the A0 0.0 / TR 0.5 table; SIM-12's line REPLACED by COMBAT #56 (`ATTRITION_GRACE_ROUNDS = 5`, `attrition_grace` per record, the enrage Story line; the acceptance test is `test_attrition_fires_at_the_enrage_round_plus_the_grace`) — A11; E4's M08 `every 6` (COMBAT #55); `Encounter.validate()` gains three refusals (m01 below two tanks; M05 without `effect.amount`; M08 window ≥ period); `Loot.ADVENTURE_ROLLS_PER_SLOT = 2` (adds `sim/core/Loot.gd` + its test to ownership); `run()` honours `opts.difficulty_mult` and `mstate.ninja_pulled` (provisions unapplied, the Critical at round 1, `consumables_unspent` reason `ninja_pulled`); the curve into the baseline header — on-ramp bands at 45 (FAIL), raid bands at 55 (WARN until wave 9) — A3; the titles for A3 / E3 / E4 / E5 in the three encounter files it already rewrites (A6); **`tools/playtest.gd` moves to this unit's ownership** with BALANCE's farm rule and a local stage predicate (no `RaidPlan.gd` edit — that file is W7-PREP's and unowned in wave 8) — A12. Order inside the wave: **W8-ITEMS's regeneration commits before this unit's sweep-and-rebaseline commit** (the daggers, off-hands and loaners move raid_entry AC/damage; a rebaseline on the pre-ITEMS tree would be stale at the close and §0.2 forbids the orchestrator re-running it). Still L.
- **W8-CRISIS** — L, as the plan sized it "if the page bought the solvency floor": the walk-in (UX #60 — `is_insolvent()`, the board's sixth card at 0 G, the Board and Town sentences), the break roll at Depart on the seeded `break` channel writing its result into `active_run.ninja_pulled` (COMBAT #58), the banner/modal/after-state, the rank-up callout (now also on a board claim — GUILD tension 3), `start_attempt`'s `difficulty_mult`. `tools/playtest.gd` leaves this unit; its `_can_still_make_progress` line becomes one handoff line calling `GameState.is_insolvent()` (A12).
- **W8-KEYS** — gains: SCREENS #40's palette/CVD change (`game/ui/Palette.gd` + `tests/unit/test_palette_cvd.gd` added to its ownership and struck from wave 8's untouched list — A8); SCREENS #33's 110 ms fade in `ScreenRouter._load_into_host` with the `reduced_motion`/`shot.gd` zero (A7 — ScreenRouter.gd is this unit's in wave 8; the docstring line W10-DELETE carried is done here); the two W8-AUD-OPT handoff lines (the `ui.quill` bind in `RaidView._append_line`, the "Audio — the scribe" row); the Forgiving row as a cycle chip (UX #38). M → M+ (four S). `test_options_layout.gd:370`'s count moves twice this wave (Forgiving, the scribe) — one commit.
- **W8-ITEMS** — its L branch, now certain: the twenty words + leather column + `WORD_COLUMN.monk/.rogue → "leather"` and the regeneration (CONTENT #2); the eight `display_name`s (CONTENT #3; the `name_pending`/`_pending/` fallbacks NOT built); the three off-hands, the four starters at +3/+4/+4/+4 Mana and the seven loaners (CONTENT #19); the daggers +5/+7 and `DAGGER_OFFSET −1` through `gen_items.gd` (COMBAT #17); `GEAR_SURCHARGE_BP 1500` (BALANCE #7); the family tag (CONTENT #18); the 68 `note` jokes (the two dagger notes per Spirit 2). Guards its goldens against `ARMED` (the loaners equal the floors — asserted) and hands `--drift` to W8-SIM-BALANCE. L.
- **W8-SCALE-1** — gains the RaidPrep card sub-line "title over kind when present" (CONTENT #25, S — A6). M.
- **W8-FACILITY** — the four tent states and the four rank props as planned, plus SCREENS #22's walker and lantern-flame counts for ranks 0-3 and the Unknown-hides-the-banners rule; the Known pennant on a pole beside the Board callout, not on the tent (A2). M+.
- **W8-AUD-OPT** — **the quill only** (a), S: `ui.quill` in `gen_sfx.py`, the Voice-bus `HOOKS` row, the two handoff lines to W8-KEYS, `test_audio_binds.gd`. The lute (b, L) moves to W10-BUFFER (A13). Wave 8 then carries three L (SIM-BALANCE, CRISIS, ITEMS), not four.

### Wave 9 (7 units — fits; heavy, with a stated shed order)
- **W9-TIERS** — the long branch only (CONTENT #53): the §9.3a rule for tiers 2-5's Adventures through `TierScaling`, the tier 2-5 titles through `gen_items.gd`'s encounter table (A6), COMBAT #55's three rules on every carrier, the raid bands asserted at 55, the top-down `pending` valve, `FLAG_DEFAULTS.legendary_quirks = true` (W9-QUIRKS's handoff), CONTENT-23's slope walk, BL-22's Tier 2 WARN line (BALANCE #16). The no-words branch, `data/_pending/`, the README "smaller game" line: not built. L.
- **W9-KITS** — L−: the Bard (Q-46, divisor 10, the canon charm as its Tier 1 Mana), Monk Guard, Rogue Front, Mage Raid Spell Buff (COMBAT #52), the one-line `WRONG_SONG` hook call for W9-QUIRKS; the dagger half is gone (W8-ITEMS's). Shed order: the Mage buff → W10-BUFFER; Guard and Front never.
- **W9-SCALE-2** — unchanged, plus one handoff line into `RaidView._line_effects` for STAGE #29's fight emote. M.
- **W9-ART** — L (the busts run — CONTENT #3 named them): `WARRIOR_GLYPH = "shield"` + `test_icons.gd:122` (STAGE #42b); `EMOTE_MAP` as two tables and `FUMBLE_BY_SEVERITY` (STAGE #29/#42e); the fallen pose, 24 px emotes, silhouettes, `log_reputation.png`; and — because it owns `stage_camp.json` this wave — the rank 4-5 dressings (statue + braziers at Renowned; second tent, guards, crest pennants, 4 braziers at Legendary) and the `at_risk`/`cleared` bubbles (A2). Its Town.gd mood lines go to W9-POLISH by handoff.
- **W9-POLISH** — `REP_ICON = "sigil"` and the tooltip (SCREENS #31); the Town mood pick (STAGE #29's three lines, by handoff); DESIGNER-37's "goes by" styling NOT done (CONTENT #37); the title render sites are NOT here (A6). Still L; shed order UI-11, UI-29, UI-06 as planned.
- **W9-REVIEW** — M (CONTENT #8): the page as planned, then the reviewing agent's keep/rewrite/cut pass in-wave, `--apply`, BL-53 and the comedy gate closed as reviewed-under-delegation; the NINJAPULL lines are NOT deleted (COMBAT #58); docs/07 §5.6 (the quirk table) and docs/06 §4.5-4.7 amendments by its docs handoff.
- **W9-QUIRKS** — bought, M: the nine `SPECS` rows, `test_quirks.gd`, the reworded `reads_as` and the Mage rename in the nine legendary files; the orchestrator's one `write_goldens.gd` run at the close covers it and W9-KITS together, as planned.

### Wave 10 (5 units + buffer — fits)
- **W10-EXPORT** — gains `PROVENANCE.md` ownership: the eight `Answer:` lines and the `Signed:` lines on the designer's behalf, `test_export.gd:403-416` deleted the same commit and replaced by the "every row signed and answered" assertion (SHIP #4); version `1.0.0`, `application/copyright` (SHIP #5); `README-player.txt`'s disclosure sentence, the gamepad sentence, the SmartScreen two clicks, "English only"; step 9 prints the zip's SHA-256 (SHIP #65/#66/§7.1). M.
- **W10-README** — the SHA-256 in the release commit and BUILD_STATE; the budget line with "measured on the second machine"; "provenance recorded". S.
- **W10-DELETE** — adds the two "Lv." branches (A1) and the `glyph_set`/`language` rows and keys (SHIP); the Voice row is NOT deleted (STAGE #46); the Raid Group remnants as planned; the `TRANSITION_MS` docstring line is already W8-KEYS's. S-M.
- **W10-CREDITS** — the seven lines as SHIP #5 words them with line 4 reading "Sound and music …" while `Audio.MUSIC_BED` is true (A5); docs/00 §4.4.2's disclosure bullet. S.
- **W10-WALK** — unchanged; records the SmartScreen prompt. S.
- **W10-BUFFER** — first item: the generated lute (STAGE #46 (b), L — `gen_music.py`, two loops, `BEDS_MUSIC`, `MUSIC_BY_SCENE`, `test_audio_music.gd`; W10-BUFFER already owns `Audio.gd` for the beds); then W9-POLISH's sheds and the Mage buff if W9-KITS shed it. If the buffer is consumed before the lute lands, the lute is "cut for 1.0, in writing" (STAGE's own shed line) and W10-CREDITS' line 4 drops "and music".

### Does it fit?
Wave 7: 7 units (2 L, 4 M+, 1 M). Wave 8: 7 units (3 L, 3 M+, 1 S). Wave 9: 7 units (3 L, 1 L−, 2 M, 1 M). Wave 10: 5 + the buffer (1 M, 4 S; the buffer carries one L). Every wave is inside 5-7; no wave carries more L than wave 6 did. The two joins the plan already scheduled (wave 8's lever-and-price close; wave 9's one regeneration) absorb the rulings' joins, with one new sequencing rule in wave 8 (W8-ITEMS's regeneration before W8-SIM-BALANCE's rebaseline commit).

## Amendments

Each: the row · the ruler's ruling · the amended ruling · why. Everything not listed here stands as ruled.

**A1 — §6 #21 / #68 (GUILD #21, SHIP #68) — the "Lv." branches.**
Ruled: GUILD deletes the two dormant `Lv. N` branches in W10-DELETE; SHIP keeps them dormant so the reference `--fixture` stays byte-identical.
Amended: GUILD's ruling stands — both branches are deleted in W10-DELETE; SHIP #68 is amended to "the fixture keeps its four levels as data and they no longer render; `diff_all.sh` is re-recorded at the wave-10 close with before/after numbers in W10-README's entry"; spec 00 §2.3's "shows `Lv. N` only when `level > 0`" becomes "no level is shown" (W7-DOCS).
Why: W10-DELETE's contract is "the last 'not in this version' branch is gone"; a card that can print a stat the game never sets is a second truth; the design wins over the reference's UI. The re-record is a close step the orchestrator runs anyway.

**A2 — §6 #22 / #42c (SCREENS #22, STAGE #42c) — the town's rank dressings.**
Ruled: SCREENS' six-row table (walkers 0-5, lanterns 6/8/10/10/+2/+4 braziers, props: Known banners + a pennant ON THE GUILD TENT; Respected notice post; Established cobbles + pennant line; Renowned second tent + two guards; Legendary statue + crest pennants), ranks 4-5 in W9-TIERS. STAGE/plan: pennant Known, post Respected, path Established, statue Renowned, in W8-FACILITY.
Amended: one table. Walkers and lantern counts per rank as SCREENS wrote them. Props: **Unknown** the two shipped banner props hidden · **Known** the banners return + one pennant on a pole beside the Board callout (never on the tent — the tent's cloth banner is facility L2's, STAGE #42c) · **Respected** the notice post; the pennant takes the crest colour · **Established** the cobble band + the pennant line of 5 · **Renowned** the statue + 2 brazier flames · **Legendary** the second guild tent, the two `knight_unlabelled` guards, crest pennants on all four tents, 4 braziers. Ranks 0-3 in W8-FACILITY (as sized); ranks 4-5 in **W9-ART** (it owns `stage_camp.json` in wave 9 per STAGE #29; W9-TIERS does not). docs/02 §9.1 rewritten to this table by W7-DOCS.
Why: two tables were dressing one tent by two keys, and the statue had two ranks; the plan's four props are already sized into W8-FACILITY and the statue is the natural Renowned prop (a town that has heard of you); Legendary gets the additions that read as a guild with a second hall and guards — the prestige rank's picture.

**A3 — §6 #9 (BALANCE #9) — the morale the curve is measured at; the raid arm of the correction rule.**
Ruled: every band at all-Common, morale 45, 200 seeds; "too easy → `target_rounds` +2, two steps at most" on every rung.
Amended: the on-ramp bands (A0, TR, A1, A2, A3) at **45** — the fresh guild, as ruled; the raid bands (E1-E5) at **55** — `balance_sweep.gd`'s `REFERENCE_MORALE`, docs/08 §9.2's costing morale and the state BALANCE #1's own farm rule delivers to the raid door. The band values are unchanged (E1 ≥ 90 · E2 ≥ 80 · E3 75 (60-90) · E4 60 (45-75) · E5 40 (30-55)). The morale-45 raid cells are printed beside the bands as the "after a wipe" reading, never asserted. For E1-E5 the too-easy arm takes at most ONE step in 1.0 (+2 rounds, HP re-derived, the row prints the HP moved), after COMBAT #55's mechanic lever has landed; a residual is a 1.0.x row. The on-ramp keeps BALANCE's two-step rule.
Why: a raid curve at a morale the loop never delivers would fire the +2 arm on E3/E4 to chase the wrong reference (today's baseline at 55 reads E3/E4 100/100); docs/08 §9.2's costed lengths are the designer-facing "how long a boss fight is" and a 27 % HP inflation on a sweep's say-so is not a number the designer would recognise as theirs. Q-90's arithmetic (P(≤ 3) = 78 % at 40 %) is morale-independent and survives.

**A4 — §6 #39 (UX #39) — TR's `lesson_report`.**
Ruled: "This is the Wipe Report. Who, what, and which round — it is all under 'By raider'. You will be seeing a lot of it."
Amended: "This is the Wipe Report. Who, what, and which round — it is all under 'By raider'."
Why: BALANCE #9's TR notice ends "Read the Wipe Report; you will be seeing a lot of it." and UX #36 prints the notice as the epigraph on the same page; one punchline, once. The notice keeps it (it is read first, on the Board).

**A5 — §6 #5 (SHIP #5) — credits line 4.**
Ruled: "Sound generated in-house from physical models".
Amended: "Sound and music generated in-house from physical models" while `Audio.MUSIC_BED` is true; "Sound generated in-house from physical models" if the lute sheds (A13). W10-CREDITS reads the constant before it writes the line.
Why: STAGE #46 brings the generated lute in; SHIP's own tension #2 said the line follows the audio ruling.

**A6 — §6 #25 (CONTENT #25) — where the titles land.**
Ruled: the `title` field, the T1 data and the two render sites in W9-POLISH; tiers 2-5 in W9-TIERS.
Amended: `Encounter.title` + TR's "The Doorman" + the RaidView boss-plate title line → **W7-REPORT** (it owns `Encounter.gd`, `encounters_tutorial_t1.json` and `RaidView.gd` in wave 7 and is adding `lesson` to the same loader); A3 / E3 / E4 / E5's titles → **W8-SIM-BALANCE** (it rewrites those three files' rows in wave 8); the RaidPrep card sub-line → **W8-SCALE-1** (owns `RaidPrep.gd` in wave 8; the field exists from wave 7); tiers 2-5 → **W9-TIERS** through `gen_items.gd` (as ruled); `test_encounters.gd`'s title assertion → W8-SIM-BALANCE. W9-POLISH carries none of it. SCREENS #45's wording becomes "no creature names from the references; the loop's 21 role titles land through spec 00 §2.3's own `title` field".
Why: in wave 9 `RaidPrep.gd` is untouched, `RaidView.gd` is W9-SCALE-2's and `Encounter.gd` has no owner (§0.3: a file not in a wave's table is untouched that wave). The words, the field and the sites are unchanged.

**A7 — §6 #33 (SCREENS #33) — where the fade lands.**
Ruled: `ScreenRouter.TRANSITION_MS = 110` in W9-POLISH.
Amended: in **W8-KEYS** (it owns `game/core/ScreenRouter.gd` and `test_a11y.gd` in wave 8; the t=0-insertion and `reduced_motion` → 0 ms pins go in `test_a11y.gd`); W10-DELETE's docstring line is done there and struck from W10-DELETE. The ruling's content (opacity only, ease-out, inserted at t=0, 0 under `reduced_motion` and `shot.gd`, no cross-fade, no slide, `ui.tab` on the first frame) is unchanged.
Why: `ScreenRouter.gd` is on wave 9's untouched list.

**A8 — §6 #40 (SCREENS #40) — the palette's owner.**
Ruled: W8-KEYS edits `Palette.CAUTION`/`POSITIVE` and the CVD functions.
Amended: the same, with `game/ui/Palette.gd` and `tests/unit/test_palette_cvd.gd` ADDED to W8-KEYS's ownership and struck from wave 8's "untouched" list; the art-gate baselines re-record at the wave-8 close (orchestrator), asserting the delta is inside the 0.07 jitter.
Why: the plan lists Palette.gd as untouched in wave 8; the ruling is right and needs the file.

**A9 — §6 #13 and #10 (GUILD #13, CONTENT #10) — the achievement edits' owner.**
Ruled: GUILD lands the reputation branch, the flag and the two `rp: 15` lines in W7-DOCS; CONTENT lands "Pocket Change" in "W8-ITEMS's data half or W10-DELETE".
Amended: both land in **W7-SAVE** — `FLAG_DEFAULTS.achievement_rp = true`, `GameState.gd:2920`'s flag read, `Achievements.claim_grant()`'s reputation branch + `rp_cap` wiring, `describe_reward()`'s dead clause deleted, `rost_one_of_each`/`rost_comfortable` `rp: 15`, and `econ_upgrade_one` → `econ_first_ten` "Pocket Change" (type economy, `items_sold` 10, the same five potions) — with `data/achievements.json` and `sim/core/Achievements.gd` added to W7-SAVE's ownership; the docs/03 §6.1 row and Q-69's close stay W7-DOCS's; the acceptance tests (claims pay 15; board RP ≤ 15 % over the scripted run; the note never prints; the record wall names no absent feature) in `test_achievements.gd`, already W7-SAVE's.
Why: W7-DOCS's contract is "no game file"; neither W8-ITEMS nor W10-DELETE owns `achievements.json`; W7-SAVE owns `GameState.gd` and `test_achievements.gd` and already touches `check_achievements()`.

**A10 — §6 #49 and #58 (BALANCE #49, COMBAT #58) — what the replay needs.**
Ruled: the attempt record stores `difficulty_mult`; the break roll fires at Depart on a seeded channel.
Amended: `active_run` (W7-SAVE) carries `difficulty_mult` (default 1.0) and `ninja_pulled` (raider id or ""), written by `record_attempt` from the run options; Continue's re-run and Results' stored-seed replay pass both back into `RaidSim.run(..., opts)`; no top-level save key (a dict entry — `FROZEN_SHAPE` holds). The attempt record in `raid_history` carries the same two keys.
Why: UX #6/#51's commit rule makes the replay a pure function of what was stored; a break roll that depends on "the mission already has an attempt this cycle" is state the replay cannot reconstruct unless its result is stored (UX tension 7); the toggle likewise.

**A11 — §6 #56 (COMBAT #56) and the plan's W8-SIM-BALANCE SIM-12 line — the grace is load-bearing.**
Ruled: COMBAT names the grace 5; the plan's W8-SIM-BALANCE line says "attrition at `round_no >= ROUND_CAP` — the +5 gone".
Amended: the plan's SIM-12 line is replaced by COMBAT #56 (`RaidSim.ATTRITION_GRACE_ROUNDS := 5`, `attrition_grace` per record, validator `>= 0`, the enrage Story line; `test_attrition_only_at_the_round_cap` becomes `test_attrition_fires_at_the_enrage_round_plus_the_grace`). BALANCE #1's register row adds one clause: "derived under an attrition grace of 5 rounds past enrage (COMBAT #56)".
Why: recomputed in ## Numbers — A2's mean kill clock at 45 is 13.1 rounds against an enrage of 14; without the grace the tails fail and the on-ramp bands cannot hold. The two batches are consistent only together.

**A12 — §6 #1 tension 1 (BALANCE) — the harness.**
Ruled: `tools/playtest.gd` (W8-CRISIS's file) gains the farm rule and a `RaidPlan.stage_of(party, db)` predicate.
Amended: `tools/playtest.gd` moves to **W8-SIM-BALANCE**'s ownership in wave 8 (the unit whose 8/8 gate depends on it and which runs it in its loop); the stage predicate is a local function in `playtest.gd` (start / adventure / raid by `source` on worn items and an armed main hand) — no `RaidPlan.gd` edit (W7-PREP's file, unowned in wave 8); W8-CRISIS's `_can_still_make_progress` extension becomes one handoff line calling `GameState.is_insolvent()` (UX #60), applied at the close. Farm runs are attempt rows; the 5-attempt cap applies only to the rung being sized; before E2-E5 the harness re-runs cleared raid rungs to `raid_entry`.
Why: W8-CRISIS is already L with the walk-in and the break roll; the farm rule is the balance gate's own precondition; one owner per file.

**A13 — §6 #46 (STAGE #46) — where the lute lands.**
Ruled: W8-AUD-OPT runs (a) the quill (S) + (b) the lute (L); if the wave sheds, the lute sheds first.
Amended: W8-AUD-OPT = the quill only (S); the lute is **W10-BUFFER's first item** (L — `gen_music.py`, two loops, `BEDS_MUSIC`, `MUSIC_BY_SCENE`, `Audio.MUSIC_BED`, `test_audio_music.gd`), cut for 1.0 in writing if the buffer is consumed before it lands. The ruling's content (Karplus-Strong, A minor pentatonic, the phrase walker, Unknown rubato / Known + frame drum at 66 BPM, −34 dBFS RMS, the camp family and the main menu only) is unchanged.
Why: with the walk-in, the break roll and the L item branch, wave 8 would carry four L units; wave 10 is the light wave by design and W10-BUFFER already owns `Audio.gd` for the beds. The shed order STAGE wrote is applied up front rather than at the wave's close.

**A14 — §6 #9's TR/A3 rows vs #57 (BALANCE, internal) — one clause.**
Ruled: TR is sized at 0.5 (BL-91's coupling answered) and TR and A3 share 183 HP / 8 × 2 with A3 carrying three mechanics to TR's one.
Amended: no number moves; BALANCE #9's row states that A3 is EXPECTED to land under TR on the first pass (three mechanics at the same budget) and that its band (40-60) and the too-hard rule (swing −1, then the ½-pulse rule on M02/M03) are the answer — so the first sweep's A3 figure is not read as a wall.
Why: the two rungs' identical budgets with different mechanic density is the one place the rule's output will surprise a reader; the row should pre-empt it.

Fourteen amendments change a unit, a wave or a sentence; none changes a canon number or reverses a ruling's intent. Every ruler's ruling stands in substance.

## Tensions (for the orchestrator)

Not contradictions — things the rulings leave for a wave to prove, and the risk if it does not.

1. **The first 200-seed pass will not match the curve on the first try, by design.** BALANCE's rows are derived under docs/08 §8.8's full tax; the sim's site-weighted roll (BL-21) taxes DPS less, so A1/A2 should land above band and A3 below TR (A14). The correction rule exists for exactly this; W8-SIM-BALANCE's commit message must show each step, and the loop must not read the first table as "BALANCE was wrong".
2. **Wave 8's inner order is now load-bearing.** W8-ITEMS's regeneration (daggers, off-hands, loaners) → W8-SIM-BALANCE's sweep + rebaseline → the close's playtest on the joined tree with the farm rule. Two units, one sequence; the plan's §0.4 line "the orchestrator runs the playtest AFTER W8-ITEMS's price change" becomes three clauses.
3. **Wave 9 is at capacity with four L-ish units** (TIERS, KITS L−, ART, POLISH). Shed order across the wave: W9-POLISH's UI-11/UI-29/UI-06 → W10-BUFFER first; the Mage buff second; the rank 4-5 dressings (A2) third — none of the three is on the shippable bar. W9-QUIRKS and W9-REVIEW do not shed (the quirks are on the roster's face; the review is the comedy gate).
4. **W10-BUFFER now has a first item (the lute).** If the release wave is on track it is the right use of slack; if wave 9 shed into the buffer, the lute is the first thing cut, in writing, and the credits line follows (A5). The buffer must not become a seventh unit.
5. **`e5_legendaries_raid.json` is touched by W8-ITEMS (names), W9-KITS (kits) and W9-QUIRKS (quirks).** The plan's single orchestrator `write_goldens.gd` at the wave-9 close covers the last two; W8-ITEMS's `display_name` edit moves no golden (a string) — assert it.
6. **The Q-13 / Q-14 departures from §9's "next three".** Both stand (## Canon); docs/15 must record them as "§9's recommendation not taken, because canon's own line is conditional" rather than close them silently, or a later reader will call them reversals.
7. **The walk-in at Respected+ is a Common on a board canon says has none.** Accepted on the ruler's reading; if a player report ever calls it a bug, the fix is the card's line, not the rule.
8. **COMBAT #17's daggers are a sidegrade after the fix, not an upgrade.** The two item notes (W8-ITEMS) carry the reading; if the playtest's Rogue column shows the Boss 1 dagger never equipped by BL-32's "Suggested", that is correct behaviour (the shared sword wins) and not a loot bug.
9. **BALANCE #11's payday spec is post-1.0 and must stay so** — W7-SAVE's freeze has no `payday_misses` key; a 1.1 that lands upkeep bumps to v18. Nothing in waves 7-10 may cite the wages as live.
10. **SHIP #4's signature is the loop's reading of the evidence, on the record.** The `Signed:` lines must keep the words "the build loop for the lead designer … under the 2026-09-15 delegation" — the designer overrules a row by rewriting its line; nothing in the tree may shorten that to a bare name.
