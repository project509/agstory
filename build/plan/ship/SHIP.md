# SHIP — shipping audit (saves, export gates, settings, keyboard, hygiene, README)

Reporter: SHIP (read-only recon, 2026-09-15, during wave 5). Tree: master @ 567ec1b + wave-5 working changes.

## Executive summary

1. **Shippable today:** the save service (v16, six-step migration chain walked against seven committed fixtures, `.bak`, `content_version` reconciled, rank canary, damaged-slot readout, Continue), the settings file, the export SCRIPT (eight gated steps, launch-checked), doc hygiene (unique docs/15 ids, gated links) and keyboard reachability (a11y_smoke green on 26 mounts). Twelve audit rows in this area describe a tree that no longer exists — SHIP-01/08/09/10/11 close them.
2. **Not shippable today:** `export_build.sh` HOLDS on 312 names (304 Tier 2-5 items + 8 Legendaries) that only the designer can supply; there is no exported .exe in the tree; no provenance/originality gate exists (docs/15 Q-21, docs/16 X3.7); the campaign is not completable (M6-BAL-04, a canon ruling).
3. **A fresh install is broken on a small laptop:** the window is 1536x1024 windowed and nothing applies `window_mode`/`vsync` (SHIP-06) — the bottom 256 px (Settings' Apply/Back, LoadSave's Back, RaidPrep's bench) are off a 1366x768 desktop. M (wave 6).
4. **The three biggest gaps:** (i) the designer's five ship-gating rulings — names, provenance, credits, the morale-45 wall, the clear-rate target (SHIP-04/17/18); (ii) the release path — fresh-profile boot test, window mode, packaging with OFL texts and a player README, a second-machine walk (SHIP-05/06/16/19); (iii) the last four docs/14 §7.1 save rows and the mid-replay quit that loses the wipe report (SHIP-02/03/15), one v17 bump.
5. **Keyboard:** the InputMap exists (M6-A11Y-01 is stale) but five of §13.1's eleven rows have no consumer and no screen has an `_unhandled_input`; F1 Codex is bound to a screen that does not exist; two rulings in `q-a11y-keys.md` never reached docs/15 (SHIP-07/20, wave 8).
6. **Perf:** the latency budget IS a gate stage (WARN by design); W5-MOUNT brings all 14 screens under 140 ms with Boot's warm-up; the budget is stated nowhere a reviewer reads and has never been measured on low-end hardware (SHIP-09).
7. **README** is right about the pin and the discipline and wrong about what runs ("no tutorials", "art pass mid-flight"), lists 6 of 10 gate stages, and says nothing a player needs (SHIP-12, wave 10).
8. **Wave order this report proposes:** 6 = close stale rows, build the provenance gate, fix the window, re-export `--dev` from a clean profile, hand the designer the five-ruling page; 7 = the v17 save bump (pending_deltas, log_tail, best_rounds, active_run-as-replay, dismiss autosave); 8 = the keyboard rows and the soft-lock invariant; 9 = polish (Load/Save rail, freeze note); 10 = release: zip, README, second-machine walk, the gate green or the exact HOLD list.
9. **Counts:** 20 findings — 12 CONFIRMED (SHIP-07 refutes the audit's framing and confirms the remainder), 5 REFUTED (stale audit rows: SHIP-01/08/09/10/11, closing twelve audit ids), 3 BLOCKED-designer (SHIP-04/17/18; 17 also carries an S tree fix). Sizes: 13 S, 6 M, 0 L/XL; nothing in this area needs more than an agent-afternoon once the rulings exist.
10. **Bar today:** 3 of 16 checks green (saves round-trip, settings persist, docs/15 ids), 4 amber, 9 red; 5 of the 9 reds are one-wave tree work, 4 are the designer's.

## Findings

### SHIP-01 — The save service is further along than the audit says: six M3-SAVE rows are done in the tree
**Status:** REFUTED (audit rows M3-SAVE-01/-02/-05/-07/-08/-10 and the "no fixture, no step" half of M6-SAVE-01 describe a tree that no longer exists).
**Evidence:**
- Town-transition autosave: `game/core/GameState.gd:492-502` `TOWN_SCREENS` (nine screens); `:531-544` `_on_screen_changed()` autosaves on a town path and `save_now()`s on the menu path; `:517-523` `_ensure_router_connected()`. Tests `tests/unit/test_savegame.gd:406` (a town transition autosaves), `:421` (a run of transitions cannot evict the encounter autosave), `:457` (menu and RaidView spend no rotation entry), `:473`, `:487` (quit to menu writes the manual slot).
- `played_seconds`: `GameState.gd:546-555` `_accrue_played_time()`, `PLAYED_SECONDS_PER_TRANSITION_CAP := 600.0` (`:510`); `SaveGame.gd:587` `played_words()`; tests `test_savegame.gd:514`, `:538`.
- Migration chain: `game/core/SaveGame.gd:77-84` registers steps 10..15 (`_v10_to_v11` .. `_v15_to_v16`, `:327-389`); `OLDEST_MIGRATABLE := 10` (`:63`) vs `SAVE_VERSION := 16` (`GameState.gd:87`); `.bak` at `:391-401`. Fixtures `tests/fixtures/saves/v10_sample.json` .. `v16_sample.json` (seven). Tests `test_savegame.gd:223` (a step for every version), `:281` (backed up before touched), `:726` (a fixture per readable version), `:737` (oldest loads clean, backed up first), `:753` (every fixture loads into a live campaign), `:768` (each fixture carries exactly its version's keys).
- `content_version` read back: `SaveGame.gd:269-275` compares the header to `CONTENT_VERSION` and calls `reconcile_content()` (`:425`), which moves item/class/encounter orphans to a body-level `orphaned[]`; tests `:830`, `:858`, `:880`, `:893`, `:910`.
- Rank canary: `SaveGame.gd:502` `rank_canary()`, called at `:277`; tests `:927`, `:946`, `:961`.
- Shape freeze: `test_savegame.gd:667-684` `FROZEN_AT_VERSION := 16` + `FROZEN_SHAPE`, asserted at `:711`.
- Damaged slot: `SaveGame.gd:523-560` `slot_summaries()` marks `damaged` with the reason; test `:634`.
**Doc:** docs/14 §7.1-§7.4 — satisfied except the rows in SHIP-02/03. Two path deviations are unrecorded: fixtures live in `tests/fixtures/saves/` not §7.3's `tests/data/saves/`, and the steps live in `SaveGame.gd` not `game/autoload/migrations/` (`SaveGame.gd:65-74` says why; "the write-up was lost with the plan file that held it"). No docs/15 row.
**Fix:** Close the six audit rows (`actual_status: done`, evidence above) so `audit_stale.py` stops ranking them; one BL- row in docs/15 (or a paragraph in docs/14 §7.3) recording the two path deviations. No code.
**Owns:** `build/plan/audit.json`, `docs/14-technical-architecture.md` §7.3, `docs/15-open-questions.md` (one BL- row).
**Size:** S.
**Wave:** 6 — hygiene first, so the later waves plan against the true queue (the 92 open count is overstated by at least these six).
**Audit:** closes M3-SAVE-01, M3-SAVE-02, M3-SAVE-05, M3-SAVE-07, M3-SAVE-08, M3-SAVE-10; M6-SAVE-01's fixture/chain half.

### SHIP-02 — Four of docs/14 §7.1's persisted blocks are still absent: `active_run`, `log_tail`, `best_rounds`, `pending_deltas`
**Status:** CONFIRMED.
**Evidence:** `grep -rn "active_run\|log_tail\|best_rounds" --include=*.gd .` returns nothing. `to_dict()` (`game/core/GameState.gd:2601-2666`) emits 47 keys, none of the four. `_pending_departures` (`GameState.gd:1726`) is appended at `:1691` at the end of `_resolve_day_tick()` and drained at `:1673` only on the NEXT tick, cleared by `reset()` at `:640`, absent from `to_dict()` and from `FROZEN_SHAPE` (`tests/unit/test_savegame.gd:668`). `last_result` (`GameState.gd:418`) is nulled by `reset()` (`:643`), set at `:962`, never serialised; `game/screens/Results.gd:161,586,638,791,1009` all read it, so Results cannot be re-entered after a reload.
**Doc:** docs/14 §7.1 rows `active_run`, `log_tail` (N=200, "lets the post-mortem survive a reload"), `pending_deltas` ("so quitting between encounters cannot eat a consequence"), progress `best_rounds`. docs/15 Q-53 DECIDED "resume from the stored seed and round index" — see SHIP-03.
**Fix:** One SAVE_VERSION bump (16 -> 17, `MIGRATIONS[16]` stamp, `tests/fixtures/saves/v17_sample.json`, FROZEN_SHAPE) carrying three keys:
(a) `pending_deltas` — serialise `_pending_departures` (raider ids + reason); test: queue a departure, round-trip, the next tick still applies it. (S)
(b) `log_tail` — the last 200 rendered PLAY_BY_PLAY lines plus the Results header facts (encounter id, cleared, rounds, loot ids), per save not per slot (no docs/15 row — record as BL-). (M)
(c) `best_rounds` — `Dictionary` encounter_id -> fewest rounds to clear, written in `record_attempt`; the Records tab can read it. (S)
**Owns:** `game/core/GameState.gd`, `game/core/SaveGame.gd`, `tests/unit/test_savegame.gd`, `tests/fixtures/saves/v17_sample.json` (+ `tests/fixtures/make_save_fixtures.gd`), `docs/14` §7.1 (a note per row), `docs/15` (BL- row).
**Size:** M (S + M + S).
**Wave:** 7 — after the wave-6 queue reconciliation, before the wave-9 format freeze; one bump for all three keys keeps the fixture count down.
**Audit:** M3-SAVE-09 (remaining blocks), M6-SAVE-01 (freeze half), relates to M3-SAVE-03.

### SHIP-03 — The attempt seed is never persisted, but the save-scum window is already closed by construction; what a mid-replay quit actually loses is the wipe report
**Status:** CONFIRMED (narrower than M3-SAVE-03 states).
**Evidence:** `game/screens/RaidPrep.gd:872` draws `seed_value` via `_state.next_raid_seed()`, `:876` runs `RaidSim.run(...)` synchronously, `:881` calls `_state.record_attempt(...)`, which autosaves (`GameState.gd:1022`) — all before RaidView plays a line. A reload therefore cannot reroll a resolved attempt (docs/07 §9 row 1 holds). What IS lost: Esc during the replay pops RaidView (`game/core/ScreenRouter.gd:198-206`, `back()`), a further Esc/quit reaches the menu, `last_result` is unsaved (SHIP-02), so Continue lands on Town with no Results and no wipe report, and the loot was already assigned.
**Doc:** docs/14 §7.1 `active_run`; docs/15 Q-53 ("resume from the stored seed and round index") presumes an incremental sim; the tree's sim is a pure function replayed by RaidView, so "resume" means "replay from the stored seed". `build/plan/handoff-a11y-keys.md` §9 already asks what Esc means mid-raid and nobody ruled.
**Fix:** In SHIP-02's v17 bump add `active_run = {encounter_id, master_seed, party_ids, resolved}` written by `record_attempt` and cleared when Results is dismissed; on Continue with `active_run.resolved`, `MainMenu.gd:376-383` / `LoadSave.gd:492-498` route to Results after re-running `RaidSim.run` with the stored seed (deterministic). Rule Esc mid-replay: recommended default — skip to the post-mortem, never to the menu; `RaidView._unhandled_input` claims `ui_cancel` (handoff §9's snippet). Note on Q-53's row.
**Owns:** `game/core/GameState.gd`, `game/screens/RaidPrep.gd`, `game/screens/RaidView.gd`, `game/screens/MainMenu.gd`, `game/screens/LoadSave.gd`, `tests/unit/test_savegame.gd`, `docs/15` (Q-53 note).
**Size:** M.
**Wave:** 7 (same bump as SHIP-02).
**Audit:** M3-SAVE-03 (re-scope), M3-SAVE-09.

### SHIP-04 — The export gate holds on exactly 312 names the designer must supply: 304 Tier 2-5 item names and 8 Legendaries
**Status:** BLOCKED-designer (the gate is correct; the content is the block).
**Evidence:** `tools/export_build.sh:78-82` `PENDING=("stats_pending:true" "shippable:false" "name_pending:true")`; a fresh grep over `data/` (this session) finds ZERO `stats_pending:true` and ZERO `shippable:false`, and `name_pending:true` in 16 files: `data/items_t{2,3,4,5}_{adventure,raid}.json` (28 + 48 items per tier, every one pending — 304 names; placeholders read `TIER2 Adventurer's Helm`, `TIER2's Boots`) and `data/legendaries/{bard,cleric,druid,mage,monk,rogue,warrior,wizard}.json` (8; `shaman.json` is already named). `tests/unit/test_export.gd:347` pins the three flags. `sim/content/ContentDB.load_all()` mounts only named tiers (BUILD_STATE HANDOFF gap 1), so the campaign ends at tier 1 and S17 cannot fire in play.
**Doc:** docs/14 §5.4 assertion 10; docs/03 §5.6 forbids inventing Legendary names; docs/15 BL-69 (tier 5 unnamed). Canon is silent on tier 2-5 material/title words (audit M5-T25-08, blocked-needs-human).
**Fix:** Nothing in the tree. Hand the designer a naming sheet: `tools/gen_items.gd` already generates the tables from a material/title word list, so the ask is ONE word-pair table per tier (material x slot title, ~16 words per tier) plus 8 Legendary names; re-run `gen_items.gd` (verify stage 2 proves it) and the gate clears. Ship option if the names never come: a `--tier1-only` release that strips the pending files from the pack (a `.gdignore`-style exclusion in `export_presets.cfg` `exclude_filter`) and ends the campaign at tier 1 with Completion's copy saying so — a designer ruling, not a loop task.
**Owns:** `data/items_t2..5_*.json` (via `tools/gen_items.gd`), `data/legendaries/*.json`, `docs/15` (BL-69 close).
**Size:** S in the tree once names exist (a generator run); the designer's afternoon otherwise.
**Wave:** 6 — put the naming sheet in the designer's hands at the start so the names can land by wave 9's release export.
**Audit:** M5-T25-08, BL-69; relates to M6-EXP-02 (done).

### SHIP-05 — There is no exported .exe in the tree and no fresh-install boot test; the launch check runs from the build directory's `user://`, never from a clean profile
**Status:** CONFIRMED.
**Evidence:** `ls build/exports` -> "No such file or directory" (deleted 2026-09-14 with the whole of `build/`, BUILD_STATE "Open risks"). `tools/export_build.sh:210-226` step 7 launches `AGuildStory.console.exe --headless --quit-after 180` and greps for errors — it boots against whatever `%APPDATA%/Godot/app_userdata/A Guild Story/` already holds on the dev machine (settings.cfg, saves), so a first-run path (no settings file, no saves, Continue hidden, the tutorial prompt) is never exercised by the gate. No test or script sets `--user-data-dir`/a temp `user://`. `tests/unit/test_savegame.gd:109` proves the SUITE never writes the player's dir, but nothing proves the EXE boots from an empty one.
**Doc:** docs/14 §10.1 ("an export that is not launched has not been verified"); docs/16 M6 ship polish. No docs/15 row for a fresh-install gate.
**Fix:** In `export_build.sh` step 7, pass Godot's `--user-data-dir <tmp>` (or set `application/config/use_custom_user_dir` + a temp `custom_user_dir_name` for the check only) so the launch check boots from an EMPTY profile; add a step 7b that boots a second time with a copied `tests/fixtures/saves/v10_sample.json` in the temp `saves/` and greps the boot log for the migration line (proves the shipped chain on the packed build). Re-run `./tools/export_build.sh --dev` so a playable .exe exists again (BUILD_STATE says it is the recipe); commit nothing under `build/exports` (gitignored).
**Owns:** `tools/export_build.sh`, `tests/unit/test_export.gd` (pin the flag), `docs/14` §10.1 (one row).
**Size:** S.
**Wave:** 6 (the .exe should exist for every later wave's playtests) and re-run in 10 as the release export.
**Audit:** M6-EXP-02 (extend), M6-FINAL-* (release checklist).

### SHIP-06 — The window is 1536x1024 windowed with no fullscreen/borderless applied: on a 1366x768 or 1280x720 laptop the commit row is off-screen on first launch
**Status:** CONFIRMED.
**Evidence:** `project.godot` `[display]` sets `window/size/viewport_width=1536`, `viewport_height=1024`, `window_width_override=1536`, `window_height_override=1024`, `stretch/mode=canvas_items`, `aspect=keep` and NO `window/size/mode` (Godot's default 0 = windowed). `GameSettings.DEFAULTS` (`game/core/GameSettings.gd:45-48`) declares `"window_mode": "borderless"`, `"resolution": "native"`, `"vsync": true`, and `grep -rn "window_mode\|vsync\|DisplayServer\.\|MODE_FULLSCREEN\|MODE_WINDOWED" game/` finds no consumer — only `apply_display_aspect()` (`:292-296`) touches the Window, and only for letterboxing. `game/screens/Settings.gd:101-102` shows the V-sync row disabled with "Display options arrive with the export build." and has no `window_mode`/`resolution` row at all (18 rows for 20 keys). A 1536x1024 window on a 1366x768 desktop (Windows' most common laptop mode) is clipped by 256px at the bottom. Measured on the fixture sheet (`build/shots/all/fixture/*.png`, rows y 768-1024): Settings' Save/Load, Back, Restore defaults and Apply buttons (x 1170-1500, y 800-960), LoadSave's Back (y ~985), RaidPrep's four Bench buttons and the strip pager (y 900-990), and Results' fallen strip plus the bottom of the What-happened log all sit in the clipped band — unreachable by mouse, and the Tab ring reaches them only blind.
**Doc:** docs/13 §15.1 row "Resolution / window mode / vsync | Native, borderless | 14"; docs/14 has no display section (the row points at it, it does not exist — an ownership gap, no docs/15 row). docs/14 §11 targets "60 fps at 1920x1080 on integrated graphics".
**Fix:** In `game/ui/Boot.gd` beside `_apply_display_aspect()` add `_apply_window_mode()`: read `window_mode` (`borderless` -> `DisplayServer.WINDOW_MODE_FULLSCREEN` with borderless flag, `windowed` -> windowed sized to fit the screen: `min(1536, usable.x)` at 3:2 with `canvas_items` scaling) and `vsync` -> `DisplayServer.window_set_vsync_mode`. Add the `window_mode` row to `Settings.ROWS` (Borderless / Windowed), enable the V-sync row, add `F11`/`Alt+Enter` as a `nav_fullscreen` action in `project.godot` handled in `ScreenRouter._unhandled_input`. Set `project.godot` `window/size/mode=3` (fullscreen) as the default so a fresh install never opens a window larger than the screen even before Boot runs. Test: `tests/unit/test_settings.gd` asserts the row exists and coerces; `tools/shot.gd` unaffected (SubViewport). Write the display defaults into docs/14 as a new §10.4 (the row docs/13 points at).
**Owns:** `game/ui/Boot.gd`, `game/core/GameSettings.gd`, `game/screens/Settings.gd`, `game/core/ScreenRouter.gd`, `project.godot` `[display]`/`[input]`, `docs/14` (new §10.4), `tests/unit/test_settings.gd`.
**Size:** M.
**Wave:** 6 — a fresh-install blocker; the .exe from SHIP-05 is unplayable on a small laptop without it.
**Audit:** no row (new — file as M6-EXP-07).

### SHIP-07 — docs/13 §13.1's keyboard map: the InputMap exists (M6-A11Y-01 is stale) but five of the eleven rows have no consumer, and no screen has an `_unhandled_input` at all
**Status:** REFUTED in part (the audit's "zero input handlers and zero InputMap actions" is false), CONFIRMED for the remainder.
**Evidence:** `project.godot` `[input]` declares `nav_toggle` (Space + joy X), `nav_prev_subject` (Q), `nav_next_subject` (E), `nav_cycle_filter` (F + joy Y), `nav_codex` (F1 + View), `nav_sort_1..4` (1-4), `nav_settings` (joy Start), and restates `ui_accept`/`ui_cancel` with joypad A/B. `game/core/ScreenRouter.gd:198-206` `_unhandled_input` consumes `ui_cancel` (back; Settings from the town, `:185-188`), `nav_codex` (`:222-226`, inert — `CODEX_SCENE` does not exist, S16 was never built) and `nav_settings`. `grep -rn "nav_toggle\|nav_prev_subject\|nav_next_subject\|nav_cycle_filter\|nav_sort_" game/` -> zero consumers; `grep -n "_unhandled_input\|_gui_input" game/screens/*.gd` -> nothing. `tests/unit/a11y_smoke.gd` (gate stage 5) proves on 13 screens x 2 states that focus starts inside the screen, Tab leaves the rail and returns, arrows step the rail — i.e. every control IS reachable by Tab/arrows/Enter, so the blocking row ("every screen operable with no mouse") holds. What does NOT hold, row by row: `1`-`4` sort on list screens (Tavern/Guildhall roster/Market have no sort mode to bind — no `_sort` symbol in any of the three), `1`-`4` speed on S11 (`RaidView.gd:611` `_speed_button` is a Tab stop only), `Q`/`E` while a detail panel is open (`RaiderDetail.gd` has no prev/next), `F` cycle filters (Guildhall Records has `_record_filter` chips at `:596`, reachable by Tab, not by F), `Space` pause on S11 (`RaidView.gd:618` `_pause_button` Tab stop only; Space on a focused Button presses it, so Space only pauses while the Pause button holds focus), `F1` Codex (no S16). `build/plan/q-a11y-keys.md` holds two rulings (Tab = group step vs control step, behind `Frame.tab_steps_within_region`; Space vs ui_accept) that were never merged into docs/15 (grep `tab_steps_within_region` docs/15 -> nothing).
**Doc:** docs/13 §13.1 (eleven rows), §13.2 gamepad (PROPOSED, OQ-12 on whether it blocks 1.0). Missing docs/15 rows: the two in `q-a11y-keys.md`. Recommended defaults: keep Tab = group step (the doc's), Space = press-the-focused-row.
**Fix:** One unit, per-screen handlers using the handoff's pattern (`build/plan/handoff-a11y-keys.md` §3): RaidView `_unhandled_input` — `nav_toggle` pause/resume, `nav_sort_1..4` speed 1x/2x/4x/Instant (respecting `skip_unlocked`), `ui_cancel` -> skip to post-mortem (SHIP-03); RaiderDetail — `nav_prev_subject`/`nav_next_subject` step the roster; Guildhall Records — `nav_cycle_filter` steps `RECORD_FILTERS`; Tavern/Market — `nav_cycle_filter` where a filter exists, otherwise nothing (sort keys need a sort mode that does not exist: drop `1`-`4` on list screens from §13.1 by a BL- row rather than build a sort nobody designed). Codex: rule S16 out of 1.0 (BL- row) and delete `nav_codex` or keep it inert — recommend delete, an inert F1 is a dead key. Merge the two `q-a11y-keys.md` rulings into docs/15. Add a `tests/unit/test_a11y.gd` case per binding (simulate the action with `Input.parse_input_event` on a mounted screen).
**Owns:** `game/screens/RaidView.gd`, `game/screens/RaiderDetail.gd`, `game/screens/Guildhall.gd`, `game/screens/Tavern.gd`, `game/screens/Market.gd`, `game/core/ScreenRouter.gd` (codex), `project.godot` `[input]`, `tests/unit/test_a11y.gd`, `docs/13` §13.1 (amend), `docs/15` (three BL- rows).
**Size:** M.
**Wave:** 8 — after SHIP-03 rules Esc mid-raid (wave 7); before the wave-9 keyboard walkthrough.
**Audit:** M6-A11Y-01 (re-scope to the five rows), M6-A11Y-02 (done), relates to Q-53/handoff §9.

### SHIP-08 — `text_scale` is live: M6-A11Y-05 is stale
**Status:** REFUTED.
**Evidence:** `game/ui/Theme.gd:48-72` reads `settings.get_value("text_scale")` through `Type.step()`; `game/core/GameSettings.gd:207` `scaled()`; `tests/unit/test_text_scale.gd` and `tests/unit/test_text_scale_layout.gd` exist; `game/screens/Settings.gd:90` row live with an empty reason; the `text150` contact sheet (`build/shots/all/text150/`) was shot 2026-09-15. Remaining 150% debts are the wave-4 follow-ups BUILD_STATE names (Guildhall sidebar clips the morale chart; Completion's CTA wraps to three lines) — W5-SCALE owns them this wave (`build/plan/report-W5-SCALE.md`).
**Doc:** docs/13 §4.4, §13 "Text scaling 100/125/150, layouts reflow by reducing rows, never by truncating".
**Fix:** Close the audit row; carry W5-SCALE's leftovers (if any) as its own wave-6 item.
**Owns:** `build/plan/audit.json`.
**Size:** S.
**Wave:** 6.
**Audit:** closes M6-A11Y-05.

### SHIP-09 — The latency budget IS a perf test (stage 8/8, WARN-only) and W5-MOUNT brings every screen under 140 ms with Boot's warm-up; M6-JUICE-07 is stale
**Status:** REFUTED (the row), CONFIRMED that the stage is WARN-only by design and must stay so.
**Evidence:** `tools/verify.sh:251-289` stage "8/8 latency budget" runs `tools/perf_probe.gd` (mount and mean frame per screen, `! mount > 140` rows) and prints WARN, never FAIL, because the laptop's clock varies ~5x between boosting and base (`:263-268`). `build/plan/report-W5-MOUNT.md:131-150`: with `--warm` 0 of 14 rows over budget (MainMenu 27 ms .. Guildhall 72 ms; frames 0.6-1.8 ms mean); cold (no warm) 6 of 14 still over. `build/plan/handoff-W5-MOUNT.md` §1-§3 asks Boot to call `router.warm()` + `SceneStage.warm()` behind its overlay and verify.sh to pass `--warm`; whether it has been applied is the orchestrator's call this wave.
**Doc:** docs/13 §12.1 (140 ms settled, 16.6 ms frame); docs/14 §11 targets (60 fps at 1080p on integrated graphics; "measure on a real low-end machine, not the dev workstation") — no such measurement exists, and the probe measures a 1536x1024 SubViewport, not a 1080p/1440p window.
**Fix:** (a) Close M6-JUICE-07; confirm the W5-MOUNT handoff landed (Boot warms; verify.sh `--warm`). (b) Keep WARN, but make the budget STATED where a reviewer reads it: BUILD_STATE's build-status line and README's stage table carry "mount <= 140 ms warm, frame <= 16.6 ms, on the reference laptop boosting (calibration loop <= 10 ms)". (c) One manual run of the exported .exe on the lowest machine available with `--perf` printing the same rows at the native window size (a `--window` flag on perf_probe that measures the real Window instead of the SubViewport) before wave 10; if none is available, the README's system requirements say so.
**Owns:** `tools/perf_probe.gd` (`--window`), `tools/verify.sh` (`--warm`), `README.md`, `BUILD_STATE.md`, `build/plan/audit.json`.
**Size:** S.
**Wave:** 6 (close + state the budget); 10 (the low-end run).
**Audit:** closes M6-JUICE-07; M6-JUICE-06 (done).

### SHIP-10 — The doc-hygiene rows DW-A1/A2/A3/A4 are all done and gated; docs/15 has no colliding ids today
**Status:** REFUTED (all four rows).
**Evidence:** `grep -oE '^### (Q|BL)-[0-9]+' docs/15-open-questions.md` -> 20 `### Q-` and 67 `### BL-` headings; the second series was renumbered into the BL- namespace (docs/15 §10 "BL - the build-loop decision log", `:661`). Table rows `<a id="q-NN">` -> 75, no duplicate anchors (`uniq -d` -> 0). The only repeated BL headings are `### BL-27` / `### BL-27 resolution` (`:931`/`:995`), BL-30 (`:1083`/`:1181`), BL-32 (`:1133`/`:1234`), BL-34 (`:1287`/`:2147`) — resolution follow-ups, not collisions. `tests/unit/test_docs_links.gd:163` `test_file_links_resolve`, `:186` `test_link_fragments_resolve`, `:210` `test_prose_doc_filenames_exist`, `:323` `test_every_register_citation_resolves_to_a_declared_entry` gate the class. `grep -rn "07-raid-simulation.md\|13-ui-presentation.md\|07-combat-and-encounters.md" docs/` -> one hit, docs/13:869 OQ-10, which is prose recording the historical filenames. `grep -n "00-index" docs/*.md` -> nothing (DW-A4's docs/03:30 sentence is gone).
**Doc:** n/a.
**Fix:** Close the four rows. Optional S: the four `### BL-NN resolution` headings could be `#### ` sub-headings so a `### ` grep counts entries; not needed.
**Owns:** `build/plan/audit.json`.
**Size:** S.
**Wave:** 6.
**Audit:** closes DW-A1, DW-A2, DW-A3, DW-A4; M6-FINAL-04 (done).

### SHIP-11 — Settings persist correctly (Apply, Restore defaults, Back and window close all write `user://settings.cfg`); the four audio keys are live — M6-AUD-02 is stale
**Status:** REFUTED (M6-AUD-02); the "settings persist" bar is green.
**Evidence:** `game/core/GameSettings.gd:30` `PATH := "user://settings.cfg"` (separate from `user://saves/`, `tests/unit/test_settings.gd:150`); `:133-155` `load_from_disk()` treats a missing file as a first run and a file with no recognised key as corrupt-and-rewrite (`:129`, `:143` tests); `:157-167` `save_to_disk()` on Apply/quit + `NOTIFICATION_WM_CLOSE_REQUEST`. `game/screens/Settings.gd:656` (Restore defaults), `:666` (Apply), `:717-721` (`_on_back` — "leaving the screen is a quit") all call `save_to_disk()`. Audio: `Settings.gd:103-110` four bus rows with empty reasons; `game/core/Audio.gd` `apply_levels()` maps each key to its bus (`project.godot` `[audio] buses/default_bus_layout`); tests `test_settings.gd:325,337,347,355`, `tests/unit/test_audio.gd`.
**Doc:** docs/13 §15.1 ("Written on Apply and on quit, never on every keystroke").
**Fix:** Close M6-AUD-02. One residue: the Settings row notes for music and voice say "The bus is here and waiting; no music is written yet" / "nothing is voiced yet" (`Settings.gd:105-106`, `:109-110`) — true, and they will ship as-is unless M6-AUD-05 (blocked-designer: who composes) is answered; if no music ships in 1.0, the two rows should say so plainly rather than "yet".
**Owns:** `build/plan/audit.json`; `game/screens/Settings.gd` (two strings, wave 10 if M6-AUD-05 stays open).
**Size:** S.
**Wave:** 6 (close); 10 (the two strings).
**Audit:** closes M6-AUD-02; relates M6-AUD-05.

### SHIP-12 — README.md is accurate on the pin and the discipline but stale on what runs, the gate's shape, and everything a player (not a developer) needs
**Status:** CONFIRMED (M6-DOC-01 correctly closed as "exists"; accuracy is the gap).
**Evidence:** `README.md:22-26` "Content is Tier 1 only, there are no tutorials, and the art pass is mid-flight" — the three tutorials are built and reachable (BUILD_STATE HANDOFF "What is playable"; `data/encounters_tutorial_t1.json`; `tests/unit/test_tutorials.gd`) and the art overhaul landed in five waves (BUILD_STATE Current focus). `README.md:74-84` lists a 6-row gate (class cache, 0/5 lint .. 4/5 sweep); `tools/verify.sh:58-269` has ten stages (0/8 lint, 1/8 parse, 2/8 generated content, 2b/8 generated art, 3/8 tests, 4/8 boot, 5/8 keyboard access, 6/8 sweep, 7/8 playtest, 8/8 latency) — a11y_smoke, the playtest, the latency probe and both generator checks are missing from the README table, and `--fast` is described as skipping "boot and the sweep" (`:70`) while verify.sh also skips playtest and latency. `README.md:160` "the grep gate docs/14 §3.5 proposes is not written" — `tools/export_build.sh:96-108` step 2/8 IS that grep. `README.md` never mentions `tools/export_build.sh`, `play.cmd`'s relationship to the exported .exe, where saves live (`%APPDATA%\Godot\app_userdata\A Guild Story\saves\`), the keyboard map, the three save slots + 3-deep autosave rotation, system requirements, or a licence (`ls LICENSE*` -> none; `export_presets.cfg:77` `application/copyright=""`, `:74` `company_name="A Guild Story"`). `tests/unit/test_readme.gd` pins the pin, the prerequisites and the cited paths (`:49-95`), so a stale sentence is not a red build.
**Doc:** docs/16 M6 ship polish; docs/14 §10.3 (README as the place three facts live).
**Fix:** Rewrite three blocks: (1) "What runs today" from BUILD_STATE's HANDOFF paragraph (tutorials, Records, Completion, audio, keyboard, CVD, saves v16) and drop "art pass mid-flight"; (2) the gate table to the ten stages with each stage's sentinel, `--fast` = stages 0-3; (3) a new "Playing the exported build" section: `./tools/export_build.sh --dev` -> `build/exports/AGuildStory.exe`, the NOT SHIPPABLE stamp and why (312 names), saves and settings paths, the keyboard map (from docs/13 §13.1 as shipped after SHIP-07), the display defaults (SHIP-06), minimum spec (docs/14 §11: integrated graphics of the last ~4 years, 1366x768 minimum once SHIP-06 lands). Add `tests/unit/test_readme.gd` cases: README names every verify stage header (`grep '^hdr "'`), names `export_build.sh`, and does not contain "there are no tutorials". Decide the licence and copyright line (designer — see Questions).
**Owns:** `README.md`, `tests/unit/test_readme.gd`, `export_presets.cfg` (copyright/company), `LICENSE` (if any).
**Size:** S (M with the licence question).
**Wave:** 10 — the README describes the shipped tree, so it is written last; the test cases can land in 6.
**Audit:** M6-DOC-01 (reopen as "accurate"), M6-FINAL-03 (handover summary).

### SHIP-13 — No solvency invariant: the playtest marks a guild "stuck" only at attempt time, not after a rest or a hire
**Status:** CONFIRMED (partial; the audit's 2026-09-15 verification still holds).
**Evidence:** `tools/playtest.gd:146` run row carries `"stuck": ""`; `_mark_stuck` is called at `:165`, `:209`, `:222`, `:229` inside the attempt loop and `_stuck_on(s, enc)` at `:231`; `grep -rn "_can_still_make_progress" tools/ tests/ game/ sim/` -> nothing. The three solvency conditions (roster >= next mission's party size; gold >= the cheapest hire with a live candidate; sellable inventory >= 60 G) are not asserted anywhere; `GameState.rest_until_recovered()` reasons `"limit"`/`"stalled"` (`GameState.gd:1416`) are not treated as a dead end. In play, the real guard is the UI: `tests/unit/test_full_loop.gd:200-224` proves every screen offers an enabled button, and full disband is disabled until onboarding completes.
**Doc:** docs/01 §6 (failure cost), docs/05 §6.3 (disband warnings), docs/15 C-28 (failure cost absent from canon). No row on "what happens when a guild cannot afford a hire and cannot field a party" — the player's only recourse is New Guild.
**Fix:** (1) `_can_still_make_progress(state, db) -> String` in `tools/playtest.gd`, called after every attempt, rest and hire; WALL lines name the condition. (2) In the game, one sentence on the Adventure's Board when the roster is below the next rung's party size and gold is below the cheapest hire: "You cannot field a party and cannot afford a recruit — sell gear at the Market or start a new guild." (docs/13 §7: the reason is printed). (3) A test in `tests/unit/test_full_loop.gd` that constructs that state and asserts the sentence. Whether a soft-locked guild gets a bail-out (a free Common recruit, a loan) is a designer question — recommended default: no bail-out, the sentence, and New Guild.
**Owns:** `tools/playtest.gd`, `game/screens/AdventureBoard.gd`, `tests/unit/test_full_loop.gd`, `docs/15` (one Q- row: bail-out or not).
**Size:** M.
**Wave:** 8 — after M6-BAL-04 is ruled (the morale-45 wall makes every playtest "stuck" today, so the invariant would only report the known wall until then).
**Audit:** M6-PLAY-02; relates M6-BAL-04.

### SHIP-14 — Load/Save lights the "Options" rail item and prints each empty slot's state twice; Continue and the damaged-slot readout are correct
**Status:** CONFIRMED (small polish); the Continue/damaged bars are green.
**Evidence:** `game/screens/LoadSave.gd:142` `Frame.build(self, {"nav": _nav(), "active": "options", ...})` — the rail highlights Options while the panel is titled Guild Slots (fixture sheet `build/shots/all/fixture/LoadSave.png`, rail x 0-210, y 190-215 lit; panel title at x 240, y 120). Each empty slot prints "Slot 1 — empty" (row title, y 185) AND "Slot 1 is empty." (the blocked reason under the buttons, y 270) — `LoadSave.gd:26-27` says the reason is printed once for Load and Delete, which it is, but the title already says it. Continue: `game/screens/MainMenu.gd:136` `SaveGame.newest_loadable()`, `build/shots/all/empty/MainMenu.png` shows Continue disabled with "No saved guild found." (x 130-450, y 1550-1600 on the composed sheet; y 775-800 at 1:1) — correct. Damaged: `SaveGame.gd:545-556` and `test_savegame.gd:634`. Destructive presses confirm inline (`LoadSave.gd:340-345` "Yes — overwrite Slot N" / "Yes — delete Slot N").
**Doc:** docs/13 §7 (reasons are printed), docs/14 §7.2 (three slots, three autosaves each). No row on whether Load/Save is a building or an Options sub-page.
**Fix:** Give the rail a `saves` id when the screen is Load/Save (or no active item), and drop the empty-slot reason line when the title already reads "empty" (keep it for the damaged case, where the reason carries information). Keep the rest. Close M3-SAVE-04's remaining half at the same time: docs/13 §5's screen inventory has no row for Load/Save (it stops at S17) and docs/15 has no BL- row for its placement behind Options — add the S18 row and the BL- row (the propagation BL-73 did for S17).
**Owns:** `game/screens/LoadSave.gd`, `game/ui/Frame.gd` (`nav_items` — one entry), `tests/unit/test_screens.gd` (the LoadSave text assertions), `docs/13` §5 (one row), `docs/15` (one BL- row).
**Size:** S.
**Wave:** 9 (polish).
**Audit:** M3-SAVE-04 (partial -> done with this).

### SHIP-15 — Two small holes in the autosave contract: `dismiss_raider` never autosaves (docs/14 §7.4's "raider fired" row is half-wired) and the transition write skips `check_achievements()`
**Status:** CONFIRMED.
**Evidence:** `game/core/GameState.gd:2299-2310` `dismiss_raider()` removes the raider and applies `peer_dismissed` to the ledger with no `autosave()`; `hire()` autosaves at `:2279` with the §7.4 citation. `_autosave_transition()` (`:566-577`) and `save_now()` (`:2987-2990`) write the state without `check_achievements()`; only `autosave()` (`:2972-2973`), `record_attempt` (`:1019`) and the legendary-departed path (`:1703`) evaluate. BUILD_STATE "Watch" states the rule ("a mutation path that does not go through `autosave()` must call `check_achievements()` itself"). Consequence: a snapshot record earned on the Market/Guildhall lights on the NEXT raid or hire, not on the next screen; a dismissal followed by a process kill (not a clean quit) is lost together with its -2 peer morale.
**Doc:** docs/14 §7.4 row "Recruit hired / raider fired — rotating"; docs/05 §12 Q12 (dismissal price).
**Fix:** `autosave()` at the end of `dismiss_raider()` (test: dismiss, assert a new rotation entry and the ledger line inside it); `check_achievements()` at the top of `_autosave_transition()` and `save_now()` (test: earn a snapshot record on a screen, `goto(TOWN)`, assert `achievements_earned` in the written file).
**Owns:** `game/core/GameState.gd`, `tests/unit/test_savegame.gd`, `tests/unit/test_achievements.gd`.
**Size:** S.
**Wave:** 7 (with the other save work).
**Audit:** M3-SAVE-01 (the row's second half), M5-QAB-6 (done; this is its watch).

### SHIP-16 — The release artefact is two loose files with no packaging step: no zip, no player README, the OFL font licences are not in the pack, the console wrapper ships beside the .exe, version 0.1.0, no file logging
**Status:** CONFIRMED.
**Evidence:** `tools/export_build.sh:229-234` step 8 prints sizes of `AGuildStory.exe`, `.pck`, `.console.exe` and stops; nothing zips or names a release. `export_presets.cfg:44-46` `export_filter="all_resources"`, `include_filter=""` — `game/assets/fonts/OFL-FiraSans.txt` and `OFL-GrenzeGotisch.txt` are not resources, so the SIL OFL texts the two bundled fonts (`FiraSans-*.ttf`, `GrenzeGotisch.ttf`) require to accompany them are not in the pack and not beside the exe. `:58` `debug/export_console_wrapper=2` ships `AGuildStory.console.exe` in the release folder (needed by the launch check; a player who double-clicks it gets a console window). `project.godot` `config/version="0.1.0"`, `export_presets.cfg:72-73` `file_version`/`product_version="0.1.0.0"`, `:77` `application/copyright=""`, `:74` `company_name="A Guild Story"`. `grep -n file_logging project.godot` -> nothing, so a crash on a player's machine leaves no log to send. The .exe is unsigned (no signing step; SmartScreen will warn on first run).
**Doc:** docs/14 §10.1 ("single `.exe` + `.pck`"); docs/16 X3.7 (originality audit as a pre-export gate — see SHIP-17). No docs/15 row on version numbering, copyright line or signing.
**Fix:** A step 9 in `export_build.sh`: copy the two OFL texts and a generated `README-player.txt` (controls, saves path, known limits, the NOT SHIPPABLE stamp when `--dev`) into `build/exports/`, then zip `AGuildStory-<version>-win64.zip` from the exe + pck + texts, EXCLUDING the console wrapper (keep it for the launch check only). `include_filter="*.txt"` is the alternative for the licences (then `res://game/assets/fonts/OFL-*.txt` are in the pack and the Completion credits can print them). Set `debug/file_logging/enable_file_logging=true` and `max_log_files=5` in `project.godot` so `user://logs/` exists. Version, copyright holder and signing are the designer's (Questions). `tests/unit/test_export.gd`: the zip step names both OFL files; the console wrapper is not in the zip list.
**Owns:** `tools/export_build.sh`, `export_presets.cfg`, `project.godot`, `tests/unit/test_export.gd`, `build/exports/README-player.txt` (generated).
**Size:** S.
**Wave:** 10 (the release wave), after SHIP-05/06 in 6.
**Audit:** M6-EXP-02 (extend), M6-FINAL-* (release checklist); relates M5-END-4 (credits/attribution).

### SHIP-17 — docs/15 Q-21 / docs/16 X3.7: the originality and provenance sign-off is a pre-export gate on paper and does not exist in the export script
**Status:** BLOCKED-designer (the sign-off), CONFIRMED (the gate is absent).
**Evidence:** docs/15 `:470-476` Q-21 "Are the nine `ideaboard/` screenshots original work authored for this project? — Blocks: shipping the entire Tier 1 item corpus … recommended default: designer confirms authorship in writing, in doc 00 §4.4, before any item table ships"; docs/15 C-32 (`:627`); docs/16 X3.7 (`:402`) "the originality audit passes — six checklist categories, design-lead sign-off, manifest checked in, running as a pre-export gate"; docs/16 `:513` lists the originality audit under "Never cut". `tools/export_build.sh` steps 1-8 contain no such gate, and no manifest or signed line exists (`grep -rn "authorship\|provenance" docs/00-vision-and-pillars.md` — §4.4 has the ban, not the confirmation). The shipped plates are the designer's own (`art/ref/specs/09-background-plates.md:152-158`, sources under `ideaboard/bare backgrounds/`), which is the strongest provenance fact in the tree and is recorded only in a spec. `data/credits.json` `status` says the same three things are unsigned (names, third-party attribution, Q-21).
**Doc:** docs/00 §4.4, docs/15 Q-21 (no recommended default beyond "ask"), docs/16 X3.7.
**Fix:** In the tree (S): a `PROVENANCE.md` at the repo root (or docs/00 §4.4 appendix) with one row per shipped asset family (bare plates, reference concepts, generated sprites, fonts, audio) and a signature line, plus step 0/9 in `export_build.sh` that HOLDS unless the file carries the designer's signed line (same shape as the pending-content gate: a content gap, not a build defect; `--dev` passes it). The designer supplies: (1) written confirmation for the nine ideaboard screenshots and the six bare plates; (2) the credits roll (`data/credits.json` `lines`); (3) the copyright holder for `export_presets.cfg`.
**Owns:** `PROVENANCE.md` (new), `tools/export_build.sh`, `tests/unit/test_export.gd`, `data/credits.json` (designer), `docs/00` §4.4 (designer's line), `docs/15` (Q-21 close).
**Size:** S in the tree; the designer's sign-off otherwise.
**Wave:** 6 (build the gate so it is visibly red for four waves); the sign-off whenever it comes.
**Audit:** M5-END-4, Q-21/C-32; relates SHIP-04.

### SHIP-18 — The designer-blocked rulings that stand between the tree and a 1.0 export, consolidated (five of the seventeen `blocked-needs-human` rows are ship gates; the rest are polish)
**Status:** BLOCKED-designer.
**Evidence:** `build/plan/audit.json` `actual_status == "blocked-needs-human"` -> 17 rows. Ship-gating (the export or the campaign cannot be completed without them): **M6-BAL-04** (a new guild is pinned at morale 45 and the Adventure ladder stops 6 of 8 guilds at A2 — `tools/playtest.gd` WARN, verify stage 7; BUILD_STATE "AWAITING A HUMAN DECISION"); **M5-T25-08** (tier 2-5 names, SHIP-04); **M5-END-4** (credits, SHIP-17); **M6-BAL-03** (no target clear-rate curve, so "balance pass 2" cannot be judged — with M6-BAL-04 the two levers are the same ruling); **Q-21** (provenance, SHIP-17). Polish-gating (the game ships without them, worse): M6-AUD-05 (music — the Settings rows say "yet"), M5-COMEDY-12 (the comedy corpus has no human funny-gate), M6-A11Y-06 (band colour ramp CVD-safety — `build/plan/q-a11y-legible.md`), M6-JUICE-02 (motion table vs "the desk does not move"), M4B-CONV-04 (which arena each encounter is fought in — every fight is the cave today, spec 09 §3 "until Q18 is ruled"), M4B-ACT-04, M3-LOOP-06, M5-QAB-4, M5-END-5, Q58-1/-3, Q59-5, M6-JUICE-04 (a "do not build" ruling — already honoured). The 18 art questions are `build/plan/artaudit/00-plan.md` §6 and the decision-register artifact (memory: they live in its artifact db).
**Doc:** docs/15 (each has or needs a Q-/BL- row); BUILD_STATE Current focus.
**Fix:** One page for the designer at the start of wave 6 with the five ship-gating rulings first, each with its recommended default and the number it moves; the loop builds waves 6-9 on the defaults BEHIND SWITCHES where a switch is possible (BAL-04's four levers are costed in its row; none can be picked by the loop). Wave 10 is the release wave and cannot be green without (1)-(3) at minimum.
**Owns:** `docs/15-open-questions.md` (designer), `build/plan/audit.json`.
**Size:** designer time; S per ruling to apply.
**Wave:** 6 (the page); 10 (the gate).
**Audit:** M6-BAL-03, M6-BAL-04, M5-T25-08, M5-END-4, Q-21.

### SHIP-19 — The build machine is the only machine: the tree lives on OneDrive, the gate needs a display server for stage 8, and no second machine has ever booted the .exe
**Status:** CONFIRMED (risk, stated in BUILD_STATE; nothing in the tree reduces it).
**Evidence:** BUILD_STATE "Open risks": "The working copy is still on OneDrive … accepted risk … Flag it." `tools/verify.sh:269-275` stage 8 runs without `--headless` ("A real display server is required"), so the full gate cannot run on a headless CI; `tools/export_build.sh:171-177` Linux preset "NOT launch-checked (no Linux runner here)". `README.md:44-52` pins the engine to one absolute path under `/c/Users/greyp/...` in `tools/env.sh`. There is no record of the .exe being run on any machine but this laptop (no `build/plan/report-*` mentions a second machine; the perf probe's calibration line is this laptop's).
**Doc:** docs/14 §11 ("measure on a real low-end machine, not the dev workstation"); docs/16 X4.4.
**Fix:** Not a tree change beyond SHIP-05/06/09: a wave-10 checklist item "the zip from SHIP-16 is copied to a second Windows machine (ideally 1366x768, integrated graphics), unzipped, double-clicked, and the first-run path (New Guild -> A0 -> Results -> quit -> Continue) is walked by hand; the perf rows are read at the native window". Record the machine and the result in `docs/_log/progress.md`. Moving the repo off OneDrive stays the designer's call (memory: the folder rename is deliberately deferred).
**Owns:** `docs/_log/progress.md` (the record), `BUILD_STATE.md` (Open risks row).
**Size:** S (an hour with a second laptop).
**Wave:** 10.
**Audit:** no row (new — file as M6-FINAL-05, the fresh-install walk).

### SHIP-20 — `nav_codex` (F1) and the Settings screen's "Controller glyphs — Gamepad support is not in this build" both describe features that half-exist; ship one truth
**Status:** CONFIRMED (small).
**Evidence:** `project.godot` `[input]` `nav_codex` is bound (F1 + joypad View); `game/core/ScreenRouter.gd:222-226` `codex()` is inert because `CODEX_SCENE` does not exist (S16 never built; docs/13 §5 lists it). The same `[input]` block binds joypad A/B/X/Y/Start (`ui_accept`, `ui_cancel`, `nav_toggle`, `nav_cycle_filter`, `nav_settings`) and `game/screens/Settings.gd:114-116` disables the Controller-glyphs row with "Gamepad support is not in this build." — a pad CAN confirm, cancel, back out and open Settings today (the comment block in `project.godot` says so), and cannot move focus (`ui_up/down/left/right` carry Godot's default joypad events, so actually it can — the D-pad steps focus neighbours). docs/13 §13.2 is PROPOSED and OQ-12 asks whether gamepad blocks 1.0.
**Doc:** docs/13 §13.1 (F1 Codex), §13.2, OQ-12 (no ruling). Recommended default: S16 Codex is out of 1.0 (nothing in the tree references its content; the Records tab and the Board's copy carry what a codex would); gamepad is "works, unverified, not a supported input" for 1.0 — the Settings reason should say that.
**Fix:** Remove `nav_codex` from `project.godot` and `codex()` from the router (or keep both behind a `CODEX_SHIPS := false` const with the F1 row struck from docs/13 §13.1 by a BL- row); change the Controller-glyphs reason to "A pad steers the menus; glyphs and rebinding are not in this build." Two BL- rows (S16 out; gamepad status).
**Owns:** `project.godot`, `game/core/ScreenRouter.gd`, `game/screens/Settings.gd`, `tests/unit/test_a11y.gd` (`test_the_builtin_half_of_the_map_is_still_intact`), `docs/13` §13.1, `docs/15`.
**Size:** S.
**Wave:** 8 (with SHIP-07).
**Audit:** M6-A11Y-01; OQ-12.

## Shippable bar

A reviewer ticks these, in this order, before a 1.0 export. Evidence column = where the tick is proven today.

| # | Check | Today | Proof / gap |
|---|---|---|---|
| 1 | `./tools/export_build.sh` (no `--dev`) prints `EXPORT OK` | RED — HOLD on 16 files | SHIP-04: 304 tier names + 8 Legendary names; the gate is correct (`tests/unit/test_export.gd:347`) |
| 2 | The provenance/originality sign-off gate is green | RED — the gate does not exist | SHIP-17: build it (S), then the designer signs |
| 3 | A fresh-install boot from an empty `user://` on a second machine: New Guild -> A0 -> Results -> quit -> Continue | NOT RUN | SHIP-05 (gate from a temp profile), SHIP-19 (the second machine) |
| 4 | The window fits a 1366x768 desktop and fullscreen/borderless toggles | RED | SHIP-06 |
| 5 | Saves round-trip across the migration chain v10 -> current, `.bak` written, damaged slot listed, Continue offers the newest loadable | GREEN | `tests/unit/test_savegame.gd:223-300`, `:726-768`, `:634`, `:615-628`; seven fixtures |
| 6 | `SAVE_VERSION` frozen with a fixture at the shipped version and the four deferred §7.1 rows written into docs/14 | AMBER | SHIP-02/03: one more bump (v17) then freeze; the docs/14 note is missing |
| 7 | Every mutation is on disk before a clean quit; a resolved attempt can never be re-rolled | GREEN with two holes | `GameState.gd:531-577`, `:2972-2990`; SHIP-15 (`dismiss_raider`, transition write's achievements) |
| 8 | Settings persist across Apply / Back / Restore / window close; every row that is disabled says why | GREEN | SHIP-11; `tests/unit/test_settings.gd:116-157`, `:269` |
| 9 | A full keyboard path through every screen, with docs/13 §13.1's eleven rows either working or struck by a BL- row | AMBER | `a11y_smoke` green on 26 mounts (stage 5); SHIP-07/20: five rows unbound, F1 inert, two rulings unmerged |
| 10 | Perf stage green or WARN with a stated budget, measured once on low-end hardware at the native window | AMBER | SHIP-09: 0/14 over budget with `--warm` (W5-MOUNT), budget not stated in README/BUILD_STATE, no low-end run |
| 11 | README accurate: what runs, the ten gate stages, the export recipe, saves path, controls, minimum spec, licence | RED | SHIP-12 |
| 12 | docs/15 ids unique; every code citation to docs/15 and build/plan resolves | GREEN | SHIP-10; `tests/unit/test_docs_links.gd:163-368` |
| 13 | Release artefact: one zip with exe + pck + OFL texts + player README, no console wrapper, version and copyright set, file logging on | RED | SHIP-16 |
| 14 | The five ship-gating designer rulings are recorded in docs/15 (BAL-04, T25-08, END-4, BAL-03, Q-21) | RED | SHIP-18 |
| 15 | The playtest (stage 7) is green, not WARN — the campaign is completable | RED — WARN by design | M6-BAL-04, a designer ruling |
| 16 | Load/Save's rail state and slot copy are right; docs/13 §5 has the S18 row | AMBER | SHIP-14 |

Green today: 5, 8, 12 (and 7 with two S holes). Everything RED that is not designer-blocked is S or M and fits in waves 6-7 except the README (10) and the release step (10).

## Questions for the designer

1. **Version and copyright.** `config/version` is `0.1.0` and `application/copyright=""`. What does 1.0 ship as (1.0.0?), and whose name goes on the copyright line and in `data/credits.json`? (SHIP-16, SHIP-17, M5-END-4)
2. **Provenance sign-off.** docs/15 Q-21: are the nine `ideaboard/` screenshots and the six bare plates under `ideaboard/bare backgrounds/` your own work, or generated, and under what terms may they ship? A one-line written answer in docs/00 §4.4 unblocks the originality gate. (SHIP-17)
3. **If the 312 names do not arrive** (M5-T25-08, BL-69): does 1.0 ship as a Tier 1 game with Completion retimed to Raid 1, or does the release wait? (SHIP-04)
4. **Esc during a raid replay**: skip to the post-mortem (recommended), or back to the prep board and lose the report? And on reload after a mid-replay quit: land on Results (recommended, replayed from the stored seed) or Town? (SHIP-03, Q-53)
5. **Fullscreen default**: borderless fullscreen on first launch (recommended, docs/13 §15.1's "Native, borderless"), or windowed sized to the desktop? (SHIP-06)
6. **The keyboard map's dead rows**: strike `1`-`4` sort on list screens and `F1` Codex (S16) from 1.0, or build a sort mode and the Codex? Recommended: strike both by BL- rows; keep `1`-`4` as raid speeds and `Space` as pause. (SHIP-07, SHIP-20)
7. **Gamepad for 1.0** (docs/13 OQ-12): "works, unverified, unsupported" (recommended) or a supported input with glyphs? (SHIP-20)
8. **Soft-lock policy**: when a guild cannot field a party and cannot afford a hire, is there a bail-out (a free Common, a loan) or only the sentence and New Guild? Recommended: no bail-out. (SHIP-13)
9. **Code signing**: an unsigned .exe trips SmartScreen on every first run. Buy a certificate for 1.0, or ship unsigned with a README note? (SHIP-16)
10. **Music** (M6-AUD-05): if nothing is composed for 1.0, may the two Settings rows say "No music in this release" instead of "yet"? (SHIP-11)

## Coverage

**Read:** BUILD_STATE.md (all), BACKLOG.md M6 section and the M5 tail, docs/14 §7 (all four subsections), §9.3, §10.1, §11; docs/13 §13-§13.2 and §15.1's display row; docs/15 §4 Q-21/Q-29/Q-53, the heading/anchor census, §10's BL headings; docs/16 M6 rows, X3.x gates, R-5/R-11, the cut list; README.md (all); `tools/export_build.sh` (all), `tools/verify.sh` stages 7-8, `export_presets.cfg`, `project.godot` (all), `.gitignore`; `game/core/SaveGame.gd` (:55-425, :502-627), `game/core/GameState.gd` (:10-60, :300-320, :418-660, :860-1030, :1660-1740, :2280-2320, :2601-2680, :2870-3018), `game/core/GameSettings.gd` (all), `game/core/ScreenRouter.gd` (:185-240), `game/screens/Settings.gd` (:60-130, :590-725), `game/screens/LoadSave.gd` (:1-175, :300-440), `game/screens/MainMenu.gd` (:130-140, :360-385), `game/screens/RaidPrep.gd` (:870-882), `game/screens/Completion.gd` (:125-145), `game/ui/Boot.gd` (:95-120), `game/screens/RaidView.gd` (grep only); `tests/unit/test_savegame.gd` (test list + freeze block), `test_export.gd`, `test_settings.gd`, `test_readme.gd`, `test_docs_links.gd` (test lists), `tests/unit/a11y_smoke.gd` (:1-60, :119-135); `tests/fixtures/saves/`; `data/items_t*.json`, `data/legendaries/*.json` (counted), `data/credits.json`; `build/plan/audit.json` (all 178 ids; full text of the 36 rows in this area); `build/plan/handoff-a11y-keys.md` §3/§9, `q-a11y-keys.md`, `report-W5-MOUNT.md`, `handoff-W5-MOUNT.md` (headings); `art/ref/specs/09-background-plates.md` §3 table. Sheets: `build/shots/all/fixture/{RaidPrep,Results,Settings,LoadSave}.png` bottom band (y 768-1024), `fixture/LoadSave.png`, `empty/MainMenu.png`, `focus/_sheet_1.png` at 1/2 scale.

**Did not get to:** `tools/playtest.gd`'s `_stuck_on` body (took the audit's 2026-09-15 verification at its word); `game/core/Audio.gd` beyond grep; `tests/unit/test_a11y.gd`'s assertions; the `text150`, `reduced`, `emoji`, `wide-*`, `tabs`, `raid-*` sheets (other reporters' areas); the migration steps' fixtures byte-by-byte; `tools/perf_probe.gd` past its header; `docs/14` §4/§5.4 assertion list against `ContentDB`; whether the W5-MOUNT handoff (Boot warm-up, verify.sh `--warm`) has been applied by the orchestrator in this wave; a run of anything (read-only; the engine is held by wave 5). No Godot process was started.
