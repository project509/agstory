# handoff-W6-LOG — edits in files W6-LOG does not own

Applied by the orchestrator at the wave's close (the docs/15 row by W6-LEDGER, which owns docs/15 this wave). Each heading is one edit: an `old:` block and a `new:` block; `python tools/apply_handoff.py build/plan/handoff-W6-LOG.md --dry-run` parses it.

No exact edits in unowned files this wave. The two docs/15 rows this unit owes (UI-23's reversal of spec 02 §2.1; CONTENT-13's `LogPlayer.COLLAPSE` switch, §0.5's list) are written out in `build/plan/q-W6-LOG.md` for W6-LEDGER to number and paste; spec 00 §2.7's switch register gains `LogPlayer.COLLAPSE = true` from the same text.

Applied in-wave by this unit, for the record: `handoff-W6-COPY.md` §2 (the boss plate's display-name line) — `RaidView._boss_plate` already printed `_encounter.display_name` alone (the name Label and the sigil's tooltip) and no kind word, so there was nothing to change; re-checked at the unit's close.
