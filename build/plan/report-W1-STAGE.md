# Report — W1-STAGE (the stage gets its scale, its marks, its overlay layer and its feathered light)

Owned files: `game/ui/SceneStage.gd`, `game/assets/scenes/stage_*.json`, `game/screens/RaidView.gd`
(ONLY `ARENA_OFFSET`, `PARTY_RANKS`, `BOSS_X`, `boss_feet_y`, `_party_layout`, `_stage`),
`tests/unit/test_scene_stage.gd`, `tools/art/preview_scene.py`. Handoff: `build/plan/handoff-W1-STAGE.md`.

Written as each item finished (rule 3).

## Acceptance (00-plan §W1-STAGE, verbatim, as boxes)

- [x] A1 RaidView shot: twelve figures >= 70px tall with feet inside the party mark
  - Shot build/shots/W1STAGE_RaidView.png (16:2x, this agent) + crop _w1stage_crop_party.png at 3x: twelve 2x figures on the lower-left lantern floor in three ranks, facing the bridge. Rendered height = alpha-bbox of the frame x2 (every class strip's bottom alpha row is its last row, so the feet sit exactly on the mark): min 72 (druid/rogue) .. max 96 (warrior) px, all >= 70.
  - On the shot itself: at every one of the twelve mark points the rows 70px above the feet are 2x2-identical pixel blocks (the NEAREST 2x signature; best-parity agreement 0.8-1.0 per mark) while the plate 6px below the feet scores 0.0-0.6 (the latter only where the front rank's body stands), so a figure >= 70px tall stands on each mark.
- [x] A2 RaidView shot: no sprite under BOSS_PLATE (931,53,384,65) or OBJECTIVE (0,88,352,106)
  - Data: the highest party head is screen y 240 (rank 2 at 336 minus the 96px warrior) > OBJECTIVE's bottom 194; the party's right edge is x <= 700 < BOSS_PLATE's 931. Boss (boss_main 99x119 at 1x): sprite rows 400..520 at x 1160..1260 — 280px under the plate's bottom edge (118). Viewed: the objective block and the boss plate sit over empty ceiling/crystal, nothing under them.
- [x] A3 RaidView shot: boss feet inside its floor rect
  - Mark feet (1210,800) -> screen (1210,520); floor [1090,730,230,120] -> screen y 450..570. Measured on the shot (diff vs the bare plate around x 1210): sprite rows 400..525 (the last rows are the contact shadow's soft edge), feet at 520 — inside. Crop _w1stage_crop_boss.png: the leviathan stands on the railed ledge facing left with the lantern at its feet; at `scale 1` (Q02) it is small beside the 2x party, which is the ruling's default, not a placement fault.
- [x] A4 Town shot: shortest standing figure >= 62px
  - stage_camp at `figure_scale 2`: heights frame_h x 2 = 72 (rogue/cleric_b/ranger_as_rogue) .. 96 (warrior); viewed build/shots/W1STAGE_Town.png — the four at the fire ring, the pair by the tents, the pair by the beds and the bridge figure all read as ~80px people beside ~210px tents, at the reference's proportion.
- [x] A5 Town shot: no two actor bboxes overlap > 25%
  - 2x frame boxes from actors.json: the only intersecting pair is warrior/mage at 10% / 12% of each box (the mage was moved (668,372)->(690,366) by the first agent for this); every other pair is disjoint.
- [x] A6 Town shot: every ambient bubble's bottom edge 4-10px above its figure's head
  - Re-authored against the kit's real 40x44 emote frame (J5): every camp bubble's tail apex is at (feet.x - 0.5, head - 6). On the shot (two are up at frame 40: over ranger_as_rogue at screen (308,132) and over variant_r05_grey at (1098,448)) the tail's last rim row is at 175 / 491 and the figure's frame top at 182 / 498 — 6px of air each, 8-10px to the first painted hood pixel of the current frame. Crop _w1stage_crop_town_bubbles.png (4x): one row of three dots, the tail pointing at the head. `test_every_ambient_bubble_hangs_over_a_head` holds the rule for all fifteen bubbles in camp/tavern/market.
- [x] A7 pulse inside/outside luminance ratio < 1.15 at the four rects STAGE-05 measured
  - Measured on this agent's shots (4px band inside the rect edge vs 16px band outside, luminance): RaidView (1440,410,96,220) 0.946; RaidPrep (233,390,150,110) 1.137; Results (1440,410,96,220) 0.923 — all < 1.15 (STAGE-05 had 2.18 / 1.83 / 2.34). RaidView (55,150,150,110) reads 1.642 — but the same rect with the pulse HIDDEN (`--set=reduced_effects=true`) reads 1.705: the rect edge cuts through the plate's own bright crystal cluster, and the quad's contribution is 1.642/1.705 = 0.96 (STAGE-05's 1.89 at that rect was the old hard quad). The moved spire pulse (868,558,84,120): 1.273 vs 1.293 hidden, contribution 0.985. Viewed _w1stage_crop_pulse_pairs.png (with / without, both rects at 2x): the light is a soft brighten of the crystals with no rectangle edge anywhere.
- [x] A8 new tests: test_figure_scale_is_honoured, test_marks_place_the_party_and_boss_in_band, test_pulse_is_feathered, test_every_prop_frame_w_divides_its_strip, test_every_arena_pulse_and_emitter_is_in_the_view_band, test_reduced_motion_holds_lights_and_bubbles, test_vignette_hides_under_reduced_effects, test_head_of_is_above_the_feet_by_frame_h_times_scale
  - All eight exist by name in tests/unit/test_scene_stage.gd (grep `^func <name>` = 1 each), plus test_overlays_sit_above_the_figures_and_carry_labels, test_ember_colour_is_data, test_speech_anchors_to_its_speaker and (this agent) test_every_ambient_bubble_hangs_over_a_head — 44 tests in the file; the whole suite: `TESTS PASSED   1713 test(s) in 80 file(s)  [33018 ms]` (build/shots/W1STAGE_tests3.log). The only stage lines in the log are the two expected push_warnings from the unknown-figure tests.
- [x] A9 test_scene_stage.gd:534-566 rewritten to iterate both arenas' marks
  - Now `test_every_creature_the_sheet_can_produce_clears_the_chrome_and_lands_on_its_mark` (:553): for both ARENAS x all six enemies/*.png — head clears BOSS_PLATE's bottom in plate rows under the mark's view, feet inside `boss.floor`, feet inside the view band, feet x inside the floor; 12 creature placements checked.
- [x] A10 test_motion.gd:139-156 green (some scene keeps an fps > 3 prop)
  - `test_the_literal_reading_would_cost_every_fire_in_the_game` passes in the suite: cave keeps 9 / dungeon 16 props above 3 fps (no prop entry was removed; only positions and emitter presets moved).
- [x] A11 refdiff RaidView/RaidPrep/Results vs 2 recorded before/after
  - Masked (arena 210,77,928,640) vs Concept 2, before = build/diff/*_diff.json (W0-GATE's 09:25 run) / after = build/diff/w1stage/ from this agent's shots: raidview mae 34.553 -> 33.813, iou 0.1144 -> 0.1163, within-8 25.88 -> 26.13; raidprep 32.209 -> 32.108, 0.0768 -> 0.0758, 34.48 -> 34.52; results 32.390 -> 32.014, 0.0745 -> 0.0748, 25.32 -> 24.76. No regression beyond the recorded jitter (BUILD_STATE: <= 0.07 mae between runs; the within-8 moves are the screen-space glow, LESSONS:116-121). The shots also carry W1-CHROME/W1-KIT/W1-FRAME's in-tree chrome, so the deltas are the wave's, not only this unit's.
- [x] S1 shot `RaidView --fixture=raid` (viewed)
  - build/shots/W1STAGE_RaidView.png: the fight reads as one — party lower-left on the lantern floor, boss lower-right on the ledge, the rope bridge between, the moved cold pulse glowing on the pool spire under the bridge; chrome (header, objective, boss plate, control row, strip) untouched.
- [x] S2 shot `RaidPrep --fixture` (viewed)
  - build/shots/W1STAGE_RaidPrep.png: the dimmed cave at RaidPrep's own (-30,-120) view (RaidPrep.gd is not mine and does not read `marks.view`; it places no party). The left crystal cluster's pulse (233,390) is now a soft brighten with no box edge (ratio 1.137); the old (900,230) quad that sat over the plateau is gone — its light lives on the pool spire, below RaidPrep's band, which is the trade STAGE-21 asked for (the fight screen sees it, the prep screen does not).
- [x] S3 shot `Town --fixture` (viewed)
  - build/shots/W1STAGE_Town.png: 2x crowd on the bare camp, the speaking line hanging over cleric_b by the tents (bottom-centre 28px above its head), two '...' bubbles up. Still to be fixed by W2-TOWN, not this unit: the Tavern and Blacksmith callouts sit on the 2x mage/rogue and the tent-side figure (recorded in the handoff's observation). Two `--set=reduced_motion=true` runs (W1STAGE_Town_rm1/rm2.png) are pixel-identical (0 differing pixels) — the seeded RNG closed W1-ICONS's observation.
- [x] S4 shot `Tavern --fixture --frames=1,20` (hearth no longer straddles; viewed)
  - build/shots/W1STAGE_Tavern.a.png / .b.png + crop _w1stage_crop_hearth_ab.png (3x): frame 1 and frame 20 are two different flame shapes on the same grate — bright-flame pixels span x 826..929 in BOTH with centroid x 886.6 vs 886.8, top row 94 in both — so the hearth burns in place; at 48 the window slid a third of a frame sideways every tick. The two captures differ only inside x 317..917, y 99..667 (the flames, the candle lights, the figures' breathing).
- [x] S5 shot `RaidView --fixture=raid --set=reduced_effects=true` (viewed)
  - build/shots/W1STAGE_RaidView_reduced_effects.png: glow off, every Pulse_* hidden (the spire and the crystal clusters are the plate's own), no motes, the eight lantern/torch flames still burning at modulate 1.0 — they read as painted flames, not the flat white-orange CRITIC-G17 feared; the party and boss still animate. Vignette gone: the top band rows 0..4 measure 74.35 vs 73.30 with it on (glow adds ~+5% there, so the vignette's own edge darken is ~6%, the 8% x edge^2 the shader states).
- [x] G1 `apply_settings(reduced_motion: bool` literal kept; no focusable added; overlay Labels inside the screen subtree; no `create_tween` in SceneStage; wipe members untouched
  - grep: `func apply_settings(reduced_motion: bool` x1; `create_tween|focus_mode|FOCUS_ALL|grab_focus` x0 in SceneStage.gd; `Color("` x0 (W1-CHROME's lint line); the RaidView diff touches no `_wipe*`/`WIPE_*`/`_run_wipe_sequence` line (test_wipe_sequence green in the suite); overlay Labels are children of the stage's `Overlay` (test_overlays_sit_above_the_figures_and_carry_labels); SceneStage.gd 762 tab-indented lines / 0 space-indented, the test file 0 tabs.
- [x] G2 parse_check OK after every edit; `verify.sh --fast` green (summary pasted below); `lint_motion.sh` MOTION LINT OK
  - parse_check OK after each of the three engine batches (16:3x, 16:5x, 17:0x). `GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` (build/shots/W1STAGE_verify_fast2.log):
    ```
    == 0/8  lint: no cross-file class_name refs ==   PASS  LINT OK   PASS  MOTION LINT OK
    == 1/8  script parse ==                          PASS  PARSE_CHECK scanned 160 script(s)
    == 2/8  generated content ==                     PASS  16 generated file(s) agree
    == 2b/8 generated art ==                         PASS  ART CHECK  generators=6  runtime files: 184 agree  0 DIFFERS  0 MISSING
    == 3/8  unit tests ==                            PASS  TESTS PASSED   1713 test(s) in 80 file(s)  [33742 ms]
    VERIFY OK
    ```
    `tools/lint_motion.sh` -> `MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)`.

## Build notes as read (what lands, where)

- `figure_scale` per scene (camp 2, cave 2, dungeon 2, tavern 1, market 1): SceneStage `_build` reads it, `add_actor` uses it when the actor has no own `scale`, `place_party` therefore inherits it.
- `marks` in both arena JSONs per STAGE "Proposed arena blocking"; `SceneStage.marks(name)` static; RaidView `_stage()`/`_party_layout()`/`boss_feet_y()` consume them.
- Overlay layer (`Overlay` Control added after the actor layer, kept above later-placed actors): `head_of`, `say_at` (Widgets.speech_plate one-arg), `number_at` (caller's Label), `bar_for` (96x23: 24 badge, 4 pips, 62x7 Widgets.bar).
- Pulse quads get a ShaderMaterial with `feather 0.35`; `pulse.color.a` still drives the value pulse.
- Vignette: full-stage ColorRect canvas shader, 8% edge darken, `MOUSE_FILTER_IGNORE`, hidden under reduced_effects.
- Under reduced_effects flame props return to modulate 1.0.
- `_process` gates lights, line rotation and bubble shuffle on `_motion_held`.
- Ember entries: `color`/`lifetime`/`size` keys and a `motes` preset.
- stage_tavern.json `frame_w: 64`; camp actors moved per TOWN-07; camp bubbles/speech re-authored for 2x.
- preview_scene.py: `--marks`, `--view raidview|raidprep|results`, draws pulse/shimmer/boss/marks/chrome.

## Judgement calls

(filled in as they are made)

## Log

- Read in order: 00-plan §0 + wave-1 ownership + §W1-STAGE, RULES.md, STAGE.md (all 21 findings + "Proposed arena blocking"), COMBAT-01/03, PIPE-04, CRITIC-G17/C06/C07/C10/C17, TOWN-03/07, LESSONS.md, SceneStage.gd (739 lines), RaidView.gd:40-130 + 268-345, test_scene_stage.gd (659), preview_scene.py (188), all six stage_*.json, Widgets.gd's bar/speech_bubble/speech_plate/tween, shot.gd header, actors.json geometry, enemies.json, Palette tokens, BUILD_STATE art-gate baselines.
- Facts fixed before coding: the sliced strips face RIGHT by default (warrior/cleric frames viewed at 4x), so cave `face: right` = no flip; boss_main's mouth points right, so the mark's `flip: true` turns it toward the party. `Widgets.speech_plate(text)` one-arg and `Widgets.bar(value, max, kind, text, size)` exist at wave start (Widgets.gd:347, :452). Git tree clean at start.
- Before numbers (build/diff/*_diff.json from W0-GATE's run, masked arena 210,77,928,640, vs Concept 2): raidview mae 34.553 / iou 0.1144 / within-8 25.88; raidprep 32.209 / 0.0768 / 34.48; results 32.39 / 0.0745 / 25.32 (BUILD_STATE: kit/raidview/results jitter <= 0.07 mae between runs).
- Scene JSONs patched by targeted text edits (scratchpad/w1stage_patch_scenes.py, keeps each file's hand formatting; every file re-parsed after the write): camp `figure_scale 2`, rogue (784,368)->(760,340) and variant_r03_brown (1045,600)->(1060,625) per TOWN-07, mage (668,372)->(690,366) so its 2x body no longer overlaps the warrior's by 30% (J1), five bubbles re-authored to `feet - 2*frame_h - 38` (bottom edge 6px above the 2x head), speech pos (560,484)->(560,443); tavern `figure_scale 1` + hearth `frame_w 48->64`; market `figure_scale 1`; cave `figure_scale 2` + `marks` (party LF ranks y 692/654/616 at scale 2 facing right, boss LR feet (1210,800) flipped, scale 1, floor [1090,730,230,120], view [0,-280]) + the STAGE-21 move (cold light (990,290)->(910,880), motes (990,300)->(910,860) `preset: motes`, (470,470) `preset: motes`, pulse [900,230,210,130]->[868,838,84,120] on the pool spire under the bridge); dungeon `figure_scale 2` + `marks` (party HF ranks y 660/622/584, boss LP feet (965,900), floor [900,830,130,110]) + STAGE-06's move (cold light (962,200)->(965,870), motes (962,220)->(965,890), pulse [930,60,80,200]->[905,830,120,110], the two upper-brazier ember emitters -> in-band braziers (716,455)/(858,509)). Counts: cave 9 props / dungeon 16 props kept (> 3 fps); shimmer+pulse total still 16 (>= 10).
- SceneStage.gd rewritten in place (tabs; `create_tween` count 0; `func apply_settings(reduced_motion: bool` literal kept): `figure_scale` + `figure_scale()`, `marks(name)` static, overlay layer (`Overlay` Control, `_lift_layers()` keeps Vignette+Overlay last after `add_actor`/`place_boss`/`add_shimmer`/`add_pulse`), `head_of`, `say_at(spr, line, hold_ms)` on `Widgets.speech_plate(line)` one-arg, `number_at(spr, label)` with a 14px stagger, `bar_for(spr, hp, max, badge, opts)` 96x23 (badge 24 at 0,0; pips 12@14 from x 30; `Widgets.bar` 62x7 at (28,15)), `PULSE_SHADER` (feather uniform 0.35 + radial roll-off, `color.a` untouched), `VIGNETTE_SHADER` (GROUND_PAGE tint, strength 0.08, width 0.08; `_notification(NOTIFICATION_RESIZED)` follows the stage size), ember presets (`embers` byte-equivalent to the old ramp; `motes` cool 1-2px 6s), `_process` gated on `_motion_held` for lights/lines/bubble shuffle + `_tick_overlays()`, `place_boss` gains `scale`. Overlay entries are validated with `is_instance_valid` BEFORE any typed assignment (a freed instance cannot be assigned to a typed var — this is why `_drop_overlay` takes an untyped param).
- RaidView.gd: only the six symbols touched (docstrings + bodies): `ARENA_OFFSET`/`PARTY_RANKS`/`BOSS_X` become documented fallbacks for a scene without marks; `boss_feet_y(height, scene := DEFAULT_ARENA)` reads the mark (feet = max(mark.pos.y, chrome clearance + h*scale), clamped into `floor`); `_party_layout()` reads `marks.party` (ranks/scale/face); `_stage()` reads `marks.view` and passes `{flip, scale}` to `place_boss`. `BOSS_HEAD_CLEAR`/`BOSS_FEET_RANGE` stay as the fallback's numbers (not in my six symbols — untouched).
- test_scene_stage.gd: the old boss test (:534-563) is now `test_every_creature_the_sheet_can_produce_clears_the_chrome_and_lands_on_its_mark` iterating both arenas' marks (head clearance from `RaidView.BOSS_PLATE` and the mark's view, feet in `floor`, feet x in floor); added `test_marks_place_the_party_and_boss_in_band`, `test_figure_scale_is_honoured`, `test_head_of_is_above_the_feet_by_frame_h_times_scale`, `test_overlays_sit_above_the_figures_and_carry_labels`, `test_pulse_is_feathered`, `test_every_prop_frame_w_divides_its_strip`, `test_every_arena_pulse_and_emitter_is_in_the_view_band`, `test_ember_colour_is_data`, `test_reduced_motion_holds_lights_and_bubbles`, `test_vignette_hides_under_reduced_effects` (four-space).
- preview_scene.py: `--view raidview|raidprep|results` (crops to the screen's band, draws HEADER/OBJECTIVE/BOSS_PLATE/control-row rects), `--marks` (feet ticks + 2x stand-ins from the class strips, boss floor rect + the boss slice from enemies/, `--boss <rank>`), pulses drawn with the shader's feather, shimmer dashed, ember emitters as circles, honours `figure_scale`. Viewed build/preview/stage_arena_cave.raidview.png (twelve 2x stand-ins on the lantern floor left of the bridge, heads well under OBJECTIVE's bottom, the leviathan flipped on the railed ledge inside its floor rect, the moved pulse on the pool spire between the two sides, nothing under BOSS_PLATE), stage_arena_dungeon.raidview.png (party on the hall floor below the stairs, boss on the lower ritual circle inside its floor rect with the pulse under it), stage_camp.png (twelve 2x figures; the rogue now stands right of the fire's back, the brown variant by the beds).
- Engine batch 1 (one lock hold, scratchpad/w1stage_check.sh): unit suite `TESTS FAILED 8/1677` — every failure is another wave-1 unit's in-progress file (test_a11y x2 and test_theme_kit x13 "expected the kit's button variations, found 0" = W1-CHROME's Theme.gd; test_frame_header x2 = W1-FRAME; test_hall_plates = W1-HALL; test_screens `test_a_shut_door` "the callout must still be on the square" = W1-KIT's building_callout; test_text_scale = W1-FRAME/CHROME's font sizes). Zero failures and zero SCRIPT ERRORs in test_scene_stage.gd / SceneStage.gd / RaidView.gd; test_wipe_sequence green (the wipe members are untouched). `tools/lint_motion.sh` → `MOTION LINT OK`.
- RESUMED 2026-09-14 13:52 by a second agent (the first was killed by a usage limit before ticking anything). Re-read in order: 00-plan §0 + wave-1 ownership + §W1-STAGE, RULES.md (§1 test contracts, §2 settings, §4 ownership, RULES-10/16), STAGE-01/02/04/05/06/07/11/18/19/20/21 + "Proposed arena blocking", COMBAT-01/03, PIPE-04, CRITIC-C06/C07/C10/C17/G17, TOWN-03/07, LESSONS.md (SceneStage/glow/backslash/lock entries), then the working tree in full: SceneStage.gd (1156 lines), the RaidView.gd diff (four hunks — ARENA_OFFSET/PARTY_RANKS/BOSS_X docstrings, `boss_feet_y`, `_stage`, `_party_layout`; every hunk is inside the six owned symbols, nothing to move to the handoff; the wipe members and `_run_wipe_sequence` are untouched), test_scene_stage.gd diff, preview_scene.py diff, the five stage_*.json diffs. No W1STAGE_* shot exists in build/shots yet — the first agent only viewed previews — so every S box is still open and every A box is measured below from the shots this agent takes.

## Judgement calls (third agent)

- J5 (the emote frame grew under the bubbles): W1-KIT's `Widgets.speech_bubble()` now draws the kit's `PanelEmote` frame (emote_frame.png, 40x44, tail apex at (x+5.5, y+44)) instead of the 33x32 bubble every scene's `bubbles` were authored against, and it adds its own `Dots` — so the first Town shot showed every "..." bubble with SIX dots (the kit's plus SceneStage's private Ellipsis) and its tail 6px INTO the head. handoff-W1-KIT.md §10/§11 (SceneStage stops adding dots; the Ellipsis class goes) target my file, so they are applied here rather than left for the orchestrator (the double dots were in my own acceptance shot); handoff-W1-CHROME §17 (the Ellipsis hex -> token) is therefore moot — applied first, then deleted with the class. Every ambient bubble in the three scenes that have figures (camp x5, tavern x5, market x5) is re-authored to one rule against the real frame: x = feet.x - 6 (the tail's apex on the head's centre line), y = head - 44 - 6 (apex 6px above the head, inside A6's 4..10). The plan only named the 2x scenes, but the 1x tavern/market bubbles had grown 12px into their figures' faces by the same change, and the bubbles are stage data this unit owns. `test_every_ambient_bubble_hangs_over_a_head` builds each shipped scene and reads the REAL Emote frame's size, so a future frame change fails loudly instead of putting tails on faces again.

## Judgement calls (resumed agent)

- J2 (vignette fit): the plan says "a full-stage ColorRect"; every screen offsets the stage by a negative `position` and sizes it `host.size - offset`, so a rect over the whole stage darkens plate rows the screen never shows (RaidView: the top band would be plate rows 0..81, all off-screen) and leaves the visible top edge bare. `_fit_vignette()` (called from `_build`, `_ready` and `NOTIFICATION_RESIZED`) covers `-position .. size` instead — the band the screen shows. Nothing else about it changed (8%, GROUND_PAGE tint, MOUSE_FILTER_IGNORE, hidden under reduced_effects); the test asserts the fitted rect for RaidView's numbers.
- J3 (TOWN-03 anchor half): `speech.speaker` is an index into the JSON's `actors` in AUTHORING order (not depth order — an author counts the list they typed). The camp's speaker is 4 (cleric_b at (455,625)): the fire-ring figures all carry their own "..." bubble or stand under the 1x-era Tavern callout, and cleric_b's plate (364..546 x 445..521) is the one spot that touches no other figure. Its "..." bubble moved to variant_r05_grey ([1134,522], 6px above the head) so a figure does not carry two bubbles. Tavern speaker 12 (variant_r06_black, the nearest head to the authored spot at 107px); market left on `pos` (nearest head 196px away; STAGE-15 re-places that crowd) — recorded in the handoff for W2-MARKET.
- J4 (A7's measure): STAGE-05's number (4px inner band vs 16px outer band) cannot go below the plate's OWN contrast at a rect drawn around bright crystals. Measured with the pulse shown vs hidden (`--set=reduced_effects=true` hides Pulse_*): RaidView (1440,410,96,220) 0.881 vs 0.823; (55,150,150,110) 1.650 vs 1.704; the new spire rect (868,558,84,120) 1.225 vs 1.235 — the quad's own step is 1.071 / 0.969 / 0.992, all < 1.15 (the old hard quad added ~2x). So A7 is ticked on the pulse's own contribution, and the absolute ratios are recorded beside it.

## Measurements from the data (before the resumed shots)

- A1 (data): class strip heights at scale 2 = [76, 78, 82, 84, 88, 90, 96] px, min 76 >= 70. Every cave feet point lands at screen y 336/374/412 with the tallest head at 240/278/316 — clear of OBJECTIVE (bottom 194) and BOSS_PLATE (x 931+); boss feet screen (1210,520), floor screen [1090,450,230,120].
- A4/A5/A6 (data, stage_camp at 2x): shortest figure 72px (>= 62); worst bbox overlap warrior/mage 10%/12% (<= 25%); all five ambient bubbles' bottom edges 6px above their figure's head (4-10).

## Log (third agent, resumed 2026-09-14 16:20)

- The second agent was killed after its engine batch: build/shots/W1STAGE_*.png (14:04) and W1STAGE_verify_fast.log (14:05, `TESTS PASSED 1710 test(s) in 80 file(s)`, VERIFY OK) exist on disk but nothing was ticked and no shot was viewed — the report's last save is 14:00. Re-read in order: 00-plan §0 + wave-1 ownership + §W1-STAGE, RULES.md (stage/RaidView/settings rows), STAGE-01/02/04/05/06/07/11/18/19/20/21 + "Proposed arena blocking", CRITIC-C06/C07/C10/C17/G17, TOWN-03/07, LESSONS.md, then the tree: SceneStage.gd in full (1209 lines), the RaidView.gd diff (every hunk inside the six symbols: the three docstrings, `boss_feet_y`, `_stage`, `_party_layout`; nothing to move), all five stage_*.json diffs, test_scene_stage.gd's new tests (:541-1080), preview_scene.py's header/functions.
- Handoffs: W1-VFX §1 (tavern `frame_w` 48 -> 64) is already in the tree (stage_tavern.json:23 says 64) — nothing to apply. W1-CHROME §17 (SceneStage.gd Ellipsis `Color("#3B2F27")` -> `Palette.INK_BUBBLE_DOTS`; the token exists at Palette.gd:136) — applied now, see below.
- W1-ICONS's observation (the camp's '...' bubble set differs between two reduced-motion runs): `_process` IS gated on `_motion_held` (the shuffle cannot tick while held), but the FIRST pick in `_build` (`idx.shuffle()`) and every later `randf_range` use the global unseeded RNG, so which two bubbles are up at frame 0 differs per run. Fix: a `RandomNumberGenerator` seeded from the scene name (`_rng`) drives the bubble pick, the shuffle interval, the line interval and the light/pulse/bob phases, so two runs of one scene match frame for frame; the test extends to assert two builds of the same data show the same bubbles.

- Files this agent changed: game/ui/SceneStage.gd (seeded `_rng` for every random draw; W1-KIT §10/§11 applied — bubbles through `Widgets.speech_bubble("dots", glyph)`, Ellipsis gone; W1-CHROME §17 applied then subsumed), game/assets/scenes/stage_camp.json (five bubbles + the note), stage_tavern.json (five bubbles), stage_market.json (five bubbles), tests/unit/test_scene_stage.gd (determinism half of test_reduced_motion_holds_lights_and_bubbles; new test_every_ambient_bubble_hangs_over_a_head), build/plan/handoff-W1-STAGE.md (the applied-handoffs observation). RaidView.gd and preview_scene.py: untouched by this agent (verified as left by the first).
- Left for others (not this unit's): the Town callouts on 2x figures (W2-TOWN, handoff observation); the market's `speech.speaker` (W2-MARKET); `tail_at` for the bubbles (W2-STAGE2); boss `scale` > 1 (Q02); RaidPrep/Results reading `marks.view` (their owners — both still show the cave at their own offsets and place no party, so nothing is wrong on screen).
- Shots viewed by this agent: W1STAGE_RaidView.png, W1STAGE_RaidView_reduced_effects.png, W1STAGE_RaidPrep.png, W1STAGE_Results.png, W1STAGE_Town.png (before and after the bubble re-author), W1STAGE_Tavern.a.png, W1STAGE_Market.png, and the crops _w1stage_crop_party.png, _w1stage_crop_boss.png, _w1stage_crop_pulse_pairs.png, _w1stage_crop_hearth_ab.png, _w1stage_crop_town_bubble.png, _w1stage_crop_town_bubbles.png.
