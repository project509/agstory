# report-W6-LEDGER — the queue tells the truth, the unrecorded rulings are rows, the recruit price is the register's, and the designer gets one page with one default per row

Wave 6, 2026-09-15. Contract: `build/plan/ship/00-plan.md` §1 "W6-LEDGER". Order inside the unit: closes → rows → `PRICE_SCALE` → the page (last).

## Acceptance

- [x] A1. audit.json closes re-diffed against HEAD and applied (the CRITIC-R2/R3 list); `python tools/audit_stale.py --top 15` names no `done` row
- [x] A2. the new audit rows exist (`m6-forgiving-guild`, `m6-ambient-effects`, `m6-e4-m05-effect`, `m6-e4-m08-window`, `m6-e4-m09-cadence`, `m6-mech-arms-register`, `m6-kit-bard`, `m6-kit-proposed`, `M6-EXP-07`, `M6-FINAL-05`, `m6-legendary-busts`, `m6-quill-hook`); `test_audit_hygiene.gd` green
- [x] A3. docs/15: the M5-OQ-1 sweep — no BL heading marked OPEN/PARTIAL without a pointer to a §6 row or a wave; the SIM-27 twelve rows; Q-97/Q-98 (audio); the CRITIC-M2/M3/M4/C16 rows; Q-53 default-taken line; Q09 TRANSITION_MS = 0; LOOP-19 BL row; W6-LOG / W6-SIM-CASCADE handoff rows applied at the close
- [x] A4. `Recruitment.PRICE_SCALE := "doc11"` selecting `[15, 60, 160, 420, 1000]`; doc 04's kept selectable; `test_recruitment.gd` green on the doc-11 table and the anti-arbitrage test; `test_playtest_invariants.gd` updated
- [x] A5. `gen_items.gd` `raid_adj` template fix; Tier 1 output byte-identical (verify stage 2)
- [x] A6. `data/tier_words.json` gains the five columns per tier and the `leather` slot (words `pending`); docs/09 §11.4 five columns
- [x] A7. `data/credits.json` `lines` gains the derivable rows plus one `[designer credit pending]` marker
- [x] A8. `GameState.gd:894` comment → BL-79
- [x] A9. BUILD_STATE.md Current focus rewritten to the ship plan; `:102` "at its default" corrected; open count equals audit.json's; 250-line limit held; BACKLOG M6-JUICE-04 struck
- [x] A10. `designer-page.md`: every §6 row, exactly one "Default:" line per row and an "Answer line:" each; the naming sheet; the solvency-floor question with 15 G
- [x] A11. q-*.md marked consumed (q-mech-arms, q-W5-SIM, q-audio, and any other consumed)
- [x] A12. `test_build_state.gd`, `test_docs_links.gd` green (run 1: 53 tests, one price-table failure fixed; docs/15's new anchors resolve; BUILD_STATE run again after its rewrite below)
- [x] A13. Shot: `Tavern --fixture=new` shows "Hire — 15 G" (viewed)
- [x] A14. `verify.sh --fast` run twice; stages 0-2b PASS, stage 3 fails 4/2,126 — all four in W6-COPY's in-flight files (see Left); every owned file green; `lint_motion.sh` OK

## Log

(appended as items land)

- A1 landed: 47 closes (the CRITIC-R2/R3 list re-diffed against HEAD 106ac8d — every row read, none found already `done`, every closing note names the code and the test that holds it), 6 re-scopes (M5-T25-09/-10, M3-LOOP-04, M6-SAVE-01, M3-SAVE-03, M6-A11Y-01), `m5-m05-interrupt-check` kept `partial` with SIM-13's residue (CRITIC-R5), M5-T25-12 closed with the M10 note, M6-JUICE-04 kept blocked with the BACKLOG strike recorded in its remaining_work. Counts 74/51/27/17/9 -> 120 done / 35 not-started / 18 partial / 17 blocked / 0 done-backlog-stale. `python tools/audit_stale.py --top 15` names 15 open rows, all genuinely open on reading (M6-SAVE-01's premise is the freeze owed; m6-ambient-effects's is types that exist without effects).
- A2 landed: 12 new rows (`m6-forgiving-guild`, `m6-ambient-effects`, `m6-e4-m05-effect`, `m6-e4-m08-window`, `m6-e4-m09-cadence`, `m6-mech-arms-register` (partial: the rows are BL-82..93 below, the code comments are the handoff), `m6-kit-bard`, `m6-kit-proposed`, `M6-EXP-07`, `M6-FINAL-05`, `m6-legendary-busts`, `m6-quill-hook`), each with spec_source / evidence / remaining_work / files / size / risk / area; `test_audit_hygiene.gd::test_the_wave_6_rows_the_ship_plan_names_exist` pins the ids.
- A3 (rows) landed: docs/15 gains BL-82..BL-93 (the twelve SIM-27 rows, text from `q-mech-arms.md` / `q-W5-SIM.md` with the switch names and the tests that hold each), BL-94 (PRICE_SCALE), BL-95 (Retreat struck), BL-96 (traits post-1.0), BL-97 (S13/S16/F1/the sort row struck), BL-98 (wishlists out), BL-99 (the four PROPOSED kits), BL-100..106 (the §7.1 rows that were W6-LEDGER's: per-tier arenas, placeholder names, the town's one plate, the 2x re-author, the two gates that stay records, instrumentation, the art defaults); Q-97 (audio direction, as built), Q-98 (beds / ambience / voice / world layer + AUDIO-11's three defaults + AUDIO-19's clear's-sound line), Q-99 (the dissolve, `TRANSITION_MS = 0`); status markers under BL-58/59/53/40/42/22 pointing at their §6 rows (M5-OQ-1 — no OPEN/PARTIAL/DEFERRED BL heading is without a pointer); Q-53 "default taken, as built", Q-60 "default taken → BL-94", Q-29 "NOT implemented → §6 #7"; §8's audio gap row → Q-97/Q-98. The W6-LOG / W6-SIM-CASCADE handoff rows: applied at the close if their handoffs carry one (checked at the end of this unit — see Left).
- A4 landed: `Recruitment.COST_BASES {doc11, doc04}` + `static var PRICE_SCALE := "doc11"`; `cost_of()` returns the base exactly at CT 1 (15 is not a multiple of 10 — doc 04's round-to-10 belongs to its multiplier; first run failed "expected 15, got 20", fixed) and doc 04's `× (1 + 0.6 × (CT − 1))` rounded to 10 above it. Tests: the doc-11 table cell by cell at CT 1/3/5, doc 04's columns under `"doc04"` with `after_each` restoring the switch, R3 at the WORST case (every slot the dearest T1 item of the plan's source, sold at the Legendary rate: Common 15 ≥ 1.5×4, all five rarities hold). `test_playtest_invariants.gd`: the floor comment names BL-94 and a new test wires door (b)'s premise (STARTING_GOLD ≥ a Common). `RUN_TESTS_ONLY=test_recruitment,test_playtest_invariants,test_tavern` → 84 green.
- A5/A6 landed: `data/tier_words.json` T1 `raid_adj` "Basic" → "Raid" (docs/09 §11.3's `Basic {RaidTitle-adj} {WeaponNoun}` = canon's "Basic Raid Sword"; DESIGNER-02's S fix), T1 `leather: "Reinforced"`, every pending tier's `candidates` lists all five columns (null where no candidate exists) plus `leather`, a top-level `leather_column` decision slot (pending; recommended default "shares material from Tier 2 on"); `tools/gen_items.gd` gains the column's meaning as a comment at the `raid_adj` read (the `Basic %s` / `Strong %s` templates already put the word in the tier slot — see Judgement calls for why the plan's "%s Raid %s" phrase was read this way); docs/09 §11.4 widened to five columns with the leather decision and the grammar. `gen_items.gd -- check`: "0 file(s) differ" (the 16 generated files byte-identical — pending tiers resolve to the TIER<n> token exactly as before); `RUN_TESTS_ONLY=test_tier_scaling,test_items` → 71 green.
- A7 landed: `data/credits.json` `lines` = the `[designer credit pending]` marker, a spacer, Godot (MIT), Fira Sans (OFL 1.1, `game/assets/fonts/OFL-FiraSans.txt`), Grenze Gotisch (OFL 1.1), "Sound: generated from physical models, tools/audio (this project)" (AUDIO-17: nothing sourced); `status` reworded to say the roll is drafted and one blank is the designer's; a `_notes` entry records why. W6-COPY's `Completion.credit_lines()` / `is_marker()` (already in the tree) print only the non-marker lines; none of them carries a lint word.
- A3 (close): BL-107 (W6-SIM-CASCADE's culprit rule, from `handoff-W6-SIM-CASCADE.md` §1 with `nnn` -> 107), BL-108 / BL-109 (W6-LOG's spec-02 row and `LogPlayer.COLLAPSE`, from `q-W6-LOG.md`) landed in docs/15. **Orchestrator: `handoff-W6-SIM-CASCADE.md` §1 is ALREADY APPLIED — its `old:` anchor survives the insert, so running it would insert a second, `BL-nnn` copy.** The `_wipe_cause` comment's citation of BL-107 is `handoff-W6-LEDGER.md` #16.
- A9 landed: BUILD_STATE's Current focus rewritten to the ship plan (wave 6's line; the designer's page; the wave-10 ship rule sentence that gives W8-SIM-BALANCE its authority and forbids everyone else; the stated perf budget; the queue's counts 120/53/17 = `audit.json`'s), the false ":102 at their recommended defaults" corrected (Q-53 built at its default, Q-29 NOT implemented -> W8-ITEMS; the MUST_MENTION strings kept), the stale "a CVD switch" and the fourth autoload in HANDOFF, the M6-JUICE-04 reversal in the progress log, the "13 blocked" risk line -> the page; exactly 250 lines (four doubled `---` separators removed, the Watch paragraph shortened). BACKLOG: the header counts (190 / 53 / 17), "screen shake on wipes" struck with the docs/13 citation, the airship/`M4B-VFX-01` line closed, the icon-set line's "still to draw" retired. `test_build_state.gd` green.
- A10 landed: `build/plan/ship/designer-page.md` — 67 rows (§6's #1-#68 with #35 folded into #21), grouped in DESIGNER.md's order (wave 6 / 7 / 8 / any) with the non-DESIGNER rows placed by their "Needed by"; per row the plan's question, the wave, exactly one "Default:" line naming the switch or unit, and one "Answer line:" (DESIGNER.md's verbatim for #1-#48; composed in the same shape for #49-#68); #1 carries default (c) with CONTENT-08's formula as HOW and SIM-19's (a) as the one alternative; #62 (the tutorial-wipe morale exemption) is its own row; §E the naming sheet (five columns x T2-T5 with the T1 canon row, leather as the sixth decision, the eight Legendaries with each file's own pronoun — warrior/rogue/wizard "he", monk "she", four unused, Natsuna "she"); §F the solvency floor at the shipped 15 G; §G the one-line answer sheet. Checked by script: 67 "**Default:**", 67 "**Answer line:**", no number missing but 35.
- A11 landed: `q-mech-arms.md`, `q-W5-SIM.md`, `q-audio.md` carry a CONSUMED banner naming their BL/Q ids and where the code comments are re-pointed.
- A13: `build/shots/w6/tavern_new.png` (`Tavern.tscn --fixture=new`, 1536x1024, viewed whole and the sidebar crop): Day 1 · Unknown, 60 G in the chip, "Hire — 15 G" on the crimson commit, "Hiring Doug1 leaves 45 G" in the footer. (Observation for W6-SHEETS's reviewer, not mine: the `new` fixture's header reads "Roster 12 of 15" where the plan's `new` sheet line says six Commons.)
- A8: already true at HEAD — `GameState.gd:894` reads "Recorded as docs/15 BL-79 (promoted from build/plan/q-tutorials.md, 2026-09-15)" (W5-DOCS did it). No edit.

## Judgement calls

- **The `raid_adj` template.** The contract says "`raid_adj` is the adjective and the template is `"%s Raid %s"`". Read against docs/09 §11.3's rule (`Basic {RaidTitle-adj} {WeaponNoun}` -> "Basic Raid Sword") and DESIGNER-02's table (raid_adj at T1 = "Raid"; the file held "Basic", which would render "Basic Basic Sword"), the tier word is the MIDDLE slot and the generator's `Basic %s` / `Strong %s` templates already put `raid_adj` there. A template literally `"%s Raid %s"` would hard-code the T1 word into every tier and give the column nothing to fill. So the fix is the T1 data value ("Basic" -> "Raid"), the column's meaning as a comment at the read, and docs/09 §11.4 saying so — with the 16 generated files byte-identical as the proof either way (`gen_items.gd -- check`: 0 differ). The capstone and healing-weapon templates (which prefix `raid_adj` where canon's T1 has no word) were left alone: changing them changes the committed Tier 2-5 files, which are not this unit's.
- **`cost_of()` at CT 1 returns the base exactly.** Doc 04's "rounded to the nearest 10" turned doc 11's 15 into 20; the rounding is the multiplier's, and at CT 1 the multiplier is 1.0. Doc 04's own columns still reproduce under `"doc04"` (every base there is a multiple of 10).
- **R3 asserted at the worst case, not the expectation.** The doc says `expected_sale_value`; the test prices every slot at the dearest item of the plan's source at the best sell rate, which is stronger and needs no distribution — all five rarities hold with room (Common 15 >= 6).
- **The five columns went under `candidates`, not as tier-level keys.** `gen_items.gd`'s `word()` falls back to the `TIER<n>` token only for an ABSENT key, and the unit owns "the `raid_adj` template only" there, so a pending tier's five columns live in its `candidates` block (null where no candidate exists) — the generator's output stays byte-identical and `test_tier_scaling`'s placeholder-token test holds.
- **The BL series marched into the Q-8x/9x range.** `test_docs_links.gd`'s guard treats a `Q-nn` citation as ambiguous once `BL-nn` exists; BL-88/90/94/96 landed on numbers fourteen code files cite as real design questions (Q-88 the ending, Q-90 the skip rule, Q-94 JSON vs .tres, Q-96 the arena). Each file joined the allow-list with its reason, and the header now says the overlap grows with every ruling. Skipping numbers to dodge it would have left a hole in the chain.
- **`m6-mech-arms-register` filed `partial`, not `done`.** The rows are landed; the code comments are a handoff applied at the close. A row that says "done" while the comments still cite a plan file is the staleness this unit exists to remove.
- **M6-JUICE-04 stays `blocked-needs-human`.** The BACKLOG line is struck under §6 #43's default (the plan's instruction), but the row itself waits for the one word — the count of 17 blocked rows is unchanged and the page asks.
- **The credits lines were shortened after the first gate run.** The four long attribution lines (with the OFL file paths) pushed Completion's sidebar 63 px past the 150% clip; proved by re-running with HEAD's empty `lines`. Three short lines fit; the licence texts ship beside the fonts and the pack's README names them.
- **The nine `done-backlog-stale` rows went to `done`** (the status was a description of BACKLOG, and BACKLOG's lines are corrected in the same pass); the queue keeps five statuses but carries none at that one now.

## Audit closures

(All applied in `build/plan/audit.json` by this unit — it owns the queue — with the closing note on each row; listed here so the orchestrator can verify.)

- **Closed as done (47):** M3-SAVE-01, -02, -07, -08, -10 (SHIP-01/LOOP-28: the transition autosave, the clock, the walked chain, `reconcile_content`, the rank canary — all held by `test_savegame.gd`); M3-TUNE-01, -03, -04 (BL-81, `data/reputation.json` the one source); M4B-CONV-01, -02 (fourteen screens on bare plates; the last crop gone); m5-mech-roll-site, m5-m04-double-spawn, m5-m01-tank-swap, m5-m03-ground-effect, m5-m07-positioning, m5-m08-fixate, m5-m09-healing-debuff (SIM-01/03; `test_mechanics_arms.gd`, `test_raid_sim.gd`); M5-T25-12 (with the M10-is-Silence note, BL-87); M5-TUT-01, -02, -06, -08 (CONTENT-07; BL-79); M5-COMEDY-03, -04, -08, -13 (CONTENT-11; BL-80, `test_mistake_lines.gd`); M5-QAB-3 (CONTENT-24); M6-AUD-01, -02 (AUDIO-02/SHIP-11, commit 756081a); M6-A11Y-07 (UI-44, the seven log glyphs); m4t-12 (UI-43, the hall family on the camp stage); M4B-VFX-01 (UI-42, the airship retired with its crop); M6-JUICE-07 (SHIP-09: stage 8 IS the perf test, WARN kept, the budget stated in BUILD_STATE); DW-A1, -A2, -A3, -A4 (SHIP-10, `test_docs_links.gd`); the nine done-backlog-stale rows M3-LOOP-01, M3-LOOP-02, M6-JUICE-01, m4t-05, m4t-13, M5-TUT-15, DW-B1, DW-E1, M6-FINAL-01 (BACKLOG corrected in the same pass).
- **Re-scoped, still open (6):** M5-T25-09 and M5-T25-10 -> `partial` (CONTENT-02: the 32 records are authored; the remainder is the per-tier sweep/acceptance test and the human read); M3-LOOP-04 (the ladder IS walked by `rung_resolved` — the residue is two GameState seams, W8-FACILITY); M6-SAVE-01 (the fixture/chain half done; the freeze is W7-SAVE's v17 bump); M3-SAVE-03 (SHIP-03's narrower `active_run` scope, W7-SAVE/W7-REPORT); M6-A11Y-01 (the bindings half done; the surviving handlers W8-KEYS's, the sort row and F1 struck by BL-97). m5-m05-interrupt-check kept `partial` with SIM-13's residue (CRITIC-R5) -> `m6-e4-m05-effect`.
- **Kept blocked with its remaining_work updated:** M6-JUICE-04 (the BACKLOG strike applied under §6 #43; the word is the designer's).
- **Filed (12):** m6-forgiving-guild, m6-ambient-effects, m6-e4-m05-effect, m6-e4-m08-window, m6-e4-m09-cadence, m6-mech-arms-register (partial), m6-kit-bard, m6-kit-proposed, M6-EXP-07 (closes on W6-SETTINGS's report), M6-FINAL-05, m6-legendary-busts, m6-quill-hook (conditional). Pinned by `test_audit_hygiene.gd::test_the_wave_6_rows_the_ship_plan_names_exist`.
- **M6-AUD-06** (Q-97/Q-98 landed): the row is still `not-started` in the queue — closing it is the orchestrator's on this note: the two entries are in docs/15 at the next free numbers with the three amendments AUDIO-06 asked for (the repair-pass choices in Q-97; the ambience/voice/world halves and AUDIO-11/19 in Q-98; the §8 gap row pointing at both); the docs/13 §12.4 and docs/14 §4 cross-references are W7-DOCS's. Left `not-started` rather than self-closed because the cross-reference half is another unit's; close it or re-scope it to those two lines.

## Left

- **Not done: closing `M6-AUD-06`** in the queue (see Audit closures — a one-line re-scope or close for the orchestrator).
- **Not done: the code-comment re-points** in `RaidSim.gd` / `Mistakes.gd` (SIM-27) — a handoff by ownership (`handoff-W6-LEDGER.md`, 16 edits, dry-run parses; `m6-mech-arms-register` closes when it is applied). `Audio.gd`'s two citations of `q-audio.md` are left as valid citations (the file exists; its banner now points at Q-97/Q-98) for W7-AUD-AMB, which next owns the file.
- **Not done: spec 00 §2.7's switch rows** (`PRICE_SCALE`, `COLLAPSE`, `WIPE_CULPRIT_DELTA`) — spec 00 is not in this unit's ownership list; the rows are written out in the handoff's note.
- **`verify.sh --fast` is not green at the moment of writing, for reasons outside this unit:** 4 of 2,126 tests fail, all in W6-COPY's in-flight work — `test_paper_doll.gd` (three RaiderDetail assertions on a null Label: LOOP-17's `_written()` rewrite mid-edit) and `test_text_scale_layout.gd` (the Board's facts line now reads "1 enemy" and the allow-list row still names "1 enemies" — that file's KNOWN table is W6-COPY's to update). The one failure this unit caused (Completion's sidebar at 150% under four long credits lines) was found in the first run, attributed by re-running against HEAD's empty `lines`, and fixed by shortening the lines. Every file this unit owns is green under `RUN_TESTS_ONLY` (62 + 84 + 71 tests across the runs above); stages 0-2b PASS: `LINT OK`, `MOTION LINT OK`, `COPY LINT OK`, parse OK, "16 generated file(s) agree with tools/gen_items.gd", "ART CHECK generators=7 runtime files: 204 agree 0 DIFFERS 0 MISSING".
- **Parse-check at the unit's start** showed `Completion.gd` and `test_mistakes.gd` failing to compile (other units mid-edit); both parsed by the final run.
- Nothing under `game/` beyond what the contract allowed was touched (`GameState.gd:894` needed no edit); `sim/` only `Recruitment.gd`; no canon number changed; the sweep untouched.
