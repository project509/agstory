# report-W4-PIP — the emoji-free figure is even, and the faces scale

Unit: wave 4, 00-plan.md §5 "W4-PIP". Findings: CRITIC-G09 (the pip figure set in a proportional face), HALL-05 / CRITIC-C09 (Fonts picks faces_30.png / faces_36.png at 125 / 150). KIT-20 stays blocked (Q14) — nothing here.
Owned: game/ui/Theme.gd, game/ui/Fonts.gd, game/assets/fonts/** (new files only), tests/unit/test_pip_figure.gd (new).
Handoff: build/plan/handoff-W4-PIP.md.
Shots: build/shots/w4pip/ (before_*, Tavern_emoji_free, Guildhall_text150, and the extra sites named in the log).

## Acceptance

- [x] A1 `Tavern --fixture --set=emoji_free=true` shows an even ten-pip figure (viewed with the Read tool, described below)
- [x] A2 `test_a11y_legibility.gd:383-515` and `test_screens.gd:856-931` green (the whole-suite run inside verify --fast)
- [x] A3 `Guildhall --set=text_scale=150` shows crisp 36px faces (viewed; faces_36.png is the sheet the 150 theme draws from)
- [x] A4 `tests/unit/test_pip_figure.gd` asserts the LabelPip font's `"|"` and `"."` advances are equal (and the same for every variation that carries the pip string today)
- [x] A5 Green: `Label.text` keeps the pip string; `Cards.morale_glyph` untouched (no edit outside the owned files); every font size through `Type.at` inside Theme.build
- [x] A6 `verify.sh --fast` green (summary lines pasted); `tools/lint_motion.sh` MOTION LINT OK
- [x] A7 Shots viewed: the two the plan names, plus the before shots and the extra pip sites

## Log (write-as-you-go)

- Read (in order): 00-plan §0 (rules, shot grammar, §0.5 contracts, §0.6 switches), the wave-4 ownership table, "### W4-PIP"; RULES.md §2 (settings table: emoji_free = pip string through Cards.morale_glyph only), §4 (Theme.gd / Fonts.gd owner-only rules, test_theme_kit pins); CRITIC-G09, CRITIC-C09, HALL-05 (verifier's corrections: gen_icons.lua makes the sheets; faces.json band 7 is right); LESSONS.md in full; report-W1-CHROME §faces + review-W1-CHROME (the with_faces structure and why system fallback is off on the copy), report-W1-ICONS (faces_30/36 are 300x30 / 360x36, .import beside them); Fonts.gd and Theme.gd in full; test_theme_kit.gd:379-500 (the pins: `faces()` fixed_size 24, `with_faces(base)` = FontVariation on a FontFile COPY with system fallback off and `fallbacks == [faces()]`, cached per base; LabelSmall carries NO fallback; every face shapes from the sheet through LabelMorale/LabelLog/LabelBody/LabelClass at scale 100); test_a11y_legibility.gd:256-320 and :383-515; test_screens.gd:856-931; test_text_scale.gd's door tests.
- Pip sites in the tree today (grep `Cards.morale_glyph(`): LabelMorale x6 (Cards.card:145, Facilities:236, Guildhall:803, Market:961, RaiderDetail:332, RaidView:1076), LabelBody x1 (RaidPrep:424 via Widgets.body), LabelSmall x1 (Tavern:527, the seat card the fixture shows), LabelName x1 (Tavern:897, the Manage row). No displayed string anywhere in game/ or sim/ carries a "|" except the pip figure (grep of every string literal), so a "|" in a Label is always a pip.
- Probe 1 (scratchpad/probe_W4-PIP.gd, one locked headless run): Fira Sans shaped advances — 13px: "|" 6.0 / "." 3.125; 15px: 7.0 / 3.59; 20px: 9.0 / 4.80; 23px: 9.3 / 5.52 (the bar ~1.7x the dot, G09 confirmed by number). A bitmap FontFile with `fixed_size = 0` carries a glyph per REQUESTED size (a size nobody registered shapes to the next font), sits in front of Fira in a FontVariation, and the face still shapes from the sheet with the bar font first (system fallback is decided by the first font). `Font.has_char`/`get_char_size` do not see such a glyph (they consult one default cache) — shaping does.
- JUDGEMENT CALL (the plan leaves the route open; the note names Cascadia Mono as the plan's "monospace OFL face" and the drawn pip row as its fallback): neither route reaches a single site inside this unit's ownership — the figure is embedded in `"%s — %d %s"` strings in nine sites of seven files this unit does not own, so a `LabelPip` on a mono face means splitting nine Labels by handoff, and a Bar-style `_draw` row beside the Label the same. The route taken is the drawn pip AT THE FONT LAYER: `Fonts.with_pips(base, size, scale)` = FontVariation{ bar font → copy of Fira with system fallback off → the scale's face sheet }, where the bar font is a one-glyph bitmap ("|" only, a pixel bar at Fira's cap height) registered per size with EXACTLY the advance Fira gives "." at that size. The dot stays Fira's period (prose is untouched — and no displayed string carries a "|" but the figure), the string stays in Label.text, the glyph door is untouched, and seven of the nine sites (every LabelMorale and LabelBody site) are even with no screen edit. No third font family, no 370KB TTF, no licence file: the figure keeps Fira's own 0.24em pitch (48px at 20px; a 0.586em mono cell would have been 117px on a 215px card). `LabelPip` is registered all the same — LabelSmall's size and colour with the bar and the faces — for the two Tavern sites that print the figure at SMALL/NAME (handoff, one token each).
- The pinned `with_faces` structure (test_theme_kit: base = a FontFile copy of Fira, `fallbacks == [faces()]`) is kept verbatim and gains `scale`; the theme's four glyph variations now build on `with_pips` instead, which the same file's shape tests still pass (the faces come from the sheet through the bar font first; letters from the copy).
- Fonts.gd (tabs): `FACE_SHEETS`/`FACE_SIZE_AT`/`FACE_ASCENT_AT` tables, `faces_at(scale)` (24/30/36; unknown scale → 24; `faces()` == `faces_at(100)`, same cached object), `_face_font(sheet, size, ascent)`, `with_faces(base, scale := 100)`, and the pips section: `PIP_BAR`, `PIP_CAP_HEIGHT`, `PIP_MEASURE_SIZE`, `_pip_font(file)` (per Fira weight — the period differs by weight), `_period_advance(file, size, features)` (read off a SHAPED "." at 16px where the server keeps advances fractional, scaled and quantised to 1/64 — above 20px the server rounds the running position, and a bar registered with the rounded 6 drifted half a pixel per pip against 5.52px dots: probe 3/4), `_register_pip` (LA8 white-on-alpha so `font_color` modulates it; ascent/descent = Fira's at that size, see the strip regression below), `with_pips`. Theme.gd (tabs): LabelMorale/LabelBody/LabelLog/LabelClass on `with_pips(..., Type.at(size, scale), scale)`; new `LabelPip` (Type.SMALL, TEXT_LOG). Both files 0 space-indented lines.
- tests/unit/test_pip_figure.gd (spaces, 9 tests): LabelPip bar == dot at every scale (and Fira's own bar is > 1px wider, so the test cannot pass on a theme that dropped the bar font); every band's figure through LabelPip/LabelMorale/LabelBody/LabelLog/LabelClass at 100/125/150 — each pip within 1px of k × the period's pitch, each glyph within 1px of the pitch (the server's running-position rounding: 7.2px lays out as 7,7,8,7,7 — for bars and dots alike), bar vs dot within half a pixel; the bar is shaped from the variation's base (a fixed_size-0 FontFile, system fallback off, one glyph at the registered size, LA8), the letters and the period from the Fira copy; an unregistered size (17) shapes "|" to Fira's own bar; caching per base/scale and one bar font per weight; LabelPip == LabelSmall's size/colour and LabelSmall keeps no fallback; the face table == Type.at(24, scale), the three sheets load at (10×size, size), ascent/descent proportion, faces_at(100) == faces(), unknown scale → 24; the 125/150 themes shape all ten faces (and the mixed roster line) from THEIR sheet, one em of the sheet's size; with_faces(base, scale) keeps its pinned shape with the scale's sheet.
- Locked batch 2 (scratchpad/batch_W4-PIP_2.sh): PARSE_CHECK OK (178 scripts); the unit test 8/9 (LabelMorale at 150: a 7.2px pitch reported as 7/8 per glyph — the running-position rounding; the per-glyph tolerance was half a pixel, now one, positions stay at one); BEFORE shots taken with HEAD's Fonts.gd/Theme.gd swapped in (both this unit's files; restored byte-identical inside the same lock, `cmp` checked) → `before_Tavern_emoji`, `before_Guildhall_emoji`, `before_Guildhall_text150`; AFTER shots. All SHOT OK; the "1 RID allocations … leaked at exit" lines are the shot tool's own and appear on the before shots too.
- REGRESSION SEEN AND FIXED (Guildhall_emoji.png, first take): the "Lowest morale first" strip drew the class line ON TOP of the morale line. Guildhall's `_line_h` measures a line by the variation's BASE font ("the text font's own height, not the tallest fallback") and the base is now the bar font, whose caches had ascent 0. The bar font now carries Fira's ascent/descent at every registered size (it stands where Fira stood), so `_line_h` reads the text's height as before; test_pip_figure pins the bar font's metrics == the Fira copy's and the variation's height == max(text, faces), unchanged. No Guildhall edit.
- Locked batch 3: PARSE_CHECK OK (178); `TESTS PASSED 9 test(s) in 1 file(s)`; nine after shots SHOT OK.

## Resumed (second agent, 2026-09-15 — the first was killed by a session restart after batch 3)

- State found on disk: Fonts.gd (02:11), Theme.gd (02:09), test_pip_figure.gd (02:11) all OLDER than scratchpad/run_W4-PIP_3.log (02:13: PARSE_CHECK OK 178, `TESTS PASSED 9 test(s) in 1 file(s)`, nine SHOT OK), so the log tested the files as they stand. Re-read all three in full: Fonts.gd's face tables, `faces_at`, `_face_font`, `with_faces(base, scale)`, the pips section (`_pip_font`, `_period_advance`, `_register_pip`, `with_pips`) are complete, no half-written function; Theme.gd's label block builds the four glyph variations on `with_pips` and registers `LabelPip`; 0 space-indented lines in either. handoff-W4-PIP.md (02:18) carries the two Tavern edits and the notes. W4-CURSOR's "LabelPip at 125 … expected ~3.84 got 4.0" was the half-pixel per-glyph tolerance batch 2 widened to one pixel (the server's running-position rounding); W4-HYGIENE's later `verify.sh --fast` ran 1956 tests in 96 files (this file included) → VERIFY OK. Re-verifying under the lock below anyway.
- Batch 4 (scratchpad/batch_W4-PIP_4.sh, one lock hold, log scratchpad/run_W4-PIP_4.log): `PARSE_CHECK scanned 178 script(s)` / `PARSE_CHECK OK`; `TESTS PASSED   9 test(s) in 1 file(s)` — the 125 LabelPip case W4-CURSOR saw is green.
- Shots viewed (Read tool), build/shots/w4pip/:
  - `Guildhall_emoji.png` (Guildhall --fixture --set=emoji_free=true): every card's morale line ("Tiny — 14 ||........", "Spoof — 31 ||||......", "Rhona — 45 |||||.....") and all twelve strip entries carry a ten-cell figure at one pitch; the class line sits under the morale line at the strip's old pitch (the `_line_h` regression is gone); Bork's "87 |||||||||." in the strip is a compact even run.
  - `crop_cards_before_after.png` / `crop_strip_before_after.png` (HEAD above, this tree below): before, the bars are ~9px and the dots ~5px so "|||||....." reads as two runs of different density; after, the bars are 2px stems at the dot's own 4.8px pitch, so the count is a length. The line height did not move.
  - `Guildhall_fixture_text150.png` (Guildhall --fixture --set=text_scale=150): the card faces and the strip faces are crisp 36px pixel faces on the 30px morale line — no 24px face on a 36px line, no blur. `crop_faces150_before_after.png`: before (top) 24px faces looking small beside 30px names; after (bottom) 36px faces, same authored pixels at 1.5x, proportionate.
  - `crop_pips150.png`: the 150 cards and strip with emoji_free on — even bars at 3px stems, 7.2px pitch.
  - `crop_sidebar150_before_after.png`: the handoff's Note — the Guildhall sidebar's Roster block at 150 clipped the morale chart's lower half at HEAD and clips it entirely now (the face-carrying LabelBody lines are 36 tall at 150 instead of 29). Screen-owner call, recorded in the handoff; nothing at 100 moved (crop_sidebar100_before_after.png).
  - `Tavern_emoji.png` / `crop_Tavern_seats.png`: the seat card's "starts at 46 |||||....." is STILL Fira's uneven figure — the site is on LabelSmall, which is pinned to carry no fallback (test_theme_kit:447-449), so the plan's Tavern acceptance lands with handoff §1 (one token). Batch 5 below shoots the handoff applied through a scratchpad preview scene so the line can be seen before the orchestrator applies it.
- Batch 5 (scratchpad/batch_W4-PIP_5.sh; scratchpad/TavernPipPreview.gd + .tscn = the real Tavern with handoff §1/§2 applied after build, never shipped; log run_W4-PIP_5.log): four SHOT OK. `Tavern_emoji_handoff.png` / `crop_Tavern_seats_handoff.png` (LabelSmall above, LabelPip below, 2x): the seat card's "starts at 46 |||||....." is an even ten-cell run at the dot's pitch — the plan's Tavern acceptance line shows. `crop_Tavern_manage_handoff.png`: the Manage row's "Bork — 87 |||||||||." even on LabelMorale (right) against the wide LabelName run (left).
- REGRESSION FOUND IN THE HANDOFF, batch 6 (`Tavern_emoji_text150.png`, the real Tavern at 150, against `Tavern_emoji_handoff_text150.png`; crops `crop_Tavern_seats150_nohandoff.png` / `crop_Tavern_seats150_handoff.png`): with §1 applied, LabelPip carried the 36px face sheet at 150, so the seat card's morale line grew from Fira's 25px to 36 and Pauline_4's card (whose "Wizard — Common" already wraps to two lines at 150) pushed its Look button ~12px below the card's bottom rim; without the handoff all four buttons sit inside their cards. RULES §2: layouts must not overflow at 150 — a handoff must not hand that over. Fix taken (owned files only): LabelPip is LabelSmall + the bar font and NOTHING else — no face sheet, system fallback open, so its line height is LabelSmall's at every scale and, with the option off, the seat card draws exactly what it draws today (HALL-05's face list never included LabelSmall; the authored face on the seat card is a Tavern-owner call that costs the card ~11px at 150 — noted in the handoff). `Fonts.with_pips` gains `faces := true`; the theme passes `false` for LabelPip; test_pip_figure splits PIP_VARIATIONS (evenness, all five) from FACE_VARIATIONS (the four that carry the sheet) and pins LabelPip's height == LabelSmall's.
- Batch 7 (scratchpad/batch_W4-PIP_7.sh, log run_W4-PIP_7.log) after the bare-LabelPip fix: `PARSE_CHECK OK` (178); `TESTS PASSED   9 test(s) in 1 file(s)` (the file now also pins: LabelPip's height/ascent == LabelSmall's at 100/125/150, its fallbacks == [the shared Fira Sans], its bar font system-fallback OPEN, no sheet rid anywhere in it, faces shape to no sheet through it; one bare variation serves every scale; the four FACE_VARIATIONS keep every previous pin). Shots re-taken and viewed: `crop_Tavern_seats150_before_after.png` (real Tavern above, handoff below, both at 150): the four cards' Look/Looking buttons sit at the same y in both, the figure even below; `crop_Tavern_seats_faces_handoff.png` (option off, handoff applied): the seat card prints the same system emoji it prints today — no .notdef box; `crop_Tavern_seats_handoff.png` (100): line positions unchanged, figure even.
- handoff-W4-PIP.md §1 rewritten for the bare LabelPip (nothing on the card moves at any scale; the face on that line is a Tavern-owner call, ~11px at 150); `python tools/apply_handoff.py build/plan/handoff-W4-PIP.md --dry-run` → APPLIED #1, APPLIED #2 (dry), Tavern.gd untouched (`git status` clean for it).
- A1 ticked: the plan's shot with the handoff applied (preview scene) shows the even figure; the shipped tree without the handoff shows it on every LabelMorale/LabelBody site (Guildhall_emoji.png). A3 ticked: Guildhall_fixture_text150.png / crop_faces150_before_after.png. A4 ticked: test_label_pip_sets_bar_and_dot_at_one_advance (bar == dot within half a pixel at 100/125/150, and Fira's own bar is > 1px wider). A5 ticked: `git status` shows only Fonts.gd, Theme.gd, test_pip_figure.gd (+ its .uid), the two plan files, scratchpad/ and the ignored build/shots/; Cards.gd/GameSettings.gd untouched; every size in Theme.build is `Type.at(...)`. A7 ticked: shots listed above, each viewed with the Read tool.
- Judgement calls carried forward from the first agent (unchanged): the font-layer bar instead of a third TTF (Cascadia Mono not copied in — the figure keeps Fira's 0.24em pitch; a 0.586em mono cell would have been 117px on a 215px card and needed nine split Labels by handoff); `Fonts.MORALE_FACE_FONT = true` (Q17) untouched.
- Batch 8, the final gate (scratchpad/batch_W4-PIP_8.sh under one lock; full log scratchpad/verify_W4-PIP.log): `GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` →
  `PASS  class cache regenerated` · `PASS  LINT OK  no cross-file class_name references in sim/ or game/` · `PASS  MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)` · `PASS  PARSE_CHECK scanned 178 script(s)` · `PASS  16 generated file(s) agree with tools/gen_items.gd` · `PASS  ART CHECK  generators=7  runtime files: 204 agree  0 DIFFERS  0 MISSING` · `PASS  TESTS PASSED   1956 test(s) in 96 file(s)  [65970 ms]` · stages 4-8 SKIP (--fast) · `VERIFY OK`.
  `tools/lint_motion.sh` → `MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)` / `and every colour under game/ is a Palette role (docs/13 §3)`.
  A2 ticked: test_a11y_legibility.gd and test_screens.gd are inside the 96 files, green. A6 ticked.
- No refdiff numbers: the plan's W4-PIP section names none (its acceptance is the two shots and the test); no generator changed (ART CHECK 0 DIFFERS 0 MISSING is the tree's, not this unit's).
- Left undone, and why: nothing in the unit's own text. The two Tavern sites are the handoff (not owned). The Guildhall sidebar at 150 (chart clipped by face-carrying LabelBody lines, already clipped at HEAD) is the handoff's Note for the screen owner. KIT-20 stays blocked (Q14).

## Files

- game/ui/Fonts.gd — FACE_SHEETS / FACE_SIZE_AT / FACE_ASCENT_AT, `faces_at(scale)`, `_face_font`, `with_faces(base, scale)`; the pips section: `_pip_font(file, system_fallback)`, `_period_advance`, `_register_pip`, `with_pips(base, size, scale, faces)`.
- game/ui/Theme.gd — LabelMorale / LabelBody / LabelLog / LabelClass on `with_pips` at `Type.at(size, scale)`; new `LabelPip` (SMALL, TEXT_LOG, the bar only).
- tests/unit/test_pip_figure.gd — 9 tests (new).
- build/plan/handoff-W4-PIP.md — two Tavern.gd one-token edits + notes.
- build/shots/w4pip/ — before_*, the nine after shots, the four handoff previews (scratchpad/TavernPipPreview.gd + .tscn, not shipped) and the crops named above.
