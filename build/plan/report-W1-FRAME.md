# Report — W1-FRAME — one header, one rail, one lockup, a wide-safe frame

Unit: wave 1, 00-plan.md §2 "W1-FRAME". Findings closed: KIT-03, HALL-19, TOWN-05, TOWN-30, KIT-23,
CRITIC-R03 (chips in one place, standing tip), KIT-04, TOWN-04, CRITIC-C13 (rail label), KIT-13, CRITIC-C12
(one lockup, `Frame.LOCKUP`), KIT-15 (`Type.num`/`Type.gold`), TOWN-08 (full-bleed scrims), COMBAT-12
(`wordmark_33` + the lockup table), CRITIC-G03 (expand).
Owned: game/ui/Frame.gd, game/ui/Type.gd, tools/art/gen_wordmark.py, game/assets/ui/wordmark_33.png
(+ .json + .import), tests/unit/test_frame_header.gd (new).
Handoff: build/plan/handoff-W1-FRAME.md (notes for the screen units; no edit this unit needs applied).

Written as each item finished (rule 3).

## Acceptance (00-plan §W1-FRAME, verbatim where it can be)

- [x] A1 `test_frame_header.gd`: the Town day chip's `tooltip_text` contains "reputation"
- [x] A2 `Type.gold(12480) == "12,480 G"` and `Type.gold(60) == "60 G"`
- [x] A3 header region (0,0,400,74) pixel-identical across the archetype-A fixture shots (measured with PIL over the shots this unit takes plus build/shots/all/fixture)
- [x] A4 no rail-label pixel right of x=206 (measured on the shots)
- [x] A5 Town shot: rail edge fades, no plate pixels between header and sidebar or sidebar and strip
- [x] A6 `wordmark_33.png` shipped with .json + .import; `96 + width <= 352 - 8` asserted; the lockup table (`Frame.LOCKUPS["33_compact"]`) exists for W2-RAIDVIEW/W2-RESULTS
- [x] A7 `test_a11y.gd` (focus order, tree != reading order, Tab leaves rail) green; a11y_smoke both sweeps green
- [x] A8 chips and rail size through `Type.at(size, Theme_.scale_of(node))`; the 150 shot no longer cuts "Day 23 · Unknown"
- [x] A9 under `expand` (1820x1024) the sidebar and strip anchor their right edge to the viewport and the extra scene width is GROUND_RAIL
- [x] S1 shots taken and viewed: `Town --fixture`, `Guildhall --fixture`, `Town --fixture --focus`, `Town 1820x1024 --fixture --set=display_aspect=expand`, `Town --fixture --set=text_scale=150`
- [x] G1 `tools/art/gen_wordmark.py`'s existing outputs byte-identical (md5 before/after)
- [x] G2 parse check OK after every edit; new test passes; `verify.sh --fast` — every stage but one green, the one red is W1-STAGE's `test_scene_stage.gd` (their files, mid-edit; summary pasted below); `lint_motion.sh` MOTION LINT OK

## Judgement calls (decided inside the plan's text)

- J1 **Header chips stay Labels, not focus stops.** W0-SHOT's observation asked the shell to decide. A chip does nothing when activated and Godot shows no tooltip on focus, so a focusable chip is exactly the "stop that does nothing" `Frame._focusable`'s own comment refuses (docs/13 §13.2's dead end). The header region stays empty, Tab leaves the rail for the scene (unchanged `test_a11y`), and `--tab=1` keeps landing on the scene's entry. The standing countdown is also printed as a Label on the Board ("120 more to Known"), so a keyboard player has a route to it. Pinned by `test_the_chips_are_labels_and_not_focus_stops`.
- J2 **Rail label: columns shift AND a step-down, then the clip.** The plan lands TOWN-04's second option (glyph x 16, label x 62, `clip_text`); measured, "Adventure's Board" at 18px is 150px wide and 62 + 150 = 212 still crosses x=206, so the label would ellipsise at 100% — against CRITIC-C13's "keep the label whole". TOWN-04's first option (render at `Type.NAV - 2` when the string overflows) is in the same cited finding, so both land: `_rail_label_size` steps from `Type.at(NAV, pct)` down to `RAIL_LABEL_FLOOR = NAV - 2` until the theme's LabelBody face fits 145px, and the ellipsis is the last guard (at 150 the long label is whole at 16px while its neighbours are at 27px; ellipsis only if even 16 does not fit). Short labels keep `Type.NAV` exactly, so nothing else moves.
- J3 **`LOCKUP` ignores the `tagline` opt.** One constant decides for every framed screen (03 §6 sameness); Town's `"tagline": ...` line is inert and noted in the handoff for W2-TOWN. Q05 flips the constant to "58".
- J4 **wordmark_33's parameters are the 58's × 0.76** (size 63→48, run 305→232), through the untouched `render_mark`: the same drawing smaller, not a resample. Output 235x47, baseline 33 (the 235 includes the outline/shadow padding, as the 44's 271 does). 96 + 235 = 331 ≤ 344. No `Type` constant added for it — RULES q3 (Type sizes untouched) and test_a11y_legibility's hand-enumerated READABLE_SIZES.
- J5 **The compact emblem crop is `Rect2(0, 0, 65, 57)`.** Spec 02 §4.1 says 65x58 at (22,12); emblem.png is 82x57 and its cream skull+bones occupy columns 0..65 (measured), so the crop is the left 65 columns at the PNG's full 57 height. An `AtlasTexture` region — the PNG (W1-CHROME's) is untouched.
- J6 **Expand: the scene still widens; the extra width is GROUND_RAIL under the plate.** W0-SHOT's 1820 shot shows `relayout` already right-anchors sidebar/strip/chips and widens the framed scene, which reads well. So the landed reading of "the extra width is GROUND_RAIL" is an `ExpandGround` ColorRect as the scene host's first child covering only the columns the canvas added (from x 928 in a framed scene, from 1536 on the camp) — zero-width at 1536 so no reference pixel moves; where a plate runs out, GROUND_RAIL shows instead of the page ground. Framed screens keep explicit placement (no anchors — RULES q10 / LESSONS).
- J7 **Full-bleed moat = the sidebar host meets the rule and the strip (y 76..726 — the row right under the 2px rule; 77 left one line of plate, measured 14:xx)**; the bronze rule becomes a scrim over the header plate (fades with it 414→440) plus a plain rule over the sidebar column (spec 03 §1: "the rule exists only over that plate and over the sidebar"). The sidebar rule reuses the existing "7A6152" so no new hex enters Frame.gd for W1-CHROME's handoff.
- J8 **Chips at scale: the host widens, the gap narrows.** `chip_host.size.x = Type.at(880, pct)` keeps the row right-anchored (ALIGNMENT_END), and `chip_gap(pct)` = 24/19/16 gives back part of what the numerals take — at 150 the four chips need ~1130px of the ~1145 right of the lockup. Identity at 100 by construction.
- J9 **The standing tip prints through `Type.num`** ("12,480 reputation · 200 more to Known") — KIT-15's rule applied to the one new numeral site; tooltips are not read by `_texts()`.
- J10 **`Type.num` is en-US with one `THOUSANDS_SEPARATOR` constant** — docs/13 §4.3 wants the separator from a string table that does not exist (RULES-14); the constant is the seam for it.

## Log

(one entry per landed item: what was done, what was seen)

### 2026-09-14 — read-in
- Read 00-plan §0 + §2 ownership + §W1-FRAME + §6 Q05, RULES.md in full, LESSONS.md, the fifteen cited findings, handoff-W0-SHOT (the `--tab=1` observation), report-W0-TEXTSCALE (the door: `Type.at`, `Theme_.scale_of`), Frame.gd / Type.gd / gen_wordmark.py in full, spec 02 §4.1, spec 03 §1/§6/§9.
- Measured on the existing shots (PIL): the rail label "Adventure's Board" spans x 74..223 at BOTH text_scale 100 and 150 — the rail's `font_size` override is a fixed `Type.NAV`, so the rail never scaled; the header at 150 (build/shots/Town_text150.png) cuts the day chip to "Day 23" at the frame edge; wide-expand AdventureBoard (1820) already right-anchors the chips/sidebar/strip through `relayout` and widens the framed scene.
- `python tools/art/gen_wordmark.py` on the untouched generator: all nine existing outputs (three PNGs, three JSONs, splash, icon_256, icon.ico) md5-identical before/after — the generator is deterministic, so adding one more call is safe.
- emblem.png (82x57): the cream skull+bones occupy columns 0..65; columns 66..81 are ground — the compact header's 65-wide skull (spec 02 §4.1) is the crop `Rect2(0, 0, 65, 57)`.

### 09:45 — code landed (Type.gd, Frame.gd, gen_wordmark.py, wordmark_33)
- Type.gd: `THOUSANDS_SEPARATOR`, `num(n)`, `gold(n)` under the scale block (tabs; no size constant touched — RULES q3, test_a11y_legibility's READABLE_SIZES list is unchanged).
- Frame.gd: `LOCKUP = "44_tagline"` + `LOCKUPS` table (58 / 44_tagline / 33_compact) + `draw_lockup(host, kind) -> right edge`; `standard_chips` → `_fill_chips` (Type.gold/Type.num, `DayChip` named, `tooltip_text = standing_tip(state)`), `refresh_chips(p, state)`, `standing_tip(state)`; rail glyph x 16 / label x 62 / `clip_text` + TRIM_ELLIPSIS / `_rail_label_size` steps Type.at(NAV, pct) down to NAV-2 until the theme's LabelBody face fits 145px; chip host width `Type.at(CHIPS_W, pct)` and `chip_gap(pct)` 24/19/16; full-bleed: `_scrim()` GradientTexture2D header (opaque 414 → 0 at 440) + divider scrim + `sidebar_rule` over the sidebar column, rail scrim (150 → 200), `rail_edge` null, sidebar host y 77..726; `expand_ground` ColorRect (GROUND_RAIL) inside the scene host, zero-width at 1536. Parts fields `header/divider/rail/rail_edge` widened to Control; `sidebar_rule`, `expand_ground`, `lockup_right` added. No new hex (the bronze "7A6152" is reused for the sidebar rule).
- gen_wordmark.py: one added call `wordmark(48, 232, "wordmark_33")` (the 58's 63/305 × 0.76) + docstring. Output: wordmark_33.png 235x47, baseline 33, cap_top 2 → 96 + 235 = 331 ≤ 344 (352 − 8). The nine pre-existing outputs md5-identical after the run (G1 below). `.import` copied from wordmark_44's with a fresh uid (`uid://c5eylllx5qy62`) and the md5 of its own res path.
- `PARSE_CHECK scanned 154 script(s) / PARSE_CHECK OK` under the lock (09:44). No space-indented line in either tab file (grep).
- [x] G1 ticked: md5sum -c over wordmark_58/44/tagline (.png + .json), splash.png, icon_256.png, icon.ico — all OK after the regenerate.

### 09:52 — test written, batch 1 queued
- tests/unit/test_frame_header.gd (four-space; 12 tests): Type.num/gold values; the mounted Town's `DayChip` tooltip contains "reputation" and "60 G"/"Day 1"/"Unknown" stay Labels; `standing_tip` counts down to Known; `refresh_chips` rebuilds four in place (and one "No guild loaded." for null); no chip is a focus stop and no chip is in any `focus_order` region; every Nav_* keeps its name/text, glyph x 16, label x 62, width 145, clip_text, TRIM_ELLIPSIS, size in [NAV-2, NAV] with "Camp" at NAV and "Adventure's Board" stepped down; `LOCKUP == "44_tagline"`, the 44 mark + tagline + rule at the same position on a full-bleed host that passed a tagline and a framed host that did not, `lockup_right == 374`; `wordmark_33` exists with a baseline, `draw_lockup(host, "33_compact")` ends ≤ 344, emblem AtlasTexture 65x57 at (22,12); full-bleed: rail/header/divider are GradientTexture2D scrims (GROUND_RAIL/GROUND_FRAME → alpha 0, 200/440 wide), `rail_edge` null, `sidebar_rule` over the sidebar, sidebar y 77..726, overlays MOUSE_FILTER_IGNORE, scene host still before the rail; framed keeps ColorRects; 1820 host: sidebar/strip/chips right-anchored, `expand_ground` = 284px of GROUND_RAIL from x 928, zero-width at 1536; text_scale 150: chip host `Type.at(880,150)`, gap `chip_gap(150)`, "Camp" at `Type.at(NAV,150)`.
- Batch 1 (one lock hold, background): `--import` (the new PNG), parse check, the whole unit suite, then the five shots `W1F_Town`, `W1F_Guildhall`, `W1F_Town_focus`, `W1F_Town_expand` (1820x1024 expand), `W1F_Town_text150` into build/shots/.

### 09:56 — batch 1 wasted by another unit's parse error (not mine to fix)
- The import pass / parse check / suite all failed on `res://game/ui/Fonts.gd` (W1-CHROME's, mtime 09:46:22): `Invalid argument for "set_glyph_advance()" function: argument 2 should be "int" but is "Vector2i"` (Fonts.gd:129). Theme.gd preloads Fonts, Widgets preloads Theme, every screen preloads Widgets — so `PARSE_CHECK FAILED (1)`, `test_a11y.gd` would not compile, `Theme_.current()` returned null and every shell test cascaded ("Cannot call method 'has_font' on a null value" from `Frame._theme_font`). My 09:44 parse check (154 scripts) was green; the tree changed under me. Rule 1: Fonts.gd is not edited here.
- Made `Frame._theme_font` null-safe anyway (a null Theme falls back to `ThemeDB.fallback_font`) — the cascade showed the hole, one guard closes it.
- A watcher (scratchpad/batch2.sh, background) polls Fonts/Theme/Widgets mtimes, parse-checks under the lock on each change, and re-runs batch 1 the moment the tree parses.

### 10:04 — batch 2 ran (recovered from disk by the resuming agent)
- The watcher's re-run produced build/shots/W1F_{Town,Guildhall,AdventureBoard,Tavern,Market,RaiderDetail,LoadSave,Settings,RaidPrep,Completion,Town_focus,Town_expand,Town_text150}.png at 10:04-10:05 with Frame.gd as of 09:51; the previous agent was killed before writing the entry. The 13:49 `.verify.log` (another unit's run) shows the tree parsing and `test_frame_header.gd :: test_every_framed_screen_draws_the_same_lockup` failing: `expected 374.0, got 375.0` — wordmark_44.png is 271 wide, so `104 + 271 = 375` beat the `maxf(right, 374.0)`.

### 14:xx — resumed: the one red test, then the tree measured
- **Lockup right edge.** `draw_lockup` now returns `TAGLINE_RULE_END` (374.0, a new const) for the tagline lockup: the mark's PNG carries a 1px outline/shadow past its glyph run (03 §6: run x 97–372) and the rule is drawn to 374 precisely so the block right-aligns there. The test asserts the const and that `TaglineRule` ends at 374. (Not a hall-screen dependency after all — the test builds bare shell hosts.)
- **A3 measured on the 10:04 shots (PIL, region (0,0,400,74), Town as reference):** every archetype-A shot differs by at most 13 in any channel with 0 pixels beyond ±24; the busiest rows are 70-73 (touching the scene band) and the bbox starts at y 14 (the cream mark). That is the SceneStage glow (screen-space post-process, LESSONS:118-123) blooming across the header from screens that mount a stage — the layout, lockup and chips are identical. Batch 3 adds `--set=reduced_effects=true` shots of Town/Guildhall/Market for the glow-free comparison.
- **A4 measured:** in the "Adventure's Board" row band (y 340..365) the rightmost label-coloured pixel is x=199 on every framed shot (the label is whole at 16px); on the 09:24 pre-change Guildhall shot it was 206+ (the overflow). Town reads 206 only because the rail scrim is transparent by x=200 and the plate's grey pixels sit there.
- **A5 measured on W1F_Town:** rows 74-75 at the sidebar columns are the bronze rule (123,98,82); row 76 was tent canvas ((25,59,36) at x 1144) — one plate line between rule and panel; rows 726-733 under the sidebar are page ground, not plate; rail row 400 fades (12,21,28) at x 100 → plate colours by x 200; header row 30 fades from (13,22,28) at x 414 → plate by 440. **Fix landed:** the full-bleed sidebar host now starts at `DIVIDER_Y + 2` (76) instead of `RAIL_TOP` (77); the test moves with it.
- **A8 measured on W1F_Town_text150:** chip band light pixels span x 458..1499 — inside the lockup's 374 and the gutter's 1519; no cut.
- **A9 measured on W1F_Town_expand (1820x1024):** chips end at x 1803 (= 1820 − 16 − 1), sidebar host 1426..1804, header spans the canvas; the extra scene width is under the sidebar on the full-bleed camp (the expand ground is a guard for plates narrower than the canvas — Guildhall expand shot added to batch 3 to see it).
- Observations that are not mine (handoff): Town's sidebar panel draws to x≈1533 (border at 1528/1533) while the framed screens' ends at 1519 — a PanelContainer widened by an uncapped Label in Town's sidebar copy (LESSONS:124); the Board callout's plate is clipped by the sidebar; the strip's disabled "View All" reason wraps one character per line.
- Batch 3 queued under one lock hold: `--import`, parse check, the whole suite, then the five contract shots + Guildhall expand + three reduced_effects shots.

### 13:56 — batch 3: parse OK, the suite, nine shots (one lock hold, 76 s)
- `PARSE_CHECK scanned 160 script(s) / PARSE_CHECK OK`; suite `TESTS FAILED 1/1708` — the one is `test_widgets_kit.gd :: test_the_label_stamp_keeps_its_contract_and_leans` (W1-KIT's, not mine; W1-ICONS' two from the 13:49 log have gone green). All 12 `test_frame_header.gd` cases pass. `tools/lint_motion.sh`: `MOTION LINT OK`. No space-led line in Frame.gd/Type.gd, no tab-led line in the test. Frame.gd's `Color("` literals: 9 distinct, the same 9 as HEAD (W1-CHROME's handoff lines still match).
- [x] A1/A2 ticked by the test run (`test_the_town_day_chip_carries_the_standing_tooltip`, `test_type_num_and_gold_carry_the_thousands_separator`).
- [x] **A3 ticked — measured, not eyeballed.** With `--set=reduced_effects=true` (glow off) the header region (0,0,400,74) of Town, Guildhall and Market is **pixel-identical: 0 differing pixels** in every pairing. With glow on, Town vs Guildhall differs in 29600 px (max 48) and Town vs its own glow-off shot in 21943 px (max 4): the SceneStage's screen-space glow (LESSONS:118-123) blooms across the band from whichever stage each screen mounts (W1-HALL's dimmed camp on Guildhall); nothing in the layout moves. The 10:04 shots (all ten archetype-A screens, Town as reference) had max 13 and 0 px beyond ±24 — same conclusion before W1-HALL's stages landed.
- [x] A4 ticked: rightmost label-coloured pixel in the "Adventure's Board" row band is x=199 on Guildhall/AdventureBoard/RaiderDetail/LoadSave/Settings/Completion (the label is whole at 16px, column x 62..206 clipped); the pre-change 09:24 Guildhall shot read 206+ (the finding's overflow).
- [x] A6 ticked: wordmark_33.png 235x47 + .json (baseline 33) + .import on disk; `test_the_compact_lockup_ends_inside_the_raid_plate` asserts `draw_lockup(host, "33_compact")` ≤ 344 and the 65x57 AtlasTexture crop at (22,12).
- [x] A8 ticked: W1F_Town_text150 viewed — the four chips read "12,480 G · 320 · Roster 12 of 15 · Day 23 · Unknown" whole at 150, light pixels x 458..1499 (lockup ends 374, gutter 1519); the rail's short labels sit at 27px and "Adventure's Board" at the 16px floor (J2 — visibly smaller than its neighbours; the alternative is an ellipsis, which CRITIC-C13 refused).
- [x] A9 ticked: W1F_Town_expand and W1F_Guildhall_expand (1820x1024) viewed — the header band spans the canvas, the chips end at x 1803 (= 1820 − 16 − 1), the sidebar host runs 1426..1804, the strip to 1790; the widened scene is covered by the camp plate (Town) and by W1-HALL's stage (Guildhall), so the GROUND_RAIL expand ground is not visible on either — it is the guard for a plate narrower than the canvas and `test_under_expand_the_sidebar_and_strip_follow_the_viewport_edge` pins its 284px.
- Shots viewed (Read tool): **W1F_Guildhall** — header/chips identical to Town's, rail whole, the roster on the dimmed camp; **W1F_Town_focus** — the 2px steel focus ring around "Camp" (976 steel pixels x 0..212, y 103..139), Tab order untouched; **W1F_Town_text150** and **W1F_Town_expand** as above; **W1F_montage** (Guildhall_expand, Town_noglow, Guildhall_noglow, Market_noglow at half size) — Market still prints "12480 G" with the sun icon because Market.gd builds its own chips (handoff line for W2-MARKET; the header lockup is identical regardless).
- **W1F_Town.png (13:57:11) came back with an EMPTY scene** — `stage_camp.json`'s mtime is 13:57:23: W1-STAGE rewrote it under the shot and SceneStage got a half-written file (the same `Parse JSON failed` noise the 13:49 verify log shows). Not a Frame defect; the retake is queued behind the gate, and A5/S1 wait for it.

### 16:17 — resumed (third agent): read-in
- Read 00-plan §0 + §2 ownership + §W1-FRAME, RULES.md in full, the report and both handoffs (W1-FRAME, W1-CHROME), Frame.gd / Type.gd / test_frame_header.gd in full, the gen_wordmark.py diff. Working tree: Frame.gd +396/-76, Type.gd +31, gen_wordmark.py +11 vs HEAD; wordmark_33.png/.json/.import untracked and present (235x47, baseline 33). `.verify.log` (16:13, another unit): `PARSE_CHECK OK` (160 scripts), `TESTS PASSED 1710 test(s) in 80 file(s)` — test_frame_header.gd is green in the tree.
- W1F_Town.png was retaken at 14:07 (the 13:57 empty-scene retake the last entry queued) with W1F_Town_noglow.png; nobody viewed or recorded them. All five contract shots are retaken below after the W1-CHROME handoff lands so the evidence matches the final code.
- Palette.gd carries every token the W1-CHROME handoff names for Frame.gd (INK_NAV*, GROUND_SEAM, HEADER_RULE, HEADER_DIVIDER, HEADER_RULE_LIT, EDGE_RAIL_CORE — same hexes). Applying its seven Frame.gd edits next.
- [x] **W1-CHROME handoff applied to Frame.gd (edits 1-7):** GROUND_SEAM, HEADER_RULE, HEADER_DIVIDER (the `bronze` local that also feeds the full-bleed divider scrim and sidebar rule), EDGE_RAIL_CORE, INK_NAV_GLYPH_ACTIVE/INK_NAV_GLYPH, INK_NAV_ACTIVE/INK_NAV, HEADER_RULE_LIT. `grep -n 'Color("' game/ui/Frame.gd` now prints nothing (KIT-10 lint line). Same hexes (test_theme_kit pins them), so no pixel moves; tabs and CRLF preserved (868/868 lines, 0 space-led).

### 16:23 — batch 4 (one lock hold, 16:23:15-16:24:30): parse, a11y_smoke, the five contract shots, verify --fast
- `PARSE_CHECK scanned 160 script(s) / PARSE_CHECK OK` after the seven token edits.
- [x] **A7 ticked.** `A11Y SMOKE PASSED   26 screen mount(s)` — both sweeps; the only `warn` is the pre-existing `fixture AdventureBoard.tscn 10 pointer-only gesture target(s)` (RULES-07, W2-BOARD's). `test_a11y.gd` is green in the suite (no FAIL line names it; 1709 of 1710 pass, see G2).
- Shots (`SHOT OK` x5, 40 frames, `--fixture`): W1F_Town, W1F_Guildhall, W1F_Town_focus (`FOCUS Nav_home<Button> at 0,105 211x55 (4 shell regions)`), W1F_Town_expand (1820x1024, `display_aspect=expand`), W1F_Town_text150 (`text_scale=150`) — all into build/shots/, all five viewed with the Read tool.
- [x] **A5 ticked — measured on the fresh W1F_Town (PIL):** rail row 400 fades `(6,18,22)` at x 0 → `(12,21,28)` at 100 → `(25,37,22)` at 175 → `(59,70,22)` at 199 → tent canvas by 201: no hard edge. Header row 30: `(13,22,28)` at 414 → `(22,36,25)` at 420 → plate greens by 439-441. Sidebar column (x 1300): rows 74-75 bronze rule `(123,98,84)`, row 76 `(33,28,25)` = the panel's own dark edge, row 77 `(139,101,74)` its border — no plate line between rule and panel. Rows 726-733 under the sidebar and under the scene (x 1000) are page ground `(0,13,18)`/`(6,17,18)`, row 734 the strip's top: no plate between sidebar and strip.
- A3 re-measured with glow on: Town vs Guildhall header (0,0,400,74) differ in 2802 px with max channel delta 4 (the stage glow, LESSONS:118-123; the 13:56 reduced_effects pairing was 0 px); Town vs Town_focus differs only in bbox (27,54)-(113,74) — the ring's bloom above Nav_home; Town vs text150 only in (362,43)-(400,74), the bloom of the chip row that now starts at x 463. The lockup, rule and chips do not move.
- A4 re-measured: rightmost label-coloured pixel in the "Adventure's Board" band on W1F_Guildhall is x=199; at x 206/207 the pixels are rail ground `(6,18,24)`/`(0,13,20)`, x 208 the border core `(70,62,63)`.
- A8 re-measured: text150 chip band light pixels span x 463..1499 (100: 542..1500); A9: expand chips span to 1784 with the plate at 1803; sidebar host 1426..1804, header band spans the canvas.
- [x] **S1 ticked — what the five shots show.** *W1F_Town*: the camp full-bleed (the 13:57 empty-scene take is gone), the lockup on its gradient plate melting into the sky by x≈440, the four chips "12,480 G · 320 · Roster 12 of 15 · Day 23 · Unknown", the rail scrim fading into the grass with no border line, "Adventure's Board" whole, the sidebar meeting the bronze rule. *W1F_Guildhall*: the identical lockup and chips at the same pixels, the opaque rail with its core at x 208, the framed roster on the dimmed camp. *W1F_Town_focus*: the 2px steel ring around "Camp" (x 0..212, y 103..160) and nothing else changed. *W1F_Town_expand*: 1820 wide, the chips ending at the new gutter, the sidebar at 1426..1804, the camp plate covering the extra width (the GROUND_RAIL expand ground is under it — the guard for a narrower plate, pinned by the test). *W1F_Town_text150*: the four chips whole with the narrower gap, the short rail labels at 27px and the long one at the 16px floor (J2), the lockup untouched. Not mine, seen in the same shots and already in the handoff: the Town sidebar panel overhanging the gutter (x≈1533; ≈1818 under expand), the Blacksmith callout clipped under the sidebar, the strip's "View All" reason wrapping one character per line, the Town sidebar copy overflowing at 150; new this batch (added to the handoff): the roster strip's event log keeps its 1536-layout width under expand while `p.strip` runs to 1803.

### 16:29 — final gate: `verify.sh --fast` under the lock (16:29:15-16:30:33), twice the same answer
```
  PASS  LINT OK  no cross-file class_name references in sim/ or game/
  PASS  MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)
  PASS  PARSE_CHECK scanned 160 script(s)
  PASS  16 generated file(s) agree with tools/gen_items.gd
  PASS  ART CHECK  generators=6  runtime files: 184 agree  0 DIFFERS  0 MISSING
  FAIL  unit tests
  FAIL  test_scene_stage.gd :: test_reduced_motion_holds_lights_and_bubbles
TESTS FAILED   1/1710 failing  [34780 ms]
VERIFY FAILED  (full log: .verify.log)
```
- [x] G2 ticked with the caveat above: all 12 `test_frame_header.gd` cases pass (no FAIL line names the file), `test_a11y.gd` passes, 1709/1710 green. The one red is `test_scene_stage.gd :: test_reduced_motion_holds_lights_and_bubbles` ("two runs of one scene must pick the same bubbles at the same ticks" — node-name lists differ), W1-STAGE's test over W1-STAGE's SceneStage.gd, both written at 16:18 while this batch queued; the test references nothing in Frame/Type/Palette. The 16:13 `.verify.log` (before their edit) was `TESTS PASSED 1710`. Rule 1: not touched here. `tools/lint_motion.sh`: `MOTION LINT OK` (also run standalone at 16:19).
- Left undone: nothing in the unit's contract. Everything the plan names for W1-FRAME is on disk: Frame.gd (chips in one place + standing tip, rail 16/62 + step-down + clip, `LOCKUP`/`LOCKUPS`/`draw_lockup`, full-bleed scrims + sidebar rule, expand ground, text-scale door, W1-CHROME's tokens), Type.gd (`num`/`gold`), gen_wordmark.py + wordmark_33.png/.json/.import, test_frame_header.gd (12 tests), the five contract shots viewed, a11y_smoke both sweeps, handoff notes for W2-TOWN/W2-BOARD/W2-MARKET/W2-RAIDVIEW/W2-RESULTS/W1-KIT and the orchestrator (handoff-W1-CHROME edits 1-7 already applied).
