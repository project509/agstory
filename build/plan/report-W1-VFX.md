# Report — W1-VFX (the combat strips, the painted fire and the cloud strips exist)

Unit: 00-plan.md §2 "### W1-VFX". Findings: PIPE-02 (asset half), STAGE-03 (asset half), COMBAT-03 (VFX asset half),
CRITIC-C04 (PIPE's names win), STAGE-12, STAGE-14 (cloud asset), PIPE-10 (cloud asset).
Owns: tools/aseprite/gen_vfx.lua (new), tools/aseprite/gen_fire.lua, tools/art/gen_clouds.py (new), game/assets/vfx/**,
art/src/vfx/**, tests/unit/test_vfx_assets.gd (new). Handoff: build/plan/handoff-W1-VFX.md.

## Acceptance

- [x] A1 `tools/aseprite/gen_vfx.lua` (new) emits the six combat strips through `L.strip` (real frames + tag + durations,
      one-row PNG), seeded LCG only, and is picked up by `build_art.sh --gen`/`--check` through the `gen_*.lua` glob:
      `fx_spark_hit` 32x32x4, `fx_slash_arc` 64x48x5 (200° crescent, thicker leading edge), `fx_bolt_arcane` 24x48x4 +
      `fx_bolt_trail` 8x2, `fx_heal_sparkle` 32x32x6, `fx_burst_impact` 64x64x5, `fx_poof_smoke` 48x48x5; ramps from
      spec 02 §9.1 / 07 §4.2 (ice/fire/void/holy).
- [x] A2 `gen_fire.lua` re-authored: 2-3 filled, banded tongues (core/hot/body/tongue/edge) with jitter on the OUTLINE
      only, ≤ 6 colours and ≤ 4 connected components per frame; PNG dimensions identical (fire_hearth 512x56,
      fire_camp 320x52, fire_torch 96x24) so every scene JSON's `frame_w` and W1-STAGE's divisibility test hold.
- [x] A3 `tools/art/gen_clouds.py` (new): `clouds_far.png` and `clouds_near.png` 1536x220 RGBA, periodic in x, seeded
      numpy fBm, palette sampled from `stage_town.png`'s own sky rows.
- [x] A4 `game/assets/vfx/vfx.json` written beside the strips: name → {frames, frame_w, frame_h, fps, tag} for every
      strip this unit ships (the six fx, the three fires) plus the two cloud layers and the two stills.
- [x] A5 `tests/unit/test_vfx_assets.gd` (new): `ResourceLoader.exists` for every entry of vfx.json, `width ==
      frames * frame_w`, `height == frame_h`; the fire geometry pinned to the scene JSONs' declarations; per-frame fire
      colour count ≤ 6 and component count ≤ 4 read from the pixels; the clouds are 1536x220 and tile in x.
- [x] A6 A `.import` sidecar for every new PNG (copied from a neighbour, uid regenerated by Godot through the lock);
      the headless boot prints no `Failed to load` for game/assets/vfx.
- [x] A7 `./tools/build_art.sh --check` ends `0 DIFFERS 0 MISSING` with generators=6 (gen_vfx picked up).
- [x] A8 A refkit side-by-side of the new hearth against Concept 1's hearth crop (330,380,90,90), viewed with Read and
      described here.
- [x] A9 Shots `Tavern --fixture --frames=1,20` and `Town --fixture` show banded flames; viewed with Read and described.
- [ ] A10 `GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` green; summary lines pasted here;
      `tools/lint_motion.sh` prints `MOTION LINT OK`.

## Log (write-as-you-go)

- Read 00-plan §0 + wave-1 ownership + W1-VFX, RULES §1-§5, PIPE-02/10, STAGE-03/12/14, COMBAT-03, CRITIC-C04,
  LESSONS, report-W0-LIB (L.strip contract: `L.strip(name, w, h, n, fps, paint, {tag, src_dir, out_dir})`, every save
  through `L.save_png/save_ase/write_text` so `--script-param out=` redirects the whole run), gen_fire.lua, lib.lua,
  build_art.sh in full.
- Facts recorded: the tree's fire PNGs are 512x56 / 320x52 / 96x24 (8/8/6 frames of 64/40/16) with exactly 6 colours
  each already — the STAGE-12 defect is the per-pixel jitter (speckle), not the colour count. `stage_tavern.json`
  declares `frame_w: 48` against the 64-wide hearth frames (a W1-STAGE fix: the plan gives that unit
  `stage_tavern.json frame_w: 64`); this unit keeps the PNG at 64x56x8 so that fix lands cleanly.
- Concept 1 hearth crop (330,380,90,90) viewed at 4x: a tall central tongue with two shorter side tongues, four clean
  bands (near-white core at the base, yellow, orange, deep red-orange edge), no speckle, dark red-brown seam where the
  fire meets the logs. The old fire_hearth at 3x: cream-filled cones dissolving into salt-and-pepper orange at every
  edge and a scatter of loose spark pixels — the speckle STAGE-12 names.
- A1 DONE: `tools/aseprite/gen_vfx.lua` — six strips through `L.strip(..., {tag="play"})`, one LCG per strip
  (seeds 101..606), all shape work per-pixel in polar coordinates (a `ray`/`disc` pair, a local Euclidean
  `edt` for round bands, a Bayer `dither_erase` for the poof) plus `L.mask_ellipse`/`L.glow_mask` from the lib.
  Output: fx_spark_hit 128x32, fx_slash_arc 320x48, fx_bolt_arcane 96x48, fx_bolt_trail 8x2, fx_heal_sparkle
  192x32, fx_burst_impact 320x64, fx_poof_smoke 240x48 (+ .aseprite sources with real frames and the tag).
  Runs in 0.5 s. Viewed at 3x (scratchpad new_vfx_x3.png): the spark is a cream/gold starburst that throws
  seven rays and breaks into tip sparks; the slash is a steel-blue→cyan→white crescent revealed from the
  trailing end over three frames, thick at the leading edge, thinning to a dotted afterimage; the bolt is a
  periwinkle teardrop with an off-centre pulsing core, four orbiting sparks and a wobbling tail; the heal
  glint opens on the axes, turns onto the diagonals and lets three motes rise; the burst is a pink-white
  core with twelve ragged red spikes that hollows into a ring while eight sparks fall; the poof is a grey-blue
  cloud with a lit top that swells, rises and dithers away. Ramps are the spec 02 §9.1 hexes verbatim (arcane,
  impact, slash), the muzzle-flash ramp with one added mid orange, 07 §4.2's white/blue sparkle and grey
  3-step; nothing pure black or white.
- A2 DONE: `gen_fire.lua` — `tongue_mask` rasterises each tongue as a smooth profile (rises from a point,
  widest near the seat, slight taper at the bottom; tip leans with a per-tongue phase, height breathes twice
  per loop); the +-1px jitter is applied to the outline extents only, re-rolled every third row and never at
  the tip. The frame is the UNION silhouette: dark `edge` = the union's 4-neighbour outline, `tongue` = the
  union's 2px rim, then body/hot/core by each tongue's OWN Euclidean depth as a fraction of its half-width
  (max over tongues), scaled down toward the tip so the cores sit low. No sparks (the scenes' ember emitters
  do that). Three iterations, each viewed at 3x: (1) union-depth bands read as one cream blob with a rim;
  (2) per-tongue rims drew red cracks through the interior; (3) union rim + per-tongue interior — three
  distinct licks with their own yellow/cream hearts inside one outlined silhouette, the look of Concept 1's
  hearth. PNG sizes unchanged (512x56 / 320x52 / 96x24); PIL counts exactly 5 colours per strip; the
  component count is asserted per frame by the test (A5). ember.png and light_soft.png untouched (their
  blocks do not use the LCG; `--check` agrees on both).
- A3 DONE: `tools/art/gen_clouds.py` — periodic value-noise fBm (5 octaves; each octave's lattice is an
  integer number of cells across 1536 so every octave wraps), contrast-normalised, smoothstep coverage,
  vertical fade concentrated in the plate's real sky (the open sky is the top ~60 rows; mountains begin
  under it), "lit from above" shading quantised to three tones sampled off stage_town.png x 600..1536 y
  0..200 (light capped at 244 — never pure white), alpha quantised to 16 steps. far: 8 cells x 48px, alpha
  max 0.60, fades from row 70; near: 4 cells x 110px, alpha 0.72, fades from row 100. Column 0 vs column
  1535 mean abs diff 0.07 (far) / 0.0 (near) — periodic. `--check` mode byte-compares a regeneration
  (`GEN_CLOUDS CHECK OK`, 0.8 s); `--out DIR` mirrors the Lua `out=` shape. Viewed composited over the
  plate at offset 0 and rolled by 768 (the seam mid-frame): cirrus streaks in the top band, a few soft
  puffs drifting over the peaks, no seam, the castle still readable under the near layer.
- A4 DONE: `game/assets/vfx/vfx.json` written by gen_vfx.lua through `L.write_text` (so `--check` gates
  it): 14 rows — the seven gen_vfx files (recorded by `emit`/`row` as they are drawn), fire_hearth/camp/torch,
  ember, light_soft (declared, gen_fire.lua) and clouds_far/near (declared, gen_clouds.py).
- `./tools/build_art.sh --check` in the shared tree: generators=6 (gen_vfx picked up by the glob),
  `build/artcheck/game/assets/vfx/` holds 13 files and NONE is listed as DIFFERS/MISSING — every file this
  unit writes is byte-reproducible. The run as a whole is red (18 DIFFERS, 76 MISSING, all under
  game/assets/ui/icons and ui/) because W1-ICONS/W1-CHROME are mid-edit on gen_icons/gen_ui/gen_wipe in the
  same tree; that is theirs to close. Never ran `--gen` (it would execute their half-written generators into
  the tree) — this unit's generators were run directly with `"$ASEPRITE" -b --script`.
- A6 (sidecars): nine `.import` files written by a scratchpad script from fire_hearth.png.import — dest md5
  is md5 of the res:// path (checked: fire_hearth's own hash reproduces), uid a fresh 62-bit id in Godot's
  base-34 alphabet (a..y, 0..8; round-trips the existing uid). Godot fills the .ctex on the next --import.
- Fire, final pass (fourth iteration): tongues narrowed (hearth 30/30/38 wide at dx 2/32/13, camp 22/22/28),
  profile `t^0.75` with a 35% taper at the seat so the silhouette narrows onto the logs, lean 0.26; bands
  re-cut so the interior is mostly hot yellow with cream cores (body r < 0.28, hot r < 0.55) — the concept's
  fire is yellow-white inside with orange only at the licks. Viewed at 3x: eight distinct frames of three
  licking tongues, one outlined silhouette each, no speckle, no loose pixels.
- A5 DONE: `tests/unit/test_vfx_assets.gd` (9 tests, four-space): manifest names every PNG in the folder and
  every row exists; every row's PNG is `frames * frame_w` x `frame_h` and animated rows carry fps > 0; the
  six combat strips and the three fires pinned to the plan's numbers; per-frame fire stats read off the
  pixels (≤ 6 colours, ≤ 4 4-connected components, none empty); animated strips differ between frames 1 and
  2; both cloud layers are 1536x220 with sky between the clouds and a wrap seam no larger than the adjacent
  column step; no authored strip bakes pure black or white (light_soft.png, a white alpha mask by design, is
  excluded by construction); every PNG has its `.import`. Run through a scratchpad one-file runner under the
  lock after `--import`: `VFX TESTS 9 run, 0 failing`. In the full suite (`tests/run_tests.gd`, same lock) no
  line names test_vfx_assets; the suite's 8 failing tests are test_a11y (focus ring), test_frame_header,
  test_icons (compile), test_screens (shut door), test_text_scale, test_theme_kit — W1-CHROME/FRAME/ICONS
  work in flight in the shared tree, none touching game/assets/vfx.
- A6 DONE: `"$GODOT" --headless --path . --import` under the lock: rc 0, `.godot/imported` gained 9 ctex + 9
  md5 for the new PNGs and re-imported the three fires; every `load()` in the test file resolved.
- A8 DONE: `build/shots/W1-VFX_hearth_refkit.png` — left: Concept 1 (330,380,90,90) at 4x; right: the same
  crop with the painted flame dimmed and fire_hearth frame 0 seated on its logs at 4x. Viewed: the concept's
  fire is taller and wispier with more graduated glow; ours is a fatter, cleaner three-lick silhouette with
  the same band order (dark red rim, red-orange, orange, yellow, cream) and the same seat line on the logs —
  the painted read STAGE-12 asks for, at the strip's fixed 64x56.
- A9 DONE: `Tavern --fixture --frames=1,20` → W1-VFX_Tavern.a.png / .b.png (38,199 pixels differ between the
  two, the hearth and the lantern glows). Viewed whole and cropped (790,30,1030,210) at 3x: the fireplace at
  screen x 860..940 y 100..190 shows a banded flame — dark rim, red-orange, orange, yellow, cream — blooming
  through the stage glow, a different lick shape at frame 20 than at frame 1; the right tongue is clipped
  because stage_tavern.json still cuts the 64px frames at `frame_w: 48` (handoff §1; W1-STAGE's edit).
  `Town --fixture` → W1-VFX_Town.png, viewed whole and cropped (600,250,780,400) at 3x: the campfire in the
  stone ring shows three licks with yellow interiors and cream cores at the seat, blooming onto the ring,
  with the twelve figures around it; no speckle anywhere.

## Resume (second agent, 2026-09-14)

- Resumed with A7/A10 open. Re-read the plan §0/§2/W1-VFX, RULES, the seven findings, LESSONS, the report and
  handoff; `git status` of the owned files matches the report (3 fires + gen_fire.lua modified; 7 fx strips, 2 clouds,
  vfx.json, 9 .import, gen_vfx.lua, gen_clouds.py, test_vfx_assets.gd new). PIL: fire_hearth 512x56, fire_camp 320x52,
  fire_torch 96x24 — the three fires keep their PNG dimensions; every fx strip is frames*frame_w x frame_h per vfx.json.
- A7 DONE: `./tools/build_art.sh --check` (Aseprite only, no engine) in 20 s:
  `ART CHECK  generators=6  runtime files: 184 agree  0 DIFFERS  0 MISSING   (sources: 179 agree, 0 differ — not gated)`
  `ART CHECK OK`. gen_fire / gen_icons / gen_items / gen_ui / gen_vfx / gen_wipe all listed. The W1-ICONS/W1-CHROME
  generators that were mid-edit at the first agent's run have since settled, so the whole run is green, not only
  this unit's 13 files.
- Spot-check of the ticked boxes, viewed with Read: `build/shots/W1-VFX_strips_x4.png` (all ten strips at 4x, one
  row each). Spark: cream dot, gold starburst, seven rays at full spread, tip sparks breaking off. Slash: a
  steel-blue/cyan/white crescent revealed from the trailing end over frames 1-3, thick at the leading edge, thinning
  to a dotted afterimage by frame 5. Bolt: four periwinkle teardrops with an off-centre core, orbiting sparks and a
  wobbling tail; the 8x2 trail is the sliver beneath them. Heal: a white/blue four-point glint opening on the axes,
  turning to the diagonals, three motes rising. Burst: a pink-white core with twelve ragged red spikes that hollows
  into a ring while the spikes fade to dull magenta. Poof: a grey-blue cloud with a lit top that swells, rises and
  Bayer-dithers away. Fires: hearth (8), camp (8), torch (6) — each frame one outlined silhouette of three licks
  with yellow interiors and cream cores at the seat, no speckle, no loose pixels; PIL confirms 512x56 / 320x52 /
  96x24 unchanged. `build/shots/W1-VFX_hearth_refkit.png` re-viewed: same band order as the concept, seated on
  the andirons. `build/shots/W1-VFX_fire_crops_x3.png` (Tavern hearth 790,30,1030,210 and Town campfire
  600,250,780,400 from the A9 shots at 3x): the hearth shows the banded flame on the andirons with its right
  edge cut straight where stage_tavern.json's `frame_w: 48` clips the 64px frame (handoff §1); the campfire is
  three banded licks blooming in the stone ring with the party around it.
- `python tools/art/gen_clouds.py --check`: `agree clouds_far.png`, `agree clouds_near.png`, `GEN_CLOUDS CHECK
  OK` (0.9 s). build_art.sh globs only gen_*.lua, so nothing in verify.sh runs this today — handoff §2 folds it
  into stage 2b without touching the `generators=6` count the plan pins.
- Test file re-checked against the contracts: four-space (zero tab-led lines), extends tests/TestCase.gd, no
  class_name, no _ready.
- `tools/lint_motion.sh`: `MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)`.
- A10 (`GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast`, rc 1) — summary lines:
  `PASS  LINT OK  no cross-file class_name references in sim/ or game/`
  `PASS  MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)`
  `PASS  PARSE_CHECK scanned 160 script(s)`
  `PASS  16 generated file(s) agree with tools/gen_items.gd`
  `PASS  ART CHECK  generators=6  runtime files: 4 agree  0 DIFFERS  0 MISSING`
  `FAIL  test_frame_header.gd :: test_every_framed_screen_draws_the_same_lockup` (x2: expected 374.0, got 375.0)
  `FAIL  test_icons.gd :: test_every_png_under_the_grid_is_in_the_manifest_and_every_row_is_on_disk`
        (class_warrior exists at both levels under game/assets/ui/icons)
  `FAIL  test_icons.gd :: test_a_missing_icon_is_an_error_not_a_silent_null` (no class_name (invariant 7/8))
  `FAIL  test_widgets_kit.gd :: test_the_label_stamp_keeps_its_contract_and_leans`
  `TESTS FAILED   4/1708 failing  [38030 ms]` → `VERIFY FAILED`.
  All four are W1-FRAME / W1-ICONS / W1-KIT work in flight in the shared tree; none of those three test files
  mentions vfx (grep), and test_vfx_assets.gd is not in the failure list (its 9 tests are inside the 1708). Every
  stage before the unit tests is green. A10 stays unticked as written: the unit's own gate lines are green and
  the suite's red is outside its files — the orchestrator's post-handoff full gate is where it closes.
- The `4 agree` in that ART CHECK line (against 184 in a direct run 2 minutes earlier) is a race, not a
  regression: build_art.sh --check regenerates into a fixed `build/artcheck` and `rm -rf`s it first, and a direct
  `./tools/build_art.sh --check` is not under the engine lock, so another agent's run wiped this one mid-compare.
  Reproduced once more: a later direct run printed `104 agree 80 DIFFERS`, and the directory was gone by the time
  the DIFFERS list was walked (every one a `cmp` against a deleted file). Proof that this unit's bytes are stable
  without the shared directory: gen_vfx.lua + gen_fire.lua regenerated with `--script-param out=<private dir>`
  plus `gen_clouds.py --out <private dir>` → `private check: 15 files, 0 differ`. A third shared run, unraced:
  `ART CHECK  generators=6  runtime files: 184 agree  0 DIFFERS  0 MISSING` / `ART CHECK OK`. Handoff §3 gives
  build_art.sh a per-process directory (`build/artcheck.$$`).
- Judgement calls (this resume): (1) no third `verify.sh --fast` — the four failures are in files this unit
  cannot touch and the lock was contested, so the engine time buys nothing this report does not already say;
  (2) handoff §2 (clouds into the gate) and §3 (the race) both go to build_art.sh, a wave-0 file no wave-1 unit
  owns — recorded as exact edits, nothing applied; (3) a first write of handoff §2 through a heredoc collapsed
  its printf escapes (LESSONS' backslash trap, again) — rewritten through a Write-tool script and verified with
  `cat -A`.
- Files this resume touched: build/plan/report-W1-VFX.md, build/plan/handoff-W1-VFX.md,
  build/shots/W1-VFX_strips_x4.png, build/shots/W1-VFX_fire_crops_x3.png. No owned generator, PNG, source or
  test changed; every ticked box stands as the first agent left it.
