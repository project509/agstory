# Review — W1-KIT — the composites, in code, with no new pixels of their own

Reviewer: adversarial, wave 1 (second resume). Verified against the TREE, not the report.
Owned files: game/ui/Widgets.gd, game/ui/Cards.gd, game/ui/Badge.gd, game/ui/Bar.gd, tests/unit/test_widgets_kit.gd (new).

## 1. Tree state

- `git status --porcelain` at review start: all five owned files present (4 M + 1 ??; `test_widgets_kit.gd.uid` is Godot's auto-generated sidecar, fine).
- Non-owned changed files (other units own them; attribution checked below): ScreenRouter.gd, AdventureBoard/Facilities/Guildhall/LoadSave/RaidView/RaiderDetail/Settings/Tavern.gd, Fonts/Frame/Palette/SceneStage/Theme/Type.gd, test_art_sources/test_scene_stage.gd, tools/art/*.py, tools/aseprite/*.lua, tools/probe/Kit.gd, assets. (findings below)

## 2. Findings (written as I go)

### F1. Owned-file diffs read in full (Widgets.gd +872/-?, Cards.gd +185/-9, Bar.gd 17 lines, Badge.gd 1 line, test_widgets_kit.gd 520 lines new)
- Widgets.gd: Motion table, Tail/Dots/Sparkline inner classes, empty_state(glyph,hint), stamp() lean + stamp_box/stamp_press/stamp_angle/tone_color, cta guard, pager, reasoned, confirm_pair/default_focus_of, tab_row/tabs_of, slot_with_reason, sparkline, damage_number/_pooled_number/_detach_number/number_rise/number_stagger, speech_bubble(kind,glyph), speech_plate(text,tail_at)+_aim_tail, building_callout(...,icon,anchor)+_centre_tail/_place_callout. Every pre-existing constructor keeps its signature/return type (checked: empty_state, stamp, cta, speech_bubble, speech_plate, building_callout all have new args with defaults only). Pinned consts SCREEN/SCENE/SIDEBAR/STRIP_Y/STRIP_H/CARD_SIZE unchanged (Widgets.gd:51-61).
- Cards.gd: card(raider, db, action, actions) band "Actions"; _first_bullet fallback; roster_strip wraps everything under host/"RosterStrip" and draws PagerPrev/PagerNext at strip-local x 111/1015 (screen 124/1028 with the strip host at x=13 — see F6); _turn_page; strip_page; event_log(rows, hint) with reasoned "View All".
- Bar.gd/Badge.gd: handoff-W1-CHROME §8-§13/§16 retokens only; every token exists in Palette.gd with the same hex (Palette.gd:137-156). Bar's numeral size now goes through Type.at(Type.SMALL, Theme_.scale_of(self)) (W0 door).
- Indentation: Widgets/Cards 100% tabs (0 space-led lines), Badge/Bar/test 100% spaces, no CRLF, no mixed-leading-whitespace lines (scratchpad/indent.py over all five files).
- Forbidden patterns in owned files: no `reduced_motion`/`reduced_effects` branch, no `class_name`, `create_tween(` only at Widgets.gd:1307, no `Color("` literal left in Bar/Badge/Widgets. The only PNG paths Widgets loads are the pre-existing arrow_left/right.png and speech_bubble.png (fallback branch, file still exists) — no NEW path named.

### F2. Non-owned changed files: none attributable to this unit
- tools/probe/Kit.gd (+242) is W1-CHROME's (report-W1-CHROME.md:8,39 lists it as owned; the diff header says "The second sheet (W1-CHROME)"). Every other modified file is in another wave-1 unit's ownership table. Nothing in this unit's report or diffs touches them; the handoff was NOT applied by the unit (verified: the nine §1-§9 `old:` blocks still match the tree byte-for-byte, so the edits are pending, not done).

### F3. Engine runs I observed (not the report's)
- `GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` → `MOTION LINT OK`, `PARSE_CHECK scanned 160 script(s)`, `ART CHECK ... 184 agree 0 DIFFERS 0 MISSING`, `TESTS PASSED   1713 test(s) in 80 file(s)  [33613 ms]`, `VERIFY OK`, exit 0. test_widgets_kit.gd has 22 `func test_` and is in the 80 files (no failure lines in .verify.log). test_screens.gd:432-443 and test_a11y.gd:279-300 are in the same passing run.

### F4. Handoff-W1-KIT.md checked against the tree
- §1-§9: every `old:` block found exactly once at the stated line (§6 at 488 vs the stated 486 — text matches, and the file says "match by the old: text"); RaiderDetail/Tavern blocks are tab-indented, Market/RaidPrep blocks four-space — correct per file. §10-§11 (SceneStage) are correctly marked ALREADY APPLIED: SceneStage.gd:953 reads `Widgets.speech_bubble("dots", glyph)` and `class Ellipsis` is gone.
- Consequence (not a defect of this unit, but a wave-visible one): until §5-§9 land, RaidPrep, Tavern and RaiderDetail draw the strip's gutter pager AND their own legacy arrows (seen in my RaidPrep shot, F7).

### F5. RULES-12 "promoted verbatim" — verified by diff
- `Widgets.slot_with_reason` vs `Market._slot_with_reason` (Market.gd:583-602): identical after dropping the `Widgets.` prefix and the indentation change; only the def line differs.
- `Widgets.Sparkline` vs `RaiderDetail.MoraleChart` (RaiderDetail.gd:682-706): identical `_draw` apart from the documented `color_fn` indirection (default `Palette.morale_color`, i.e. the same value) and the dropped `const Pal` preload. Same pixels by construction.

### F6. a11y smoke and review shots I took (one lock hold; scratchpad/shots.log)
- `tests/unit/a11y_smoke.gd`: `A11Y SMOKE PASSED   26 screen mount(s)`; the only `warn` is the pre-existing AdventureBoard "10 pointer-only gesture target(s)" (W2-BOARD's). Town fixture: `tab ring=4 focusable=13 reachable=14 disabled=2`.
- `Town --fixture` → build/shots/review-W1-KIT-Town.png (SHOT OK 1536x1024). Viewed, plus 2x crops (`-callouts2x`, `-gutters2x`, `-strip`, `-loghead3x`, and `-callouts-before-after` = pre-wave build/shots/all/fixture/Town.png beside it).
- `RaidPrep --fixture` → review-W1-KIT-RaidPrep.png; `Results --fixture=raid` → review-W1-KIT-Results.png; `Town --fixture --focus --tab=12` → review-W1-KIT-Town-focus.png (shot.gd's log: `TAB 11 -> PagerPrev<Button> at 124,843 16x44`, `TAB 12 -> Nav_home` — the strip's ◄ IS in the Tab ring; my --tab=12 landed one past it, so the ring in that shot is on Nav_home); `Town --fixture --set=text_scale=150` → review-W1-KIT-Town-t150.png (+ `-t150-crop`).
- What the Town shot shows: five PanelCallout plates, each with the 32px SlotMini well at the left (empty — W2-TOWN passes the icon), title Button + subtitle at x+60, and a 14x8 tail hanging from the bottom centre in the plate's own fill/rim (visible under Guildhall, Tavern, Market, Adventure's Board; the Blacksmith's is hidden under the Market plate — F7). The locked Blacksmith: title/subtitle/well dimmed, the reason "Canon lists this one as a maybe. Disabled in this build." in CAUTION on two lines inside the plate. Camp speech plate (PanelBubble, no tail), emote bubbles as the 40x44 cream frame with ONE row of dots. Strip: ◄ mini plate at screen x 124-140 (centre 132), ► at 1028-1044 (centre 1036), "1/3" at y≈1008 under the right gutter; no pager over a card. Event log head: "Recent Events" left, the disabled "View All" right (F8) with "Records opens this" in CAUTION under it, "Nothing has happened yet." centred in the well.
- RaidPrep: the kit's gutter pager + "1/3", every card's "Bench" action, AND RaidPrep.gd's own legacy arrows over card 4 (x≈815-885, y≈980) and "Tonight's 12 · page 1/3" under card 1 — handoff §5-§6 pending (F4), as the report says. Results: four "FALLEN" Label stamps leaning the same ~7° in DANGER at 0.85 (`stamp()` as Results.gd holds it); the report panel and the two Buttons unchanged.
- Pixel measurements (PIL over the Town shot): at y=745 the rims are at x=136 (Available panel's right) and x=140 (card 1's left) — the real gutter is 3px, not spec 10 §2 R1's 8px, because the 12-portrait mini-grid grows the panel to 123px (Cards.gd sizes it 110); the ◄ plate (124-140) therefore sits 12px over the Available panel's right edge on the Town. On RaidPrep (empty bench, panel 110) it is cleanly in the gutter. Right gutter: card 4's rim at 1026, the log's at 1045, the ► plate at 1028-1043 — clean.

### F7. BLOCKER — `building_callout`'s clamp uses the framed SCENE rect in the plate's parent's space; on the Town (full-bleed, the only consumer) that is the wrong rect, and the "defect 2 fix" moved three of the five designer-placed callouts
- Code: Widgets.gd:1258-1268 `_centre_tail` and :1273-1284 `_place_callout` clamp `plate.position` into host-local (0,0)-(929-w, 640-h-8). That equals Frame's scene band only when the host sits at SCENE.position (framed screens). Town.gd:107 builds `framed: false`, so Frame.gd:351-352 puts the scene host at (0,0) 1536x726 and Town.gd:163 places callouts in SCREEN coordinates (`"at"`: Guildhall 224,292 · Tavern 640,190 · Market 860,414 · Blacksmith 900,292 · Board 420,580). The clamp therefore forbids x > 929-w (the scene's real right edge is 1139) and permits x < 210 (under the rail).
- Effect, measured on review-W1-KIT-Town.png against the pre-wave build/shots/all/fixture/Town.png (`-callouts-before-after.png`): Blacksmith moved from x=900 to 653 (plate now 655-928, over the fire pit in the middle of the camp, not "right of the quartermaster's tent"); Market moved from 860 to 781 (its right rim pinned to 928 too); Adventure's Board moved up from y=580 to 552. The Blacksmith's bottom rim (y≈413) now butts the Market's top rim (y≈415) and its tail is drawn INTO the Market plate (hidden under it). At text_scale=150 (`-t150-crop.png`) the two plates coincide and the Market's title is completely covered — only "Buy / Sell" peeks out.
- The report's claim "now clamped to the scene's right edge (x 653-928) instead of running under the sidebar" is FALSE as a screen statement: the scene's right edge on the Town is x=1139 (sidebar at 1142); the plate stops 211px short of it. An honest clamp would have put the 276px plate at x=863 (a 37px move); the unit moved it 247px and stacked it on the Market. The pre-wave placement (x=900, 34px under the sidebar) was less wrong than the fix.
- Why it blocks: (a) the plan's KIT-01 clamp ("plate clamped inside Frame.SCENE") is mis-implemented for the ONLY screen that calls building_callout, and `_place_callout` — the anchored path W2-TOWN must use — carries the same rect, so any anchor right of x≈929-w/2 (the Market's veg beds, the Blacksmith's tent) will be dragged left with a long diagonal tail; (b) Widgets.gd has NO owner in wave 2 (00-plan §3 ownership table), so W2-TOWN cannot fix it and its acceptance ("a tail whose apex sits on the building") would hang on a workaround the handoff does not mention (parenting the callouts under a (210,77)-offset 929x640 child); (c) the no-anchor clamp is a scope extension the plan did not ask for (KIT.md:57 puts the clamp on the anchored path; the no-anchor path is "the caller places the plate, as today") and it regressed the hub's shot; (d) the report describes the result as correct. The fix is a few lines in the owned file: clamp against the scene band expressed in the parent's space (e.g. `Rect2(SCENE)` translated by the parent's global position, or `Rect2(Vector2.ZERO, parent.size)` when the parent IS the band), or take a `bounds: Rect2` parameter with SCENE as the default for framed hosts; and drop the clamp from the no-anchor path, or keep it only inside the parent's own rect. Then re-shoot Town and compare with build/shots/all/fixture/Town.png — Blacksmith at 863, Market at 860, Board at 580.

### F8. MAJOR — the reasoned "View All" is unreadable on every hub event log
- review-W1-KIT-Town-loghead3x.png: "View All" renders as a near-black underlined word on the dark panel — the theme sets no `font_disabled_color` for LinkButton (Theme.gd:453-458), so Godot's default disabled ink applies, and `reasoned()` then multiplies the whole control by 0.55 (Widgets.gd:490). The CAUTION line "Records opens this" is readable; the control it explains is not, so the reader sees a reason floating under nothing. Same on Town/AdventureBoard/Market/Guildhall/RaiderDetail (all call `Cards.event_log`) and at 150% (`-t150-crop.png`).
- The plan's KIT-21 deliverable is met to the letter (a disabled `Widgets.link("View All", true)` with the reason Label) but not in effect. Fixable inside the owned files: `reasoned()` (or `link()`) overrides `font_disabled_color` with a visible muted token before dimming. The report describes this head as fine ("the disabled 'View All' link right with 'Records opens this' under it") — the unit viewed the shot and did not notice.

### F9. Minor findings
- M1 (F6): on the Town the ◄ plate overlaps the Available panel's right 12px because the 12-portrait mini-grid grows the panel past the 110 Cards.gd assigns; RaidPrep is clean. Cosmetic; W3-KIT2 (size the panel from its content, or place the arrow from the panel's actual right edge).
- M2: `Cards._turn_page` rebuilds the strip and re-grabs the same arrow, but nothing re-runs `Frame.focus_order`/`_wire_regions`, so the rebuilt PagerPrev/PagerNext have no focus_next/neighbour paths — Tab/arrows from a keyboard-pressed arrow fall to Godot's geometric search (the §13.2 wander). a11y_smoke only walks the initial ring, so it cannot see this. `opts.on_page` consumers (or the strip) should call the screen's `wire_shell_focus` — record for W2/W3.
- M3: TOWN-29's quote fallback is limited to cards with no action band (Cards.gd:124-125) — a scoping choice the plan does not make, recorded only in a code comment, not in the report's judgement calls. Harmless (Raid prep's Bench cards keep their shape) but undocumented.
- M4: `event_log` now caps drawn rows at 6 (`fit` = (262-28-36-1)/29, Cards.gd:376) where callers pass 7; no test reads the count; the plan did not ask. Fine, but the report does not say so.
- M5: the acceptance "`damage_number(-317,"hit").text` does not change after a tween tick" is asserted as wiring (number_rise returns null outside a tree; the code tweens only position:y/modulate:a and never touches text — Widgets.gd:1042-1048). The harness has no frames (run_tests.gd runs from `_initialize`), the same convention as test_motion.gd. Accepted.
- M6: "RaiderDetail shot vs the pre-change shot mae 0" was not measured. As written it is unattainable by this unit: its own mandated KIT-12/KIT-21 changes move RaiderDetail's strip (gutter pager, "View All"), and RaiderDetail.gd (W1-HALL's) still draws its private MoraleChart until handoff §1-§2 land. The verbatim promotion is proven by diff (F5). Unverifiable, not a fault.
- M7: the acceptance "stamp()'s Label is a direct child" is met by `stamp_box()`; `stamp()` stays a Label (judgement call 1, sound — four unowned screens type the return as Label and the plan's own Build notes pin return types). `stamp()` now leans 2-7° at 0.85 wherever it is used (Results/Tavern/Roster/RaidView's wipe word) — a static docs/13 §7 lean, not motion, so no reduced_motion door is owed; RaidView still rotates its own box to 0 under reduced motion.
- M8: `confirm_pair` grabs focus (deferred) on `tree_entered`; a screen that rebuilds a pending confirm inside `build()` will have it take focus after the router's `initial_focus_target` grab. Arguably right (the safe half of a pending destructive choice should hold focus); noting it so W3-OPTIONS/W3-DETAIL expect it.

### F10. §0.5 / Green line checks
- Texts and names: "Recent Events", "Nothing has happened yet.", every `empty_state` string, card texts, "Button" inside "Callout", `PagerPrev`/`PagerNext` all as the plan names them; `cta` keeps `.text` (trim is display-only); tab_row keeps label texts. No asserted string touched (suite green).
- Every visible BaseButton FOCUS_ALL: pager_button/tab buttons/confirm halves are Buttons with the default FOCUS_ALL; `link()` sets FOCUS_ALL; disabled ones are excluded by the instrument. Disabled controls: reasoned/tab_row/slot_with_reason/building_callout all print the reason in a Label.
- Tweens: `create_tween(` only at Widgets.gd:1307; `stamp_press`/`number_rise` durations come from `GameSettings.motion_duration(ms[, floor])` found through `Services.find` (raw ms only when the autoload is absent, i.e. tests). No `reduced_motion` branch anywhere in the owned files. Overlays: Tail/Dots are Node2D (no mouse filter at all), Sparkline/speech plate/stamp box/damage number MOUSE_FILTER_IGNORE.
- No `$GODOT` outside the lock: the unit added no scripts under tools/; the test file invokes nothing. No new PNG, no .import owed. Nothing parented to the router host. No `class_name`. No `_ready()` logic. Font sizes set directly go through `Type.at` (Bar.gd:83, Widgets.gd:1211/1221-1222, Cards.gd:172, BUBBLE_PITCH at Widgets.gd:1134).
- Judgement calls vs §6: none of Q01-Q18 was decided by this unit (Q04's chrome is W1-CHROME's PanelBubble; Q13's strings are Town.gd's and rendered verbatim). The stamp geometry, callout geometry, pager placement and the confirm/reason idioms are on §6's "decidable without the designer" list.

## 3. Acceptance lines (plan "### W1-KIT" Acceptance + Shot + Green)

| Line | Met | Evidence |
|---|---|---|
| `stamp()`'s Label is a direct child | met (via `stamp_box`) | test_widgets_kit.gd:94-111 green; Widgets.gd:346-376; judgement call 1 (M7) |
| `confirm_pair` yields two Buttons with the given texts and the no-button focused after `grab_focus` | met (wiring) | test :145-157 green; Widgets.gd:630-658 (`default_focus` meta + deferred grab on tree_entered); harness has no tree |
| `reasoned` disables and prints the reason in a Label | met | test :162-180 green; Town shot: Blacksmith reason in CAUTION inside the plate |
| `damage_number(-317,"hit").text == "-317"` and does not change after a tween tick | met (wiring, M5) | test :185-207 green; number_rise never touches `text` (Widgets.gd:1028-1049) |
| `roster_strip` pages twelve names across three pages via `PagerNext` | met | test :212-242 green (R01-R04 / R05-R08 / R09-R12, wraps); Town shot "1/3" |
| `building_callout` keeps `Widgets.button_of` semantics | met | test :264-287 green; Town: "Tavern" etc. are Buttons named "Button" in "Callout" (test_screens Town block green) |
| test_screens.gd:432-443 and test_a11y.gd:279-353 green | met | in the 1713-test VERIFY OK run I ran |
| RaiderDetail shot vs the pre-change shot mae 0 | unverifiable (M6) | not measured by the unit; promotion verbatim by diff (F5); the unit's own mandated strip changes move that screen |
| Shot `Town --fixture`: tail and icon column visible | met — but see F7 | review-W1-KIT-Town-callouts2x.png: wells + tails on all five; three plates moved off their placements by the clamp |
| Shot `RaidPrep --fixture`: strip paging, no pager over a card | met for the kit's pager; the screen's legacy pager is still over card 4 | review-W1-KIT-RaidPrep.png; handoff §5-§6 pending, correctly disclosed |
| Shot `Results --fixture=raid`: stamps rotated | met | review-W1-KIT-Results.png: four FALLEN at ~7°, 0.85 |
| Green: texts unchanged; pager FOCUS_ALL inside the strip region; create_tween only in Widgets; MORALE_BAND_EMOJI only Cards/GameSettings | met | F10; a11y smoke `TAB 11 -> PagerPrev at 124,843`; MOTION LINT OK |

## 4. False claims in the report
- "the locked Blacksmith ... now clamped to the scene's right edge (x 653-928) instead of running under the sidebar" — the scene's right edge on the Town is x=1139; the plate was moved 247px left to the middle of the camp and stacked on the Market plate (F7). The numbers 653-928 are a host-local reading of a rect that is not the Town's scene band.
- "Strip: ◄ at x≈131 ... in the gutters" — the ◄ centre is at 132 but on the Town it sits 12px over the Available panel (F6/M1); only on RaidPrep is it in a gutter.
- The event-log head described as fine — "View All" is unreadable (F8).
- Everything else I checked holds: 1713/1713, VERIFY OK, MOTION LINT OK, a11y 26 mounts with only the AdventureBoard warn, handoff anchors exact, §10-§11 already applied, the verbatim promotions, no `Color("` in Bar/Badge/Widgets, indentation per file.

## 5. Verdict

**FAIL** — one blocker (F7: the callout clamp is the wrong rect for the only consumer, regressed the hub's shot, will fight W2-TOWN's anchors, and Widgets.gd has no wave-2 owner to fix it) and one major (F8: the KIT-21 link is invisible), both in the unit's own files and both a few lines to fix. Everything else in the contract is met and green; the minors (M1-M8) are listed for W3-KIT2 / the consumers and do not need this unit's return.

Unowned files touched by this unit: none.

---

# Repair-pass review — W1-KIT (second review, 2026-09-14)

Reviewer: adversarial, wave 1 (second resume), reviewing the REPAIR PASS the implementer reports against the FAIL above (F7 blocker, F8 major, M1-M8). Verified against the TREE. Findings numbered R1… and written as I go.

## R1. Tree state and owned-file diffs (read in full)
- `git status --porcelain`: Widgets.gd / Cards.gd / Badge.gd / Bar.gd `M`, test_widgets_kit.gd `??` (+ its .uid sidecar). No git command run by me beyond status/diff/ls-files.
- Diff sizes: Widgets.gd +1107 diff lines (file now 1379), Cards.gd 307, Badge.gd 1 line, Bar.gd 17 lines, test 638 lines / 24 `func test_`. Badge/Bar unchanged since the first review (same 17/1-line retoken diffs; tokens exist in Palette.gd:137-156 with the same hex).
- Repair-pass additions found in the tree: Widgets.gd `SCREEN_ROOT_META` (:1284), `_callout_bounds` (:1302-1319), `_clamp_to_band` (:1325-1328), `_centre_tail(plate, tail, bounds)` / `_place_callout(..., bounds)` (:1335-1354), `building_callout(..., bounds: Rect2 = Rect2())` (:1212-1213), `link()` sets `font_disabled_color` from `_link_ink()` (:568, :582-586). Cards.gd `SCREEN_FOCUS_META` (:372), `_rewire_focus` (:405-416), called from `_turn_page` after `_fill_strip` + `on_page` and before the deferred re-grab (:395-399). Tests: `test_the_callout_band_is_the_scene_rect_in_the_parents_space` (:312-394), `test_turning_a_page_rewires_the_screens_focus` (:597-620), event-log test asserts the `font_disabled_color` override == theme LinkButton `font_color` (:586-593).
- Indentation (python byte scan): Widgets 753 tab-led / 0 space-led, Cards 493/0, Badge 0/52, Bar 0/55, test 0/486; no mixed-leading-whitespace line, 0 CRLF in all five. `git ls-files --eol` i/lf w/lf.
- Forbidden patterns: `create_tween(` only Widgets.gd:1379 (and a comment :1359); no `reduced_motion`/`reduced_effects` in any owned file; no `class_name` (only `Enums.class_name_of`, pre-existing); no `Color("` literal in the four kit files; the only non-preload `load(` in Widgets is the pre-existing `speech_bubble.png` fallback (:1105), Cards' two are pre-existing bg/atlas paths. No new PNG, no .import owed.
- Non-owned changed files: the same set as the first review (all in other wave-1 units' ownership tables); nothing in the repair-pass diff or report touches them. Handoff §1-§9 `old:` blocks re-checked below (R5).

## R2. Reading the F7 fix (`_callout_bounds`) against Frame.gd
- `Frame._publish_focus_hooks` (Frame.gd:323-325) sets `frame_focus_order` on the screen host inside `Frame.build()` — BEFORE Town.gd:159-163 adds the callouts to `p.scene` — so the meta is present the first time `item_rect_changed` fires. The walk (`while n is Control and not n.has_meta(...)`) therefore sums only the scene host's own `position`: Town (full-bleed, Frame.gd:351-352 → scene at (0,0) 1536x726) → band = SCENE ∩ (0,0,1536,726) = (210,77)-(1139,717); framed (scene at (210,77), Frame.gd:345-346) → band = (0,0)-(928,640) after the cut to the host's 928. Matches the report. A shot host above the screen (shot.gd's letterbox) is excluded by the stop-at-meta rule — correct since `position` is parent-local anyway.
- Termination: `_centre_tail`/`_place_callout` only assign `position` when it differs; the second `item_rect_changed` computes the same `want` → no loop. With the PanelContainer still 0x0 (before layout) the clamp is a no-op for Town's five `at` values; the real move happens when layout sizes the plate.
- `Frame.focus_order(p)` (Frame.gd:744-755) DOES call `_wire_regions`, so Cards' `_rewire_focus` (calling the meta Callable) re-wires Tab/arrow paths for the rebuilt arrows — the M2 fix is real, not just a call into a lister. No screen passes `extra` to `focus_order` (grep), so re-calling without extras drops nothing.

## R3. Handoff-W1-KIT.md re-checked against the tree (python: each `old:` block counted in its file)
- §1-§9: every `old:` found exactly once (§6 at RaidPrep.gd:488 vs the stated 486, as before; the file says match by text). Indentation of old/new blocks: RaiderDetail/Tavern tabs, Market/RaidPrep spaces — right per file. §10-§11 (SceneStage) marked ALREADY APPLIED and indeed absent from the tree (SceneStage.gd:953 `Widgets.speech_bubble("dots", glyph)`). Nothing applied by the unit; the repair pass added only an "API notes added by the repair pass" section (handoff:405-435) — notes, no edits — carrying F7/150%/M7/M8/M1/M2 for W2-TOWN, W2-RAIDVIEW/RESULTS, W3-ROSTER, W3-OPTIONS/DETAIL, W3-KIT2.

## R4. Engine runs I observed (scratchpad/verify.log; .verify.log)
- `GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` → `LINT OK`, `MOTION LINT OK`, `PARSE_CHECK scanned 160 script(s)`, `16 generated file(s) agree`, `ART CHECK generators=6 184 agree 0 DIFFERS 0 MISSING`, `TESTS PASSED   1715 test(s) in 80 file(s)  [32603 ms]`, `VERIFY OK`, exit 0. 1715 = the report's number (1713 at the first review + the two new tests). No backtrace in .verify.log names Widgets.gd, Cards.gd or test_widgets_kit.gd (the `!is_inside_tree()` grab_focus and `Must be an ancestor` lines trace to test_wipe_sequence/RaidView and test_a11y — pre-existing harness noise).

## R5. Review shots I took (one lock hold, scratchpad/shots.log; all `SHOT OK 1536x1024`) — viewed with Read
- `Town --fixture` → build/shots/review-W1-KIT-fix-Town.png (+ crops `-callouts`, `-seam4x`, `-loghead3x`, `-gutter3x`). Rim pixels (97,91,84) sampled with PIL: Blacksmith left rim x=863, right rim x=1138 (plate 863..1138 = 276 wide, top rim y=292, bottom rim y=413) — the 37px move the first review asked for, the right rim now visible beside the sidebar (1142); Market left rim x=860, top rim y=414 — unmoved; Board left rim x=420, top rim y=580 — unmoved; Guildhall/Tavern unmoved. F7's regression is gone: no plate in the middle of the camp, nothing under the sidebar. All five plates show the 32px well, title Button + subtitle at x+60, and the 14x8 tail; the Blacksmith's tail (centre x≈1001) is drawn over the Market plate's top-right corner because the Blacksmith's bottom rim (413) abuts the Market's top rim (414) — the designer's `at` values (Market 414, Blacksmith 292 + a 122-tall locked plate), the same abutment as the pre-wave shot; cosmetic, W2-TOWN's anchors (R7 minor).
- Event-log head (3x crop): "View All" is a readable grey underlined word at the panel's right with "Records opens this" in CAUTION under it. PIL: brightest glyph pixel (101,103,105) on panel (0,13,22) = 3.46:1, 210 glyph pixels ≥ 3:1 — exactly the report's numbers. F8 fixed (above the 3:1 UI floor; a disabled control, so the 4.5:1 text floor does not apply — judgement call 9 is inside docs/13 §7's "dimmed" treatment and inside the unit's own files).
- Gutter (3x crop): the ◄ plate still sits ~12px over the Available panel's right rim on the Town (M1, left to W3-KIT2 — R7).
- `Town --fixture --set=text_scale=150` → review-W1-KIT-fix-Town-t150.png (+ `-t150-before-after.png` = pre-wave build/shots/all/text150/Town.png beside it, 2x). "View All" readable at 150. The locked Blacksmith's three-line reason makes its plate ~185 tall and it covers the Market plate's title (only "Buy / Sell" shows); the pre-wave shot shows the SAME cover (an "M" peeking out then, nothing now — the plate is 37px further left). Designer's `at` coordinates, not the clamp; disclosed in the report and the handoff; W2-TOWN's anchors + one-line reason resolve it. Minor (R7).
- `Town --fixture --focus --tab=11` → review-W1-KIT-fix-Town-focus.png (+ `-focus-crop`): shot.gd log `TAB 3/7/11 -> PagerPrev<Button> at 124,843 16x44`; the 2px steel ring (74,131,163) is on the ◄ mini plate at x 122-141 — the strip's arrow is in the Tab ring and shows its ring. (My `--press="Next page"` attempt exited 9 — the pager Buttons have no `text`, only a tooltip, which is what the plan specifies; my grammar error, not a defect.)
- `RaidPrep --fixture` → review-W1-KIT-fix-RaidPrep.png: the kit's ◄/► at x≈132/1036 clean beside the 110-wide "Bench 0" panel, "1/3" under the right gutter, no kit pager over a card; RaidPrep.gd's OWN legacy arrows still over card 4 (x≈815-885, y≈980) + "Tonight's 12 · page 1/3" — handoff §5-§6 pending, disclosed.
- `Results --fixture=raid` → review-W1-KIT-fix-Results.png: four "FALLEN" Label stamps leaning ~7° in DANGER at 0.85 on the party cards; the report panel, "Back to the board"/"Return to town" and Results' own pager unchanged.
- a11y smoke (same hold): `A11Y SMOKE PASSED   26 screen mount(s)`; Town fixture `tab ring=4 rail=6 focusable=13 reachable=14 disabled=2` (identical to the first review); the only `warn` is AdventureBoard's pre-existing "10 pointer-only gesture target(s)" (W2-BOARD's).

## R6. §0.5 / Green line re-check after the repair
- Texts/names: no asserted string or node name changed by the repair (the diff adds no Label text; `PagerPrev`/`PagerNext`/`PageCaption`/`ViewAll`/`Reason`/`Callout`/`Button`/`Tail`/`Icon` as before). test_screens.gd:432-443 and test_a11y.gd:279-353 are in the green 1715 run.
- Every visible BaseButton FOCUS_ALL: pager arrows are plain Buttons (default FOCUS_ALL; a11y ring reaches PagerPrev), `link()` sets FOCUS_ALL explicitly (Widgets.gd:568); the disabled "View All" and the locked Blacksmith title have their reason Labels adjacent ("Records opens this"; "Canon lists this one as a maybe. Disabled in this build.").
- Tweens: `create_tween(` only at Widgets.gd:1379; no `reduced_motion` branch; the repair added no motion. Overlays: Tail is a Node2D (no mouse filter), the plates keep their filters as before.
- No `$GODOT` outside the lock: the unit added no script under tools/. No new PNG, no .import owed, no baked text. Nothing parented to the router host. No `class_name`, no `_ready()` logic. Font sizes through `Type.at` as before.
- Handoff edits: exact old→new blocks in the right indentation (R3).
- Judgement calls 7-11 vs §6: the no-anchor clamp against the scene band (7), the band's origin walk (8), the disabled link's ink = rest ink dimmed (9), TOWN-29/M4 scoping (10), M1 deferred (11) — none is a Q01-Q18 item; the callout geometry and the reason idiom are on §6's "decidable without the designer" list. Q13's strings are rendered verbatim; Q04's chrome is W1-CHROME's. No designer-reserved decision taken.
- The mirrored meta literal: `Widgets.SCREEN_ROOT_META` and `Cards.SCREEN_FOCUS_META` are third and fourth copies of `Frame.META_FOCUS_ORDER` ("frame_focus_order"); the two new tests set the literal themselves rather than reading `Frame.META_FOCUS_ORDER`, so a rename in Frame.gd would pass the unit tests and silently break the band and the re-wire. Minor (R7) — one `assert_eq(Widgets.SCREEN_ROOT_META, Frame.META_FOCUS_ORDER)` in the test (tests may preload Frame) closes it.

## R7. Minor findings (repair-pass review; none needs the unit's return)
- R7a (was M1): on the Town the ◄ plate (screen 124-140) overlaps the Available panel's right rim by ~12px because the head "Available   12" grows the 110-wide panel to 123; RaidPrep's "Bench 0" panel is clean. The plan's "gutters at x 131 and 1035" is met to the letter (◄ centre 132); the fix changes the head Label or the panel width (Cards._fill_strip:262-264) and is assigned to W3-KIT2 in the handoff. Cosmetic.
- R7b: at text_scale=150 the locked Blacksmith plate covers the Market plate's title — pre-existing at the designer's `at` values (before/after crop), disclosed; W2-TOWN's anchors + one-line reason. At 100% the Blacksmith's bottom rim abuts the Market's top rim and its tail is drawn over the Market's top-right corner — same coordinates, same resolution.
- R7c: `SCREEN_ROOT_META` / `SCREEN_FOCUS_META` literals are not pinned to `Frame.META_FOCUS_ORDER` by any test (the new tests set the literal themselves). One assert closes it; W3-KIT2 or the next Widgets owner.
- R7d (was M3-M8, unchanged): TOWN-29 fallback only on band-less cards; event_log capped at 6 rows; damage_number/confirm focus asserted as wiring; RaiderDetail mae 0 by diff; stamp() leans wherever used; confirm_pair's deferred grab — all now recorded as judgement calls 10-11 and in the handoff's API notes, which is what the first review asked for.
- Consequence, not a defect of this unit: until handoff §1-§9 land, RaidPrep/Tavern/RaiderDetail show the strip's gutter pager AND their legacy arrows (seen in my RaidPrep shot).

## R8. Acceptance lines (plan "### W1-KIT" Acceptance + Shot + Green), re-judged on the repaired tree

| Line | Met | Evidence |
|---|---|---|
| `stamp()`'s Label is a direct child | met (via `stamp_box`; `stamp()` stays a Label — judgement call 1, sound: four unowned screens type it Label and the Build notes pin return types) | test :94-111 green; Widgets.gd stamp_box |
| `confirm_pair` yields two Buttons with the given texts and the no-button focused after `grab_focus` | met (wiring; harness has no tree) | test :145-157 green; `default_focus` meta + deferred grab on tree_entered |
| `reasoned` disables and prints the reason in a Label | met | test :162-180 green; Town shot: Blacksmith reason in CAUTION inside the plate; "Records opens this" under View All |
| `damage_number(-317,"hit").text == "-317"` and does not change after a tween tick | met (wiring) | test :185-207 green; number_rise never touches `text` |
| `roster_strip` pages twelve names across three pages via `PagerNext` | met | test :212-242 green; Town/RaidPrep "1/3"; a11y ring reaches PagerPrev |
| `building_callout` keeps `Widgets.button_of` semantics | met | test :264-287 green; Town "Tavern" etc. are Buttons named "Button" in "Callout" |
| test_screens.gd:432-443 and test_a11y.gd:279-353 green | met | in my 1715/1715 VERIFY OK run |
| RaiderDetail shot vs the pre-change shot mae 0 | unverifiable (unattainable by this unit: its mandated strip changes and three other units moved that screen); the verbatim promotion is proven by diff (first review F5) | — |
| Shot `Town --fixture`: tail and icon column visible | met — F7 regression gone | review-W1-KIT-fix-Town.png: wells + tails on all five; rims at Blacksmith 863-1138, Market 860, Board 420,580 |
| Shot `RaidPrep --fixture`: strip paging, no pager over a card | met for the kit's pager; the screen's legacy pager (RaidPrep.gd's, handoff §5-§6) still over card 4 | review-W1-KIT-fix-RaidPrep.png |
| Shot `Results --fixture=raid`: stamps rotated | met | review-W1-KIT-fix-Results.png: four FALLEN at ~7°, 0.85 |
| Green: texts unchanged; pager FOCUS_ALL inside the strip region; create_tween only in Widgets; MORALE_BAND_EMOJI only Cards/GameSettings (+ the pre-existing sim/Enums/Morale and lint_motion mentions) | met | R6; MOTION LINT OK; a11y `TAB 11 -> PagerPrev at 124,843` |
| First review F7 (blocker): clamp against the scene band in the parent's space; Town re-shot with Blacksmith 863, Market 860, Board 580 | met | R2 (code), R5 (rim pixels), test :312-394 |
| First review F8 (major): the reasoned "View All" readable | met | R5: 3.46:1, 210 glyph px ≥ 3:1, viewed at 3x |
| First review M2: re-wire focus after a page turn | met | Cards.gd:395-416; test :597-620; Frame.focus_order calls _wire_regions |

## R9. Claims in the repair-pass report checked against the tree
- All of the repair pass's measurable claims hold: 1715/1715, VERIFY OK, MOTION LINT OK, a11y 26 mounts + one pre-existing warn, 24 tests, Blacksmith 863..1139 / Market 860 / Board 420,580 (my rim pixels: 863-1138 inclusive, 860, 420/580), "View All" 3.46:1 with 210 px ≥ 3:1 (my PIL gives the identical numbers), the 150% stack pre-existing (my before/after crop), handoff anchors exact, §10-§11 already applied, no unowned file attributable to the unit, no git command evidence.
- The first review's three false claims are struck through in the report with bracketed corrections (report:64-74) rather than silently rewritten — acceptable.
- No new false claim found.

## R10. Verdict (repair pass)

**PASS** — the blocker (F7) and the major (F8) are fixed in the unit's own files and verified on the tree with my own shots and pixel reads; M2 is fixed; the remaining items are minors (R7a-R7d), each disclosed in the report and routed in the handoff to the unit that owns the decision. verify --fast, the unit suite (1715), lint_motion and the a11y smoke are green in my runs.

Unowned files touched by this unit: none.
