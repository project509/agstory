# Handoff — W0-GATE (the art gate scores every screen and its noise is gone)

Exact edits this unit needs in files it does not own. One edit per heading, `old:` then `new:`, the
file's own indentation. Empty means nothing was needed.


(No edits. Two observations for other units live in `build/plan/report-W0-GATE.md`, not here, because
neither is an edit this unit needs: the `tabs` sheet's `Guildhall_Records` tile shows the Roster tab
because `Records` is a disabled chip under the fixture at Unknown (W0-SHOT's acceptance line), and the
`empty` sheet's Completion tile prints an empty guild name — " cleared the last fight on day 1" — with
no save loaded (Completion.gd's owner).)
