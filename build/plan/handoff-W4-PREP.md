# handoff-W4-PREP — edits this unit needs in files it does not own

Shape: `## N. <path>:<line>`, then `old:` and `new:` fenced blocks, one edit per heading, indentation
matching the file. Nothing here is applied by the unit. `python tools/apply_handoff.py FILE --dry-run`
must parse it.

(none — nothing this unit needs lives outside its two owned files; the card-kit note in the report is a layout choice for its owner, not an exact edit)
