# Handoff — W4-CURSOR

Exact edits for files this unit does not own. One edit per heading; `old:`/`new:` fenced blocks;
`python tools/apply_handoff.py build/plan/handoff-W4-CURSOR.md --dry-run` must parse it.

## Observations (no edit)

- The plan's shot line `Town --fixture --focus --tab=8` lands on `Nav_home` again: Town has FOUR shell
  regions (rail, scene, sidebar, strip — the header chips are not focusable) and
  `Frame.tab_steps_within_region` is false (the docs/13 §13.1 default), so Tab steps region to region and
  `--tab=1` is the Guildhall callout (`TAB 1 -> Button<Button> at 418,182`), `--tab=4` the rail again.
  The unit's outline shot is `--tab=1`; if the q-a11y-keys switch ever flips to TRUE, `--tab=8` becomes
  the Tavern callout (six rail items, then Guildhall, then Tavern). No edit: the plan's number is a
  reading of the switch, not a contract.
- `Widgets.building_callout`'s docstring says "the whole plate is the hotspot", but only the title Button
  is pressable (Town.gd wires `btn.pressed`); the plate body takes no click. The outline lights on the
  plate's hover too (PIPE-15 names the plate), so a pointer over the subtitle sees the ring and a click
  there does nothing. Making the plate press its Button through `gui_input` would add a Town row to
  a11y_smoke's "pointer-only gesture target" warn (a PanelContainer wired to gui_input with no focus), so
  it is left for a later wave to decide (a Button-family plate, or the ring on the title's hover only).
- `game/ui/shaders/outline.gdshader` is loaded by path (`Widgets.OUTLINE_SHADER`); a `.gdshader.uid`
  sidecar appears the first time the editor scans the tree. Nothing to do.
- Stage sprites (PIPE-15's other consumer) are SceneStage's — W4-LIFE owns it this wave. The shader's
  `fill` 0 / `width` 2 mode is the sprite treatment: `spr.material = ShaderMaterial(outline.gdshader)`
  with `color` a Palette token, on a sprite whose frame has >= 2px of transparent margin. Not wired here.

## 1. game/ui/Boot.gd:48

Why: PIPE-15 — the pointing hand (every BaseButton's default `mouse_default_cursor_shape`) is
`game/assets/ui/cursor_hand.png` (32x32, generated by gen_ui.lua, imported), but project.godot's
`display/mouse_cursor/*` carries ONE image (the arrow, landed by this unit), so the hand shape is
registered at runtime. Boot is the one scene that runs before any screen and already applies the
display choice before the first frame; the hotspot is the fingertip (12,1), pinned by
tests/unit/test_w4_cursor.gd. Boot.gd is four-space indented.

old:
```
    _apply_display_aspect()
    _build_host()
    _build_overlay()
```
new:
```
    _apply_display_aspect()
    _apply_cursor_hand()
    _build_host()
    _build_overlay()
```

## 2. game/ui/Boot.gd:104

Why: the function §1 calls. `Input.set_custom_mouse_cursor` takes a Texture2D (the imported PNG) and
the hotspot in image pixels; the arrow shape stays project.godot's. Guarded so a build without the
texture (a stripped export) keeps the OS hand rather than erroring.

old:
```
        GameSettings.apply_display_aspect(get_window(), String(settings.get_value("display_aspect")))


```
new:
```
        GameSettings.apply_display_aspect(get_window(), String(settings.get_value("display_aspect")))


## PIPE-15 (W4-CURSOR): the kit's pointing hand beside project.godot's arrow.
## `display/mouse_cursor/*` carries one image, so the hand — every BaseButton's
## default shape — is registered here, hotspot on the fingertip.
func _apply_cursor_hand() -> void:
    var hand: Texture2D = load("res://game/assets/ui/cursor_hand.png") as Texture2D
    if hand != null:
        Input.set_custom_mouse_cursor(hand, Input.CURSOR_POINTING_HAND, Vector2(12, 1))


```
