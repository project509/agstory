# Report — W1-ICONS (one icon grid, one generator, one lookup)

Unit: 00-plan.md §2 "### W1-ICONS". Findings: CRITIC-G07, CRITIC-C03, PIPE-06, PIPE-07, PIPE-13 (tier 2),
KIT-07, KIT-08, KIT-16, KIT-17, COMBAT-05, COMBAT-06, COMBAT-09, COMBAT-13, TOWN-02, TOWN-11, TOWN-26,
TOWN-21/m4t-03, COMBAT-19 (asset halves only — no screen code this wave).
Owns: tools/aseprite/gen_icons.lua, game/ui/Icons.gd (new), game/assets/ui/icons/** (PNGs + .import),
game/assets/ui/faces_30.png / faces_36.png (+ .json + .import), art/src/ui/icons/**, tests/unit/test_icons.gd (new).
Handoff: build/plan/handoff-W1-ICONS.md.

## Acceptance

- [x] A1 `gen_icons.lua` emits the whole grid, span-authored and deterministic (no math.random): class_<9> + class_warrior_shield 16, emote_<10> 16, log_<12> 22, state_<8> 24, mech_<12> 24 (regenerated, same names), bar_hp/bar_focus 24, rank_<6> 28 (re-authored: cream rim + device), building_<5> 32, empty_<3> 32, item_{whetstone_kit,mana_draught,guild_feast} 32, furnishing_<6> 32, sigil_<6 boss ranks> 48, lock_16, blot_12_{a,b,c}, arrow_up/arrow_down 16, faces_30.png/faces_36.png (+ .json).
- [x] A2 Every existing consumed name keeps its dimensions (rank_* 28, mech_* 24, face_N 24, faces.png 240x24, slot_* 32, arrows 16, item_* 32, cog_24, fast_forward_24); faces.png/face_N/slot_*/item_* potions/arrow_left/right/cog/ff stay byte-identical.
- [x] A3 `game/ui/Icons.gd` (new, tabs): `Icons.at(role, key) -> Texture2D` with the size table, `push_error` on a missing file (never a silent null), `Icons.size(role) -> int`, `Icons.WARRIOR_GLYPH := "swords"`.
- [x] A4 Every new PNG has a .import beside it (fresh uid) and one lock-wrapped boot imports them without `Failed to load`.
- [x] A5 `tests/unit/test_icons.gd` (new, spaces): iterates Enums.CLASS_KEYS, Enums.MECHANIC_KEYS, the state list, the log kinds, the five buildings, the ten emotes (plus every other role) and asserts each file exists at the grid size and `Icons.at()` returns it; every PNG under icons/** is named by the generator's emit manifest (no orphan).
- [x] A6 Each rank sigil's rim vs #020B12 >= 3:1 (measured with tools/art/cvd.py's luminance; pinned in test_icons.gd too).
- [x] A7 `./tools/build_art.sh --check` prints `0 DIFFERS 0 MISSING`; `tests/unit/test_art_sources.gd` stays green.
- [x] A8 Shots: `Town --fixture` and `Guildhall --fixture` before/after the rank re-author differ only in chip 4's rank sigil (isolated by swapping the six PNGs inside one locked run); a 4x montage of icons/** viewed with Read and described here.
- [x] A9 `GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` green; summary lines pasted; `tools/lint_motion.sh` OK (game/ui/Icons.gd is under game/).

## Judgement calls (rule 7)

- J1 (where the new files live) `tests/unit/test_art_sources.gd` (not mine) walks the TOP LEVEL of game/assets/ui/icons non-recursively and refuses any PNG that is not in its hand-transcribed `GEN_ICONS`/`GEN_ITEMS` lists or an all.json `ui_crop` row (also not mine). Editing the test is forbidden (rule 1) and leaving it red blocks every other agent (rule 4). So the NEW grid is emitted into `game/assets/ui/icons/grid/` and the regenerated legacy names (rank_*, mech_*) stay at the top level where their consumers load them. `Icons.gd` is the only file that knows this (§0.2: it is the only file that names icon paths). The handoff carries the exact edit that adds `grid/` to the walk and the new emit list to `GEN_ICONS`, and `test_icons.gd` does the hygiene itself in the meantime: it walks icons/** RECURSIVELY and requires every PNG to be in the generator-written manifest `icons/grid/icons.json` or in test_art_sources' domain.
- J2 (`class_warrior.png`) the top-level 15x12 hand crop is a `ui_crop` row that `test_reproducible_ui_crop_rows_are_the_shipped_pixels` re-cuts byte-for-byte, and nothing in game/ loads it (audit m4t-01); it is left untouched and the 16x16 generated glyph lives at `grid/class_warrior.png`.
- J3 (provision key) the plan writes `item_{whetstone,...}` while the sim's SKU is `whetstone_kit`; the file is `item_whetstone_kit.png` so the Market can call `Icons.at("item", sku)` with no mapping table (the sim key is the contract, the plan's spelling was shorthand).
- J4 (mech keys) `Enums.MECHANIC_KEYS` are `m01..m12`; the files are named by the `Enums.Mechanic` enum key lower-cased (`mech_tank_swap`), which is what RaidView already does. `Icons.at("mech", k)` accepts both spellings (`Icons.mech_key("m01") == "tank_swap"`).

## Log (write-as-you-go)

- Read 00-plan §0 + §2 ownership + W1-ICONS, RULES §1-5, the 18 cited findings, LESSONS (determinism, built-vs-armed, checker holes), report-W0-LIB, report-W0-MANIFEST, review-W0-MANIFEST, gen_icons.lua + lib.lua (io/mask/stroke/fill/strip sections), test_art_sources.gd, Enums/Comfort/Consumables key lists, enemies.json, Cards/Frame/RaidView/SceneStage icon consumers, an .import file.
- Montage of today's 99 icons at 4x (build/shots/W1-ICONS-before-montage.png) viewed: the six rank sigils (row 8) are dark shields whose rims barely lift off the ground; mech_* (rows 6-7) are soft resampled VFX blobs (purple void, orange burst, red slash, cyan crescent, green ring, blue orb, red spikes, a purple streak); badge_0..6 are the concept's 18px faces/coin/scroll crops; the slot glyphs are 1px grey-blue outlines.
- Reference crops viewed (build/shots/W1-ICONS-ref-*.png): A1-S8 emotes = cream rounded speech bubbles with a dark 1px outline and a small tail at bottom-left carrying "...", "!", "?" and a smile, plus a red heart and a "zZ"; Concept 1 log column = 18px coloured discs (red frown, lavender grimace, amber bust, red frown, amber grimace, green face, gold coin); Concept 2 combatant panel = a red shield with a face and a blue drop, each ~24px, left of the HP/focus bars; Concept 1's class glyph beside "Warrior" is a light grey-blue star-shaped burst (crossed weapons read), ~15x12.
- gen_icons.lua rewritten (1 file, ~1100 lines): sections 1 faces (verbatim), 1b faces 30/36 (new), 2 slot glyphs (verbatim), 2b provisions (verbatim + whetstone_kit / mana_draught / guild_feast), 3 rank sigils (re-authored), 5 arrows/cog (verbatim + arrow_up/down), 6 class, 7 emotes, 8 log discs, 9 state + mech plates, 10 bar glyphs, 11 buildings, 12 empty-state, 13 furnishings, 14 boss sigils, 15 lock + blots, 16 the manifest. New lib-level helpers inside the file: `mask_rrect`, `drawn_mask` (strokes -> mask), `mask_flip_y/transpose`, `sprite()` (light body / open-edge shade / outside dark ring), `plate()` (badge rim from one base colour), `scaled_spans` (24-grid features onto 30/36).
- First dry run into the scratchpad (`--script-param out=`): 43 top-level + 71 grid PNGs + icons.json + 3 face sheets. Byte-compare against the tree: the 25 untouched legacy PNGs and faces.png/faces.json are IDENTICAL; only the 12 mech_* and 6 rank_* differ (the intended re-authors). A2 DONE.
- Montage viewed (build/shots/W1-ICONS-montage.png, 89 icons at 4x + the 30/36 sheets at 2x): the six shields now lift off the ground with a cream rim (blank, chevron, double chevron, star, laurel + diamond, crowned skull on a gold glow); class glyphs are light steel with a warm-grey ring (swords, shield, fist, dagger, cross, leaf, totem, lute, orb, hat); the emotes are cream bubbles with a bottom-left tail (dots, red !, ?, heart, zZ, blue drop, skull, tankard with foam, note, red anger mark); the log discs are coloured badges (red frown, green cross, steel sword, violet eye, periwinkle cog, bronze hourglass, grey scroll, green up / red down arrow, lavender gem, gold coin, blue bust); state/mech plates are rounded squares tinted by role (amber downed, maroon skull, red cracked heart, steel shield, bare slate swap plate, violet eye, blue slashed drop, green slashed cross; tank steel / healer green / dps ember / support violet for the twelve mechanics); bar_hp a red shield with a cross, bar_focus a blue drop; buildings a hanging banner, tankard, scales, anvil + hammers, notice board with two papers; empties a quill, a bench, a shelf in the engraved slot style; furnishings a straw cot, a bed with a red blanket, a steaming bowl, a skull on a shelf, a locket with a heart, a cauldron over fire; sigils a fanged maw, crystal core, spiked crawler, stone fist, flame, curled tentacle; lock and three red blots. Second pass fixed what read wrong at 4x (build/shots/W1-ICONS-fixes.png viewed at 6x): dagger and sword thickened, ground_effect became concentric floor rings (the drips read as a face), the void core became a crystal with rays (the ringed orb read as a ship's wheel) and the crawler a legged oval with back spikes (it duplicated the core), the mage orb lost its stand (it read as an urn), downed gained a down-arrow over the fallen figure, the laurel's leaves moved 1px inside the rim.
- A6 DONE: rank rim contrast vs #020B12 (WCAG luminance, same pipeline as cvd.py; minimum over the whole outline ring): unknown 11.63:1, known 12.03, respected 15.93, established 15.62, renowned 10.00 (the green laurel leaves touch the ring), legendary 14.57 — all >= 3:1; test_icons.gd pins the same measurement.
- Generated into the tree (`Aseprite -b --script tools/aseprite/gen_icons.lua`, my generator only — `build_art.sh --gen` would also run the other units' half-edited generators into files I do not own). `git status` on game/assets + art/src shows my writes as exactly: 12 `M mech_*.png`, 6 `M rank_*.png` (+ their .aseprite), `?? icons/grid/` (71 PNG + icons.json), `?? faces_30/36.png/.json`; art/src/ui/faces.aseprite unchanged (deterministic). The other modified files in that listing (cta_plate, wax_seal, wipe_blot, fire_*, stage_*.json) are W1-CHROME / W1-VFX / W1-STAGE writing concurrently.
- A4 (imports): 73 `.import` sidecars written by a scratch script from rank_unknown.png.import's block with a fresh uid (alphabet sampled from the repo's 100 existing uids, no collision) and `path`/`dest_files` rebuilt from md5(res path) — the rule rank_unknown's own sidecar demonstrates (md5("res://.../rank_unknown.png") = f78eb1ce… as in the file). The `.ctex` files come from the `--import` boot in the locked run below.
- A7 DONE (first measurement, before the Godot runs): `./tools/build_art.sh --check` -> `ART CHECK  generators=6  runtime files: 184 agree  0 DIFFERS  0 MISSING   (sources: 179 agree, 0 differ — not gated)`, `ART CHECK OK` (6 generators now: W1-VFX's gen_vfx.lua has landed beside mine).
- `tools/lint_motion.sh` -> `MOTION LINT OK` (Icons.gd has no tween and no glyph door). Indentation: Icons.gd 50 tab-indented lines, 0 space-indented; test_icons.gd 0 tabs.
- Icons.gd written (static API: `size`, `roles`, `mech_key`, `file_name`, `path`, `exists`, `at`; consts `SIZES`, `PREFIX`, `WARRIOR_GLYPH := "swords"`, `STATE_KEYS`, `LOG_KINDS`, `BUILDING_KEYS`, `EMPTY_KEYS`, `EMOTE_KEYS`). test_icons.gd written (10 tests). Locked run 1 (parse check + `--import` + the four before/after shots) launched in the background.
- RESUMED (second agent, same session scratchpad). Read the report, handoff, Icons.gd, test_icons.gd, gen_icons.lua (all 1388 lines), the run logs. Diagnosis of the two red tests: (1) `test_a_missing_icon_is_an_error_not_a_silent_null` asserted `not src.contains("class_name")` and Icons.gd's own header comment said "no class_name — invariant 7/8" (LESSONS: "a file that states a rule trips a grep for that rule") — comment reworded to "registers no global class". (2) `test_every_png_under_the_grid_is_in_the_manifest_and_every_row_is_on_disk` forbade a name at both levels, and J2 deliberately leaves the 15x12 `class_warrior.png` hand crop at the top level under the 16x16 `grid/class_warrior.png` — the test now carries `SHADOWED_CROPS := ["class_warrior"]` and for that one name asserts the top-level file is NOT cell-sized (a second generated twin would still fail). Indentation re-checked: Icons.gd 50 tab lines / 0 space lines; test_icons.gd 0 tabs, every indent a multiple of 4.
- The earlier A8 shots (W1-ICONS-run1.log) were taken while W1-CHROME's Fonts.gd had a parse error (`set_glyph_advance` Vector2i) so Boot failed to load in every one of the four runs, and their before/after diff bbox covered the whole screen (the fire/shimmer animate between runs). Retaking them in one locked batch (`w1icons_batch.sh`: parse check -> old sigils + --import -> 2 shots -> new sigils + --import -> 2 shots -> verify --fast) with `--set=reduced_motion=true --set=reduced_effects=true` so the world layer holds still and chip 4 is the only legitimate difference.
- A8 montage (final, from the CURRENT tree, manifest order): build/shots/W1-ICONS-montage-final.png — 114 icons at 4x + the 24/30/36 face sheets at 2x, viewed with Read. Row 1: the ten morale faces (red shouting/frown/tear, amber lidded/neutral/smile, green grin/arched eyes, two hearts, the last with sparkles), then the sword and the shield slot glyphs. Row 2: helm, breastplate, greaves, boot, ring slot glyphs; red round flask, blue vial, gold hip flask, sack with "?", then the whetstone on its strop with the oil flask, the round mana bottle, the roast with a bone and steam on a platter. Row 3: the six rank shields — plain grey, brown with a tan chevron, blue with two cream chevrons, steel with a four-point star, bronze with a green laurel and a cream diamond, gold with a crowned skull on a glow — every one with a visible cream rim; then left/right chevrons, the double fast-forward, up/down chevrons, the cog. Row 4: class glyphs (crossed swords, heater shield with cross, fist, dagger, flared cross, leaf, totem, lute, orb with three sparks, pointed hat) and the first two emotes. Rows 5-6: the remaining emotes (?, heart, zZ, blue drop, skull, tankard, note, red anger mark — cream bubbles, ink outline, bottom-left tail), the twelve log discs (red frown, green cross, steel sword, violet eye, periwinkle cog, brown hourglass, grey scroll, green up-arrow, red down-arrow, lavender gem, gold coin, blue bust), and the state plates (amber fallen figure with a down arrow, maroon skull, red cracked heart, steel shield, bare slate plate for swap_stack). Rows 7-8: violet eye (fixated), blue slashed drop, green slashed cross, then the twelve mechanic plates (swap arrows, burst, concentric floor rings, three adds over a mass, bolt, flame with an amber core, crosshair, eye, slashed cross, slashed drop, wedge, stairs), bar_hp red shield with a cross, bar_focus blue drop, the five buildings (hanging banner, tankard, scales, anvil with two hammers, notice board with two papers), the quill. Row 9: bench, shelf, the six furnishings (straw cot, bed with a red blanket, steaming bowl, skull on a shelf, gold locket with a heart, cauldron over fire), sigils: fanged maw, crystal core with four rays, spiked crawler with legs. Row 10: stone fist, flame, curled tentacle, the padlock, three red blots. Then the three face strips (240x24, 300x30, 360x36) — the 30/36 faces keep the same expressions with the features scaled onto the larger disc. Nothing reads as text; nothing is pure black or white (test_no_grid_pixel_is_pure_black_or_white pins the grid).
- A1 verified against the tree (resume): icons/grid/icons.json lists 114 files — face 10, slot 7, item 7 (4 potions + whetstone_kit/mana_draught/guild_feast), rank 6, arrow 4, control 2, class 10 (9 + warrior_shield), emote 10, log 12, state 8, mech 12, bar 2, building 5, empty 3, furnishing 6, sigil 6, lock 1, blot 3 — plus the three face sheets; `grep math.random gen_icons.lua` hits only the comment that forbids it.
- A2 re-verified (resume): `git diff --stat -- game/assets/ui/icons faces.png faces.json` = exactly 18 binaries (12 mech_*, 6 rank_*), 0 other; faces.png/faces.json/art/src/ui/faces.aseprite unmodified, so every other legacy PNG is byte-identical to HEAD.
- A4 (resume, disk half): every PNG under icons/** and faces_30/36.png has a .import beside it (walk: 0 missing); no duplicate uid across game/assets/**/*.import; `.godot/imported/` holds the .ctex for the grid names (class_bard, sigil_boss_main, lock_16, faces_30/36 checked). The boot half (`Failed to load`) is read off the locked batch below.
- A6 re-measured (resume, PIL, WCAG luminance, min over the 1px outline ring, alpha >= 254): unknown 11.63:1 (60 px ring), known 12.03, respected 15.93, established 15.62, renowned 10.00 (64 px — the laurel tips touch the ring), legendary 14.57 — same numbers as the first pass; test_icons.gd pins >= 3.0 per rank.
- Handoff anchors re-checked against tests/unit/test_art_sources.gd (lines 20, 24, 113, 124-125, 135): all five `old:` blocks still match verbatim.
- `tools/lint_motion.sh` (resume) -> `MOTION LINT OK  tweens and morale glyphs each go through one door`.
- Locked batch 2 (build/shots/W1-ICONS-run2.log, one `with_godot_lock.sh bash w1icons_batch.sh`, ~2 min queue behind another agent's engine): `PARSE_CHECK scanned 160 script(s) / PARSE_CHECK OK`; `--import` with the HEAD rank sigils -> no `Failed to load` / error line; `SHOT OK` Town-before, Guildhall-before; sigils restored + `--import` (grid + faces_30/36 + the new ranks) -> no `Failed to load` / error line (A4 boot half DONE); `SHOT OK` Town-after, Guildhall-after; then verify --fast. The six rank PNGs in the tree were byte-compared against the generated copies after the run: restored.
- A9 DONE — `verify.sh --fast` summary lines (scratchpad w1icons_verify.log, 0 `SCRIPT ERROR` / `Failed to load` lines):
  `PASS  LINT OK  no cross-file class_name references in sim/ or game/`
  `PASS  MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)`
  `PASS  PARSE_CHECK scanned 160 script(s)`
  `PASS  ART CHECK  generators=6  runtime files: 184 agree  0 DIFFERS  0 MISSING`
  `PASS  TESTS PASSED   1710 test(s) in 80 file(s)  [35132 ms]`
  `VERIFY OK  (full log: .verify.log)`
  A5 DONE (test_icons.gd's 10 tests are inside the 1710, both former reds green); A7 DONE (0 DIFFERS 0 MISSING; test_art_sources.gd green in the same run); A3 DONE (Icons.gd parses, its door is exercised by every `_check` in the test).
- A8 DONE — before/after read with the Read tool: W1-ICONS-Town-before/after.png and W1-ICONS-Guildhall-before/after.png are the full fixture screens (Town: the camp plate with the five callouts, the raider bubble "I think I'm ready for a raid this time!", the four-card desk strip, the sidebar; Guildhall: the Roster tab, eight morale cards, Rest panel, "Lowest morale first" list), header chips "12,480 G · 320 · Roster 12 of 15 · Day 23 · Unknown" on both. Pixel diff (scratchpad diff_shots.py): Guildhall = 70 differing pixels, bbox (1264,25)-(1284,47), ALL inside chip 4's 28x28 sigil rect — the plan's "unchanged pixel-for-pixel except chip 4" holds exactly. Town = the same 70 sigil pixels plus one 40x44 patch at (298,144)-(338,188) that is NOT an icon: the camp stage's timed "..." speech bubble over the raider by the left tent, present in the before shot and gone in the after shot (viewed at 3x in W1-ICONS-Town-outside-chip4.png: cream bubble with three ink dots | bare tent canvas | diff). It is SceneStage's bubble scheduler firing on a different frame between two runs a minute apart (reduced_motion holds the actors and fire, not that timer) — W1-STAGE's layer, no file of mine draws it; every other pixel of the 1536x1024 Town is identical. Chip 4 itself (W1-ICONS-*-chip4.png, 6x, before | after): the old "Unknown" sigil is a dark grey shield with a faint grey inner corner that sinks into the #020B12 chip; the new one keeps the same 28px footprint and dark grey body and gains the 1px cream rim with a lighter top-left highlight, so the shield now reads as a shape beside "Day 23 · Unknown".

## Result

All nine boxes ticked. Left to the orchestrator: the five test_art_sources.gd edits in the handoff (the grid walk + manifest read), after which `test_icons.gd`'s recursive hygiene and test_art_sources' top-level walk overlap harmlessly. Nothing in this unit touches a screen, a Label, or a tween; wave 2/3 consumers call `Icons.at(role, key)`.
