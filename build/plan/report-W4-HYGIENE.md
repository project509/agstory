# Report — W4-HYGIENE — the records tell the truth and the guards close

Findings: RULES-04 + COMBAT-21 (LIES rows for RaidView's wipe comment; M6-JUICE-08 evidence names the two
PNGs), RULES-05 (LIES rows keyed on `stage_town.json`; M4B-VFX-01 evidence), PIPE-14 (docs/12 §7.1/§7.6/§7.7
rewritten to the tree; §7.7 cites spec 11 §2; §7.5 gets the MCP protocol), TOWN-14 (BUILD_STATE's plate table
corrected to the tree), KIT-10 (the `Color("` lint line), CRITIC-G16 (spec 06 §3's crimson rule), TOWN-17
(`test_sidebar_fit.gd`), §0.6 (the switch register in spec 00 §2), audit.json rows M4B-CONV-02/03,
M6-JUICE-05/08, m4t-01/02/03/08/09 closed or re-scoped, the 14 refdiff numbers as the baseline.
Owns: tests/unit/test_project_hygiene.gd, tests/unit/test_sidebar_fit.gd (new), tools/lint_motion.sh,
build/plan/audit.json, BUILD_STATE.md, docs/12-art-direction.md, art/ref/specs/00-canon-reconciliation.md,
art/ref/specs/06-ui-component-kit.md (+ docs/_log/progress.md, the overflow of BUILD_STATE's progress list).
Handoff: build/plan/handoff-W4-HYGIENE.md. Assets: none. Shots: none (records) — the wave-3 sheets stand.

## Acceptance

- [x] A1 `test_project_hygiene.gd` green with the new LIES rows (RaidView "art that does not exist" / "has no blot"; MainMenu "none of which SceneStage can build" / "cloud drift and sail motion are M4B-VFX-01")
  - Four rows appended to `LIES` (RULES-04, COMBAT-21, RULES-05 + the W3-MENU interim phrase), each keyed on the asset that makes the phrase a lie (wipe_blot.png x2, stage_town.json, clouds_far.png); `grep` confirmed none of the four phrases is in the tree today (RaidView.gd:242-244 and MainMenu.gd:100-104 name the PNGs and the audit item instead), so the rows guard retired text rather than a live lie. `LIES_OWED` stays empty.
  - Seen: scratchpad/run_W4-HYGIENE.gd (hygiene + sidebar_fit + motion pinned) → `TESTS PASSED 24 test(s) in 3 file(s)`; parse_check OK over 178 scripts.
- [x] A2 KIT-10: `grep -rn 'Color("' game --include=*.gd | grep -v Palette.gd` empty, and `lint_motion.sh` goes red if one is reintroduced (proved by reintroduction); the five strings test_motion.gd:97-116 pins intact; lint text additive
  - The grep prints one line, SceneStage.gd:178, which is a `##` comment (the lint strips comments first, so it is not a hit). A third `check` line: pattern `Color\("`, allowed `game/ui/Palette.gd`, dirs `game sim` (tools/ is outside the rule: probes and instruments ship nothing). The two existing check lines, the `MOTION LINT OK` verdict line and its four failure-hint lines are byte-identical; one verdict continuation line and two hint lines added; a header paragraph explains the third door.
  - Seen: `./tools/lint_motion.sh` → `MOTION LINT OK …` exit 0 on the tree; the SAME script copied unmodified to a scratch root holding `game/probe.gd` with `var rim := Color("E4C9AA")` (and a `Color("000000")` in a comment) → `LINT game/probe.gd:3 names a hex colour …`, `MOTION LINT FAILED 1 call site(s)`, exit 1 — the comment line is not flagged, the code line is. test_motion's `test_the_lint_that_keeps_it_the_only_door_is_wired` passes in the pinned run.
- [x] A3 TOWN-17: `test_sidebar_fit.gd` green on all 13 routes, both sweeps (empty guild, reference fixture) — `OVERFLOW_PENDING` empty (RaidPrep held a row for an hour; see Judgement calls)
  - The runner never lays out (nothing is inside the tree in `_initialize`, and a container's minimum-size cache primed while the panel was empty is never invalidated outside the tree), so the test walks the `Sidebar` panel itself: wrapped Labels re-wrapped at the column width by the engine's own `_shape` (set the width, re-set the text, read the uncached `get_minimum_size()`), VBox/HBox/Margin/Panel by separation and margins, a vertical ScrollContainer as 0, everything else by its own `get_minimum_size()`. A self-test proves a long wrapped Label at 100px counts as >2.5 lines and a scroll costs nothing. Four routes are anchored as must-have-a-sidebar (Town, Board, Tavern, Settings) so the walk cannot pass by finding none; MainMenu/RaidView/Results have no sidebar and are recorded as such.
  - Measured (wants / host): empty — Town 546/650, Board 370/635, Tavern 36/635 (its column scrolls, W2-TAVERN), Guildhall 88/635, Market 318/635, RaidPrep **672/635**, RaiderDetail 175/635, Completion 374/925, Settings 472/925, LoadSave 426/925; fixture — Town 520, Board 545, Market 520, RaidPrep **703**, RaiderDetail 408, Completion 671/925, the rest as empty. Cross-checked against the wave-3 sheets: RaidPrep's rendered panel bottom is at y=767 (empty) and y=822 (fixture) against SIDEBAR_BOTTOM 714 (build/shots/W4HYG_RaidPrep_{empty,fixture}_side.png, crops of build/shots/all/*/RaidPrep.png; edge rows found by scanning x=1148) — the estimate is a slight UNDER-count (688/743 rendered), so it is a floor, never a false alarm. The other nine routes' rims sit inside their hosts on the sheets.
  - Host arithmetic pinned: framed 635 (SIDEBAR_TOP..SIDEBAR_BOTTOM), the full-bleed Town 650 (DIVIDER_Y+2 .. STRIP_Y-8, TOWN-08).
- [x] A4 PIPE-14: docs/12 §7.1 tree matches `find art -maxdepth 2`; §7.6 describes `build_art.sh --gen/--check`; §7.7 cites spec 11 §2's Linear ruling; §7.5 carries PIPE Part 3's MCP protocol; `test_docs_links.gd` green
  - §7 intro paragraph says which subsections are as-built; §7.1 is the real listing (art/.gdignore, src/{ui,ui/icons,items,vfx,enemies,bg}, export/<14 categories>, ref/{manifests,specs}, the three empty first-draft dirs) plus the game/assets map and the truth rule as it actually works (generator = source, `.aseprite` = editable record, `--check` = the gate, `test_art_sources` for sliced provenance); §7.3 clause 2 says `verify_tags.lua` never existed; §7.4's lead notes the example paths are illustrative; §7.5 = the status (fixed 2026-09-13, intermittent), the three-bin tool map and the session protocol from PIPE Part 3; §7.6 = `build_art.sh --gen/--check/--tags`, verify stage 2b, the generator→output table, the tools/art Python list, PIPE Part 4's eight conventions as landed; §7.7 = no atlas/importer, the Linear ruling citing spec 11 §2 and project.godot with the per-node NEAREST opt-in, no text in PNGs; §1's ownership line and §9 Q12 (PowerShell — moot) corrected. scratchpad/patch_docs12_W4-HYGIENE.py did the splice.
  - Seen: `find art -maxdepth 2 | sort` (30 entries) against §7.1 — every entry present; a grep for verify_tags / export_all / export.manifest / art/characters finds only the sentences that say they never existed; test_docs_links.gd passes in the pinned run (no bare `NN-name.md` in the new text; spec citations carry their `art/ref/specs/` prefix).
- [x] A5 TOWN-14: BUILD_STATE's plate table says what the tree does (Town/AdventureBoard/Completion + the hall family on `stage_camp`, MainMenu on `stage_town`, Settings inherits); the "what is missing is the screens" paragraph retired
  - The table under "### The art directive…" rewritten from the tree: stage_camp → Town (-46,-62), AdventureBoard (-20,-80), Completion (-46,-94), the hall family via `Guildhall.bare_stage()` at `HALL_FRAMING = "same"`; stage_town → MainMenu only; cave = `DEFAULT_ARENA` for every fight; dungeon authored, unrouted; Settings inherits (`Settings.STAGE_FOR`). A paragraph records what the table used to claim and why it was wrong (Town.gd:184, AdventureBoard.gd:180, Completion.gd:50 all load stage_camp). The "five still load guildhall_plate.png … waits on a plate nobody has supplied" paragraph is replaced by "every screen stands on a bare plate" + the four crops nothing loads (M4B-CONV-02's remainder) + what SceneStage builds now (incl. W4-LIFE's in-flight layers).
  - Seen: the SceneStage.load / SCENE constants grep over game/screens/*.gd and Settings.gd:151-163 are the source of every cell.
- [x] A6 §0.6: the switch register lands in spec 00 §2 with the designer question each switch waits on, and Q13's answer in the tree ("Under construction.", cfd2c29)
  - art/ref/specs/00-canon-reconciliation.md §2.7: a 13-row table — switch (file), default, the other branch, the 00-plan §6 question — for figure_scale, marks.boss.scale, `Guildhall.HALL_FRAMING` ("same" / "tight" from `FRAMINGS`), the bubble chrome, `Frame.LOCKUP` (the three `LOCKUPS`), overhead bars (`RaidView._bar_ids`), dof/vignette, transitions (no constant exists — ScreenRouter has none by ruling; M6-JUICE-02 names the switch form), `Town.HUB_CTA`, `RaidView.EXITS_GATED`, `Results.COMEDY_LINE`, `Fonts.MORALE_FACE_FONT`, `Icons.WARRIOR_GLYPH`; then Q13's answer (the blurb "Under construction.", cfd2c29, the lock reason still "maybe", "Pauline_4" open) and a "not switches" paragraph (DEFAULT_ARENA, the Blacksmith rail item, BL-78). Header's Updated date bumped.
  - Seen: every constant grepped in the tree before the row was written (Guildhall.gd:125, Frame.gd:75/84, RaidView.gd:213/292, Results.gd:94, Town.gd:55, Fonts.gd:27, Icons.gd:34, the scene JSONs' figure_scale and boss.scale, Town.gd:110 "Under construction.", :563 the reason).
- [x] A7 CRITIC-G16: spec 06 §3 states the crimson rule
  - After the states table: "crimson = spends time, gold or a raider; everything else is secondary", one per screen at most, the list of the crimson commits as built (Depart, Hire, Sell, Commission, Rest until recovered — it costs days, Go to prep / Open the board, See how it ended, Back to town — the guild carries on), Settings' Apply as `ButtonSecondaryLit` (W3-OPTIONS), RaidView's wax "The report" as the same grade in wax, the screens with none, `Widgets.cta()` as the one constructor.
  - Seen: the grep for Widgets.cta / ButtonCta / wax_button over game/screens/*.gd is the list.
- [x] A8 audit.json: M4B-CONV-02/03, M6-JUICE-05/08, m4t-01/02/03/08/09, M4B-VFX-01 closed or re-scoped to the tree
  - DONE (remaining_work emptied, a dated note saying what landed, where, and the test that pins it): M4B-CONV-03 (BL-78, W1-HALL; Q03 behind HALL_FRAMING), M6-JUICE-05 (W2-RAIDVIEW's `damage_number` + `number_at`, test_raid_overlays:155), m4t-01 (nine grid glyphs + the shield, WARRIOR_GLYPH; the silhouette gate explicitly NOT claimed), m4t-02 (Cards.class_glyph on card line 1 and RaidView; the two-line rows not owed), m4t-03 (all six provision icons, Icons.at("item")), m4t-08 (14 diff_all targets), m4t-09 (badges by kind through the grid; badge_N crops loaded only by the Kit probe). M6-JUICE-08's evidence now names wipe_blot.png/wax_seal.png/gen_wipe.lua and the RaidView docstring rewrite. RE-SCOPED: M4B-CONV-02 partial (the four crops nothing loads + spec 09 §3/§4), M4B-VFX-01 partial (rows 4/5/7 landed — the "NO animated layer" prose corrected; row 8 + sails with W4-LIFE). Every row passes test_audit_hygiene's shape rules (checked by a script before the run); keys reordered to the file's convention; the file round-trips byte-exact except the ten rows.
  - Seen: `git diff --stat build/plan/audit.json` 65+/72-, only those rows; test_audit_hygiene.gd passes in the pinned run.
- [~] A9 BUILD_STATE at 321 lines (was 361): the two progress entries (M5-TUT-04/-05, M6-BAL-02) AND the wave-0/1/2 summaries moved to docs/_log/progress.md as dated entries in chronological order after ART WAVE 3 (newest first: WAVE 2, 1, 0, then the 09-11 entries); the wave-3 paragraph compressed to a pointer (its text is the log's entry verbatim); the stale "Next task: audit_stale … M6-JUICE-05" paragraph corrected (JUICE-05 is done); a "Wave 4 in progress — W4-HYGIENE" paragraph; the MCP environment note; the build/ deletion risk shortened to its rule. The wave-3 baselines block is untouched and is THE baseline (diff_all not re-run — rule 2, and the orchestrator's close records the next set).
  - Not at 250: what remains is orientation (HANDOFF, the binding decisions, invariants, commands, paths, the directive), not history; the named mechanism (move the progress tail) is exhausted. Cutting further means deleting rulings this unit did not write.
- [x] A10 Handoffs filed: run_tests.gd settings redirect (+ GameSettings.PATH as a static var, test_settings.gd untouched), tools/shot.gd's tab indentation, stage_town.json's stale paragraph
  - build/plan/handoff-W4-HYGIENE.md, seven edits: §1 `GameSettings.PATH` const → `static var` (the SaveGame.SAVE_DIR precedent); §2 run_tests.gd points it at `user://test_settings.cfg` (not under test_saves/ — test_settings.gd:153-154 forbids "save" in the path) and clears it; §3 playtest.gd and §4 a11y_smoke.gd get the `SAVE_DIR` redirect shot.gd/perf_probe.gd already carry (both wrote real autosaves: "Playtest NNNNN", the fixture raid); §5 stage_town.json's stale "still need art that does not exist" paragraph (W4-LIFE's file this wave; the LIES row to add after it lands is named there); §6 tools/shot.gd whole-file leading-tab → four-space conversion, generated from the tree by scratchpad/write_handoff_W4-HYGIENE.py (old = the file as it stands, so it no-ops if anyone touches the file first; 389 tab-led lines, no non-leading tabs); §7 LESSONS.md:23 record-truth. Plus three observations (RaidPrep's sidebar for W4-PREP; stage_town's note for W4-LIFE; the badge_N crops and four bg crops nothing loads).
  - Seen: `python tools/apply_handoff.py build/plan/handoff-W4-HYGIENE.md --dry-run` → APPLIED #1..#7, exit 0.
- [x] A11 `GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` green; `tools/lint_motion.sh` prints MOTION LINT OK
  - verify --fast (6m30s under the lock, 2026-09-15): `PASS  LINT OK` · `PASS  MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)` · `PASS  PARSE_CHECK scanned 178 script(s)` · `PASS  16 generated file(s) agree with tools/gen_items.gd` · `PASS  ART CHECK  generators=7  runtime files: 204 agree  0 DIFFERS  0 MISSING` · `PASS  TESTS PASSED   1956 test(s) in 96 file(s)  [237066 ms]` · stages 4-8 SKIP (--fast) · `VERIFY OK`. The 1,956 includes the other wave-4 units' in-flight files (the tree is shared) — test_sidebar_fit's 5 tests and the hygiene file's 8 are in it.
  - `./tools/lint_motion.sh` → MOTION LINT OK (both lines), exit 0; no game/ file touched by this unit.

## Judgement calls

- **RaidPrep's sidebar overflowed at the wave start and RaidPrep.gd is W4-PREP's this wave.** The plan assumed "every screen passes" the guard; the first measurement said RaidPrep did not (the column — mission well, rewards, verdict, team, provisions, Depart, tally, Back — wanted 672/703px in a 635px band; TOWN-17 named the Tavern and the Board, both fixed in wave 2, and never measured RaidPrep). Not this unit's file, so the test carried RaidPrep in `OVERFLOW_PENDING`: a row asserted to STILL overflow (the `LIES_OWED` flip mechanism), so the day the column fits the test fails with "delete the row". That day was the same afternoon — W4-PREP's in-flight RaidPrep.gd (a `SidebarScroll` ScrollContainer and the provisions moved to the strip, its own comments citing G15 / TOWN-17) flipped the row on the next run (wants 178/153 outside the scroll), the row is deleted, and the guard now covers RaidPrep like every other route. Chosen over a silent skip because a skipped route is the blind spot the guard exists to close, and over failing the wave because a finding in a file you do not own is a handoff — the observation stands in the handoff as the record. If W4-PREP's change does not land, the guard goes red on RaidPrep with the number, which is the correct state.

## Left

- BUILD_STATE.md is 321 lines against the ~250 target (A9): the history is out; the rest is orientation this unit will not delete.
- The `stage_town.json` LIES row waits on handoff §5 (W4-LIFE's file this wave); the row text is in the handoff.
- The four unreferenced `game/assets/bg/*_plate.png` crops and the seven `badge_N.png` crops (Kit probe only) are M4B-CONV-02's and m4t-09's noted remainders — not this unit's files.
- `tools/shot.gd`'s reformat is handoff §6 (whole-file old/new), applied by the orchestrator, not here.
- No shots of this unit's own (records only); the two RaidPrep crops under build/shots/W4HYG_* are evidence for A3, read with the Read tool and described there.

## Files

Edited: tests/unit/test_project_hygiene.gd (+4 LIES rows), tools/lint_motion.sh (+1 check, +3 verdict/hint lines, header paragraph), build/plan/audit.json (10 rows), BUILD_STATE.md, docs/12-art-direction.md (§1 line, §7 intro, §7.1, §7.3 clause 2, §7.4 lead + gotcha 4, §7.5, §7.6, §7.7, §9 Q12), art/ref/specs/00-canon-reconciliation.md (§2.7, header date), art/ref/specs/06-ui-component-kit.md (§3 paragraph), docs/_log/progress.md (+5 entries).
New: tests/unit/test_sidebar_fit.gd (+ its generated .uid), build/plan/report-W4-HYGIENE.md, build/plan/handoff-W4-HYGIENE.md, build/shots/W4HYG_RaidPrep_{empty,fixture}_side.png, scratchpad/{run_W4-HYGIENE.gd, batch_W4-HYGIENE_1.sh, write_handoff_W4-HYGIENE.py, patch_docs12_W4-HYGIENE.py, patch_buildstate_W4-HYGIENE.py, patch_audit_W4-HYGIENE.py, tick_report_W4-HYGIENE.py, BUILD_STATE.before.md, progress.before.md}.
