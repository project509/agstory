# Review — W3-ENEMIES (the boss moves and the townsfolk breathe)

Adversarial review against the TREE. Written as findings land.

## 0. Method
- git status --porcelain; git diff on every owned file; untracked owned files listed.
- Run the unit's test file + verify --fast through the lock; record what was observed.
- Re-take at least one acceptance shot into build/shots/review-W3-ENEMIES-*.png and view it.
- Walk every Acceptance line, the Green line, §0.5, §6 reservations, rule 7 isolation, handoff parse.

## 1. Tree vs owned list
- `git status --porcelain`: 115 rows. Owned and changed: game/ui/SceneStage.gd (M), tests/unit/test_scene_stage.gd (M), tools/art/gen_actors.py (M), game/assets/actors/actors.json + 44 variant_r*.png + warrior.png (M), game/assets/enemies/enemies.json (M); untracked owned: tools/aseprite/gen_boss_anims.lua, game/assets/enemies/anim/ (12 PNG + 12 .import), art/src/enemies/ (12 .aseprite). The 11 other sliced actor strips (cleric*, knight*, mage*, ranger*, rogue*, warrior_b) are byte-identical to HEAD (not in git status).
- Changed files NOT in the owned list (attributable to other wave-3 units by 00-plan §4's ownership table; nothing in this unit's diffs or report touches them): GameState.gd, Widgets.gd, Cards.gd, Badge.gd, Bar.gd?, Boot.gd, test_widgets_kit.gd, `D game/assets/bg/boss_sludge_maw.png(+.import)` (staged deletion — W3-KIT2's row names it) · Guildhall.gd, Roster.gd, Facilities.gd, docs/13-ui-ux.md (W3-ROSTER) · RaiderDetail.gd, PaperDoll.gd, test_paper_doll.gd (W3-DETAIL) · LoadSave.gd, Settings.gd, test_options_layout.gd (W3-OPTIONS) · RaidView.gd, test_raid_beats.gd (W3-RAIDVIEW2) · MainMenu.gd, Completion.gd, test_menu_lockup.gd (W3-MENU) · Town.gd (?), test_event_feed.gd (W3-KIT2) · wave-0/1/2 report/handoff .md edits and build/plan/recovered-2215/ (orchestrator). None charged to this unit.
- SceneStage.gd diff read in full (`cat -A`): every inserted line is TAB-indented; test_scene_stage.gd insertions are four-space; gen_actors.py is spaces (LF, as before).
- Boss strips (numeric, build/shots/_review_w3en_strips.py): six ranks, `frames*frame_w x frame_h` matches the PNGs (1860x131, 2400x121, 1788x105, 2172x165, 1476x91, 912x82; glow same canvas); frame 0 == the still placed bottom-centre for all six (0 visible px differ); idle 0-3 bbox identical except the top row moving 1px (breath), bottom-row centre constant (feet planted); hit 4 = flash (saturation drops 27→19 on main), 5 = 2px recoil; death 6-11 saturation 27→0, mean alpha 228→126 (~0.55), bbox widening to one side (topple); glow hot pixels (>=207) 28/28/0/28 over idle on main (the pulse), gone by death frame 9.
- Townsfolk (numeric): 56 entries, 12 sliced / 44 breath, every `frames >= 4` (warrior padded 3→4, flagged `pingpong_padded`); for all 44 breath strips: frame 3 == frame 0, frame 1 != frame 2, frame 1 differs from frame 0 only above 0.6*h, bottom-20% alpha centroid identical across frames. `python tools/art/gen_actors.py --check` → `GEN_ACTORS CHECK OK (57 files)`, rc 0.
- Viewed: review-W3-ENEMIES-strip-main-a/b.png (sludge maw 12 frames at 3x: breath, cream flash frame, 2px recoil, then six frames greying, leaning left and fading; eyes lit through death frame 8), review-W3-ENEMIES-strip-elite-a.png, review-W3-ENEMIES-strip-trash.png (same grammar on the tentacle), review-W3-ENEMIES-townsfolk.png (six figures x 4 frames at 6x: 1px head/chest step, feet on one row; the warrior's frame 4 is its frame 2).
- Observation (NOT this unit's): boss_elite.png (the pinned byte-copy still) carries a 28x4 grey smear at its top-left — the clipped bottom of a sheet caption from the reference (review-W3-ENEMIES-elite-still-top.png); boss_main.png carries a purple sliver of a neighbouring sprite at its top-left. Both are W0-MANIFEST slice rects (art/ref/manifests/all.json), not this unit's, but the strips inherit them 12x. Neither is legible as text. Minor; a rect fix belongs to the manifest owner.

## 2. Tests and verify --fast (observed)
- This unit's test file alone (build/shots/_review_w3en_run.gd, same protocol as run_tests.gd, through the lock): `REVIEW W3-ENEMIES test_scene_stage: 69 run, 0 failing` — including `test_a_boss_with_a_strip_has_idle_and_hit_tags`, `test_every_boss_strip_matches_its_table`, `test_a_strip_boss_hit_and_death_play_their_tags`, `test_reduced_effects_unlights_the_boss_eyes_and_keeps_it_breathing`, `test_a_new_speaker_retires_the_previous_plate`, `test_every_townsfolk_breathes_in_four_frames`, `test_no_actor_is_a_statue`, `test_the_pixels_back_the_measurement_up`, `test_the_manifest_and_the_strips_agree`, `test_scale_flip_and_tint_are_honoured`, `test_the_boss_breathes_and_reduced_motion_stops_it`, `test_a_verb_on_the_boss_composes_with_its_bob`, `test_every_creature_the_sheet_can_produce_clears_the_chrome_and_lands_on_its_mark`.
- `parse_check.gd`: `PARSE_CHECK scanned 173 script(s)`, `PARSE_CHECK OK`.
- `verify.sh --fast` (through the lock, 23:44-23:48): `PASS class cache regenerated` · `PASS LINT OK` · `PASS MOTION LINT OK` · `PASS PARSE_CHECK scanned 173 script(s)` · `PASS 16 generated file(s) agree with tools/gen_items.gd` · `PASS ART CHECK generators=7 runtime files: 197 agree 0 DIFFERS 0 MISSING` · `FAIL unit tests — TESTS FAILED 4/1908 failing [242992 ms]` · `VERIFY FAILED`. The four: `test_options_layout.gd :: test_the_title_screen_side_prints_each_reason_once_per_row` (W3-OPTIONS' file), `test_roster_layout.gd :: test_the_panel_leaves_room_for_two_full_rows_and_the_top_of_a_third` (W3-ROSTER's), `test_widgets_kit.gd :: test_a_placed_callout_is_kept_inside_the_scene` and `:: test_the_callout_band_is_the_scene_rect_in_the_parents_space` (W3-KIT2's Widgets.gd callout clamp — the test bodies build `Widgets.building_callout` and never touch SceneStage). None is in a file this unit owns or in a test that reads this unit's files; test_scene_stage (69), test_art_sources, test_raid_beats, test_raid_overlays, test_motion, test_vfx_assets all pass in the same run. Noted, not charged. (The implementer's run at 23:17 saw 7 failures in the same families; three have since been fixed by their owners.)

## 3. Shots (re-taken, viewed)
All through the lock, §0.3 grammar, one batch (build/shots/_review_w3en_batch.sh → review-W3-ENEMIES-batch.log; every shot `SHOT OK`).
- `RaidView --fixture=raid --frames=1,20` → review-W3-ENEMIES-RaidView.a/.b.png. Viewed .a whole: E5, the sludge maw on the right ledge at 1x facing the party, two lit orange-red eyes. Crop review-W3-ENEMIES-boss-crop-ab.png (4x, a beside b, viewed): frame a — both eyes a hot core with a soft halo (11x11 around each eye: brightest (255,195,148) / (255,191,146), 15-16 px >= 200 — the > 1.0 cluster A7 asks for, inside the boss bbox); frame b — the eyes in the dim pulse state (brightest (160,165,173) / (175,191,187)), the head one row lower (rows 401..529 differ; 5618 px differ inside the boss box). The breath and the pulse are both visible between the two frames.
- `--set=reduced_motion=true --frames=1,20` → review-W3-ENEMIES-RaidView-rm.a/.b.png: 0 px differ inside the boss box (eyes lit at (255,195,148) in both — frame 0 held, no pulse, no breath); 123k px differ elsewhere (the party's held frames do not; torches/shader stopped — the remaining delta is W3-RAIDVIEW2's UI); S4 holds.
- `--fixture=raid:clear --advance=all` → review-W3-ENEMIES-RaidView-clear-all.png. Viewed small (review-W3-ENEMIES-clear-all-small.png): "Adventure 0 is defeated", 0 / 66 on the plate. Crop review-W3-ENEMIES-clear-boss-crop.png (3x, viewed): the tentacle is dead — grey-violet, half-transparent (the stones show through), leaning right (away from the party — the strip's 12° topple flipped with the face), suckers unlit. The toppled boss half of A7.
- `--fixture=raid --advance=all` → review-W3-ENEMIES-RaidView-all.png; crop review-W3-ENEMIES-all-party-crop.png (viewed): the twelve lie grey and toppled with skull badges — the fallen-figures half of A7. The maw's eyes read (137,142,150) at capture (a dim pulse frame): the boss alive, as the E5 fixture is a wipe.
- `Town --fixture --frames=1,20` → review-W3-ENEMIES-Town.a/.b.png. Viewed .a whole (camp at 2x, the crowd round the fire). 108977 px differ in the scene band between the two frames (crowd + fire + shimmer); reduced-motion variant (review-W3-ENEMIES-Town-rm.a/.b.png): 0 px differ in the band — every figure holds frame 0 (S4). Crop review-W3-ENEMIES-town-pairs.png (four figures at 4x, a beside b, viewed): each pair differs by a head/face row stepping one art pixel (two screen pixels) — a breath, not a pop; the feet rows are the same. (On-screen crops are linear-filtered at 2x, so the raw-strip check in §1 is the exact proof of planted feet.)

## 4. Acceptance lines
| Line | Met | Evidence |
|---|---|---|
| RaidView shot shows bloom on the boss's eye (a > 1.0 pixel cluster inside the boss bbox) | met | review-W3-ENEMIES-RaidView.a.png, boss box (1120,380)-(1300,530): both eyes brightest (255,195,148)/(255,191,146) with 15-16 px >= 200 in an 11x11 around each; crop review-W3-ENEMIES-boss-crop-ab.png viewed (halo visible) |
| …and, after `--advance=all`, the toppled boss or fallen figures | met | review-W3-ENEMIES-RaidView-all.png (fixture raid = wipe): twelve grey toppled figures with skull badges (crop viewed); review-W3-ENEMIES-RaidView-clear-all.png (`--fixture=raid:clear`): the tentacle grey-violet, half-faded, leaning away, unlit (crop viewed) |
| `test_a_boss_with_a_strip_has_idle_and_hit_tags` | met | `ok test_a_boss_with_a_strip_has_idle_and_hit_tags` in the review run (69/69) |
| `test_no_actor_is_a_statue`, `test_the_pixels_back_the_measurement_up` green | met | both `ok` in the review run |
| the geometry test (`frames*frame_w x frame_h`) | met | `ok test_the_manifest_and_the_strips_agree` (actors) and `ok test_every_boss_strip_matches_its_table` (bosses); my own PIL check: all 56 actor strips and 12 boss strips match their rows |
| `test_scale_flip_and_tint_are_honoured` green | met | `ok` in the review run |
| every manifest entry `frames >= 4` | met | actors.json: 56 entries, min frames 4 (44 breath = 4, sliced 4-5, warrior 3→4 `pingpong_padded`); enemies.json: 12 per rank |
| `build_art.sh --check` 0 DIFFERS | met | verify --fast: `ART CHECK generators=7 runtime files: 197 agree 0 DIFFERS 0 MISSING` (gen_boss_anims.lua in the glob; enemies.json written through lib.lua's write_text so the out= prefix gates it). gen_actors.py --check: `GEN_ACTORS CHECK OK (57 files)` — not yet in build_art.sh (handoff §1) |
| STAGE-08: idle 4 (1px breath + emissive eye layer 1.4-1.8), hit 2 (flash + 2px recoil), death 6 (desaturate + 12° topple + fade); enemies.json `frame_w/fps/tags` | met | §1's numeric strip check + contact sheets viewed; enemies.json rows carry frames/frame_w/frame_h/fps/strip/glow/tags {name: [first,last,fps]}; glow at modulate 1.6. (The eye layer pulses over 4 levels rather than "2-frame" — a superset, observation only) |
| `place_boss` → AnimatedSprite2D through `_strip_frames` with tags; bob kept for single-texture fallbacks; NO integer upscale | met | SceneStage.gd:466-504 (`place_boss`) / :521-566 (`_place_boss_strip`); `test_the_boss_breathes_and_reduced_motion_stops_it` green on a strip-less ImageTexture; scale = the mark's (1); test pins `Vector2.ONE`; scene JSONs untouched |
| STAGE-16: 4 ping-pong frames at 5 fps for `breath` entries, same keys, frames 4; median-frame outlier rule + feet alignment | met | actors.json keys identical to HEAD (56), kinds unchanged, frame geometry unchanged; breath 4 @ 5.0; `drop_outliers` still reports knight/rogue/warrior by name; feet centroid identical across all 44 breath strips (§1) |
| Shot: `RaidView --fixture=raid --frames=1,20`, `--advance=all`, `Town --fixture --frames=1,20` (breath, not a pop), `--set=reduced_motion=true` variants hold frame 0 | met | §3: all re-taken and viewed; rm variants 0 px differ in the boss box / scene band |
| Green: `place_boss` still returns a node with `BossShadow`; `apply_settings` holds every actor and the boss; `actor_for_class` untouched | met | `BossShadow` sibling built before the strip branch and asserted; apply_settings loop over `_bosses` (:2019-2027, and :2069-2072 for the glow under reduced effects) + rm shots; `actor_for_class` has no hunk in the diff, class_actors.json clean |

## 5. Green line and §0.5 contracts
- `place_boss` keeps its name and parameter list; return type widened `Sprite2D` → `Node2D` (the wave note allows it; RaidView.gd in the tree declares `_boss_sprite: Node2D` at :165 (HEAD had `Sprite2D` at :162 — W3-RAIDVIEW2's edit); every RaidView use of `_boss_sprite` is Node2D-safe: modulate, rotation, scale, is_instance_valid). `BossShadow` sibling still created before the branch (SceneStage.gd:471-479); the creature node is still named `Boss` in both branches.
- `apply_settings(reduced_motion: bool, reduced_effects: bool)` literal unchanged (:2006); it now also stops every `_bosses` entry on frame 0 (last frame when the animation is `death`) and, under reduced effects, returns the glow child to modulate 1.0. `actor_for_class`, `from_data/load/place_party/motion_held/party_state_style/DEFAULT_ARENA` untouched (diff shows no hunk in them). class_actors.json and the scene JSONs are not in git status.
- No `create_tween` (0 hits), no `class_name` (0), no `Color("` outside a pre-existing comment, no new `reduced_motion` branch (the stage's existing `_motion_held`/`_effects_off` state is the world-layer door §0.5 names; the new code reads those, as :387/:449/:1046 already did at HEAD). `MOUSE_FILTER_IGNORE` on the say plate kept.
- Rule 7: every symbol the SceneStage diff calls exists at HEAD in SceneStage.gd (`_end_act_on`, `_drop_overlay`, `_lift_layers`, `_read`, `_frame_h_of`, `head_of`, `FLASH_COLOR`, `party_state_style`) or Widgets.gd at HEAD (`speech_plate`, `content_of`); the diff adds no call into Widgets/Cards/Badge/Bar/GameState (grep of the `+` lines: none). gen_boss_anims.lua calls only lib.lua functions present at HEAD (`hex/chan/mix/rgba/blend/px/import_png/strip/write_text`); lib.lua untouched.
- Indentation: SceneStage.gd inserted lines all tabs (`cat -A` read); test file spaces; gen_actors.py spaces.
- `.import` sidecars: 12, one per new PNG, fresh unique uids (0 duplicates in anim/, 0 project-wide), `process/fix_alpha_border=true` like the neighbours; `.godot/imported` carries the .ctex files.
- Handoff: `python tools/apply_handoff.py build/plan/handoff-W3-ENEMIES.md --dry-run` → `APPLIED #1 tools/build_art.sh`, `#2`, `#3 tests/unit/test_art_sources.gd`, rc 0, and both target files remain clean in git status (the dry run wrote nothing). §1's old block is verbatim build_art.sh:101-107 (two-space bash); §2/§3 match test_art_sources.gd:20 and :135-136 (four-space). §3's `enemies/anim` branch is inserted before the `enemies` branch, so no ambiguity with `ends_with`.
- No `$GODOT` call anywhere in the unit's new scripts (gen_boss_anims.lua is Aseprite-only; gen_actors.py is PIL-only).
- No text baked into any new PNG: the strips' only painted pixels are the glow layer (eye picks + 1px halo); the body is the still moved/rotated/tinted. Viewed (section 1). The pre-existing caption smear on boss_elite.png is inherited, not introduced.
- Nothing parented to the router host: the boss and its glow are children of the stage (a `Control` the screen owns); the say plate goes to the stage's `Overlay`.
- Nit: the desaturation rounds one near-black source pixel to exactly (0,0,0) in boss_main/boss_main_2/boss_mini's death frames (1 px each, at faded alpha); boss_elite's still already carried 2 pure-black pixels. docs/12 §4's ban is on the palette file; this is a rounding artefact, minor.
- Nit: `apply_settings(true, …)` reached mid-`hit` (a 180 ms window) leaves a strip boss on hit frame 0 (the flash frame) since it parks on `frame = 0` of whatever animation is current and `animation_finished` never fires on a stopped sprite. Unlikely (the screen applies settings on enter) — minor.
- Nit: `head_of(strip boss)` is now the frame's top, `pad_y` (5-17 px) above the creature's visible head because the frame is padded for the topple; the HP bar/eye burst sit a few px higher than before. Cosmetic, minor.

## 6. Judgement calls vs §6
- Q02 (boss mass / integer upscale): NOT decided — `scale: 1.0` in every rank row, `marks.boss.scale` untouched (scene JSONs not in git status), `spr.scale = Vector2(sc, sc)` with sc from the mark; the test pins `anim.scale == Vector2.ONE`. OK.
- Q18 (sprite pass / re-author): not touched — every body pixel is the slice's, moved; the only paint is the emissive layer STAGE-08 asks for. OK.
- J1 (strips in `anim/`, stills stay byte copies): forced by test_art_sources.gd, which the unit does not own; the handoff widens the walk. Reasonable.
- J3 (glow as a second strip at modulate 1.6): STAGE-08 says "emissive eye/mouth layer at 1.4-1.8"; 1.6 is inside. OK.
- J4 (per-tag fps 5/11/9): docs/12 §5.2 says idle 160 ms (6.25 fps), hit 90 ms (11), death 110 ms (9). The unit chose idle 5 fps "the townsfolk's cadence" (STAGE-16's number) — a judgement the plan leaves open (STAGE-08 names no idle rate); recorded in the report. OK.
- J5 (per-creature ramps: red/fire/moss/void, no #FFFFFF): STAGE-08 names only the void ramp; choosing a hue per creature is presentation, not a §6 reservation. Recorded. OK.
- J6 (strip verbs replace the procedural recoil/flash/rotation, dead tint still written): within the unit's own SceneStage ownership; RaidView's tint write composes; observation written to the handoff. OK.
- J7 (warrior ping-pong 3→4): the plan's "median-frame outlier rule" drops the mis-cut frame; padding by walking back invents no pose. OK.
- J8 (say_at: one plate on the stage): handoff-W2-RAIDVIEW's observation, explicitly offered to this unit by the wave note ("if you take it … with a test"); test_raid_overlays.gd:282 (one bubble over the offender) stays compatible. OK.
- Nothing §6 reserves for the designer was decided.

## 7. Verdict
**PASS.** No blocker; every acceptance line met on the tree; the unit's tests 69/69; verify --fast red only on four tests in three other units' files (noted, not charged); ART CHECK clean; handoff parses; nothing reserved for the designer was decided; no unowned file touched.

Minors (listed, none blocking):
1. Desaturation rounds one near-black source pixel to exactly (0,0,0) in the death frames of boss_main / boss_main_2 / boss_mini (1 px each, faded alpha). A `max(1, …)` clamp in `draw_toppled` would keep docs/12 §4 literal.
2. `apply_settings(true, …)` reached during a strip boss's 180 ms `hit` parks it on hit frame 0 (the flash frame) until the next verb.
3. `head_of(strip boss)` sits `pad_y` (5-17 px) above the visible head because the frame is padded for the topple; the HP bar / eye burst ride a few px higher than on the still.
4. The boss's emissive layer pulses over four levels (STAGE-08 said "2-frame"); a superset, recorded here only.
5. Inherited, not this unit's: boss_elite.png's top-left caption smear and boss_main.png's neighbour sliver (W0-MANIFEST rects) are now in 12 frames each. Not legible text; a rect fix belongs to all.json's owner.
6. The unit's shot-of-record numbers (e.g. "2046 px differ inside the maw's bbox") are capture-time dependent; mine read 5618 for the same pair. Not a false claim — both show breath + pulse.

False claims found in the report: none. Every checked claim (12 .import sidecars with fresh uids, `_boss_sprite: Node2D` already in RaidView.gd, 69/69, 197 agree, handoff parses, the four shots' content, actors.json keys unchanged, warrior 3→4) matched the tree.

Unowned files touched by this unit: none (the 30-odd other modified files all fall in other wave-3 units' ownership rows and nothing in this unit's diffs, report or handoff touches them).
