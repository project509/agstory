# report-W3-ROSTER — the roster reads in two seconds

Unit: W3-ROSTER (00-plan.md §4). Owns `game/screens/Guildhall.gd`, `Roster.gd`, `Facilities.gd`,
`docs/13-ui-ux.md` (the OQ-4 record only), `tests/unit/test_roster_layout.gd` (new).
Started 2026-09-14 21:05. Findings: HALL-03 + KIT-06 + CRITIC-C15, HALL-04, HALL-09 (roster half), HALL-10,
HALL-11, HALL-12, HALL-13, HALL-14, HALL-20, HALL-22, KIT-08, CRITIC-G04, HALL-19 (verify).
Handoff: `build/plan/handoff-W3-ROSTER.md`.

NOTE 22:30: both plan files of this unit were found deleted from disk (the report held only the lines
appended after 21:55; the handoff was gone) — not by this unit. Rewritten in full; every later entry is
appended, and the files are re-checked before each log line.

## Acceptance (00-plan.md "### W3-ROSTER")

- [x] Guildhall shot: no control outside a card border — `W3R_Guildhall.png`: Cheer up / Manage in the card's band (y 370..398 of a rim ending 410), the shut one's "They are fine." under it inside the rim (after `_fit_band`); `ROW_PITCH` 270; `test_both_actions_live_inside_the_card_and_a_row_is_one_card_tall`
- [x] >= 2 full rows + the third row's tops at 12 raiders — rows at y 148..410 and 418..680, the third row's top rims at 688..695 (`W3R_Guildhall.png`); `test_the_panel_leaves_room_for_two_full_rows_and_the_top_of_a_third` does the arithmetic (596 − tab row − 6 >= 541)
- [x] all 12 fixture raiders in the strip, two lines each — three columns of four (`W3R_Guildhall.png` y 780..960: Tiny, Spoof, Rhona, Greg / Pip, Cindy, Steve, Hal / Nev, Clive, Gruk, Bork); `test_the_strip_lists_twelve_in_two_lines_each`, `test_past_twelve_the_strip_says_how_many_more_and_where_they_stand`
- [x] a scrollbar visible when rows exceed the fold — the 8px bronze grabber at x 1118..1125, y 148..~560 over the SCROLL_TRACK (measured on `W3R_Guildhall.png`); `test_the_grid_scrolls_in_a_container_whose_bar_sits_in_the_margin`
- [x] one lit tab with an underline; no reason text past its tab's column — `W3R_crop_tabs_band.png`: Roster on the plate + gold underline, Raid Group's caption from x 343 under its own tab, Records' from x 715, both inside their cells, the rule under the row; Facilities lit on `--press=Facilities`, Records on the clear fixture; `test_the_tabs_are_the_kits_row_with_each_reason_under_its_own_tab`, `test_pressing_a_tab_rebuilds_the_row_with_that_tab_lit`
- [x] the stamp legible in greyscale — `W3R_crop_stamp_grey.png` (cvd.py render, region 230,148,215,262): "AT RISK" in its rimmed box across Tiny's portrait reads in greyscale; `test_an_at_risk_raider_wears_a_stamp_box_across_the_portrait`
- [x] lit chips with the filter word — Morale / All lit (gold rim + gold text) on every fixture shot; `test_the_filter_word_is_printed_only_while_a_filter_is_on` presses At risk and reads "Showing 0 of 12 · At risk", then All → "Showing 12 of 12"; `test_sort_and_filter_chips_are_lit_by_their_pressed_state`
- [x] morale-band histogram well under the sidebar summary — ten bars, 1·1·8·1·1 counted above them on the fixture (`W3R_Guildhall.png` y 468..524); `test_the_histogram_counts_every_raider_once_per_band`
- [x] iconed record rows — padlock (dimmed) on locked rows, the gem on the earned "First Blood" (`W3R_Guildhall_Records.png`); `test_every_record_row_leads_with_a_state_icon`
- [x] a lit picker tile on Facilities — Bork's tile in a 2px gold rim (`W3R_crop_facilities.png`); `test_the_facilities_picker_lights_exactly_the_chosen_tile`
- [x] empty-state glyphs — the bench over "No raiders yet…" and "Nobody on the books…", the quill over the log (`W3R_Guildhall_empty.png`), the shelf on an empty filter; `test_empty_states_carry_the_kits_glyph_and_their_sentence`
- [x] no `sun.png` in the three files (HALL-19) — `grep -n sun.png` over Guildhall/Roster/Facilities is empty (21:58)
- [x] docs/13 OQ-4 record updated (CRITIC-C15) — docs/13-ui-ux.md:863, the "Placement recorded 2026-09-14" sentence in the Proposed-default cell; the link test's prose form (backticked path), no anchor
- [x] `test_roster_layout.gd` (new, 15 tests) passes — suite #4 and the final `verify.sh --fast` (00:40)
- [x] `test_raider_detail.gd:302-333`, `test_starting_roster.gd`, `test_rest.gd`, `test_comfort.gd`, `test_screens.gd:634-810`, `:856-931` green — every run of the suite; 1908/1908 at the end
- [x] shots viewed (all in build/shots/, each described in the log): `W3R_Guildhall.png` (fixture), `W3R_Guildhall_Facilities.png` (`--press=Facilities`), `W3R_Guildhall_Records.png` (`--fixture=raid:clear --press=Records` — the plain fixture's Records tab is shut at Unknown, shot.gd exit 12), `W3R_Guildhall_emoji.png`, `W3R_Guildhall_text150.png`, `W3R_Guildhall_empty.png`, plus `W3R_Guildhall_focus.png` (`--focus --tab=8`), the crops `W3R_crop_tabs_band.png`, `W3R_crop_picker.png`, `W3R_crop_facilities.png`, `W3R_crop_stamp_grey.png`, and the before shot `W3R_before_Guildhall.png`
- [x] refdiff Guildhall vs 3 masked recorded before/after — log 23:32
- [x] `verify.sh --fast` green (00:40): `PASS LINT OK` · `PASS MOTION LINT OK` · `PASS PARSE_CHECK scanned 173 script(s)` · `PASS 16 generated file(s) agree` · `PASS ART CHECK generators=7 runtime files: 197 agree 0 DIFFERS 0 MISSING` · `PASS TESTS PASSED 1908 test(s) in 92 file(s) [222021 ms]` · stages 4-7 SKIP (--fast) · `VERIFY OK`. `tools/lint_motion.sh`: MOTION LINT OK. `a11y_smoke.gd`: A11Y SMOKE PASSED 26 screen mount(s) (00:05 run).

## Judgement calls (recorded as made)

1. Sort/Filter chips and the "Showing N of N · filter" line live in the SIDEBAR, not the panel's title bar (docs/13 §9.1's wireframe and spec 10 R2 put them above the list). Arithmetic: the panel's inner height is 596; HALL-03's acceptance (two full rows + the third's top at pitch 270) needs >= 541 of grid; the tab row with its captions and rule is 45 and the column gap 6 → 545. A toolbar line above the grid (28 + gap) makes two full rows impossible whatever else moves. The sidebar was half empty (HALL-09) and the filter state belongs beside the filter chips (docs/13 §8.5 "always visible as text"). The design wins over the wireframe (memory rule 2026-09-10); the chips are still `ButtonChip` toggles, the words still Labels.
2. Tab captions at `Type.STACK` (11px) on ONE line at 100%, wrapped at their 100% width above that. HALL-10 asks for "a 12px caption"; Type has 11 and 13. At 13px the two captions ("Chalk tonight's twelve…" 300px, "There is nothing to record…" 455px) either overflow the 888 row on one line or wrap to two lines, and two lines cost the grid the second row's bottom (525 < 532). At 11px one line each fits (110+248+110+375+12 = 855) and the grid keeps 545. The tab Button stays 110x28 at its cell's left (`SIZE_SHRINK_BEGIN`); the caption sizes the cell, so "no reason text past its tab's column" holds by construction. Reason texts verbatim.
3. The strip's two-second read is THREE columns of FOUR, not two of six: the plan's "pitch 36" cannot hold `LabelMorale` (20px, ~26 line) over `LabelClass` (15px, ~18 line), and the panel's pad leaves 234, not 262. Twelve entries either way (the raid size), the card's own type, pitch 46, a trailing "+N more, all above X" Label past twelve.
4. The scroll host is widened by the bar's 8px into the panel's margin (`Roster.SCROLLBAR_W`): a ScrollContainer reserves its bar's width from the content, and four cards at the reference pitch are 887 of the 888 — the fourth card's rim would have been under the bar.
5. HALL-22's "gold rim" for the chosen picker tile is the kit's `ButtonMini` PRESSED state (steel rim, lit plate — the same selection chrome as `PanelRoundSelected` on the Tavern's card and the focus ring): the plan names "ButtonMini pressed StyleBox from W1-CHROME", and one selection colour across the kit beats a second. The unselected tiles are no longer dimmed to 0.55 (every face at full strength; the pressed plate carries the selection).
6. Record-row icons (HALL-20) go through `Icons.at` from a three-row table in Guildhall.gd (locked → lock_16 dimmed, earned → log_loot, claimed → log_gold) because `Cards.badge_icon`'s record states are W3-KIT2's this wave (rule 7); the switch-over is handoff-W3-ROSTER §1.
7. "Manage" with no RaiderDetail scene (a bare mount without a router) is now disabled WITH a reason ("Not built in this version yet.") instead of silently — docs/13 §7.

## Left undone / notes for later units

- Rhona's "Shaman — Slightly Annoy…" on the card (100%): W3-KIT2's `OVERRUN_TRIM_ELLIPSIS` on the card's band line trims a state word docs/13 §8.1 says is never abbreviated — handoff observation; Cards.gd is not this unit's.
- The kit's card is ~12px too tall for a disabled action's reason inside 262: `Roster._fit_band` tightens the returned card (column separation 2, reason box 0, reason at Type.STACK) as the interim; the kit-side fit is the handoff's second observation, and `test_the_panel_leaves_room…` measures every card's minimum against 262 so a taller kit card is a red test, not a cut rim.
- `Widgets.TAB_MIN` (KIT2, in flight) is 110x32; this screen's tabs are 110x28 by `custom_minimum_size` after `tabs_of()` — the 4px is the difference between the third row's top showing and not. If the kit wants 32 everywhere, the row needs the panel 8px taller (PANEL_AT.y 8 → 4, PANEL_SIZE.y 624 → 632) to keep the acceptance.
- The Raid Group tab stays shut with its reason (HALL-21 is Q18's).
- The hall level card stays on the Facilities sidebar (HALL-09's second half waits on HALL-18's art, Q18); the Facilities sidebar got the level ladder instead so it is not half empty.
- `RECORD_FILTERS` (S14's type filter) — the constant that was never built — is now the Records sidebar's chip row and filters the wall; the heading still counts the whole wall ("Records  N of 40").
- shot_all's `Guildhall_Records` row will keep SKIPPING on `--fixture` (Records is shut at Unknown; shot.gd exit 12). The wall was shot on `--fixture=raid:clear --press=Records` (the clear promotes the guild to Known); if the sheet wants the wall, that row's fixture is `raid:clear`.
- `build/plan/report-W3-ROSTER.md` and `handoff-W3-ROSTER.md` were deleted from disk by something else at ~22:2x (the report came back holding only lines appended after 21:55 — `>>` recreated it; the handoff was gone). Rewritten in full at 22:31; nothing of this unit's was lost since the log was in context.
- `tools/with_godot_lock.sh` broke a "1789445756s old" lock under this unit's probe run (the stale check treats a lost `stat` race as age 0) — handoff §2.

## Log

- 21:05 read the plan §0/§4/§6, RULES, the cited findings, LESSONS, the W2-MARKET/W2-TAVERN handoffs and reports, the three owned files and the kit at HEAD. Report + handoff created.
- 21:55 baseline: `build/shots/W3R_before_Guildhall.png` shot and viewed — actions under the cards ("Cheer up / Manage" y 478..508, "They are fine." y 512), the two tab reasons as one sentence across the panel (y 148, running to x 1136 past the panel's inner edge at 1118: the row's uncapped width had widened the whole column), four identical tab buttons, chips lit by font colour, the small "AT RISK" caption on the portrait, no scrollbar with row 2 cut at y 700, the strip's seven single-line rows (Hal, Bork, Gruk, Rhona missing) and its lower half empty, the sidebar empty from y 460 to 640. refdiff vs 3 masked (0,77,1536,649) BEFORE: mae 33.606 · rmse 67.442 · structure 0.1634 · iou 0.0867 · palette 0.3879 · within-8 57.02%.
- 22:20 Roster.gd rewritten (cards with `Cards.card(actions)`, `stamp_box`, chips in the sidebar as `ButtonChip`, the filter-state line, the histogram well, empty glyphs, the scroll host widened into the margin, focus note/regrab); Guildhall.gd rewritten (tab_row with captions shaped under their tabs, focus region regrab, the strip as 3x4 two-line entries, record icons, sidebar scroll, event_log hint + glyph); Facilities.gd (lit picker tile via the toggle's pressed state, empty glyph). PARSE_CHECK OK (170). docs/13 OQ-4 row carries the placement record. `tests/unit/test_roster_layout.gd` written (14 tests).
- 22:25 first fixture shot attempt hit a transient "Could not resolve script Cards.gd" — W3-KIT2 mid-edit; retrying.
- 22:30 found both plan files deleted; rewritten.
- 22:45 second fixture shot (`W3R_Guildhall.png`, viewed): actions inside every card, "They are fine." inside the rim under a shut Cheer up after `_fit_band` (the first cut had it clipped at the rim — the kit's band is ~12px too tall for a 262 card with a reason; handoff observation), the third row's top rims at y 688..695, the 8px bronze grabber at x 1118..1125 (measured (135,107,89) at y 200/400, track (28,38,49) at y 600/690), the tab row: Roster lit on the plate + gold underline, both captions under their own tabs (RG caption x 343.., Records caption x 715..1108), the sidebar: Rest, Roster facts, histogram (1·1·8·1·1), Sort with Morale lit, Filter with All lit, Back to town at y 667. Strip: 3x4, all twelve in two lines. Defect seen, not mine: Rhona's "Shaman — Slightly Annoy…" trimmed with an ellipsis on the card — KIT2's `text_overrun_behavior` on the band line at 100% (docs/13 §8.1 never abbreviates the state word); handoff observation added.
- 22:50 `W3R_Guildhall_Facilities.png` viewed: Facilities lit, picker's chosen tile with a 1px steel rim only (crop `W3R_crop_picker.png`) — too faint at 1x → the chosen tile now takes a 2px EDGE_READY_GOLD rim (judgement 5 revised: gold, the token the kit's chips press with). The Facilities sidebar had ~240px of empty run → the hall's ladder (`_ladder`, every rung with its cost) fills it.
- 22:55 `W3R_Guildhall_Records.png` (on `--fixture=raid:clear`, seed 25 — the plain fixture is Unknown and the tab is shut, which shot.gd reports as exit 12 and shot_all skips): rows lead with the padlock (dimmed) / the gem (Earned "First Blood" with Claim); "Records  3 of 40"; the sidebar was EMPTY → now carries the tallies, the coin-cap line and S14's type filter chips (`RECORD_FILTERS`, finally built) which filter the wall.
- 23:00 `W3R_Guildhall_text150.png` viewed: captions wrap to two lines inside their cells; the cards' bands were cut at the rim at 150 → rows now grow to the tallest card's minimum at that scale (`row_height()`; 262 at 100%); the strip's two-line entries overlapped at 150 → the pitch and the row count now come from the theme's line heights (fewer rows + "+N more" at 150).
- 23:05 `W3R_Guildhall_empty.png` viewed: the grid's empty state sat at the panel's bottom and the strip's did not show at all — probed (`w3r_probe.gd`, headless): an autowrapped Label placed by hand reports a 465px minimum (one glyph per line at width 0) → both empty states now sit in a MarginContainer that hands them their rect.
- 23:15 the lock script broke a "1789445756s old" lock during the probe run — `stat` lost a race and the age fell back to 0; handoff §2 is the fix (retry, never break, when stat fails).
- 23:25 re-shot all six (batch under one lock; PARSE_CHECK OK 173). Viewed: `W3R_Guildhall_Records.png` — the sidebar now carries "3 of 40 earned · 0 claimed", "Paid out 0 G of the 0 G the board may pay" (15% of a 4 G lifetime income floors to 0 — honest), the Show chips (All lit); `W3R_Guildhall_Facilities.png` + crop `W3R_crop_facilities.png` — Bork's tile in a 2px gold rim, the ladder's four rungs under "Next", the empty run under the sidebar now ~90px; `W3R_Guildhall_emoji.png` — pips in every card, the strip and the quarters line, no glyph anywhere; `W3R_Guildhall_empty.png` — "No raiders yet…" under the bench glyph in the panel's upper middle, "Nobody on the books…" under the bench in the strip, the histogram's ten faint stubs, Rest disabled with "There is nobody to rest."; `W3R_Guildhall_text150.png` — rows grew to hold the band (the first row ~300 tall), captions on two lines in their cells; the strip's overlap at 150 was shot before the pitch fix landed — re-shot below.
- 23:30 CVD: `tools/art/cvd.py build/shots/W3R_Guildhall.png --out build/diff/W3R_cvd --region 230,148,215,262`; `W3R_crop_stamp_grey.png` (colour beside greyscale, 2x) viewed — "AT RISK" reads as a light word in a rimmed box over the dark portrait in greyscale; the morale number's red is gone in greyscale and the word carries it (docs/13 §8.4's point).
- 23:32 refdiff Guildhall vs 3, mask 0,77,1536,649 — BEFORE mae 33.606 · rmse 67.442 · structure 0.1634 · iou 0.0867 · palette 0.3879 · within-8 57.02% → AFTER mae 34.65 · rmse 68.206 · structure 0.1615 · iou 0.0841 · palette 0.358 · within-8 55.06%. Palette improves; mae +1.0. Per-region mae vs the concept (PIL): header 93.6→93.6, rail 18.7→18.7, scene 59.8→59.9, strip-log 19.2→19.5, sidebar 38.6→40.4, strip-left 19.1→21.2 — the whole move is the ink the findings asked for (twelve two-line entries where seven one-liners were; chips and a histogram where the column was empty) against a concept whose strip is four cards. Recorded, not chased.
- 23:45 `verify.sh --fast` #1: 0/5 PASS · LINT OK + MOTION LINT OK · PARSE_CHECK 173 · 16 generated agree · ART CHECK 197 agree 0 DIFFERS 0 MISSING · TESTS FAILED 6/1908: two mine (`test_the_panel_leaves_room…` measured the tab row at 63 — the pre-layout minimum of an autowrapped caption is its text wrapped at width 0, not the 45 the shot shows; `test_past_twelve…` — the "+1 more" Label sat 4px under the panel floor because a Label never goes under its minimum), one mine by the text-scale door (`test_every_screen_builds_its_theme_through_the_door`: `_caption_width` called `Theme_.get_theme()` bare — now the caption's own theme font at the unscaled size, shaped after the row is in the tree), three not mine (`test_options_layout` ×1 and `test_widgets_kit` ×2 — W3-OPTIONS / W3-KIT2 in flight). Fixed all three of mine; the test now sums the row from what the screen sets (tab, underline, one caption line at the theme's height, rule) and asserts each caption fits its cell on one line.
- 00:05 suite #2 (run_tests + a11y_smoke + three shots under one lock): TESTS FAILED 4/1908 — one mine (`test_the_panel_leaves_room…`: the test measured the caption with the node's default-typed font lookup (271px) while the screen measures the variation's font (251px), and summed the underline and the caption though they live in different cells; now the screen's own lookup and max-of-cells) and the same three of W3-OPTIONS/W3-KIT2. `A11Y SMOKE PASSED 26 screen mount(s)`: Guildhall empty owner='Camp' tab ring=3 rail=6 focusable=18 reachable=18 disabled=5; fixture focusable=42 reachable=42 disabled=5; no warn. `W3R_Guildhall_focus.png` (`--focus --tab=8`) viewed: the 2px steel ring on "Rest a day". `W3R_Guildhall_text150.png` re-viewed: the strip reflows to three rows (pitch from the theme's heights) — and the "+3 more, all above 45" line overlapped the third row's second line → the trailing line's height is now reserved before the rows are counted (two rows + "+6 more" at 150; twelve and no line at 100).
- 00:10 `tools/lint_motion.sh`: MOTION LINT OK.
- 00:25 suite #4 (after the strip's line heights come from the text font, not the face fallback — the fallback's 30px line had made four rows + the trailing line overflow the panel at 100%, so a 13-raider roster showed nine): `TESTS FAILED 1/1908` — W3-OPTIONS' `test_options_layout.gd` only. `W3R_Guildhall.png` and `W3R_Guildhall_text150.png` re-shot and viewed: the strip at pitch 46 / line 2 at +24 again at 100%; two rows + "+6 more, all above 45" on its own line at 150.
- 00:40 final `verify.sh --fast`: VERIFY OK, 1908/1908 (the OPTIONS file had gone green in the meantime). Done.
