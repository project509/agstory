# report-W6-SHEETS — sheets of a real guild, the tooltip sweep, the fresh-profile export and the provenance gate

Wave 6 of the ship plan (build/plan/ship/00-plan.md §1). Written as the work lands; ticked boxes are on disk.

## Acceptance

- [x] `tools/shot.gd` gains `--fixture=new` (GameState.new_game through the fixture seam) and `--fixture=play` (a legal Known save); `--fixture` byte-identical
- [x] `tools/fixture_reference.gd` gains `apply_new()` and `apply_play()` (rank Known at its threshold, feed rows from the clears, `level` 0 everywhere, two of each consumable, the raid behind the guild)
- [x] `tools/shot_all.sh` gains `new`, `play` (13 routes each) and `hover` (one `--hover=x,y` per tooltip site, coordinates pinned)
- [x] `test_w0_shot.gd` covers the two new fixtures
- [x] `test_widgets_kit.gd` gets ONE test at the end: a PopupPanel given `build_tooltip()`'s VBox reports `get_contents_minimum_size().y < 80`
- [x] `PROVENANCE.md` (new): one row per shipped asset family, `Signed:` blank
- [x] `tools/export_build.sh` step 0 holds on an unsigned PROVENANCE.md (`--dev` passes and stamps); step 7 boots with `--user-data-dir <tmp>`; step 7b boots with `tests/fixtures/saves/v10_sample.json` in the temp profile and greps the migration line
- [x] `test_export.gd` asserts the `--user-data-dir` flag, the 7b fixture line, and that step 0 holds when PROVENANCE.md has no signature
- [x] `export_build.sh --dev` run: NOT SHIPPABLE stamp names BOTH holds (pending files + provenance unsigned); a playable .exe exists under build/exports (not committed)
- [x] Shots: `build/shots/all/new/_sheet_1.png` (Day 1, the starters, no "Lv."), `play` Town (Known, six-row log), `hover` sheet (a two-line plate at every site) — viewed
- [x] `verify.sh --fast` run twice; this unit's files green in both, the red rows are other units' mid-wave edits — summary pasted below

## Log

- fixture_reference.gd: `apply_new()` = `new_game(GUILD_NAME)` (the name decides the seed, as the menu does); `apply_play()` = new guild + `add_gold(500)` + two of each SKU through `buy_consumable` + the ladder A0 -> TR -> A1 cleared through `record_attempt` (seed search per rung from SEED, `_record_clear` shared with `apply_clear`, behaviour unchanged). Judgement calls: A1 cannot be legally cleared with a tutorial unresolved (RaidPlan.locked_reason walks the ladder), so the play guild has THREE attempts, the Tutorial Raid being "the one raid behind the guild"; TR clears 1/20 today so its seed search is the long one (still < 1 s). The one SET number is the pre-ladder RP, derived at runtime (`play_rp_before(db)` = Known's threshold minus the three first-clear awards) so the A1 clear is the promotion and Known sits at exactly 120.
- shot.gd: `--fixture=new` / `--fixture=play` parsed beside `raid:clear`; prints `FIXTURE new: day 1, 60 G, roster 12` / `FIXTURE play: day 4, N G, Known, seeds A0=.. TR=.. A1=..`; exit 10 when a rung will not clear. `--fixture` untouched.
- test_w0_shot.gd: four new tests (new = Day 1/60 G/Unknown/level 0/A0 open/empty log; play = every rung cleared once, Known at rp_floor, lifetime counters agree, day 4, two of each SKU, >= 6 feed rows, newest row "Cleared ...", A2 open; deterministic incl. to_dict hash; the reference fixture pinned unchanged). `RUN_TESTS_ONLY=test_w0_shot`: TESTS PASSED 8 test(s).
- shot_all.sh: sheets `new` and `play` (every SCREENS row with `--fixture=new` / `--fixture=play`, the MainMenu included — on `play` the clears autosaved so the menu shows Continue), and `hover` with five pinned rows. `--sheet=new`: 13 ok; `--sheet=play`: 13 ok; `--sheet=hover`: 5 ok. Viewed `build/shots/all/new/_sheet_1.png` (Town: Day 1 · Unknown, 60 G, Roster 12 of 15, "Day 1. Nothing yet — the board is up the road.", cards with no "Lv."; Board: A0 open, "0 reputation · 120 more to Known") and `play/_sheet_1.png` (Town: Day 4 · Known, 120 RP, 160 G, six rows on the log headed "Cleared Adventure 1 — Encounter 1 — 4 G."; Board: A0/TR/A1 CLEARED, A2 next, "280 more to Respected"; RaidPrep: provisions 0/2 of each; RaidView/Results: the A1 clear replayed and reported with loot).
- The hover sweep (UI-49, one `--hover` per site, `hover/_sheet_1.png` viewed, HOVER/POPUP lines read): RaiderDetail 584,167 -> the kit's two-line plate, 180x56 (was 180x571). Market 290,350 (Sell window, `--fixture=play` because the reference fixture holds no loot) -> Godot's one-line plate 274x34 "Adventurer's Healing Focus — Main Hand". RaidView 176,908 (a party card's head cell) -> one-line plate 158x34 "Head — Worn Iron Cap". Tavern 237,787 -> an EMPTY "arrives with" cell, POPUP none: no candidate is dressed on any fixture — `Recruitment.gear_plan()` has no caller anywhere in the tree (another built-not-armed), so the Tavern's gear-cell tooltip cannot be shot until someone dresses recruits. AdventureBoard 1197,375 -> the reward slot, POPUP none: no tooltip on it. Only RaiderDetail's site uses `Widgets.tooltip_for`; the other three text sites are plain `tooltip_text`, so the "two-line plate at every site" half of the acceptance is three handoff edits (Market.gd, RaidView.gd, Tavern.gd) plus one described entry for the Board's reward cell — handoff-W6-SHEETS.md.
- While the sheets ran, another unit's RaidSim.gd was mid-edit (Parse Error: `_wipe_cause()` not found) and `RaidSim.run` aborted `_record_clear` with a 0 instead of -1, so one `play` pass rendered a Day-1 guild wearing the play flag. `apply_play` now checks the STATE (`has_cleared`) after every rung and refuses; the sheets were re-shot on a parseable tree.
- test_widgets_kit.gd: one test appended at the end, `test_the_tooltip_window_is_sized_off_a_laid_out_box` — a PopupPanel given `build_tooltip()`'s VBox reports `get_contents_minimum_size().y < 80` (handoff-W5-TOOLS §2's probe shape: 571 before the pre-size loop, 56 after) and a width within TOOLTIP_W plus the panel margins. W5-KIT3's file otherwise untouched (appended after its last test; 4-space file).
- PROVENANCE.md: eight rows, one per family — the six bare plates (designer's own, spec 09), the reference concepts + asset sheets (two carry "ChatGPT Image" in the filename — the question is written on the row), the sliced/re-drawn sprites, the generated UI/icons/VFX/wordmark, the two OFL fonts, the generated audio, the nine screenshots + the item corpus (Q-21 verbatim), and names/strings/copy (docs/00 §4.4.1 categories 1, 2, 6). Every `- Signed:` blank. The file says how to sign and that the loop never may.
- export_build.sh: step 0/8 counts `^- Signed:` rows vs signed rows, HOLDs and lists the unsigned headings, `--dev` SKIPs; step 7/8 boots from an EMPTY profile — `fresh_profile()` (mktemp) + `launch_from` with `APPDATA`/`LOCALAPPDATA` (Windows) and `XDG_*` (Linux) pointed at it — and asserts the engine created `Godot/app_userdata/A Guild Story` under the temp root and wrote no saves/; step 7b/8 seeds a second fresh profile with `tests/fixtures/saves/v10_sample.json` as `saves/slot_0.json`, boots, greps the errors, and asserts the file is byte-identical and no `.bak` appeared (a boot must not migrate — migration is Continue's, `SaveGame.load_into`). The final stamp names every hold. Judgement call recorded in the script header: Godot 4.7.1 has NO `--user-data-dir` flag (`--help` lists none), and PROBED on the exported build: `AGuildStory.console.exe --headless --user-data-dir <tmp>/x` runs with rc 0, prints nothing, and still writes to `%APPDATA%/Godot/app_userdata/A Guild Story` — the flag SHIP-05 named would have been a silent no-op that looked like a fresh profile. The env-var redirect is the mechanism the engine actually honours, and the step asserts the wire. There is also no "migration line" to grep: nothing in the game prints at boot about a save, and the menu only reads the header; 7b asserts what a boot CAN prove and the report says so.
- test_export.gd: four tests — the manifest exists with one Signed: line per heading and names the plate/font/audio/ideaboard families; step 0's hold + stamp couplings, and the manifest IS unsigned in the tree (the assertion that goes red the day a person signs, to be deleted then); the fresh-profile mechanism (`fresh_profile()`, `APPDATA="$win_profile"`, `XDG_DATA_HOME`, the app_userdata assertion, and the script naming `--user-data-dir` as absent); step 7b's fixture line (`cp "$V10_FIXTURE" ".../saves/slot_0.json"`), the fixture parsing as v10, `cmp -s` and the `.bak` check. `RUN_TESTS_ONLY=test_export,test_widgets_kit`: TESTS PASSED 53 test(s).
- `./tools/export_build.sh --dev --skip-gates` (the gate stage is verify.sh, run separately below; the orchestrator's full verify is the close): 0/8 HOLD 8 of 8 unsigned (listed) + SKIP; 1/8 HOLD 16 files name_pending + SKIP; 2/8 PASS; 4/8 PASS; 5/8 PASS Windows Desktop; 6/8 five PASS; 7/8 PASS "boots clean for 180 frames from the packed build, first-run profile at /tmp/aguildstory-profile.SRyW43"; 7b/8 PASS "boots clean with v10_sample.json as slot 0; the file is untouched"; 8/8 AGuildStory.exe 105M, .pck 21M, console 88K; `DEV EXPORT OK  NOT SHIPPABLE: 16 file(s) hold unresolved content; provenance unsigned (8 of 8 rows in PROVENANCE.md)`. Nothing under build/exports is committed (gitignored). Log: build/shots/w6sheets/export_dev_skipgates.log.
- handoff-W6-SHEETS.md: §1-§4 exact edits (Market.gd x2, RaidView.gd, Tavern.gd) converting the plain `tooltip_text` sites to `Widgets.tooltip_for` (dry-run: 4 APPLIED); §5 the Board reward cell, described (W6-COPY rewrites the block); §6 the undressed-recruits finding. No docs/15 row needed (no Q- row governs this unit; Q-21's gate is the row's own recommended default — the designer signs).

## Verify

`GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` (first run, mid-wave):

```
== 0/8  lint == PASS LINT OK · PASS MOTION LINT OK
== 1/8  script parse == PASS PARSE_CHECK scanned 188 script(s) · PASS COPY LINT OK
== 2/8  generated content == PASS 16 generated file(s) agree · 2b/8 PASS ART CHECK 204 agree
== 3/8  unit tests == TESTS FAILED   13/2126 failing  [42267 ms]
VERIFY FAILED
```

Every one of the 13 is in another unit's file, mid-edit, and names that unit's own change: `test_a11y_legibility.gd` x3 (the event-log sign table — W6-COPY's UI-51), `test_docs_links.gd` (`test_screens.gd:672` cites Q-21 — W6-COPY's test edit), `test_paper_doll.gd` x4 and `test_raider_detail.gd` x2 (RaiderDetail's copy — W6-COPY), `test_rest.gd` x2 (the rest sentence), `test_roster_layout.gd` x2 and `test_screens.gd` (the Blacksmith callout, the tabs — W6-COPY), `test_text_scale_layout.gd` x12 ("Clive Skullmender" from the new name pool, "1 enemy", the Completion roll — W6-COPY/W6-LEDGER). None reads a file this unit owns; this unit's three test files (`test_w0_shot`, `test_export`, `test_widgets_kit`) are green inside the same run and alone (`RUN_TESTS_ONLY`: 8 + 53 tests). No game/ or sim/ file touched, so `lint_motion.sh` was not owed (it passed inside the gate anyway). Second run, ~10 minutes later (the others' edits settling): `TESTS FAILED   9/2126 failing  [44259 ms]` — `test_text_scale_layout.gd` x6, `test_paper_doll.gd` x4, `test_rest.gd` x2, `test_a11y_legibility.gd` x3, `test_build_state.gd` x1 (`test_every_path_the_file_names_exists` — BUILD_STATE.md is W6-LEDGER's and is mid-edit). Still none in a file this unit owns; `test_w0_shot`, `test_export`, `test_widgets_kit` green. Logs: build/shots/w6sheets/verify_fast.log, verify_fast2.log.

## Audit closures

No audit row closes outright — the findings this unit carries (LOOP-01, UI-15, UI-03b's fixture half, UI-39's fixture half, UI-49 + CRITIC-R1, CRITIC-C11, LOOP-31, SHIP-05, SHIP-17) map onto rows that are already `done` (M6-PLAY-01, m4t-08, M6-EXP-02) or onto the designer's own signature. Notes for the orchestrator's re-diff:

- **M6-EXP-02** (done): extended, not reopened — `tools/export_build.sh` now has step 0 (provenance hold), a fresh-profile step 7 and step 7b (v10 save in the profile); `tests/unit/test_export.gd` pins all three. Held by `test_step_zero_holds_while_the_manifest_is_unsigned_and_dev_passes`, `test_the_launch_check_boots_from_an_empty_profile`, `test_step_7b_boots_with_the_oldest_migratable_fixture_in_the_profile`.
- **M5-END-4 / Q-21 / C-32** (blocked-needs-human): the gate the row's remaining_work asks for now exists and is visibly red — `PROVENANCE.md` row 7 is Q-21 verbatim, and `export_build.sh` step 0 holds on it; the human half (the signature, the credits `lines`, the copyright holder) is unchanged and is what §6 row 4 of the ship plan waits on. Held by `test_the_provenance_manifest_exists_with_a_signature_line_per_family`.
- **M5-OQ-1** (partial): its item (1) — "needs the designer's written confirmation of authorship" — now has a place to be written (PROVENANCE.md row 7's `Signed:`) and a gate that reads it. The row stays partial on its other items.
- **M6-FINAL-05** (not-started): the "fresh profile" half of W10-WALK is now what the launch check does on every export on this machine; the second-machine walk remains that row's.
- **m4t-04** (not-started, UI-39): the fixture half ("the guild has two of each consumable so the chalkboard is shot with stock") is `--fixture=play` (`play/RaidPrep.png` shows the provisions at 0/2 each); the two icon sites (RaidPrep `_provision_button`, Results' rally row) are not this unit's and stay open.
- **UI-49 / CRITIC-R1** (no audit row; KIT-22 done): the assertion lives in `test_widgets_kit.gd` (`test_the_tooltip_window_is_sized_off_a_laid_out_box`); the sweep is the `hover` sheet plus handoff §1-§5.

## Left

- **The "two-line plate at every site" half of the hover acceptance** is three handoff edits, not a shot: Market (x2), RaidView and Tavern set plain `tooltip_text` and those files are other units' this wave. The sheet shows Godot's one-line plate on Market/RaidView, an empty cell on the Tavern and no plate on the Board; handoff-W6-SHEETS §1-§5 is the fix, and the Tavern row cannot light up until recruits are dressed (§6: `Recruitment.gear_plan()` has no caller — a finding outside this unit's files).
- **`--user-data-dir`** as the acceptance names it does not exist in Godot 4.7.1 and is silently ignored by the exported build (probed: rc 0, no message, user data still under %APPDATA%). The fresh profile is the engine's data-path variables redirected to a temp root, with the app_userdata assertion so a redirect nothing honoured is a FAIL; `test_export.gd` asserts that mechanism and that the script names the missing flag.
- **The "migration line" for step 7b** does not exist: nothing prints at boot about a save and the menu reads only the header; migration runs on Continue. 7b asserts what a boot can prove (no error with a v10 save present; the file byte-identical; no `.bak`). A boot-time header print or a `--load-latest` argument would be a game/ change (Boot.gd/MainMenu.gd) for a later owner if the packed-chain proof is wanted on the .exe itself; the chain is proven in-tree by `test_savegame.gd`.
- **`export_build.sh --dev` was run with `--skip-gates`** (stage 3 is the full ten-stage verify, which is the orchestrator's close and was red mid-wave on other units' edits); every other step ran and the .exe exists. The stamp names both holds as the acceptance asks.
- **The `new` sheet's RaidPrep/RaidView/Results tiles** are the screens' empty/default states (RaidPrep defaults to E1 with no selected encounter; RaidView/Results print "No account was written") — honest routes a Day-1 player reaches only through the Board; noted so a reviewer does not read the RaidPrep tile as a Day-1 mission.
- **"six Commons"** in the plan's shot line is W6-LEDGER's StartingRoster change; today `new_game()` hands twelve, and the `new` sheet shows twelve. The fixture calls the seam; whatever the roster becomes is what the sheet shows.
- **The `play` guild's pre-ladder RP** (80, derived) is the one number the fixture sets rather than earns; recorded in `fixture_reference.gd` beside `play_rp_before()`.
