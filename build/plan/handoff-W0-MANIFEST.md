# Handoff — W0-MANIFEST (key `W0-MANIFEST`)

Everything below is outside my file ownership (`art/ref/manifests/all.json`,
`game/assets/enemies/enemies.json`, `tools/art/place_enemies.py`, `tests/unit/test_art_sources.gd`).
Each edit is `## N. <path>:<line>` with an `old:` and a `new:` block. Nothing here was applied.

(none — the unit finished 2026-09-14 with no edit outside its ownership; wave 0 expected none)
