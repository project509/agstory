# report-W3-OPTIONS — Load/Save and Settings finish

Unit: wave 3, 00-plan.md §4 "W3-OPTIONS". Findings: HALL-15, HALL-16, HALL-17, HALL-25, CRITIC-G05, CRITIC-G06, CRITIC-G16, KIT-03 (verify), RULES-02 (verify).
Owned: game/screens/LoadSave.gd, game/screens/Settings.gd, tests/unit/test_options_layout.gd (new).
Handoff: build/plan/handoff-W3-OPTIONS.md (1 Theme.gd edit + notes for the kit).
Shots: build/shots/w3opt/ (before_*, LoadSave_fixture/empty/raid/confirm, Settings_fixture/text150/focus6/focus4); diffs in build/diff/w3opt/.

## Acceptance

- [x] LoadSave shot: three rows with identical button geometry (nine Buttons at one width — 168x36; 200 does not fit HALL-02's 590 panel, see judgement 1)
- [x] LoadSave shot: each empty-slot phrase once ("Slot 2 is empty." once per row, ONE reason Label per phrase adjacent to both disabled buttons)
- [x] LoadSave shot: no ISO-8601 string ("Written 14 Sep, 22:37" local; "Save format vN" on the row's tooltip, on the meta line only when the version is the reason)
- [x] LoadSave: `_confirm_pair` → `Widgets.confirm_pair`; "Yes — overwrite Slot 1"/"Keep it"/"Yes — delete Slot 1" texts kept (LoadSave_confirm.png)
- [x] LoadSave: `Widgets.reasoned` for every disabled row/button (CRITIC-G06) — dim, padlock, CAUTION reason
- [x] LoadSave: panel ends at content (HALL-16), one rim line (panel y 77..≈568 on the fixture, camp under it)
- [x] Settings shot: On rows visibly lit (ButtonChip pressed state for engaged values: On, or value != default)
- [x] Settings shot: every row the same pitch (row min-height 40 → 41px pitch measured on the shot; 60 at 150%)
- [x] Settings shot: volume as a pip strip beside the number (five `Widgets.bar` pips for the six-step ladder's audible steps; the number stays Button.text)
- [x] Settings shot: the panel ending <= 60px under the last row (≈33px), water shimmer visible in the freed band (y 880..1005)
- [x] Settings: `reasoned` for every disabled row and sidebar button (CRITIC-G06)
- [x] Settings: Apply demoted to the secondary-lit variation (CRITIC-G16) — `APPLY_VARIATION = "ButtonSecondaryLit"`, Theme.gd registration in the handoff; plain Button until then
- [x] KIT-03 verified: no `sun.png` / `_chips()` in either file; both call `Frame.standard_chips(`
- [x] RULES-02 verified: test_text_scale's Apply/Restore re-mount tests green in every run; `Settings --fixture --set=text_scale=150` shot viewed (rows scale, the table scrolls, nothing clips after the name-wrap fix)
- [x] `tests/unit/test_options_layout.gd` green: 14 tests, all green in run 4 (runs 1-3 had one red on the row's second reason Label being auto-renamed by the engine — fixed by naming it "Reason2" in `_slot_row`)
- [x] `test_loadsave_screen.gd`, `test_settings.gd` green unchanged; `test_a11y.gd` green (runs 1 and 2)
- [x] Shots viewed: `LoadSave --fixture`, `LoadSave` (empty), `Settings --fixture`, `Settings --fixture --set=text_scale=150`, `Settings --fixture --focus --tab=6` (+ `--tab=4`, `LoadSave --fixture=raid`, `--press="Delete Slot 1"`)
- [x] refdiff before/after recorded (LoadSave vs 3 masked, Settings vs 3 masked; chrome-only reading too)
- [x] `verify.sh --fast` green — run 4 (00:44): VERIFY OK, TESTS PASSED 1908 test(s) in 92 file(s); `lint_motion.sh` MOTION LINT OK

## Log (write-as-you-go)

- (The file lost its head — everything above the "Settings.gd patched" entry — between two appends around 22:10; the tree lives in OneDrive. Rebuilt from the session at 00:10 with the same content.)
- Read: 00-plan §0/§4/W3-OPTIONS, RULES §1-§5, HALL-15/16/17/25, CRITIC-G05/G06/G16, KIT-03, RULES-02, LESSONS, report-W1-HALL (LoadSave/Settings geometry: panel at the origin, PANEL_W 590 → inner 526px, BAND_H 100), Widgets.gd 585-710/750-900 (button/reasoned/confirm_pair/button_of/panel/bar), Theme.gd 186-446 (ButtonChip pressed = gold rim + ACCENT_GOLD text; no secondary-lit variation exists), Bar.gd, Icons.gd (`Icons.at("lock", "16")` resolves grid/lock_16.png — RaidView uses it), test_loadsave_screen/test_settings/test_text_scale/test_hall_plates (constants read: LoadSave.CAMP_OFFSET, PANEL_W; Settings.ROWS, STAGE_DEFAULT, VOLUME_STEPS, _next_volume, stage_for; no sun.png; `Frame.standard_chips(` present).
- Before shots (viewed): `build/shots/w3opt/before_LoadSave.png` — three empty rows, Load/Save/Delete at 148/108/148px, "Slot N is empty." printed twice per row (under Load and under Delete), the panel runs to the window's bottom with ~550px empty under row 3. `before_Settings.png` — 17 rows at 36..40px pitch (wrapped notes push their rows), identical chrome on On/Off/1x/Keep/100%/100/80, rows end y≈780 with the panel to y≈905 (125px empty), Apply crimson in the sidebar.
- Refdiff BEFORE (vs concept 3, mask 0,77,1536,649): LoadSave mae 33.646 rmse 65.145 structure 0.1937 iou 0.0779 palette 0.2649 within_8 51.22; Settings mae 36.107 rmse 67.007 structure 0.1935 iou 0.0857 palette 0.2896 within_8 43.28.
- Judgement calls (plan leaves the choice): (1) nine Buttons at ONE width, but 200x36 cannot fit — W1-HALL narrowed the panel to 590 (inner 526) for the fire-ring band and test_hall_plates pins PANEL_W < 928 — so BTN_W = 168 (3x168 + 2x10 = 524). (2) The confirm pair replaces the row's whole button row while it asks (a plate wider than one button cannot sit beside two fixed ones); "Yes — …"/"Keep it" texts kept; Keep it → `_refresh()` restores the three. (3) `reasoned` is the one door for the dim/padlock/CAUTION Label; each row moves the kit's own "Reason" Label under the button row and prints each phrase ONCE (HALL-15), Settings moves it into the description column so every row keeps ROW_H 40 (a reason under a 36px control would make the four disabled rows 56px and the 17-row table would no longer fit the panel). (4) Panels are sized by arithmetic — fixed row heights + measured non-wrapping line heights — because an autowrapped Label reports its minimum height at width 0 before layout (LESSONS: the two-pass wrap) and tests never run a layout frame. (5) Volume pips: one pip per audible step of the six-step ladder (5 pips; 0 = an empty strip), each a `Widgets.bar(1|0, 1, "mana")` 12x10 — the kit's bar, no new pixels. (6) Apply names `"ButtonSecondaryLit"` (falls back to Button until the Theme.gd handoff lands) at 48px, no longer the crimson CTA (CRITIC-G16). (7) An empty row keeps a blank meta line where a full row prints "Written …", so the buttons sit at one height in every row.
- LoadSave.gd rewritten (tabs): BTN_W 168 / BTN_H 36 on all nine Buttons (`_row_button` → `Widgets.reasoned(b, reason, Icons.at("lock","16"))`, the "Reason" Label taken out of the box and printed once per phrase in the row's `SlotReasons` row); `_confirm` → `Widgets.confirm_pair(yes, "Keep it", act, keep)` standing in for the whole button row; `written_words(created_at)` → "Written 12 Sep, 18:37" local (static, MONTHS table; empty/unparsable → ""), "Save format vN" on the SlotLine tooltip of a readable row and on the meta line only when the row is blocked by its version; `_fit_panel()` sums `_panel_chrome` (measured, nothing wraps in the header — the helper sentence is broken with an explicit newline) + rows' `custom_minimum_size.y` (each `2*ROW_PAD + _line_body + 3*ROW_GAP + _line_small*2 + buttons_h`, `_line_h` read from a probe Label under the themed screen) + a 2-line notice reserve + PANEL_SLACK 6, capped at the host; `panel_fit()` seam for the test. `_wrap_reason`/REASON_W gone. Parse-check: LoadSave.gd clean (the one PARSE FAIL at that moment was RaidView.gd:836, W3-RAIDVIEW2's file mid-edit).
- Settings.gd patched (tabs, `build/shots/w3opt/patch_settings.py`): ROW_H 40 via `Type.at(ROW_H, scale)` on every `Row_<key>` HBox; `_chip(text, engaged)` = ButtonChip toggle (`button_pressed` = engaged: On, or value != DEFAULTS[key]) with a local `hover_pressed` override read off the theme; `_volume_control` = `Volume_<key>` cell [Pips: five `Widgets.bar` 12x10 "mana", lit up to the level][chip "%d"]; `_option_row` wraps the control in `Widgets.reasoned(ctrl, reason, lock_16)` and moves the "Reason" Label into the description column; `_fit_panel` = measured chrome + Σ row heights + PANEL_SLACK, capped at host - BAND_H; sidebar's Save/Load, Restore defaults, Apply through `_reasoned`; Apply = `Widgets.button("Apply")` with `APPLY_VARIATION = "ButtonSecondaryLit"` at 48px; `panel_fit()` seam. `button_with_reason`/`wax_button`/TEXT_SLATE gone from the file. PARSE_CHECK OK (170 scripts) at that moment.
- Handoff written: Theme.gd — `hover_pressed` + `font_hover_pressed_color` on ButtonChip, and `ButtonSecondaryLit` through `_button()`; `apply_handoff.py --dry-run` parses it (1 edit, would apply). Notes for the kit (reasoned placement/width, pips) appended, nothing to apply.
- tests/unit/test_options_layout.gd written (spaces): 14 tests — nine Buttons at (BTN_W, BTN_H) and the row fits the panel's inner width; one "Reason" Label per empty row with both buttons disabled/dimmed/padlocked and Save enabled without a lock; the title-screen side prints "Slot N is empty." + "No guild loaded." once each per row; a full row prints one "Written D Mon, HH:MM" and no ISO string, "Save format vN" only on the SlotLine tooltip; `written_words` shape/local/empty/unparsable; Delete → one "Confirm" plate with Yes/Keep it, "No" is `default_focus_of`, the three come back on Keep it; `panel_fit()` within 60px of content and below the host; every visible Button FOCUS_ALL; Settings: every Row_ at row_h == Type.at(40,100); On/2x lit and Off/Keep/100% not, a press cycles and rebuilds; music 60 → 3 of 5 pips lit, voice 0 → none, numbers stay Button.text; disabled rows: one Reason Label in the row, control disabled + padlock + 0.55; panel within 60px of content and above BAND_H; Apply is APPLY_VARIATION and no ButtonCta on the screen; at 150 rows are Type.at(40,150) and the panel keeps the band.
- Shots batch 1: `LoadSave_fixture.png` (viewed) — three rows, nine identical 168x36 buttons (padlock on the dimmed Load/Delete), "Slot N is empty." ONCE per row in CAUTION under the buttons, the panel ends at y≈520 with the camp's lower half (the seated figure by the lantern, the bridge and the stream) under it and the fire ring beside it; one rim line. Defect seen: the reason line touches the rule under it — fixed with ROW_PAD 8 and a centred row. `LoadSave_empty.png` (viewed) — "No guild loaded." chip in the header; each row prints "Slot N is empty." and "No guild loaded." once each, all nine buttons dimmed with padlocks. `LoadSave_raid`, `Settings_*` came out BLANK: Cards.gd (W3-KIT2, mid-edit) fails to compile → Guildhall.gd → Settings.gd "Failed to compile depended scripts"; not mine, retaking once the tree parses.
- Shots batch 2 (tree parsing again, 172 scripts): `Settings_fixture.png` (viewed) — 17 rows at one 41px pitch (y 168, 209, 250 … 824), "On" on the lit chip (gold rim, gold text) and every Off/1x/Keep/100% on the resting chip, the four audio rows with five blue pips beside the number (80 → 4 lit, 100 → 5), the four disabled rows dimmed with a padlock and their reason in CAUTION under the note, the panel ending at y≈878 (≈33px under the last row) with the stream's water and rocks in the band y 880..1005, Apply on the plain secondary plate (the "ButtonSecondaryLit" fallback until Theme.gd's handoff lands). `LoadSave_raid.png` (viewed) — slot 1 "A Guild Story, just started" over "Written 14 Sep, 22:37" (the file's mtime is 22:37 local: the UTC stamp is converted), all three button rows at one geometry, "Slot 2 is empty."/"Slot 3 is empty." once each. `LoadSave_fixture.png` retaken (viewed) — rows now breathe (ROW_PAD): reason line, 8px, rule, 8px, next sentence; the panel ends at y≈568, ≈40px under row 3.
- Refdiff AFTER (vs 3, mask 0,77,1536,649): LoadSave mae 36.994 rmse 66.202 structure 0.2349 iou 0.092 palette 0.4119 within_8 33.95 (before 33.646 / 65.145 / 0.1937 / 0.0779 / 0.2649 / 51.22); Settings mae 36.323 rmse 66.992 structure 0.1899 iou 0.0901 palette 0.3518 within_8 40.55 (before 36.107 / 67.007 / 0.1935 / 0.0857 / 0.2896 / 43.28). Structure and layout_iou improve on both; mae/palette/within_8 move because the band under the scene mask (window y 726+, x 210..1140) was opaque panel before and is camp now — the same effect W1-HALL recorded. With that band masked as well (`--mask 210,726,930,300`, chrome only): LoadSave 47.967 → 47.966 mae, 46.79 → 46.79 within_8 (identical); Settings 52.205 → 50.405 mae, 32.53 → 35.96 within_8, iou 0.1159 → 0.1169 (the sidebar's crimson Apply gone). No chrome regression.
- `Settings_focus6.png` (batch 1, viewed): the ring sits on the rail's Camp item. The shot log says why — `TAB 5 -> Button at 1174,786` (the sidebar's Save / Load), `TAB 6 -> Nav_home`: under `Frame.tab_steps_within_region = false` (docs/13 §13.1 read literally, the shell's default) Tab LEAVES a region, so the cycle is rail → scene's first chip → sidebar → rail and six Tabs are two laps. The plan's `--tab=6` therefore shows the rail's ring; a `--tab=4` shot (below) shows the ring on the first option chip. Not a defect of this unit; recorded.
- `written_words` guard added (a stamp not shaped `YYYY-MM-DDTHH:MM:SS` returns "" instead of letting `Time.get_unix_time_from_datetime_string` print an error). The Bash heredoc collapsed the GDScript `\` continuation into a literal (LESSONS) — caught by re-reading the block, rewritten as two `if`s with the Edit tool.
- verify --fast run 1 (23:27, `build/shots/w3opt/verify_fast_1.log`): LINT OK, MOTION LINT OK, PARSE_CHECK 173 scripts, `TESTS FAILED 6/1908`: one of mine — `test_the_title_screen_side_prints_each_reason_once_per_row` counted Labels named exactly "Reason" and the engine renames the second sibling ("Reason2"), so the row's two reasons counted as one (the shot shows both) — fixed in the test (`_reason_labels` matches the prefix); the other five are other units' files mid-edit: test_roster_layout x2 (W3-ROSTER: "grid 527 < 541"), test_text_scale (Guildhall.gd calls Theme_.get_theme() itself — W3-ROSTER), test_widgets_kit x2 (callout clamp — W3-KIT2). test_loadsave_screen, test_settings, test_text_scale's Apply/Restore tests, test_hall_plates, test_a11y all green.
- `Settings_text150.png` (viewed): rows at the 60px pitch (Type.at(40,150)), "150%" lit, the table scrolling inside the panel capped at the band (water visible y 905..1005) — and a DEFECT: the unwrapped name Labels ("Audio — voice of the scribe" at 22px) widened the name column and pushed the control cell past the panel's right edge, chips clipped at x≈1138. Fixed: the name Label wraps at COL_NAME_W (LESSONS: one uncapped Label moves every column after it). Retaking.
- Shots batch 3 (viewed): `LoadSave_confirm.png` (`--fixture=raid --press="Delete Slot 1"`) — the kit's DANGER-rimmed plate stands where slot 1's three buttons stood, "Yes — delete Slot 1" in DANGER filling the row, "Keep it" beside it wearing the focus ring (the safe default), the row taller by the plate's margins and the panel grown with it (ends y≈583); rows 2 and 3 untouched. `Settings_text150.png` retaken — the name column now wraps, the control cell (chips at x 948..1097) sits inside the panel beside a visible scrollbar, 60px rows, "150%" lit, the table scrolls, the water band stays. `Settings_focus4.png` (`--focus --tab=4`: rail → 1x → Save / Load → rail → 1x) — the 2px steel ring on the "1x" chip, the kit's focus ring on the chip variation. `Settings_fixture.png` retaken after the name-wrap change — pixel-for-pixel the same layout at 100% (no name wraps at 15px), refdiff unchanged.
- `tools/lint_motion.sh`: MOTION LINT OK.
- Final refdiff (vs 3, scene band masked): LoadSave mae 36.994 / structure 0.2349 / iou 0.092 / within_8 33.95; Settings mae 36.323 / structure 0.1899 / iou 0.09 / within_8 40.55 (see the before/after and chrome-only reading above).
- verify --fast run 2 (00:06, `verify_fast_2.log`): `TESTS FAILED 4/1908` — the same test of mine (my prefix match assumed "Reason2"; the engine's auto-name is "@Label@N", the CLASS name, so no name match can find it) plus test_roster_layout x1 and test_widgets_kit x2 (other units). Run 3 (00:28, `verify_fast_3.log`, full log `verify_full_3.log`): LINT OK, MOTION LINT OK, PARSE_CHECK 173, ART CHECK 197 agree 0 DIFFERS 0 MISSING, `TESTS FAILED 1/1908` — only mine, same cause; every other unit's file green in that run. Fix: `_slot_row` names a row's second reason Label "Reason2" itself (the kit's "Reason" stays on the first), the test matches `contains("Reason")`. Run 4 pending.
- verify --fast run 4 (00:44, `build/shots/w3opt/verify_fast_4.log`), summary lines verbatim:
  PASS  LINT OK  no cross-file class_name references in sim/ or game/
  PASS  MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)
  PASS  PARSE_CHECK scanned 173 script(s)
  PASS  16 generated file(s) agree with tools/gen_items.gd
  PASS  ART CHECK  generators=7  runtime files: 197 agree  0 DIFFERS  0 MISSING
  PASS  TESTS PASSED   1908 test(s) in 92 file(s)  [223863 ms]
  VERIFY OK
- `tools/lint_motion.sh` run directly: MOTION LINT OK.

## What is left / for the orchestrator

- Apply `handoff-W3-OPTIONS.md` #1 (Theme.gd: `ButtonChip` hover_pressed + `ButtonSecondaryLit`); until then Apply renders on the plain secondary plate and a lit chip under the pointer uses the per-button override Settings.gd sets from the theme's own pressed box. After it lands, a `Settings --fixture` shot should show Apply a step brighter than Back/Restore defaults.
- Nothing in this unit was left undone. Two shots beyond the plan's five were taken to see states the fixture cannot show (`LoadSave --fixture=raid` for the timestamp line, `--press="Delete Slot 1"` for the confirm plate, `--focus --tab=4` for the ring on a chip); all viewed.
- Contract greps: no `create_tween`, no `reduced_motion` branch, no `class_name`, no `_ready()` logic (both `_ready` only call `build()` as before), no changed Label/Button text; tabs only in the two screen files, spaces only in the test; `git status` shows only my three files plus the two plan files.
