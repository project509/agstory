# Report — W0-SHOT (the instrument grows the nine flags every acceptance line needs)

Owned files: `tools/shot.gd`, `tools/fixture_reference.gd`. No test file is named by the
plan for this unit (shot.gd is a tool, not a screen); none is created, because ownership
is absolute and `tests/unit/` is not on the list.

Written as each item finished.

## Acceptance (00-plan §W0-SHOT, verbatim)

- [x] A1 `shot.gd -- Town.tscn build/shots/Town_focus.png 40 1536x1024 --fixture --focus` shows a 2px EDGE_STEEL ring on `Nav_home` at rail x 0..206, y 105..160
- [ ] A2 `--tab=1` moves it to the first header chip — NOT MET as worded (review F10): the ring lands on the scene's entry (Guildhall callout button, 242,310 196x38) because the chips are Labels and Tab leaves the region; the instrument half (N verified grabs) is met. Recorded in the handoff for W1-FRAME/W1-KIT; see J1
- [x] A3 `--press="Records"` on Guildhall shows the Records wall ("Legendaries 0 of 9")
- [x] A4 `RaidView --fixture=raid --advance=all` shows the WIPE. beat
- [x] A5 `--frames=1,60` on MainMenu writes two PNGs that differ only in the water rects (to within the water's screen-space glow: 36 px in the 6 rows just above the third rect's top edge, review A5 / this report's measurement — accepted as the rects' own bleed)
- [x] A6 `Boot.tscn --hold-boot` shows the paperwork card
- [x] A7 `MainMenu 1820x1024 --set=display_aspect=expand` is 1820 wide (and `keep` centres 1536x1024 on clear-colour bars)
- [x] A8 `Results --fixture=raid:clear` prints the clear branch ("See how it ended" needs `--completed` too — see judgement calls)
- [x] A9 exit codes documented in the header; an unknown `--flag` exits 2 (W0-GATE's SKIPPED contract)
- [x] G1 `parse_check` OK after every edit; `verify.sh --fast` green (summary pasted below)

## Judgement calls (decided inside the plan's text)

- J1 `--tab=1` ends on the scene's first focusable, not a header chip. `Frame.FOCUS_REGIONS = [rail, header, scene, sidebar, strip]` (Frame.gd:477) with `tab_steps_within_region = false` (:485): Tab leaves a region for the next region's entry (:620-621); arrows step within. The header region is empty on every screen today because `Frame.standard_chips()` builds Labels only (:416-460), so one Tab from Nav_home reaches the scene region's entry — on Town the Guildhall callout's title button (242,310 196x38). The instrument does exactly RULES-13's Fix (`find_next_valid_focus()` N times, each grab verified against `SubViewport.gui_get_focus_owner()`); where the ring lands is the shell's wiring, owned by nobody in wave 0. Left as measured; W1-FRAME/W1-KIT decide whether chips become focusable.
- J2 `--press="Records"` needs `--fixture=raid:clear`. Records opens at rank Known (Guildhall.gd:146-156). The fixture's rank stays Unknown on purpose — 00 §2.5's ruling reads chip 4 as "Day 23 · Unknown", and promoting the fixture would move every refdiff baseline W0-GATE records this wave. A wipe pays no RP; a clear reaches the award site (GameState.gd:1190, `Reputation.rank_after`). Pressing a disabled button is now exit 12 with the tab's own reason, never a silent no-op (the first agent's Guildhall_records.png was the Roster tab and nothing said so).
- J3 "See how it ended" is the campaign-complete link (Results.gd:912-928, `completed and not completion_seen`), not part of the clear branch. `--fixture=raid:clear` alone shows the clear branch (payout, per-item Give rows, an enabled "Suggested — give everything"); `--completed` on top shows the link. Both shots are taken; the acceptance line is read as "the clear branch, and the link with --completed".
- J4 Every shot starts from docs/13 §15.1 defaults (`GameSettings.reset_all()`), never from this machine's user://settings.cfg (which held reduced_motion=true, text_scale=150). CORRECTED (review F12): the file IS written at quit — GameSettings saves on PREDELETE — so the tool reloads it in `_quit()` before every exit and the write puts back what it held; the first version of this call said "nothing is written back", which was false and had wiped the developer's text_scale=150 (repair log below has the byte-identical proof).
- J5 `--press` on a disabled Button is exit 12 (reason printed). Exit table row added; W0-GATE's SKIPPED contract (exit 2 = unknown flag) is unchanged.
- J6 Saves are redirected to `user://shot_saves` (purged on start) before any fixture runs, as tests/run_tests.gd does with `SaveGame.SAVE_DIR`. Found because the player's real `user://saves/slot_0_auto_0.json` was rewritten at 08:38:19 — the second `--fixture=raid:clear` recorded its attempt (record_attempt autosaves, docs/14 §7.4). This was pre-existing for `--fixture=raid`; it is fixed in the tool, not undone for the file already overwritten (see the log for what the file now holds).
- J7 No file under tests/ is created: this unit's ownership list is `tools/shot.gd`, `tools/fixture_reference.gd` and rule 1 is absolute, although §0.2 wants a `tests/unit/test_<key>.gd`. The test is written in full in handoff-W0-SHOT.md §1 for the orchestrator to apply; its assertions were run as a scratch probe (scratchpad/probe_fixture.gd, saves redirected) under the lock — result in the log.
- J8 `--advance=N` = N revealed entries via the screen's own `_process(0.02)` steps, then `set_process(false)` so the count holds through the capture frames; `all` prefers `_on_skip()` (docs/13 §11.4's any-input skip), else steps to `is_finished()`. `--frames=A,B` replaces the positional frame count (two captures, `.a.png`/`.b.png`).
- J9 `--hold-boot` cannot intercept Boot's deferred `_boot()` inside the SubViewport, so it lets it run, empties Boot's ScreenHost and re-shows BootOverlay: the captured frame is the boot card over the empty host — the composition of the game's real first frame, minus the router's mount. Boot.gd is untouched (W0-TEXTSCALE owns it).

## Log

### 2026-09-14 resume (second agent)
- The first agent was killed by a usage limit after writing the whole draft (+456/+50 in the two owned files) and taking eleven shots (build/shots/*, 20:55-21:14 on 09-13, all AFTER shot.gd's last edit at 20:53) but before ticking anything. Read: 00-plan §0 + §W0-SHOT, RULES §3 + RULES-13, CRITIC-G02/G03/G11/G12, COMBAT q7, LESSONS, both owned files in full, the diff.
- Grep of shot.gd: all nine flags are parsed (`--focus`, `--tab=`, `--press=`, `--advance=`, `--fixture=raid:clear`, WxH + `_place_host()` keep/expand, `--frames=`, `--hold-boot`, unknown `--` -> `_usage()` exit 2) and every API they call exists (`ScreenRouter.wire_shell_focus/initial_focus_target` static at :226/:173, `register_host` :45; `RaidView._on_skip` :1222, `_player` :119, `_process` :1007; `LogPlayer.total/revealed_count/is_finished`; `RaidPlan.suggest_party` :442; `ContentDB.encounter_at_slot` :546; `RaidSim.Result.cleared` :178; `GameState.build_loadout/spend_loadout/record_attempt`; `GameSettings.get_value/set_value`; Boot's `ScreenHost`/`BootOverlay` names :47/:57).
- Plan: view the eleven existing shots and measure the two numeric claims (ring rect/colour, frame diff), then re-take every acceptance shot in ONE lock hold so the evidence is current against the tree the other wave-0 agents have since edited, view those, then parse_check + verify --fast.
- Viewed the eleven existing shots. A1's ring is real: rail band 0..212 x 100..166 is dominated by (74,131,163) = EDGE_STEEL #4982A2 (73,130,162) after the ring's blend, over the rail ground. A4 shows the WIPE. stamp + dim + exits; A6 the paperwork card; A7-expand is 1820 wide; A8 shows "Cleared." with the Cracked Charm hand-out. Three did NOT show the thing: A2's ring sat on the Guildhall callout (not a header chip), A3 showed the Roster tab (Records disabled under `--fixture`: the tab is gated on rank >= Known, Guildhall.gd:146-156, and its `pressed` is only connected when open, :186-190), and A5's two PNGs were byte-identical (0 differing px).
- ROOT CAUSE of A5 (and a suspiciously 150%-sized MainMenu_wide_keep from 21:03): `%APPDATA%/Godot/app_userdata/A Guild Story/settings.cfg` (mtime 09-14 08:34) holds `reduced_motion=true`, `text_scale=150`; `GameSettings._ready()` = `load_from_disk()`, so every shot inherited this machine's options and `SceneStage.apply_settings(true, ..)` held the shimmer `motion` uniform at 0. FIX (shot.gd `_build`): `GameSettings.reset_all()` before `--set` — docs/13 §15.1 defaults, nothing written back; prints `SETTINGS defaults[ + {sets}]`. Header paragraph added. `reset_all` emits `changed("")`, whose only listener is Audio.apply_levels (Audio.gd:312-315) — harmless.
- FIX for the silent A3: `--press` on a `disabled` Button now prints its reason (tooltip) and exits 12 instead of emitting to nothing. Header exit table has row 12. A3's command therefore needs a recorded attempt so the rank is Known: `--fixture=raid` (record_attempt promotes at GameState.gd:1190 regardless of outcome; the Results clear shot's chip already read "Day 24 · Known").
- 09-14 batch 1 (one lock hold, scratchpad/shots.sh; full engine output in scratchpad/shots_full.log): PARSE_CHECK OK (151 scripts); A1 `FOCUS Nav_home<Button> at 0,105 211x55 (3 shell regions)`; A2 `TAB 1 -> Button<Button> at 242,310 196x38`; A3 (with `--fixture=raid`) exit 12 "--press 'Records' is disabled — There is nothing to record until you have raided. The board opens at Known." (a wipe pays no RP so nothing promotes — the award site GameState.gd:1190 is only reached on a clear); A4 `ADVANCE all via _on_skip()`; A4b `ADVANCE 10 revealed=10/160 in 178 steps`; A5 two files; A6 `HOLD-BOOT overlay shown, host emptied`; A7 expand 1820x1024, keep 1820x1024 and 1920x1080; A8 `FIXTURE raid:clear A0 seed=25` twice (deterministic); probes: `--bogus` exit 2 + usage, `--press=Nope` exit 9, disabled press exit 12, `--tab=x` exit 2. Every shot now prints `SETTINGS defaults[ + {sets}]`.
- Viewed every fresh shot (Read tool) and measured (scratchpad/measure.py):
  - A1 Town_focus.png: the steel ring sits around the "Camp" rail item; steel bbox in the rail band = x 0..211, y 103..161 (1371 px) around Nav_home's rect 0..210 x 105..159 — 2 px outside the bounds on each side plus the edge row/column (column x=100: rows 103,104,105 steel then 106 = EDGE_STEEL_DIM, the button's own border; row y=132: cols 210,211,212 steel). Colour (74,131,163) vs EDGE_STEEL #4982A2 = (73,130,162). The rest of the screen is the fixture Town (12480 G, 320, Roster 12 of 15, Day 23 · Unknown, four cards, "Nothing has happened yet.").
  - A2 Town_tab1.png: the rail band has only the rail's own dim border (468 steel-ish px, no ring); the ring is on the "Guildhall" callout's title button at 242,310 196x38 inside the scene. See J1 for why that is not a header chip.
  - A4 RaidView_wipe.png: red ink blot with the "WIPE." stamp centred on the arena, the dim over the plate, the line "A wipe costs time, consumables, morale and money — never the save.", exits "Back to the board" (focus-ringed) and "The report" (CTA), all twelve HP bars at 0 (page 3/3 shows Nev/Clive/Spoof/Tiny Dead), log at the end. Observation for W2-RAIDVIEW, not this unit: the header still reads "Round 11 of 22" after `_on_skip()` (the round label is updated by the dwell path, not by `reveal_all()`).
  - A4b RaidView_adv10.png: "Round 1 of 22", "Mistakes this attempt 5", boss 7142/7150, party page 2/3 with full HP bars, the first five log rows — ten entries revealed and frozen (`set_process(false)`), no wipe.
  - A5 MainMenu_frames.a/b.png: both 1536x1024; diff bbox (1,252)-(1480,1024); 34172 differing px inside the three `shimmer` rects of stage_town.json ([0,250,190,450], [0,690,600,334], [1270,900,210,124]) and 36 outside, all in the 64px cells just above the third rect (x 1344..1407, y 832..895) — the screen-space glow's reach (LESSONS: glow is screen-space). _frames_water_crop.png (a | b | diff x8) shows the shimmer speckle across the harbour water and nothing on the town. Before the defaults fix the same command produced 0 differing px.
  - A6 Boot_hold.png: the paperwork card alone on GROUND_PAGE — wordmark, "Bad People. Worse Decisions.", rule, "Reading the guild's paperwork…" — nothing of the main menu behind it.
  - A7 MainMenu_expand.png: 1820x1024; the menu column and aerial as at 1536, and x 1536..1819 is a plain GROUND-coloured band ((0,13,18) at x 1700) — CRITIC-G03's "what fills x 1536..1820 is unknown" now has an answer for W1-FRAME. MainMenu_wide_keep.png: 1820x1024, canvas 1536 wide centred with 142 px bars each side ((0,13,18) at x 10 and 141; canvas begins at 142); MainMenu_1080p_keep.png: 1920x1080, canvas scaled 1.0547 to 1620x1080 with 150 px bars. Bar pixels equal the Town shot's own GROUND_PAGE areas ((0,13,18) at (5,1015)) — project.godot's default_clear_color = GROUND_PAGE #030B13 (W0-SPLASH), rendered through the same pipeline; so the bars are exactly what the letterboxed window paints.
  - A8 Results_clear.png: "Cleared.", "A0 — Adventure 0", tally (Rounds 9, Mistakes 5, Damage dealt 212, Healing done 70, Survivors 4 of 4), "Everyone came home. This is unusual.", Loot "Payout 4 G", "Suggested — give everything" enabled, "Sell all unusable (+0 G)" disabled with "Everything here fits somebody.", the "Cracked Charm of Power · Trinket" row with Sell 1 G / a Bork · Warrior (+1) dropdown / Give, chip "Day 24 · Known", four cleared cards, seven log lines. Results_clear_completed.png (`--completed` on top): the same plus the "See how it ended" CTA in place of the two exits — see J3.
- SIDE EFFECT FOUND AND CLOSED (J6): `%APPDATA%/Godot/app_userdata/A Guild Story/saves/slot_0_auto_0.json` now holds guild "A Guild Story", day 24, gold 12484 (12480 + the 4 G payout), attempts {t1_tut_a0: 1}, created_at 2026-09-14T13:38:19Z — the raid:clear fixture, written by `record_attempt`'s autosave during the A8 shot. The manual `slot_0.json` (day 2, 64 G, 09-12) is intact; `slot_0_auto_1/2` are "Playtest 56433" (verify.sh's playtest stage, 00:17 — that stage also writes into the real user dir; pre-existing, not this unit's file, noted for W4-HYGIENE). shot.gd now sets `SaveGame.SAVE_DIR = "user://shot_saves"` and purges it before any fixture runs (the runner's own pattern, tests/run_tests.gd:11-13). The overwritten autosave is not restored — its previous content is unknown.
- Scratchpad note: `scratchpad/batch2.sh` was replaced under me by a different script from outside this session (a lint+verify runner), so every later scratch file carries a `w0shot_` prefix.
- 09-14 batch 2 (one lock hold, scratchpad/w0shot_batch2.sh): PARSE_CHECK OK (151 scripts) with the save redirect in; `Guildhall.tscn --fixture=raid:clear --press=Records` exit 0 (`FIXTURE raid:clear A0 seed=25`, `PRESS Records`); the same with `--focus` exit 0; probe `PROBE seed=25 rank_wipe=0 rank_clear=1 fails=0` / `PROBE OK` — the handoff test's assertions all hold (wipe recorded at E5; clear recorded at A0 in-range seed; A0 selected; one attempt; promotion Unknown→Known; deterministic seed and payout).
  - A3 Guildhall_records.png (viewed): Records tab active in the tab row, "Records  3 of 40" (two spaces, the LabelSection RULES §1 pins), "Legendaries 0 of 9", chips 12484 G · 325 · Roster 12 of 16 · Day 24 · Known, Recent Events "Bork/Gruk/Rhona/Greg: cleared". Observation for the hall units, not this one: the wall is those two lines in an otherwise empty panel.
  - Guildhall_records_focus.png (viewed; `--press=Records --focus`): the Records tab stays active and the ring sits on Nav_home ("Camp", x 0..211, y 103..161) — the queue order is presses, advance, focus, tabs, so a pressed surface can be shot with its focus state; `initial_focus_target` still opens on rail[0] as a11y_smoke requires.
- Redirect proven (batch 2 listings): before/after two `--fixture=raid:clear` shots the real `saves/` dir is byte-for-byte the same listing (slot_0.json 09-12 13:37, auto_0 08:38:19, auto_1/2 09-13 19:17) and `shot_saves/slot_0_auto_0.json` appeared at 08:45:17 — the fixture's autosave lands in the tool's own dir now.
- `GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` (inside batch 2, after every edit):
  ```
  == 0/8  lint: no cross-file class_name refs ==
    PASS  LINT OK  no cross-file class_name references in sim/ or game/
    PASS  MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)
  == 1/8  script parse ==
    PASS  PARSE_CHECK scanned 151 script(s)
  == 2/8  generated content matches its generator ==
    PASS  16 generated file(s) agree with tools/gen_items.gd
  == 2b/8  generated art matches its generator ==
    PASS  ART CHECK  generators=5  runtime files: 76 agree  0 DIFFERS  0 MISSING
  == 3/8  unit tests ==
    PASS  TESTS PASSED   1604 test(s) in 72 file(s)  [33620 ms]
  VERIFY OK  (full log: .verify.log)
  ```
  (1604, not 1603: another wave-0 unit's test file landed meanwhile; none of the 1604 is this unit's — see J7.) game/ untouched, so lint_motion ran only as verify's stage 0 (OK).

## Repair pass (review-W0-SHOT.md, 2026-09-14; third agent)

- [x] R1 (major F12) every exit of shot.gd puts the player's own options back before `quit()` — `user://settings.cfg` byte-identical (md5) before and after a `--set` shot and a flagless shot; header :43 and J4 corrected
  `_quit(code)` reloads the file first; nine runs under one lock hold (three clean, three error exits 2/9/12, A1/A5/A8) each left settings.cfg at md5 c7d5a3d18f6c0eeb474e2b5e0948fc5d = the BEFORE copy (`cmp` identical every time; only the mtime moved, which is PREDELETE writing the reloaded values back). Header paragraph and J4 now say what happens.
- [x] R2 (minor A2/F10) recorded for W1-FRAME/W1-KIT in the handoff; A2 box corrected to "instrument half met, shell half not"
  Handoff now carries the observation paragraph (Frame.gd:421-436 chips are Labels; :485/:620-621 Tab leaves the region; where the ring lands on Town). A2 unticked above with the same citation. Nothing inside the owned files can move the ring onto a Label.
- [x] R3 (minor F2) tabs in tools/shot.gd left as HEAD had them (a mixed file is what Godot rejects); noted
  `git show HEAD:tools/shot.gd` is tab-indented; the file stays 389 tab-led / 0 space-led after this pass. Rule 5's "tools/*.gd use four spaces" is true of every other tools file; converting this one is a whole-file reformat of lines this unit did not write (rule 1) and gains nothing Godot cares about. Left for W4-HYGIENE to decide.
- [x] R4 (minor F5) stale "(none yet)" line removed from the handoff
  Replaced by the A2 observation paragraph; §1 (the test file) is unchanged.
- [x] R5 (minor A5) the 36 glow px above the third water rect noted against A5
  A5 line annotated; the earlier log entry already had the measurement (36 px, x 1344..1407 y 832..895 in the 64-px cells above rect 3 — the reviewer's finer count puts them at (1399..1404, 894..895)).
- [x] R6 affected shots re-taken under the repaired code and viewed; parse_check + `verify.sh --fast` green; summary pasted
  Town_focus / Town_settings_probe / Town_plain / Town_ts100_probe / MainMenu_frames.a+.b / Results_clear taken 09:06-09:07 and read (descriptions in the repair log); PARSE_CHECK OK 151, `TESTS PASSED 1604 test(s) in 72 file(s) [31180 ms]`, VERIFY OK — inside the same hold, after the last edit (shot.gd mtime 09:06:11 < batch start 09:06:41).

### Repair log

- Read the review, this report, the handoff, shot.gd and GameSettings.gd in full. The reviewer is right: `GameSettings._notification()` (GameSettings.gd:159-161) calls `save_to_disk()` on `NOTIFICATION_PREDELETE`, the autoload is freed by `quit()`, and after `reset_all()` + `--set` the values it writes are the shot's. The earlier check ("reset_all does not save") answered the wrong question.
- State of the developer's file when this pass began: `%APPDATA%/Godot/app_userdata/A Guild Story/settings.cfg` mtime 09:04:25, md5 c7d5a3d18f6c0eeb474e2b5e0948fc5d, `text_scale=150`, `reduced_motion=false` (someone had already put the 150 back after the reviewer's 08:59 probe; that content is what this pass must preserve, byte for byte).
- CODE (tools/shot.gd): every statement-position `quit(N)` (21 sites, via `sed 's/^\(\t*\)quit(\([0-9]*\))/\1_quit(\2)/'`) now goes through a new `_quit(code)` that does `GameSettings.load_from_disk()` first — so PREDELETE's write puts back exactly what the file held — and prints `SETTINGS restored from user://settings.cfg`. From `_init` (the `--bogus` exit 2 path) the autoloads do not exist yet, the lookup finds nothing, and it is a plain `quit()`, which is what HEAD did. `load_from_disk()`'s only `changed` listener is `Audio._on_setting_changed` (Audio.gd:210-211) — a level re-apply at quit. Header :43 rewritten ("The file is put back before the tool quits: ..."); `_quit`'s doc comment says why. File is still 389 tab-led / 0 space-led lines.
- Proof plan (scratchpad/w0shot_repair.sh, one lock hold): md5 + copy of settings.cfg BEFORE; parse_check; `Town --fixture --set=reduced_motion=true` (the reviewer's probe), flagless `Town` (the file holds text_scale=150 — a flagless shot used to reset it to 100), `Town --fixture --set=text_scale=100`, the three error exits (`--bogus` 2, `--press=Nope` 9, disabled `--press=Records` 12), then A1/A5/A8 re-taken; `cmp` against the BEFORE copy after every exit; real `saves/` listed before and after; `verify.sh --fast` last.
- BATCH (09:06:41-09:08:17, lock held once; scratchpad/w0shot_repair.log): PARSE_CHECK OK (151). Every shot now prints `SETTINGS defaults[ + {...}]` at build and `SETTINGS restored from user://settings.cfg` at exit — including the exit-9 and exit-12 runs; the exit-2 `--bogus` run prints neither (it dies in `_init`, before the autoloads). After EACH of the nine runs: `SETTINGS-FILE IDENTICAL to BEFORE`, md5 c7d5a3d1… (text_scale=150, reduced_motion=false). Real `saves/` listing identical before/after (auto_0 still 08:52:42); `shot_saves/slot_0_auto_0.json` rewritten at 09:07:11 by the A8 fixture, as designed.
- Viewed (Read tool): Town_focus.png — steel ring around "Camp", 1428 px of EDGE_STEEL-ish in the rail band, bbox x 0..212 y 103..161, column x=100 rows 103-105 = (74,131,163)/(77,132,163), row 106 the button's own dim border — same numbers as the review's. Town_settings_probe.png (`--set=reduced_motion=true`) and Town_ts100_probe.png (`--set=text_scale=100`): the fixture Town (12480 G · 320 · Roster 12 of 15 · Day 23 · Unknown) at 100% text, differing from each other only in the scene band (whole-screen diff bbox (203,74)-(1427,774); rail 63 px, strip 1 px — ambient motion, since one shot has it frozen). Town_plain.png (flagless): "No guild loaded.", Available 0, "Nothing has happened yet." — at 100% text although the file held 150: RAIL and STRIP regions are 0 px different from the reviewer's review-W0-SHOT-Town_plain.png (taken when the file held 100; that one was a `--fixture` shot, so the chips/sidebar differ). MainMenu_frames.a/.b: both 1536x1024, 22059 differing px inside stage_town's three shimmer rects, 36 outside, first at (1402,894) — the same 6-px glow bleed above rect 3 the review measured. Results_clear.png: "Cleared.", A0 — Adventure 0, Rounds 9 / Mistakes 5 / Damage 212 / Healing 70 / Survivors 4 of 4, Payout 4 G, the Cracked Charm of Power row, Day 24 · Known, 7 log lines — `FIXTURE raid:clear A0 seed=25` again.
- verify --fast (same hold):
  ```
  PASS  LINT OK  no cross-file class_name references in sim/ or game/
  PASS  MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)
  PASS  PARSE_CHECK scanned 151 script(s)
  PASS  16 generated file(s) agree with tools/gen_items.gd
  PASS  ART CHECK  generators=5  runtime files: 76 agree  0 DIFFERS  0 MISSING
  PASS  TESTS PASSED   1604 test(s) in 72 file(s)  [31180 ms]
  VERIFY OK  (full log: .verify.log)
  ```
- FOUND WHILE PROVING R1 (for W4-HYGIENE, not this unit's file): the batch's FINAL stamp, taken right after verify, shows settings.cfg CHANGED — mtime 09:08:17, md5 7a5ebee451d41b2b5b219dbaf087ed36, `text_scale=100`, `reduced_motion=true` — after nine shot.gd exits had left it untouched. That is `verify.sh --fast`'s unit-test stage (verify.sh:138, `tests/run_tests.gd`): the runner redirects `SaveGame.SAVE_DIR` (:12) but not GameSettings, and test_a11y_legibility / test_motion / test_settings / test_text_scale / test_wipe_sequence call `set_value("reduced_motion", true)` / `set_value("text_scale", ...)` on the autoload; whatever the last test left is what PREDELETE writes to the developer's real file when the runner quits. The reviewer's corroboration for F12 ("at 08:57 it held text_scale=100") is therefore this stage, not shot.gd — the unit's batch 2 ran verify --fast inside it — and the md5 the reviewer saw after their probe (7a5ebee4…) is the same 100/true file. Every agent's verify run does this. Not fixable in the owned files; recorded in the handoff for W4-HYGIENE.
- Put the developer's file back to the state this pass found it in (the BEFORE copy: 150/false, c7d5a3d1…) with a `cp` under the lock — done 09:09:59, md5 c7d5a3d1… again (scratchpad/w0shot_restore.log). The next verify run by anyone will change it again until W4-HYGIENE isolates the runner's settings.
- Ownership check at the end: `git status --porcelain` shows only tools/shot.gd, tools/fixture_reference.gd (unchanged this pass) and the two plan files from this unit; shot.gd 389 tab-led / 0 space-led lines; 21 `_quit(` call sites, 0 bare statement `quit(` left.

## Result

All nine flags exist, parse, and were exercised from the command line with the §0.3 spellings; every acceptance shot was taken under this session's code, viewed, and measured where the line carries a number. Two instrument faults nobody had listed were found by looking at the shots and closed: the tool inherited this machine's user://settings.cfg (J4) and the raid fixtures autosaved into the player's real saves (J6). One new exit code (12) closes a silent failure (J5). The review's major (F12: the tool then wrote its defaults back into that file at quit) is closed by `_quit()` reloading the file before every exit — proven byte-identical across nine exits of every kind.

Files: `tools/shot.gd` (+479/-54 vs HEAD after the repair), `tools/fixture_reference.gd` (+50: `apply_clear`, `CLEAR_SLOT`, `CLEAR_MAX_SEEDS`), this report, `build/plan/handoff-W0-SHOT.md` (§1: `tests/unit/test_w0_shot.gd`, new file, four tests, assertions proven by the probe; observations for W1-FRAME/W1-KIT and W4-HYGIENE).

Shots (build/shots/, all viewed): Town_focus, Town_tab1, Guildhall_records, Guildhall_records_focus, RaidView_wipe, RaidView_adv10, MainMenu_frames.a/.b (+ _frames_water_crop), Boot_hold, MainMenu_expand, MainMenu_wide_keep, MainMenu_1080p_keep, Results_clear, Results_clear_completed.

## Left, and why

- No file under `tests/` (J7): ownership. The orchestrator applies handoff §1; it passes by construction of the probe, but has not been run as a `test_*.gd` under `run_tests.gd`.
- A2 is not met as worded (J1, review F10): the chips are Labels today and Tab leaves the rail region; the instrument cannot put a ring on a Label. Handed to W1-FRAME/W1-KIT as an observation in the handoff.
- A8's "See how it ended" needs `--completed` as well (J3): Results.gd's own gate, not a fixture matter.
- `user://saves/slot_0_auto_0.json` on this machine was overwritten by the fixture at 08:38 before the redirect existed; its prior content is unknown and not restorable. The manual slot is intact.
- Observations for other units, not acted on: verify.sh's unit-test stage rewrites the developer's real `user://settings.cfg` with whatever the last test set (100/true today) because run_tests.gd redirects saves but not settings (W4-HYGIENE; repair log); verify.sh's playtest stage also writes "Playtest NNNNN" autosaves into the real user dir (W4-HYGIENE); RaidView's header stays at "Round 11 of 22" after `_on_skip()` (W2-RAIDVIEW); the Records wall is two lines in an empty panel (W3-ROSTER/hall units); under `expand` x 1536..1820 is a bare GROUND band (CRITIC-G03 → W1-FRAME).
