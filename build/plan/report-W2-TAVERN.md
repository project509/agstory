# report-W2-TAVERN — candidate cards carry their gear, the sidebar fits, the caption is a plate

Unit: W2-TAVERN (00-plan.md §3). Owns `game/screens/Tavern.gd`, `tests/unit/test_tavern_layout.gd` (new).
2026-09-14 17:50-18:45. Findings closed: TOWN-18, TOWN-19, TOWN-17, CRITIC-G05, KIT-10, TOWN-31, KIT-03, CRITIC-G04.
Handoff: `build/plan/handoff-W2-TAVERN.md` (no edits needed in files this unit does not own).

## Acceptance (00-plan.md "### W2-TAVERN")

- [x] Tavern shot: cards show three gear slots and a coloured rim — `build/shots/W2-TAVERN-Tavern.png` + 2x crop `W2-TAVERN-crop-cards.png`: three `SlotMini` cells beside the portrait (empty on the fixture's Common recruits, filled from `equipped_items` + `Cards.gear_icon` when a recruit arrives with gear), 2px rarity rim (Common grey) on cards 2-4, the selected card in `PanelRoundSelected` steel, every portrait in a rarity rim (`Widgets.slot_rarity`).
- [x] One caption plate top-left in the callout chrome — node `Caption` (`PanelCallout`, scene (14,14), min-driven): tab pair via `Widgets.tab_row` ("Board" lit + gold underline, "Manage" quiet), "house 1 of 4 · 4 seats", "At Unknown the room is mostly Common adventurers." (`Reputation.recruit_tiers`), "Legendaries 0 of 9", "Ask around again — 50 G" + its rule, "Take the room next door — 120 G, one more seat" dimmed with "Unknown guilds cannot commission this. Reach Known." under it (reasoned, wrapped), the notice inside the plate (hidden until there is one). Both Buttons left the sidebar (test).
- [x] Recent Events on the strip — measured on the shot: panel rim x 1045..1518, y 734..995 (strip-local 1032..1505, i.e. the plan's rect); `Cards.event_log(_strip, _state, 7, "Raids and rests write here.")`, on both tabs.
- [x] Sidebar bottom at y <= 714 — measured: the rim's bright row at x=1300 is y=712; the content ends ~y 560 on the fixture; at text 150 the column scrolls with the 8px bar inside the rim.
- [x] Footer under the cards — Label `Footer` at strip-local (127, 266): "Roster 12 of 15  ·  Hiring Linda leaves 12,420 G" (`Type.gold`; "needs N more" when short), stops at x 975 clear of the pager caption.
- [x] `test_tavern_layout.gd` (11 tests): the Sidebar panel's `size.y <= SidebarHost.size.y` AND its combined minimum, on the board, the Manage tab and an empty board; a three-bullet candidate's column fits the inner height with wrapped Labels shaped at 330 (no bar needed); a four-bullet Legendary keeps the panel bounded; the card's `Gear` row has 3 cells and the rim/variation contracts; a stamped Legendary card holds 262 at text_scale 150; the plate carries tabs/house/floor/meter/reroll/upgrade/notice; the strip carries the log + footer on both tabs; the confirm pair's node/texts/default focus; Look from Manage returns to the board; "Back to town" is `ButtonQuiet`; no `_chips`.
- [x] `test_tavern.gd`, `test_screens.gd:856-931`, `test_a11y_legibility.gd:793-848` green; "Ask around again", "Take the room next door", "Not tonight", "Hire — " are Button text (the first two on the plate).
- [x] Dismiss confirm → `Widgets.confirm_pair("Yes — let them go (-2 morale to everyone else)", "Keep them", ...)` in the Manage row; texts unchanged; "Keep them" is the default focus (test_tavern's `let them go` press still works).
- [x] Selected card rim → `theme_type_variation = "PanelRoundSelected"` (KIT-10); no hex anywhere in Tavern.gd.
- [x] "Back to town" → `ButtonQuiet` (a Button; TOWN-31/CRITIC-C14).
- [x] Dead `_chips()` deleted; `Frame.refresh_chips(_frame, _state)` now runs on every `_refresh` so the gold/roster chips follow a hire (KIT-03/TOWN-05).
- [x] `--press=Manage` shot audited (`W2-TAVERN-Tavern_Manage.png`): before, the strip was empty and the list clipped its last row with no bar; now the list hangs under the plate (y 268..695, follows the plate's rect through `item_rect_changed`), the 8px scrollbar shows, the strip keeps the board + the log + footer, the sidebar carries "Manage", its warning and Back to town. Not fixed (not in Tavern.gd's findings): the Manage sidebar is sparse.
- [x] `--set=emoji_free=true` shot viewed (`W2-TAVERN-Tavern_emoji.png`): "starts at 46 |||||....." and the band word on every card and Manage row.
- [x] `--set=text_scale=150` shot viewed (`W2-TAVERN-Tavern_text150.png` + crop `W2-TAVERN-crop-cards150.png`): the plate grows to ~1040x300 inside the scene, the list follows it, the sidebar scrolls (bar inside the rim, Back to town below the fold), the cards hold 262 with the class line WRAPPING ("Wizard —"/"Common" — the first cut trimmed it to "Comm…", which docs/13 §4.4 forbids), bullets reduced to a scrolling sliver on the busiest card (reflow by scrolling, the allowed mechanism), footer intact.
- [x] Green: every bullet printed (test_tavern `test_every_backstory_bullet_is_face_up`); `_candidate_card(who, index)` still returns a PanelContainer whose bullets sit in a v-scrolling ScrollContainer and whose minimum height is <= 262 (legibility test green).
- [x] refdiff Tavern vs 3, mask 0,77,1536,649 — before (wave-1 sheet 17:39): mae 34.913 · rmse 67.463 · structure 0.1803 · iou 0.1038 · palette 0.3498 · within-8 52.53% · DIFFERENT. After (`W2-TAVERN-Tavern.png` 18:34): mae 36.938 · rmse 69.39 · structure 0.1833 · iou 0.1116 · palette 0.2826 · within-8 48.63% · DIFFERENT. Mixed: iou, structure and palette improve; mae +2.0 and within-8 -3.9pt. Per-region mae vs the concept (PIL, same mask): header 92.8→92.9, sidebar 39.5→39.9, rail 18.6→18.6, strip-log 19.7→19.9, footer band 8.1→10.6 (the new footer text), strip-cards 20.4→24.1 — the whole move is the candidate card: Concept 3's roster card runs its three 47px slots along the bottom under a quote; the Tavern card puts three mini slots beside the name because its bottom is the bullets and Look (docs/04 §3.5, the 262 contract). Recorded, not chased: the card is where the design wins over the reference's placement (memory rule 2026-09-10).
- [x] `verify.sh --fast` (18:40): 0/5 class cache PASS · 0/8 LINT OK + MOTION LINT OK · 1/8 PARSE_CHECK scanned 166 script(s) PASS · 2/8 16 generated file(s) agree PASS · 2b/8 ART CHECK 184 agree 0 DIFFERS 0 MISSING PASS · 3/8 `TESTS FAILED 1/1801` — the one red is `test_town_layout.gd :: test_the_locked_blacksmith_is_the_same_height_dimmed_with_a_one_line_reason` (W2-TOWN's in-flight file; nothing of mine is in its stack) · 4-7 SKIP (--fast). `tools/lint_motion.sh`: MOTION LINT OK. a11y_smoke run separately (18:42): `A11Y SMOKE PASSED 26 screen mount(s)`; Tavern empty sweep focusable=9 reachable=12, fixture sweep focusable=20 reachable=20, no warn.

## Judgement calls (recorded as made)

1. Rarity rim vs the selected rim (TOWN-18 and KIT-10 want the same pixels): every unselected card wears the 2px rarity rim (the theme's PanelRound box duplicated with only the rim recoloured/widened; Legendary adds a soft outer glow via the box's shadow); the SELECTED card wears `PanelRoundSelected` untouched — one selection idiom for W3-ROSTER to share — and keeps its rarity on the name colour, the class line and a rarity-rimmed portrait (`Widgets.slot_rarity`, on every card).
2. Gear slots are the mini cell (37px) beside the name, not `Cards.card`'s 47px bottom row: the bottom row would cost the bullets 50 of their ~104px; beside the name it costs 21. The class line moved under the portrait row at the card's full width and wraps at 150 (shaped at 191 before its first layout so headless minimums are honest); the Legendary "1 OF 1" stamp sits at the end of that row so a wrapped class line costs a stamped card nothing.
3. The band word stays the morale block's second line (canon's two-line row, node `Morale`); the rarity word stays in the class line rather than a chip — a `Widgets.chip` is header-sized and the 191px card has no row for it.
4. "Looking" on the selected card is an ENABLED Button in the `NavItemActive` plate (the kit's active-tab idiom; a press is the harmless re-selection a tab press is) — a disabled "Looking" would owe a reason Label the 262px card cannot spend (docs/13 §7 contract).
5. The caption plate is two columns (wide and short, docs/13 §9.4's header band) rather than one tall column: it covers the bar's back shelves, not the patrons or the round table. Its width and height follow its minimum (`size = ZERO`), so 150% never clips a word; the Manage list is bound to its rect.
6. The collection meter moved from the sidebar footer to the plate's facts column: LESSONS' objection (the caption was never rebuilt) no longer holds — the plate's facts are rebuilt in `_refresh()` with the sidebar — and it is a fact about the save, beside the house's other facts; test_tavern's four meter tests (incl. the empty board) are green.
7. The strip shows the board on the Manage tab too (the list is in the scene panel; the room's offer is the same either way); "Look" from Manage flips to the Board tab so the press shows the candidate.
8. TOWN-17's second half: the sidebar column is wrapped in a ScrollContainer built by the SCREEN inside Frame's panel (never inside Frame), bar inside the 18px pad; the common case fits without it (the test does the arithmetic with wrapped Labels shaped at 330).
9. `Widgets.reasoned` keeps its reason on one line by design; the Tavern's upgrade blocker ("Unknown guilds cannot commission this. Reach Known.") is wider than the plate's column at 150, so `_reasoned()` here gives the Reason Label a width and lets it wrap (the kit comment invites exactly that). No handoff needed.
10. Focus on rebuild: every region here is a rebuild (the kit's own strip and tab row included), and a rebuilt control drops keyboard focus. `_refresh()` notes the region that held focus BEFORE the clear, re-runs the shell's focus wiring (the `frame_focus_order` Callable, as Cards._turn_page does) and hands focus back to the same region (active tab / first focusable). Not required by any finding; without it a keyboard Hire/Dismiss/Manage press left no focus owner.

## Left undone / notes for later units

- The Manage tab's sidebar is sparse (section, one sentence, Back to town). Not a finding of this unit; a W3 pass could carry the roster's facts there.
- The Concept-3 placement of the slot row (bottom, 47px) is deliberately not followed on the Tavern card (judgement 2); the Roster card (Cards.card) keeps it.
- The wave's `verify.sh --fast` was red only on W2-TOWN's test file at the time of my run; re-run after the wave's handoffs.

## Log

- 17:53 report + handoff files created. Baseline refdiff scored on the wave-1 sheet's Tavern.png. Read the pre-unit fixture shot and the 08:50 Manage shot: sidebar rim at y≈775 over the strip, empty strip corner, "house 1 of 4" bare on the plate, Manage strip empty, list clipping its last row with no visible bar.
- 17:58 Tavern.gd rewritten (tabs); PARSE_CHECK OK (160).
- 18:02 first suite run red across the tree: `game/ui/SceneStage.gd` (W2-STAGE2) did not compile; every Tavern mount died at `SceneStage.load`. Fixed my own defect it exposed: tests must roll the board before reading `tavern_board[0]`.
- 18:10 suite: 4/1770 failing, none mine (market_grid, scene_stage, town_layout).
- 18:16 four shots taken and viewed; defect: class line trimmed at 125px ("Wizard — Comm…") → moved under the portrait row.
- 18:25 150 shot still trimmed at 191 → class line wraps, pre-shaped; stamp moved beside it. Suite: 1/1801 failing (test_town_layout, W2-TOWN).
- 18:34 all four shots retaken and viewed (fixture, Manage, emoji, text150) + crops; measurements: sidebar rim y=712, log panel 1045..1518 x 734..995; refdiff after recorded.
- 18:40 verify --fast (summary above), lint_motion OK, a11y_smoke 26 mounts PASSED.
