# Report — W4-LIFE (the camp walks, the sails turn, the gulls cross, the lanterns flame)

Unit: 00-plan.md §5 "W4-LIFE". Owns `game/ui/SceneStage.gd`, `game/assets/scenes/**`,
`tests/unit/test_scene_stage.gd`, `tools/aseprite/gen_fire.lua`, `tools/art/gen_actors.py`,
`tools/art/patch_plate.py` (new), `tools/art/preview_scene.py` (the `--animated` flag),
`game/assets/vfx/{sail_a,sail_b,gull,banner_wave,flame_lantern}.png` (+ .import, new files only),
`game/assets/bg/stage_town.png` (the windmill hub patch only, a recorded PY step).
Handoff: `build/plan/handoff-W4-LIFE.md`. Written as each item lands (rule 3).

## Acceptance (00-plan §W4-LIFE, as boxes)

- [x] A1 `paths` in the scene JSON `{who, points, px_per_s}` — a new layer type: JSON key + `apply_settings` handling + a test; the walker plays a `walk` animation derived by gen_actors.py (a bob walk from the idle frames) and falls back to `idle`
- [x] A2 gen_actors.py emits the walk strips byte-deterministically; `python tools/art/gen_actors.py --check` stays `GEN_ACTORS CHECK OK`; every manifest entry keeps `frames >= 4`; the 44 breath entries untouched
- [x] A3 banner sprites cut from the camp plate's pennants with a 4-frame wave (`banner_wave.png`, LUA in gen_fire.lua) as a new `banners` layer, held under reduced motion
- [x] A4 a 3-frame 6x8 lantern flame strip (`flame_lantern.png`, LUA in gen_fire.lua) on the camp's tent lanterns (props at the lights' positions), held under reduced motion, over-drive dropped under reduced effects
- [x] A5 windmill sails as `rotor` props (`sail_a.png`, `sail_b.png` cut from stage_town.png) rotating about their hub; the plate's painted sails inpainted away by `tools/art/patch_plate.py` (a ~40x40 hub patch, the exact command recorded below); held under reduced motion
- [x] A6 gulls as a 3-frame 12x6 strip (`gull.png`) on a slow spline (`flyers` layer), held under reduced motion
- [x] A7 `preview_scene.py stage_camp --animated` shows two actors on the path
- [x] A8 tests: every new layer type hidden/held under the two settings; `test_motion.gd:139-156` green (some scene keeps `fps > 3`)
- [x] S1 `MainMenu --frames=1,60` differs at the windmills and the sky (viewed)
- [x] S2 `Town --frames=1,60` differs at the lanterns and the banner (viewed)
- [x] S3 the `--set=reduced_motion=true` twins of S1 and S2 are identical (viewed)
- [x] G1 SceneStage contracts (§0.5): names, signatures, back-to-front by `pos.y`, shimmer uniforms, `apply_settings` literal; no focusables; no `create_tween`; `.import` sidecars for every new PNG
- [x] G2 `./tools/build_art.sh --check` prints 0 DIFFERS 0 MISSING; parse_check OK after every edit; `verify.sh --fast` green (summary pasted); `tools/lint_motion.sh` MOTION LINT OK

## Judgement calls (rule 8)

(appended as they are made)

## Log

- 01:35 Read in order: 00-plan §0 (+ the wave-4 ownership table) and §W4-LIFE; RULES.md in full (§1's test_scene_stage / test_motion rows, §2, §4, §5); TOWN-27, STAGE-14, STAGE-15 (for the market band), spec 09 §5 (rank 2's 3-frame 6x8 flame, rank 7/8), spec 03 §9 layer 4 (banners); LESSONS.md in full; report/handoff-W3-ENEMIES (gen_actors --check gate, the anim/ subdirectory precedent J1, the say_at rule), report-W2-STAGE2 (the `scroll` layer, the chimney stacks, patch_bubbles repointed), report-W1-VFX / W3-MENU (the cloud strips, the MainMenu comment); then the owned files in full: SceneStage.gd (2163 lines, tabs), test_scene_stage.gd (function list + the strip/actor/scroll/smoke/verb tests), gen_fire.lua, gen_actors.py, preview_scene.py, patch_bubbles.py (the pattern), lib.lua's API, build_art.sh, the six scene JSONs, vfx.json + test_vfx_assets.gd (NOT mine — it walks game/assets/vfx non-recursively and fails a PNG vfx.json does not name).
- 01:40 Measured on the plates (viewed W4LIFE_town_windmills_4x / millA_8x / millB_8x / camp_half / camp_ground_2x / banner_top_4x / banner_right_4x / banner_left2_4x / lanterns_6x): windmill A hub (1337,328), blades an X of length ~32 and width ~9; windmill B hub (1291,434), length ~34, width ~10. Camp banners (crimson cloth, gold sigil, hanging from a crossbar): T cloth x 871..901 y 86..157; L cloth x 531..556 y 167..236; R cloth x 1314..1342 y 708..782 (R is off every consumer's band: Town shows plate x 46..975). The ten lantern flames (warm-bright blobs) sit 5-10 px off their PointLight2D positions, e.g. light (335,305) -> flame (326.5,305.2), (515,566) -> (512,567). Open ground for a path: dirt x 550..1000, y 480..700, trees from (700,640).
- 01:55 A2 landed: gen_actors.py gains `WALKERS` (six townsfolk), `leg_split` / `stride` / `walk_frames` (the pose; body dipped 1 with the right leg lifted 2; the pose; the left), `FPS_WALK` 6; each writes `<key>_walk.png` + a `kind: "walk"` row with `walk_of`. `--check` before writing named exactly the 6 MISSING strips + actors.json DIFFERS (the 56 existing strips byte-identical); after `gen`: `GEN_ACTORS CHECK OK (63 files)`; 62 actors. Viewed W4LIFE_walk_frames_6x.png (six walkers x four frames): frames 1 and 3 lift one foot (right, then left) with the head dipped one pixel; frames 0 and 2 are the pose. Six .import sidecars written by build/shots/_w4life_imports.py (fresh uids, md5 dest names, from variant_r02_black's).
- 02:05 A5 (assets half) landed: tools/art/patch_plate.py (new) carries the two windmills as data (`ROTORS`: sail_a hub (1337,328) blades 32x9 at 43/133/223/313 deg, sail_b hub (1291,434) blades 34x10 at 45/135/225/315, hub r 5, 96x96 canvases), cuts each sail as the plate's own pixels inside a hard-edged blade+hub mask, and paints the footprint out of the plate by diffusion (mean of the clean pixels within 2, rim inward, then two 3x3 passes inside the mask). Compared three fills at 4x (viewed W4LIFE_sails_preview.png and W4LIFE_patch_compare.png: original | perpendicular lerp | diffusion | cv2 Telea | cv2 NS): the perpendicular lerp streaked; diffusion, Telea and NS all leave the same soft X under the blades, so the numpy diffusion stays and no cv2 dependency lands. The composite of the sails at rotation 0 over the patched plate differs from the untouched plate by 0 px. THE RECORDED STEP: `python tools/art/patch_plate.py` (no arguments; the recipe is the file's `ROTORS` table) — on its first run it copied the untouched plate to `art/src/bg/stage_town_raw.png` (4101051 bytes, the pristine record beside the other raw plates) and then wrote `game/assets/bg/stage_town.png` (RGB, 2414 px changed, all inside x 1264..1363 y 302..461 — the two mills; the rest byte-identical to the raw) and `game/assets/vfx/life/sail_a.png` + `sail_b.png`; `python tools/art/patch_plate.py --check` prints `PATCH_PLATE CHECK OK (3 files)`. gen_clouds.py samples plate rows 0..200 only, so its --check is unaffected (confirmed by the art gate below).
- 02:15 A3/A4/A6 (assets) landed in gen_fire.lua: `flame_lantern` 6x8x3 at 8 fps (the tongue machinery of the big fires with no dark edge row: body/tongue rim, hot inside, core at the seat — viewed W4LIFE_flame_gull_12x.png: three leaning tongues, bright at the seat); `banner_wave` (40x80x4, crop of stage_camp.png at 866,80) and `banner_wave_b` (40x88x4, crop at 526,158) — the cloth span per row (crimson rule, sigil included) rides a travelling ripple growing from 0 at the crossbar to 2 px at the hem, written relative to the rest pose so frame 0 IS the crop (checked: frame 0 == plate crop, alpha 255 everywhere; frames 1-3 differ by 613/842/743 px) — viewed W4LIFE_banner_wave_3x.png / _b_3x.png: the hem ripples, the crossbar and pole hold; `gull` 12x6x3 at 5 fps (wings up / level / down, off-white #E6E2D6 with grey tips, viewed). All written through lib.lua into `game/assets/vfx/life/` with `life.json` (the sails declared there too, as vfx.json declares the clouds). `./tools/build_art.sh --check`: `generators=7  runtime files: 204 agree  0 DIFFERS  0 MISSING` — ART CHECK OK (the three fires, ember and light_soft regenerated byte-identical). Sidecars for the four Lua PNGs + the two sails from fire_torch's .import.

- J1 (recorded 02:15) The five new PNGs the plan names at `game/assets/vfx/*.png` live one directory down, at `game/assets/vfx/life/` with their own `life.json`: `tests/unit/test_vfx_assets.gd` (not mine) fails BY NAME any PNG in game/assets/vfx that `vfx.json` does not name, and `vfx.json` is written only by `tools/aseprite/gen_vfx.lua` (W1-VFX's file; a hand-edited row would make `build_art.sh --check` DIFFER on vfx.json). The same shape W3-ENEMIES used for `enemies/anim/` (its J1). The scene JSON names the strips by full path, so nothing else cares where they sit; a handoff offers gen_vfx.lua the rows if the orchestrator would rather they moved up.
- J2 `art/src/bg/stage_town_raw.png` (not in the owned list) is created by the recorded patch step as the untouched plate: without it the step cannot reproduce (a second run would cut sails from an already-patched plate) and `--check` has nothing to regenerate from. It is the same file the note's "raw plates live in art/src/bg/*_raw.png" describes, for the one plate that had none.
- J3 The banners and the lantern flames are `props` entries (a new `anchor: "topleft"` for the opaque banner crops; a prop without an `emissive` key now draws untinted — every shipped prop carries one, so nothing changes for them) rather than new layer types: they are strips on the plate exactly as the hearth is, and the held/dropped rules the props already have are the rules the plan asks for. `paths`, `rotor` and `flyers` are the three NEW layer types (JSON key + apply_settings + a test each).
- J5 The travellers (walkers, sails, gulls) are HELD by reduced_motion and LEFT by reduced_effects: they are pieces of the world like the crowd (which RULES §2 keeps animating under reduced effects), and hiding a sail would bare the patch under it. The tests pin both halves.
- J6 The rotor sprite is not NEAREST-filtered (every other figure is): a painterly 96x96 cut of the plate turning through 360° crawls under a nearest sample; at rotation 0 it is pixel-exact either way (integer position, centred canvas).
- J7 The camp's two strolls are short (105 px and 75 px, ping-pong) because the Town's Blacksmith and Board callouts (plate x 556..946 y 462..542 and 606..846 y 638..712 under the Town's framing) and the speech plate leave a 2x body (78 px tall) only two clear patches of dirt; a walker passing under a callout would break TOWN-07's "no callout on a figure" for the moving figures the way it holds for the standing ones. Longer walks need the callouts re-anchored (W2-TOWN's file) or the figure scale ruled (Q01).
- J8 The gull got a slate underside after the first MainMenu pair lost the off-white bird over the harbour's foam — spec 09 §5 says "gulls", not their colour; contrast on both grounds is the design need.
- J4 The walk strips are second manifest rows (`<key>_walk`, kind `walk`, 4 frames at 6 fps) for six townsfolk, not extra frames on the idle strips: `test_every_townsfolk_breathes_in_four_frames` pins every breath entry to exactly 4 planted frames, and `test_the_manifest_and_the_strips_agree` needs one geometry per key. Six, not all 56: TOWN-27 asks for "bob/walk variants for 4-6 actors".
- 02:30 A1/A5/A6 (code) landed in SceneStage.gd (tabs, 0 space-indented lines; PARSE_CHECK OK, 178 scripts): the doc header's JSON shape gains `paths` / `rotor` / `flyers` and the props' `anchor: "topleft"`; `LIFE` / `LIFE_MANIFEST` consts; `_walkers` / `_rotors` / `_flyers`; static path arithmetic (`_points_of`, `_path_of` (arc-length table), `_along`, `_advance` (ping-pong or loop), `_spline` (Catmull-Rom, 12 samples a segment)); `add_walker(entry)` (through `add_actor`, renamed `Walker_<who>_<n>` with its `Shadow_<who>_walk<n>`, the `<who>_walk` strip added as a "walk" animation by `_add_walk_animation`, facing by travel direction, phases spread), `_settle_depth` + `_move_to` (a moving figure keeps the back-to-front rule every tick — the slot is computed in the CURRENT list and `move_child`'s remove-then-insert index handled once), `add_rotor` (Sprite2D `Rotor_N` about its centre, filtered like the plate), `add_flyer` (AnimatedSprite2D `Flyer_N` on the spline, life.json's row for frame_w/fps), the three `_tick_*` inside `_process`'s reduced-motion gate; `apply_settings` holds the flyers on frame 0 (walkers are `_actors` and were held already; rotors keep their angle because the tick is gated); `_build` reads `rotor` after the props, `paths` after the actors, `flyers` after the scroll layer (over the clouds); props: `anchor: "topleft"` = `centered = false`, and the warm over-drive only when `emissive` is present (every shipped prop has one). The banners and the lantern flames are props in stage_camp.json (10 flames on the measured painted glass with phases; two banners top-left at their crop origins); stage_town.json gains two `rotor` rows (0.18 / -0.14 rad/s) and three `flyers` (two circling the harbour mouth, one under the peaks).
- 02:35 preview_scene.py `--animated` (A7): renders 24 frames over 6 s (props on their fps, walkers on their paths, rotors turned, flyers on their splines — the same arithmetic in Python), writes build/preview/<name>.anim.gif and a four-frame filmstrip <name>.anim.png; `path_of` (res://) renamed `path_of_res`. Viewed W4LIFE_camp_walkers_strip.png (t = 0, 1.5, 3, 4.5): two walkers on the paths, moving between frames.
- 02:40 Tests written (spaces, appended to test_scene_stage.gd): `test_a_walker_walks_its_path_and_faces_the_way_it_goes`, `test_a_walker_without_a_walk_strip_still_walks_on_its_idle`, `test_a_walker_keeps_the_back_to_front_order_while_it_moves`, `test_a_rotor_turns_about_its_centre`, `test_a_flyer_rides_a_closed_spline`, `test_travellers_hold_under_reduced_motion_and_stay_under_reduced_effects`, `test_a_banner_is_an_untinted_topleft_prop_and_a_flame_is_over_driven`, `test_the_life_strips_match_their_table`, `test_the_banners_rest_on_the_plates_own_pixels`, `test_the_camp_lanterns_flame_and_two_townsfolk_walk`, `test_every_walk_strip_is_the_pose_with_a_lifted_foot`. Batch 1 (build/shots/W4LIFE_batch1.log, one lock hold): `--import` rc 0, PARSE_CHECK OK (178), this file through scratchpad/run_W4LIFE.gd: 80 run, 1 failing — the foot-lift test read the bottom ROW and three figures stand with the right foot a row higher; widened to the bottom four rows (a two-row lift always empties two of them). Batch 2 (W4LIFE_batch2.log): `W4LIFE TESTS 80 run, 0 failing`; the four two-frame shots SHOT OK.
- 02:50 Viewed the batch-2 shots: W4LIFE_MainMenu.a/.b — differing px 75640; inside the windmill box (1240,270,1400,490) 5558 px (crop W4LIFE_crop_menu_mills_ab.png at 3x, viewed: the upper mill's sails turned ~10° clockwise, the lower ~8° the other way, the patched plate under them hidden), the sky 28394 px (the clouds), the harbour 10091 px (shimmer + a gull); W4LIFE_MainMenu_rm.a/.b: 0 px differ (S3, the menu half). W4LIFE_Town.a/.b — differing px 113501; the north banner's screen box 1004 px, the west banner's 1237 px, the walk band 15043 px (crop W4LIFE_crop_town_walkers_ab.png, viewed: the grey-haired walker 18 px further along at frame 60); W4LIFE_Town_rm.a/.b: 0 px differ. Two things the crops showed and I fixed: (1) the gull (crop W4LIFE_crop_menu_gull1_ab.png) is off-white over the harbour's white foam and vanished there — it now carries a slate underside one pixel below the wing (W4LIFE_gull2_12x.png viewed over dark and pale grounds); (2) the Town's Blacksmith callout spans plate x 556..946, y 462..542 and the Board's 606..846, 638..712 (measured off the shot: W4LIFE_Town_a_half.png / W4LIFE_camp_paths_plan.png viewed with the rects drawn) — path A ran under the Blacksmith plate and path B's phase-0.5 start was hidden behind it. Both paths re-authored onto the dirt the callouts leave: A [[575,632],[680,626]] (the band between the two plates, short of the pines), B [[350,615],[352,690]] (the grass left of the crates, under the speech plate's foot); preview re-rendered (W4LIFE_camp_walkers_strip2.png viewed: both walk, neither under chrome). Art gates after the gull: ART CHECK OK 204 agree 0/0; PATCH_PLATE CHECK OK; GEN_ACTORS CHECK OK; MOTION LINT OK.
- 03:05 Handoff written (build/plan/handoff-W4-LIFE.md; `apply_handoff.py --dry-run` finds both anchors, tree untouched): §1 build_art.sh gains `python tools/art/patch_plate.py --check` beside gen_actors' (until it lands the sails + the patched plate are gated by the script's own --check, green); §2 MainMenu.gd's `_build()` comment (W3-MENU's) says the sails are still owed — the new text keeps test_menu_lockup's two pinned strings ("water shimmer is on", "M4B-VFX-01", the airship still owed); two observations (M4B-VFX-01 now owes only the airship; the life strips' folder and the six gen_vfx.lua rows if the orchestrator wants them moved up).
- 03:08 G1 greps over SceneStage.gd: each of the eight contract functions defined once; `const DEFAULT_ARENA := "stage_arena_cave"`; the literal `func apply_settings(reduced_motion: bool, reduced_effects: bool)` at :2392; `create_tween` / `focus_mode` / `class_name` 0; 20 MOUSE_FILTER_IGNORE sites; the scene JSONs carry 16 shimmer+pulse rects inside 1536x1024 and 39 prop/actor entries above 3 fps (test_motion.gd:139-156's need — the ten new flames run at 8). Node names: `Walker_<who>_<n>` + `Shadow_<who>_walk<n>`, `Rotor_<n>`, `Flyer_<n>`; the walkers are `_actors` entries (held by the existing loop); back-to-front by `pos.y` holds for the standing crowd and is re-settled for a walker every tick (`test_a_walker_keeps_the_back_to_front_order_while_it_moves`).

## Resumed after the session restart (the previous agent was killed at ~03:08, after the G1 greps and before verify --fast)

- 10:30 Read 00-plan §0 + the wave-4 table + §W4-LIFE, RULES.md (§1's scene-stage/motion rows, §2, §4), TOWN-27, STAGE-14, LESSONS.md, the report and handoff above; then audited the tree against every log line: `git status`/`git diff --stat` show exactly the owned files (SceneStage.gd +407, test_scene_stage.gd +382, gen_fire.lua +170, gen_actors.py +94, preview_scene.py +198, stage_camp.json +42, stage_town.json +24, patch_plate.py new, stage_town.png patched, art/src/bg/stage_town_raw.png new, game/assets/vfx/life/ with 5 PNGs + 6 .import + life.json, six `*_walk.png` + .import under actors/).
- 10:32 Gates without the engine: `./tools/build_art.sh --check` → `ART CHECK generators=7 runtime files: 204 agree 0 DIFFERS 0 MISSING` / ART CHECK OK; `gen_actors.py --check` → `62 actors (12 sliced, 44 breath, 6 walk)`, `GEN_ACTORS CHECK OK (63 files)`; `patch_plate.py --check` → `PATCH_PLATE CHECK OK (3 files)`; `lint_motion.sh` → MOTION LINT OK. One lock hold (scratchpad/batch_W4-LIFE_verify1.sh, log build/shots/W4LIFE_batch3.log): `PARSE_CHECK scanned 178 script(s) / PARSE_CHECK OK`; `scratchpad/run_W4LIFE.gd` → `W4LIFE TESTS 80 run, 0 failing [1631 ms]` — no half-written function survived the kill.
- 10:33 Re-measured the final (02:28) shot pairs with scratchpad/diff_W4-LIFE.py: MainMenu a/b 73673 px differ — the windmill box (1240,270,1400,490) 5565, the sky rows 0..200 26592, the harbour 23548; MainMenu_rm a/b 0. Town a/b 115975 px differ — the west banner 1760 in its box, the lantern band 75842, the walk band 10816; Town_rm a/b 0 (S3 holds for both). Viewed W4LIFE_crop_menu_mills_ab.png: the upper mill's sails turned clockwise, the lower's the other way, no seam under either. Viewed W4LIFE_crop_town_walkers2_ab.png: the walker on the grass by the crates is lower at frame 60, the grey-haired walker ~20 px further right past the lantern, the lantern flame lit, neither figure under a callout. Viewed W4LIFE_crop_town_bannerT_ab.png: the hem of the north banner ripples between the frames, the pole and crossbar hold.
- 10:34 A7 re-run: `python tools/art/preview_scene.py stage_camp --animated` → `24 frames over 6s -> build/preview/stage_camp.anim.gif, filmstrip (t=0, 1.5, 3, 4.5) -> build/preview/stage_camp.anim.png`. Viewed the filmstrip (a 2x2 grid at half scale): the white-haired walker steps right past the tent lantern and the dark-haired one moves down the grass by the crates from panel to panel — two actors on the path.
- 10:35 G1 re-greps: `from_data`/`load`/`place_party`/`place_boss`/`apply_settings`/`motion_held`/`party_state_style`/`actor_for_class` each defined once, `const DEFAULT_ARENA := "stage_arena_cave"`, the literal `func apply_settings(reduced_motion: bool, reduced_effects: bool)` at :2392; `create_tween` 0, `focus_mode` 0, `class_name` 0; SceneStage.gd has 0 space-indented lines, test_scene_stage.gd 0 tab-indented lines; six walk `.import` sidecars present. `apply_handoff.py --dry-run` on the handoff: both anchors found (`APPLIED #1 tools/build_art.sh`, `APPLIED #2 game/screens/MainMenu.gd`), tree untouched.
- 10:40 G2: `GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` (log build/shots/W4LIFE_verify_fast.log), rc 0:
  ```
  PASS  LINT OK  no cross-file class_name references in sim/ or game/
  PASS  MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)
  PASS  PARSE_CHECK scanned 178 script(s)
  PASS  16 generated file(s) agree with tools/gen_items.gd
  PASS  ART CHECK  generators=7  runtime files: 204 agree  0 DIFFERS  0 MISSING
  PASS  TESTS PASSED   1956 test(s) in 96 file(s)  [67778 ms]
  VERIFY OK
  ```
  test_motion.gd's fps>3 requirement, test_vfx_assets.gd (the life/ folder is outside its non-recursive walk) and W3-ENEMIES' boss/say_at pins all pass inside that run. Every box above is ticked on this evidence.

## Left / not done

- Nothing in the unit's text is left. The two handoff edits (build_art.sh's patch_plate gate, MainMenu.gd's comment) wait for the orchestrator; the five life strips sit in `game/assets/vfx/life/` rather than `game/assets/vfx/` (J1; the move recipe is in the handoff's observation). Refdiff numbers: the plan asks none of this unit (the motions are held at frame 0 in the gate's stills; the Town/MainMenu masked bands cover the scene).
