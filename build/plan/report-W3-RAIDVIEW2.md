# Report — W3-RAIDVIEW2 (the beats: VFX triggers, the wipe's page, the settle, the pause, the boss plate)

Unit: 00-plan.md §4 "W3-RAIDVIEW2". Owns `game/screens/RaidView.gd` (TABS) and
`tests/unit/test_raid_beats.gd` (new, SPACES). Handoff: `build/plan/handoff-W3-RAIDVIEW2.md`.
Started 2026-09-14. Written as each item lands (rule 3). RE-CREATED at 22:17 after the whole of
build/plan/ (108 tracked files + build/.gdignore) vanished from the working tree at ~22:13-22:16 — not
by this unit; the tracked files were put back from HEAD with `git show` (build/shots/w3rv2_restore_plan.py,
no checkout/reset), and this file was rewritten from the working notes. See the Log.

## Acceptance (00-plan §W3-RAIDVIEW2, as boxes)

- [x] A1 STAGE-03 + STAGE-09 + PIPE-02 — the Verb→verb table in `_append_line`: ATTACK → `act(attacker, "attack")` + slash/bolt + `hit` + burst on the target; HEAL → `act(healer, "cast")` + green burst; MECHANIC → boss eye flash; STATE_CHANGE dead → `act(spr, "death")`; MISTAKE → `act(spr, "fumble")`; the per-class VFX family defaults by role (Q18, recorded below, never resolved).
  - `_stage_verbs(e)` runs after the fold on every appended line; `_attack_beat` / `_heal_beat` / `_mechanic_beat` / `_death_beat` / `_boss_death_beat` / `_fumble_beat` call only verbs SceneStage.gd carried at the wave's start (`act`/`hit`/`burst`/`bolt`/`slash`/`head_of`, each grepped; a test pins it). Flourishes fire only while `_live` (G08: a skip lands 200 lines in one frame); a death is a destination and lands on a skip too, so `--advance=all` shows the twelve toppled (W3RV2_all_party_x2.png). The boss's line is an `impact` burst + `hit` on the raider (nothing in its hands to slash with). `_body_of(spr)` (between `head_of` and the feet) is where every strike lands, at the target's own scale. Seen: W3RV2_paused.png — Greg mid-fumble with the '!' over his head; W3RV2_story10_x2.png — three fumbles frozen with their marks; W3RV2_numbers10_x2.png — the impact burst's strip over Bork under two -30s. The boss was ABSENT from every batch-1 shot (W3-ENEMIES' new enemies/anim strips were not yet imported on this machine: 'Unable to open .godot/imported/boss_main.png...ctex', a SceneStage error, not this screen's), so the slash over the boss is re-shot in batch 2 after verify's import step.
- [x] A2 COMBAT-15 — the WIPE. stamp presses across the LOG panel at -7° with the cost line beneath; the blot bleeds under it on the panel; the seal rests at (1440, ≤ 952) straddling the panel's lower-right; `test_wipe_sequence.gd:283-286` and `:294-297` green unchanged.
  - `WIPE_STAMP_RECT := Rect2(1044, 781, 474, 90)` (LOG_RECT's width, 40px under its top); the box centred in it, the 320x150 blot centred on the same point (y 751..901, inside the panel's 741..981), the cost line at y 877 across the panel; `WAX_SEAL_REST := Vector2(1440, 946)` — the seal's centre on the panel's bottom edge (981), its right edge 6px inside the panel's (COMBAT-15's corrected 'y <= 952'). Blot and box stay DIRECT children of the screen, blot first (`WipeCostPlate`/`WipeCost` follow the box). Seen in W3RV2_all_log_x2.png: the stamp over the rows, the bleed under it in the panel, the seal on the corner, '[R11] WIPE.' as the last line. Two legibility fixes from that crop: the red word vanished in the red bleed (now a 3px GROUND_PAGE outline on the word, the damage numbers' device) and the cost line landed across 'Rhona is DEAD.' (now on its own 0.92 SURFACE_PANEL strip). Re-shot in batch 2.
- [x] A3 COMBAT-17 — each log row lands in a MarginContainer whose `margin_top` goes 1→0 over `Motion.LOG_SETTLE` through `Widgets.tween`; a 1px EDGE_SLATE rule right of the round divider.
  - `_settle(row)` wraps every row (and every joke indent) in a MarginContainer 'Settle' whose `margin_top` goes 1 -> 0 through `Widgets.tween(wrap).tween_method(...)` over `motion_duration(Motion.LOG_SETTLE)`; outside a tree (the suite) or during a skip the margin is 0 at once. The wrapper is what `_log_rows` ages, so W2's age-ramp test reads the same modulate. `_round_marker(n)`: an HBox of the 'Round N' Label (text unchanged) and a 1px EDGE_SLATE ColorRect 'Rule' expanding to the row's right edge, 6px up from the floor. Seen in W3RV2_all_log_x2.png: 'Round 11 ----'.
- [x] A4 COMBAT-21 + RULES-04 — the wipe docstrings name the assets and `gen_wipe.lua`; RaidView.gd contains no "has no blot" and no "art that does not exist".
  - The 'FIVE OF THE SIX BEATS' block is now 'SIX BEATS, FIVE HERE': it names wipe_blot.png, wax_seal.png, gen_wipe.lua and build_art.sh --check, and the one owed beat is t=1,800's slide behind Q09. grep over RaidView.gd for 'has no blot' / 'art that does not exist' = 0 / 0 (test_the_wipe_docstrings_tell_the_truth pins both, plus the asset and generator names).
- [x] A5 CRITIC-G13 — the `log_manual_advance` dwell shows a `Widgets.stamp("PAUSED — mistake", Palette.CAUTION)` Label on the log panel with the Pause button focused; one finding per verbosity mode recorded.
  - `_dwell_on` pauses, raises `_paused_stamp` (built once through `Widgets.stamp('PAUSED — mistake', Palette.CAUTION)`, a direct child after the log panel, its right edge 12px inside the panel's at y 745) and grabs focus on Pause when it is in a tree; `_sync_controls` hides it the moment the player is no longer paused; a pause the PLAYER chose never wears the word (`_dwelling`). Seen in W3RV2_paused.png: the amber leaning stamp on the panel's top-right, 'Resume' with the steel focus ring, Greg's panel rimmed, the account stopped on his Severe mistake at line 2. The per-mode findings are J8.
- [x] A6 CRITIC-G08 — a test steps the player at Speed.TWO for one second and counts StyleBox swaps on any one panel ≤ 3.
  - test_at_two_x_no_panel_swaps_its_stylebox_more_than_three_times_a_second: 50 steps of `_process(0.02)` at Speed.TWO on the mounted screen, a per-panel transition count of `has_theme_stylebox_override('panel')` after every step, worst panel <= 3 (the rim moves once per round at 2x and a round is 1.3 s — W2's coalescing, now measured). Green in both suite runs.
- [x] A7 COMBAT-05 — `Icons.at("sigil", rank)` in the 48px slot; affix cells 30px at pitch 34; the plate name stays `display_name` (Q12a).
  - `_boss_rank_key()` reads the rank off the encounter sprite's file name (`boss_main_2`), so Cards' rank rule is not copied; `Icons.exists('sigil', key)` guards it, the body is the fallback. Cells: `x = 1001 + 34 * i`, 30x30 at y 122 (spec 02 §9.2). Seen in W3RV2_adv10.png: the bare 48px jaw glyph in the slot at (943,61), five 30px affix cells at pitch 34 under the bar. The plate reads 'Raid 1 — Encounter 5' (Q12a).
- [x] A8 COMBAT-14 — `const EXITS_GATED := false` (Q12c); when true the exits disable with the reason "The account is still being read" until `is_finished()`.
  - `_gate_exits()` reparents both exits into `Widgets.reasoned` boxes at their own positions (Reason above, box bottom-pinned to the row, the button keeping its size); `_release_exits()` re-enables, un-dims and hides the reasons; `_footer` calls the gate only under `EXITS_GATED`, `_process` releases on the first finished frame and `_on_skip` too. Both seams are exercised directly by test_the_gate_behind_the_switch_locks_and_releases_the_exits; the default is pinned by test_exits_gated_is_the_plans_default_and_the_exits_are_live (both exits live and no reason printed at goto).
- [x] A9 COMBAT-07 — the boss consumed through `stage.place_boss(...)` only, held as Node2D; no SceneStage function called that did not exist at the wave's start.
  - `var _boss_sprite: Node2D = null`; `_target_sprite`/`_actor_sprite` return Node2D; `_settle_lifts` keeps its `node == _boss_sprite` branch first. test_the_boss_is_held_as_a_node2d pins the declaration; test_every_stage_call_here_existed_at_the_waves_start greps each `_stage_node.<fn>(` the screen names against `func <fn>(` in SceneStage.gd.
- [x] A10 `tests/unit/test_raid_beats.gd`: the trigger table fires a stage verb per Verb (through `from_data` stubs), the flash-rate test, the paused Label exists; `test_wipe_sequence.gd` and `test_log_player.gd` green unchanged.
  - tests/unit/test_raid_beats.gd — 24 tests, spaces, green in batch 1 (`TESTS FAILED 9/1856`, none mine) and in verify (`8/1898`, none mine): the switch (default + gate/release seams), the role table (every ROLE_KEYS row, the recorded default), the docstrings, the Node2D declaration, the stamp/blot/seal geometry on the log panel, the settle wrapper, the round rule, the PHASE heading, the dwell's stamp + Resume + a player's own pause, the G08 rate, the sigil + affix cells, and the verbs through a `SceneStage.from_data` probe (two figures + a boss; the stage clock stepped by hand): melee attack -> lunge + slash/burst + the boss's flash + return to the mark; boss attack -> impact burst + the raider's flash, no slash; heal -> the caster's emissive lift + the sparkle; death -> the 45-degree topple, `_dying`, and a replay standing it up; mistake -> the fumble's lunge + the 'Mark_' '!' Label; mechanic -> the void burst at `_eye_of`; a skip -> no strike but the death lands; reduced motion -> no lunge, the flash still lands; a fallen actor -> no UI lift and no refresh snap mid-lerp; every `_stage_node.<fn>(` the screen names exists as `func <fn>(` in SceneStage.gd. test_wipe_sequence.gd (19), test_log_player.gd, test_raid_overlays.gd (19) green unchanged in both runs.
- [x] S1 shot `RaidView --fixture=raid --advance=10` (viewed): a slash or burst over the last ATTACK's target and a mid-fumble figure with "!" after a MISTAKE.
  - build/shots/W3RV2_adv10.png (40 frames) + W3RV2_adv10_f8.png (8 frames) + the 2x crops W3RV2_adv10_crops.png / W3RV2_adv10_f8_crops.png (all viewed, batch 2 — the boss on the floor now that verify's import step had built W3-ENEMIES' strips): over the boss (the last ATTACK's target, 'Pip hits Main Boss for 4.') the impact burst — the pink additive starburst on its maw at 8 frames, its dispersing particles and a later strip frame at 40 — under two '-4' numbers; over Bork the boss's own impact burst under two '-30's; Tiny (the tenth line's MISTAKE) mid-fumble with the '!' over his head; five mistakes counted; the boss plate with the bare jaw sigil and the five 30px affix cells at pitch 34. The slash arc fires on the same line (the rogue's family) but the burst's starburst sits on top of it in both takes.
- [x] S2 shot `--advance=all` (viewed): the stamp across the log panel, the blot under it, the seal at the panel's lower-right.
  - build/shots/W3RV2_all.png + W3RV2_all_log_x2.png + W3RV2_all_party_x2.png (viewed): the WIPE. box over the log rows with the bleed under it on the panel, the cost line beneath, the seal straddling the panel's lower-right corner, '[R11] WIPE.' as the last line; twelve figures toppled 45 degrees and grey under their skull plates and blot pips; 'Back to the board' focused; 25 mistakes counted. The first take's two legibility faults (A2) are fixed for the batch-2 re-shot.
- [x] S3 shot `--set=log_manual_advance=true --advance=3` (viewed): the PAUSED stamp with Pause focused.
  - build/shots/W3RV2_paused.png (viewed): 'PAUSED — mistake' in CAUTION, leaning, on the log panel's top-right; the Pause button reads 'Resume' and carries the steel focus ring; Greg mid-fumble on the floor with the '!' and his joke bubble; the log stopped on 'Greg (Rogue) — Severe — Dropped a Mecha...'. (The account paused on line 2, so `--advance=3` ends there: the instrument's loop runs its step cap against a paused player and the shot is the paused state.)
- [x] S4 shot `--fixture=raid --set=reduced_motion=true --advance=all` (viewed): the stamp arrives instantly, no rotation, the 60 ms floor.
  - build/shots/W3RV2_rm_all.png + W3RV2_rm_all_log_x2.png (viewed): the same page as S2 — stamp, bleed, cost line, seal, toppled twelve — with the stamp at its static lean and no tilt motion (the `WIPE_STAMP_TILT_DEG * motion_scale()` rule W2 built); the two log crops differ only inside the stamp's own rect (diff bbox (129,24)-(358,153) in the 2x crop: the press's end state vs the direct set).
- [x] G1 `WIPE_*` constants and members, `_run_wipe_sequence` literals, no `await create_timer`, exactly one "WIPE." Label; the a11y focus owner unchanged; `Motion` constants through `Widgets.tween`; `lint_motion.sh` OK.
  - grep over RaidView.gd: the five WIPE_BEAT_* and WIPE_STAMP_*/WIPE_DIM/WIPE_BLEED_MS/WIPE_BLOT/WAX_SEAL/FRAME constants at their values; `func _run_wipe_sequence(elapsed_ms: float)`, `_run_wipe_sequence(float(WIPE_BEAT_EXITS_MS))`, `if _wipe_beats.has(method)` present; `await get_tree` absent; `_wipe_shown/_wipe_dim/_wipe_blot/_wax_seal/_wipe_stamp_label` unchanged; exactly one 'WIPE.' Label (test_wipe_sequence + test_raid_overlays green). a11y_smoke.gd: `A11Y SMOKE PASSED 26 screen mount(s)`, RaidView owner '1x' in both sweeps, disabled=1 (the locked skip), no warn (build/shots/w3rv2_a11y.log). `Motion.LOG_SETTLE` is consumed through `Widgets.tween` + `motion_duration`; `tools/lint_motion.sh` -> `MOTION LINT OK`.
- [x] G2 parse_check OK after every batch; `verify.sh --fast` summary pasted; RaidView.gd tab-indented, the test space-indented.
  - parse_check OK after every batch (170 -> 172 scripts as other units added files); RaidView.gd 0 space-led lines (2454 lines, tabs), test_raid_beats.gd 0 tab-led lines (599 lines, spaces). verify summary in the Log.

## Judgement calls (rule 8)

- J1 (Q18 — the VFX family default, recorded, not resolved): `VFX_FAMILY` keys `Enums.ROLE_KEYS`. Attack: main_tank / melee_dps_offtank / melee_dps -> `slash` (the arc strip over the target + an `impact` burst); main_tank_healer / raid_healer / chain_healer / support -> `bolt` of kind `arcane`; aoe_caster -> `bolt` of kind `fire`; single_target_caster -> `bolt` of kind `arcane`. Support (a HEAL line): `heal` (the sparkle strip) for every role. The boss: `impact` burst + `hit`, no slash. A role missing from the table is treated as melee. The designer's per-class mapping (PIPE-02's question) replaces rows here and nowhere else.
- J2 (the boss's eye): SceneStage has no eye anchor; `_eye_of(boss)` is the head point plus a quarter of the body's height — the upper quarter of each of the six slices is where its eye/mouth cluster sits. The MECHANIC burst is `void` (spec 02 §3.5 paints the mechanic cells in the void family); W2's one-frame modulate flash on the boss and the affix cell's lift stay in `_line_effects` (the UI layer G08 governs).
- J3 (the interim one-frame hit flash): W2's `_lift(target, TEXT_BODY, 0)` on an ATTACK was COMBAT-07's placeholder for the stage's `hit`; `stage.hit` now flashes the figure for `Motion.HIT` on the stage clock and recoils it 2px, so the placeholder is removed rather than doubled. (`Palette.HIT_FLASH` landed from W2's handoff; the stage's FLASH_COLOR is the same value and is what `hit` uses.)
- J4 (a fallen figure is not brightened): the actor of a 'dead' STATE_CHANGE line no longer gets the 1.15 lift, and `_refresh_party_sprites` / `_settle_lifts` leave its modulate alone while `_dying[id]` holds (the act's 400 ms on the screen clock) — the death act lerps the grey and the refresh was snapping it under the lerp. `_stage_verbs` runs BEFORE `_refresh_bars` for the same reason.
- J5 (verbs and the skip): a skipped account (`_live == false`) fires no flourish — 200 lines in one frame would be 200 bursts — but every death act lands (a destination), so the floor after 'Skip to the end' shows who fell; the boss topples when its bar reaches zero (`_boss_death_beat`, the same reading that greys it). A replay (tier change) stands everyone back up (`_reset_player` zeroes the rotations the finished acts left).
- J6 (where 'PAUSED — mistake' sits): the plan says 'on the log panel'; it is a direct child of the screen after the panel with its right edge 12px inside the panel's top-right, so the newest row — the mistake the account stopped on — stays readable under it.
- J7 (COMBAT-15's seal rest): '(1440, <= 952)' — 946 puts the 72px seal's centre on the panel's bottom edge and its right edge 6px inside the panel's, straddling the lower-right the way a seal on a page's corner does; both test_wipe_sequence checks hold.
- J8 (G13's per-mode findings; fixed here where RaidView owns them). Story: the tier carries mistakes, states, mechanics and system lines only, so no ATTACK/HEAL verb reaches the floor in Story mode; the fold still runs to each revealed line's sequence, so the bars, the tints and the floor's fumbles/deaths agree with the log (W3RV2_story10_x2.png: three frozen fumbles with their marks, no strikes) — correct by §11.1 (verbosity is a filter), nothing to fix. Play-by-play: the default and the acceptance mode; every verb fires (W3RV2_adv10.png / W3RV2_numbers10_x2.png); the one change is J3's (no doubled flash). Numbers: adds the PHASE rows ('-- Pull --'); they were landing as lines of the account — settle, age ramp, a log glyph, an over-head bar switch — but a phase divider is a heading like the round marker, so `_append_line` now prints it as a slate LabelSmall heading with no settle, no age ramp, no verb and no bar switch (test_a_phase_row_is_a_heading_not_a_line_of_the_account).
- J9 (the stamp's ink on the bleed): the plan puts the word on its own bleed on a dark page — red on red. The word keeps DANGER (it is the ink) and gains a 3px outline in GROUND_PAGE, the paper showing through the pressed edge; the cost line gets a 0.92 SURFACE_PANEL strip so it never lands across a row. Neither touches a test contract (one 'WIPE.' Label, blot below box).

## Log (write-as-you-go)

- Read in order: 00-plan §0 (+ §4's ownership table, §W3-RAIDVIEW2, §6 Q12/Q18); RULES.md (§1 test_wipe_sequence/test_motion rows, §2, §4, §5); COMBAT-03/05/07/14/15/17/21, STAGE-03/09, PIPE-02, CRITIC-G08/G13, RULES-04; LESSONS.md in full; handoff-W2-STAGE2 (the verbs' shape), handoff-W2-RAIDVIEW, report-W2-RAIDVIEW, report-W2-STAGE2; RaidView.gd (1997 lines) in full; SceneStage.gd's overlay + verb + VFX sections (:419-1215) and apply_settings/_process; Widgets.gd's Motion/stamp/stamp_box/stamp_press/reasoned/tween; Icons.gd; Cards.encounter_sprite; test_wipe_sequence.gd, test_raid_overlays.gd, test_log_player.gd (the RaidView half); tools/shot.gd's --advance and frame loop; LogPlayer's API; Enums' Role/RoleGroup/Verb/LogTier.
- Facts fixed before coding: (1) the unit suite has nothing inside a tree, so `Widgets.tween` answers null there and `grab_focus` fails — every tween site keeps its "null → arrive at once" branch and the Pause focus is guarded by `is_inside_tree()`; (2) `place_boss` returns Sprite2D today and AnimatedSprite2D after W3-ENEMIES — `_boss_sprite` becomes Node2D; (3) the sigil files are `sigil_boss_<rank>[_2].png`, so the key is the enemies PNG's basename; (4) `--advance=N` feeds the SCREEN's `_process` only — the stage's clock does not move during the synthetic steps, so every verb fired by the N lines is at t=0 on the stage's clock when the capture frames begin; (5) spec 02 §9.2's affix row is `1001/1035/1069/1103, 122, 30, 30`; (6) `Widgets.stamp(text, tone: Color)` returns a Label, `stamp_box(word, tone: String)` a PanelContainer; (7) tests/run_tests.gd has no filter — one suite run per batch.
- 22:17 INCIDENT, not mine: `build/plan/` (every tracked report/handoff/plan file, 108 of them) and `build/.gdignore` were gone from the working tree (`git status`: 109 ` D` rows; the directory itself re-appeared at 22:16:49 holding only W3-ENEMIES' freshly rewritten handoff). build/shots/ was untouched. The missing .gdignore would have made the next engine run import 600 shot PNGs, so the 109 tracked files were restored from HEAD with `git show` (read-only; no checkout/reset/clean). Any wave-3 unit's UNTRACKED report/handoff written before ~22:13 is gone unless that unit rewrites it — the orchestrator is told in this unit's result.
- Batch 1 (one lock, 22:05-22:11): `run_tests.gd` -> `TESTS FAILED 9/1856` — all 24 of test_raid_beats.gd green; the nine reds are other units' in-flight files (test_menu_lockup, test_paper_doll, test_scene_stage's manifest-vs-strips, test_starting_roster, test_text_scale's Guildhall bare call, test_widgets_kit's callout clamps) — none reads RaidView. test_wipe_sequence.gd, test_log_player.gd, test_raid_overlays.gd green unchanged. Six shots taken and viewed (S1-S4, J8): W3RV2_adv10 / _all / _paused / _rm_all / _story10 / _numbers10.png. Every shot's engine log carried `Failed loading resource: res://game/assets/enemies/anim/boss_main.png` + `Cannot call method 'get_height' on a null value` from SceneStage's new `place_boss` (W3-ENEMIES, mid-flight: the strips' .import files exist, the imported .ctex not yet) — the boss is absent from all six, which is why no strike over the boss is in batch 1. Also fixed after the crops: `_wipe_exits` guards its grab_focus with `is_inside_tree()` (the backtrace noise every RaidView test carried).
- Batch 2 (one lock, 22:40-22:47; a first attempt at 22:30 hit W3-KIT2's Cards.gd mid-edit — every screen failed to parse for a minute, then parse_check was OK again on its own): `GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` (build/shots/w3rv2_verify_fast.log): PASS class cache regenerated · PASS LINT OK · PASS MOTION LINT OK · PASS PARSE_CHECK scanned 172 script(s) · PASS 16 generated file(s) agree · PASS ART CHECK generators=7 runtime files: 197 agree 0 DIFFERS 0 MISSING · FAIL unit tests `TESTS FAILED 8/1898 failing [215455 ms]` · SKIP x4 (--fast) · VERIFY FAILED. The 8 reds by file: test_menu_lockup.gd (W3-MENU), test_options_layout.gd (W3-OPTIONS), test_roster_layout.gd (W3-ROSTER), test_scene_stage.gd's manifest-vs-strips (W3-ENEMIES), test_text_scale.gd's Guildhall bare `get_theme()` call (W3-ROSTER), test_widgets_kit.gd's callout clamps (W3-KIT2) — none reads RaidView; test_raid_beats / test_raid_overlays / test_wipe_sequence / test_log_player all green. `tools/lint_motion.sh` -> MOTION LINT OK. Shots: W3RV2_adv10.png (40), W3RV2_adv10_f8.png (8), W3RV2_all.png (40, the legibility fixes in), W3RV2_fixture.png; all viewed (S1, S2 re-read: the outlined WIPE. reads on the bleed, the cost line on its strip, the seal on the corner, the boss standing since a wipe leaves its bar above zero). a11y_smoke run separately under the lock: PASSED, 26 mounts.
- refdiff RaidView vs Concept 2, mask 210,77,928,640 — BEFORE (W2-RAIDVIEW's recorded after, W2RV_emoji.png): mae 34.031 / layout_iou 0.1187 / within-8 24.74; AFTER (W3RV2_fixture.png, `--fixture=raid`, with W3-ENEMIES' boss and W3-KIT2's kit in the tree): mae 34.058 / layout_iou 0.1185 / within-8 24.77 — no regression (RULES-11: the chrome-side changes are the 48px sigil glyph and the 30px cells).
- Handoff: two observations, no edits — the one-plate-at-a-time rule for `say_at` (W3-ENEMIES / orchestrator; three joke plates still stack in W3RV2_story10_x2.png) and the build/plan deletion incident. `python tools/apply_handoff.py build/plan/handoff-W3-RAIDVIEW2.md --dry-run` -> 'no edits found' (parses; nothing to apply).
- Left for others / not done: nothing of the unit's own scope. Q12a (the plate's creature name), Q12c (`EXITS_GATED`), Q18 (`VFX_FAMILY` per class) stay behind their switches at the recommended defaults. The boss's tags (W3-ENEMIES) are never named here; when their `place_boss` lands the boss's `hit`/`death` acts already run on whatever Node2D it returns. The joke plates' stacking is the stage's rule (handoff observation).
