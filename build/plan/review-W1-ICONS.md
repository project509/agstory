# Review — W1-ICONS (one icon grid, one generator, one lookup)

Reviewer: adversarial, second resume of wave 1. Everything below is verified against the TREE (git diff, files on disk, my own locked runs and shots), not against the report. Written as I go.

## 1. Tree vs ownership

Owned: `tools/aseprite/gen_icons.lua`, `game/ui/Icons.gd` (new), `game/assets/ui/icons/**`, `game/assets/ui/faces_30.png`/`faces_36.png` (+ .json + .import), `art/src/ui/icons/**`, `tests/unit/test_icons.gd` (new).

`git status --porcelain` (186 lines) — the unit's footprint, checked file by file:
- `M tools/aseprite/gen_icons.lua` (+962/-65, read in full — sections 1b faces 30/36, 2b three provisions, 3 rank re-author, 5 arrow_up/down, 6-16 the grid + manifest).
- `M art/src/ui/icons/rank_*.aseprite` x6, `M game/assets/ui/icons/rank_*.png` x6, `M game/assets/ui/icons/mech_*.png` x12 — the two intended re-authors, nothing else at the top level is modified (`git diff --stat -- game/assets/ui/icons` = exactly 18 binaries).
- `?? game/assets/ui/icons/grid/` — 71 PNG + 71 .import + icons.json (143 entries; walk: 0 PNG without a sidecar).
- `?? art/src/ui/icons/grid/` (71 .aseprite), `?? art/src/ui/icons/mech_*.aseprite` x12 (mech_* had no source before — they were resampled crops), `?? art/src/ui/icons/faces_30.aseprite`, `faces_36.aseprite`.
- `?? game/assets/ui/faces_30.{png,json,png.import}`, `faces_36.{png,json,png.import}`.
- `?? game/ui/Icons.gd` (+ .uid), `?? tests/unit/test_icons.gd` (+ .uid).
- `?? build/plan/report-W1-ICONS.md`, `handoff-W1-ICONS.md`.
- `game/assets/ui/faces.png`, `faces.json`, `art/src/ui/faces.aseprite` (W1-CHROME's `game/assets/ui/*.png` / `art/src/ui/*.aseprite` domain, but written by gen_icons.lua since before this wave): mtime 09:53 today, `git status` clean — regenerated byte-identical. No ownership breach.

Changed files NOT in the owned list (cannot be attributed by me; none of them is named in this unit's report as its own write, and the report explicitly attributes them to W1-CHROME / W1-VFX / W1-STAGE / W1-HALL / W1-KIT / W1-FRAME):
`art/src/ui/{cta_plate,wax_seal,wipe_blot}.aseprite`, `art/src/vfx/fire_*.aseprite` x3, `game/assets/scenes/stage_*.json` x5, `game/assets/ui/{cta_plate,wax_seal,wipe_blot}.png`, `game/assets/vfx/fire_*.png` x3, `game/core/ScreenRouter.gd`, `game/screens/{Facilities,Guildhall,LoadSave,RaidView,RaiderDetail,Settings}.gd`, `game/ui/{Bar,Cards,Fonts,Frame,Palette,SceneStage,Theme,Type,Widgets}.gd`, `tests/unit/test_scene_stage.gd`, `tools/art/{gen_wordmark,preview_scene}.py`, `tools/aseprite/{gen_fire,gen_ui,gen_wipe}.lua`, `tools/probe/Kit.gd`, plus the untracked W1-CHROME/W1-VFX/W1-FRAME/W1-KIT/W1-HALL assets, tests and reports, and `aguildstory.zip` (untracked, root — not this unit's; nothing in gen_icons.lua or the report writes a zip). Nothing in this unit's generator writes outside `art/src/ui/icons/`, `game/assets/ui/icons/`, `game/assets/ui/faces*.{png,json}` and `art/src/ui/faces.aseprite` (grep of every `save_png`/`save_ase`/`write_text` target in gen_icons.lua).

`.import` sidecars: `class_warrior.png.import` (grid) read in full — identical to `rank_unknown.png.import`'s block except uid/path/source_file; `path` hash = md5("res://game/assets/ui/icons/grid/class_warrior.png") = 6d465adb… (verified with md5sum). `faces_30.png.import` same shape. `grep -h ^uid= game/assets/**/*.import | sort | uniq -d` → empty (no duplicate uid).

Verdict for §1: no ownership breach found.

Late addition (found after the listing above): `tests/unit/test_art_sources.gd` shows ` M` with mtime 16:16:06 today — it was clean in my first `git status` at ~16:12. `git diff` on it is EXACTLY the five edits of handoff-W1-ICONS.md (WALK + grid, GEN_ICONS_MANIFEST const, `emitted` dictionary, the `or dir.ends_with("grid")` / `or emitted.has(base)` line, the grid anchor assert), four-space indented, matching the file. The unit's report was last written at 14:01 and says "Nothing here was applied by the unit"; the write came 2h15m later while W1-FRAME/W1-KIT/W1-STAGE reports were being updated (16:17-16:18). I cannot attribute it — it is consistent with the orchestrator applying the handoff (the prompt says that is the remaining step) and with some other agent applying it. Flagged, not counted against this unit; the orchestrator knows whether it applied it.

## 2. Tests and verify --fast (observed)

One locked batch (`GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh bash <scratchpad>/review_icons_batch.sh`, 16:19:34-16:21:14; log in the scratchpad `review_icons_run.log`, full verify log `review_icons_verify_full.log`):
- `parse_check.gd`: `PARSE_CHECK scanned 160 script(s)` / `PARSE_CHECK OK`.
- Two shots (below): `SHOT OK build/shots/review-W1-ICONS-Town.png 1536x1024`, `SHOT OK build/shots/review-W1-ICONS-Guildhall.png 1536x1024`.
- `./tools/verify.sh --fast`:
  - `PASS  LINT OK  no cross-file class_name references in sim/ or game/`
  - `PASS  MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)`
  - `PASS  PARSE_CHECK scanned 160 script(s)`
  - `PASS  16 generated file(s) agree with tools/gen_items.gd`
  - `PASS  ART CHECK  generators=6  runtime files: 184 agree  0 DIFFERS  0 MISSING   (sources: 179 agree, 0 differ — not gated)`
  - `FAIL  unit tests` — `TESTS FAILED   1/1710 failing  [35458 ms]`, the one failure: `test_scene_stage.gd :: test_reduced_motion_holds_lights_and_bubbles` — "expected [[&"@TextureRect@59448", …]], got [[&"@TextureRect@59434", …]]; two runs of one scene must pick the same bubbles at the same ticks" — it compares auto-generated `@TextureRect@NNNN` node names across two runs. Both `game/ui/SceneStage.gd` and `tests/unit/test_scene_stage.gd` are ` M` and belong to W1-STAGE (its report was being written at 16:18). Nothing under game/ or sim/ references `Icons.gd`, `icons/grid` or `faces_30/36` (grep: only the two json sidecars and test_icons.gd), so no file of this unit is on that test's path. **Not this unit's red.**
  - `VERIFY FAILED` (because of that one test), 0 `SCRIPT ERROR` / `Failed to load` lines in the full log.
- The unit's own file, run alone through the lock with a scratchpad runner (`run_one_icons.gd`, loads `res://tests/unit/test_icons.gd`, calls every `test_*`): **11/11 pass** (the report says "10 tests" — there are eleven), assertion counts 65 / 64 / 124 / 234 / 63 / 294 / 91 / 991 / 21952 / 141 / 9. The door check in the same run: `Icons.at("class", "definitely_not_a_class")` printed `ERROR: Icons.at("class", "definitely_not_a_class"): no icon file — gen_icons.lua emits none by that name (role size 16)` via push_error at Icons.gd:133 and returned `<null>` — never a silent null. A3's push_error is observed, not just asserted by string.
- `verify_fast_observed`: **fail** (1/1710, attributable to W1-STAGE's in-progress files, not to W1-ICONS).

## 3. Shots retaken (§0.3 grammar, through the lock)

`tools/with_godot_lock.sh "$GODOT" --path . --script res://tools/shot.gd -- res://game/screens/Town.tscn build/shots/review-W1-ICONS-Town.png 40 1536x1024 --fixture --set=reduced_motion=true --set=reduced_effects=true` and the same for Guildhall → `build/shots/review-W1-ICONS-Guildhall.png`. Both `SHOT OK … 1536x1024`, both viewed with Read:
- Town: the camp plate with the five callouts, the raider bubble "I think I'm ready for a raid this time!", two "…" stage bubbles (left tent, right garden), the four-card desk strip, the sidebar; header chips `12,480 G · 320 · Roster 12 of 15 · Day 23 · Unknown`. Chip 4 carries a dark shield with a visible light rim.
- Guildhall: Roster tab, eight morale cards, Rest panel, "Lowest morale first" list, same header. (The vertical "Records open…" text at the right edge of both shots is in another unit's in-progress files — no file of W1-ICONS draws it.)
- `build/shots/review-W1-ICONS-Town-chip4.png` (chip 4 at 4x, viewed): the "Unknown" shield is a dark grey body with a 1px cream rim and a lighter top-left highlight; it reads as a shape beside "Day 23 · Unknown" instead of sinking into the chip.

Independent measurement (PIL, numpy; scratchpad scripts):
- Template match of the NEW `rank_unknown.png` composited over #020B12 against my two shots: best MAE 0.82 at (1260,23) in both; HEAD's `rank_unknown.png` (from `git show HEAD:…`) at the same spot: MAE 7.79. So the screens load the regenerated file at the same 28px footprint and position.
- The implementer's own pair, re-diffed by me: `W1-ICONS-Guildhall-before.png` vs `-after.png` = **70 differing px, all inside the 28x28 sigil rect (1260,23)-(1288,51)**; Town before vs after = the same 70 px + 1468 px in bbox (298,144)-(337,187) = the camp stage's timed "…" speech bubble by the left tent (a SceneStage scheduler element, not an icon). Their before shots match HEAD's sigil (MAE 0.82) and their after shots match the new one — the swap was real.
- My Guildhall shot vs the implementer's Guildhall-before: exactly 70 px, all inside chip 4's sigil rect. My Guildhall shot vs their Guildhall-after: **0 differing pixels**. My Town shot vs their Town-before: 70 px in chip 4 + the stage bubbles (bbox (300,160)-(1127,671): two "…" bubbles fired on different frames), nothing else.

Montage: `build/shots/review-W1-ICONS-montage.png` (all 114 manifest rows at 4x in manifest order + the 24/30/36 face strips at 2x; viewed) and three zooms (`review-W1-ICONS-zoom-ranks-classes-emotes.png` 8x, `review-W1-ICONS-zoom-log-state-mech.png` 6x, `review-W1-ICONS-zoom-32-48.png` 4x; viewed). What I saw: ten faces (red shouting/frown/tear, amber lidded/neutral/smile, green grin/arched, two hearts); seven engraved slot glyphs; four potions + whetstone on a strop with an oil flask, a round blue bottle with a cream label, a roast on a platter with a bone and steam; six rank shields — plain grey, brown + tan chevron, blue + two cream chevrons, steel + four-point star, bronze + green laurel + cream diamond, gold + crowned skull on a glow — every one with a clearly visible light rim; left/right chevrons, fast-forward, up/down chevrons, cog; ten class glyphs (crossed swords, heater shield with cross, fist, dagger, flared cross, leaf, totem, lute, orb with three sparks, pointed hat) in the two-tone steel/warm-grey; ten cream emote bubbles with an ink outline and a bottom-left tail (three dots, red "!", "?", red heart, "zZ", blue drop, skull, tankard with foam, note, red anger mark); twelve coloured log discs (red frown, green cross, steel sword, violet eye, periwinkle cog, brown hourglass, grey scroll, green up arrow, red down arrow, lavender gem, gold coin, blue bust); eight state plates (amber fallen figure with a down arrow, maroon skull, red cracked heart, steel shield, a bare slate plate for swap_stack, violet eye, blue slashed drop, green slashed cross); twelve mechanic plates (swap arrows, burst, concentric floor rings, three adds over a mass, bolt, flame with an amber core, crosshair, eye, slashed cross, slashed drop, wedge, stairs); bar_hp red shield with a cross, bar_focus blue drop; five light-grey buildings (hanging banner, tankard, scales, anvil with two hammers, notice board with two papers); quill, bench, shelf; six furnishings (straw cot, bed with a red blanket, steaming bowl, skull on a shelf, gold locket with a heart, cauldron over fire); six steel sigils (fanged maw, crystal core with rays, spiked crawler with legs, stone fist, flame, curled tentacle); padlock; three red blots; the 30 and 36 face strips keep the expressions with the features scaled onto the larger discs. No pure black/white anywhere (my own scan: 0 of 117 files). Letters/punctuation: only the three emotes named by the plan (`emote_exclaim` "!", `emote_question` "?", `emote_zzz` "zZ") and the pre-existing `item_unknown` sack "?" (byte-identical to HEAD) — see §5.

## 4. Acceptance lines (plan "### W1-ICONS")

| Line | Met | Evidence |
|---|---|---|
| `test_icons.gd` iterates Enums.CLASS_KEYS, Enums.MECHANIC_KEYS, the state list, the log kinds, the five buildings, the ten emotes and asserts each file exists at the grid size and `Icons.at()` returns it | met | test_icons.gd:118-165 (`_check` = raw PNG size == cell AND `Icons.at()` non-null at the cell); 11/11 pass in my locked single-file run; the same file is inside verify's 1710 (only W1-STAGE's test is red) |
| each rank sigil's rim vs #020B12 ≥ 3:1 (`tools/art/cvd.py`) | met | my PIL measurement (WCAG luminance, same 0.2126/0.7152/0.0722 weights as cvd.py:85, min over the opaque outline ring): unknown 11.63, known 12.03, respected 15.93, established 15.62, renowned 10.00, legendary 14.57 — identical to the report's numbers; pinned by test_icons.gd:184 |
| `build_art.sh --check` 0 DIFFERS | met | my verify run: `ART CHECK  generators=6  runtime files: 184 agree  0 DIFFERS  0 MISSING` (the check regenerates into build/artcheck and `cmp`s every file — icons.json and faces_30/36.json included) |
| Shot: Town --fixture and Guildhall --fixture unchanged pixel-for-pixel except chip 4's rank sigil | met | §3: Guildhall exactly 70 px inside the sigil rect; Town the same 70 px plus the stage's timed "…" bubble (SceneStage scheduler, W1-STAGE's layer, not an icon; the report says so and shows it in `W1-ICONS-Town-outside-chip4.png`) |
| a 4x montage of `icons/` viewed with the Read tool | met | the report's `W1-ICONS-montage-final.png` exists (13:55) and is described row by row; my own `review-W1-ICONS-montage.png` viewed and described in §3 |
| Green: no screen code, no Label text; no tween | met | `git status` shows no screen file among the owned changes; Icons.gd/test_icons.gd: no `create_tween`, no `reduced_motion`, no `_ready`, no `add_child` (grep); `MOTION LINT OK` |
| Build note: `Icons.at` holds the size table and push_errors on a missing file; `Icons.size(role)`; regenerating keeps dimensions | met | Icons.gd:37-45 SIZES, :133 push_error (observed firing in my run, returned null); test_legacy_names_keep_their_dimensions counts the 43 legacy generated files at their old sizes; `git diff --stat` = only rank_* / mech_* bytes changed |
| Assets list (report A1) | met, one spelling | 114 manifest rows: face 10, slot 7, item 7, rank 6, arrow 4, control 2, class 10, emote 10, log 12, state 8, mech 12, bar 2, building 5, empty 3, furnishing 6, sigil 6, lock 1, blot 3 (my count from icons.json) + faces_30/36 (300x30, 360x36). `item_whetstone_kit` instead of the plan's `item_whetstone` (J3, the sim SKU) |

## 5. Contracts (§0.5 / RULES)

- Label/Button texts and node names: untouched — the unit changed no .gd under game/screens or game/ui except the new Icons.gd; the shots show the header/rail strings unchanged.
- Indentation: `game/ui/Icons.gd` 50 tab-indented lines, 0 space-indented (`grep -c '^\t'` / `'^ '`); `tests/unit/test_icons.gd` 0 tabs, four-space blocks. Both parse (PARSE_CHECK OK, 160 scripts).
- No `create_tween`, no `reduced_motion` branch, no `_ready()`, no `class_name` in Icons.gd (grep; test_icons.gd:334 contains the literal only inside its own assert string — the lint greps sim/ and game/, not tests/, and passed).
- `$GODOT` outside the lock: the unit added no shell script; gen_icons.lua is Aseprite-only.
- Baked text: viewed every new PNG (§3). None carries a word. Three emotes carry punctuation/letter pictograms: `emote_exclaim` ("!"), `emote_question` ("?"), `emote_zzz` ("zZ"). The plan orders exactly these names "in the A1-S8 outline style" (the reference bubbles carry "...", "!", "?" and "zZ" — report, ref crop), while the same asset line says "a numeral or '!' is always a Label over a bare plate" and §0.5 says "No text baked into any PNG (RULES-14)". The plan contradicts itself for these three; the generator header records the choice ("the emote marks are pictures of punctuation, not font glyphs") but the report's judgement-call section (J1-J4) does not. **Minor** — for the orchestrator/designer (RULES-14 is on Q16's list): if RULES-14 is read strictly, the three become bare bubbles with a Label at wiring time (W2-STAGE2 / W3-KIT2), a generator-only change.
- `.import` beside every new PNG: 71 grid + faces_30 + faces_36 (walk: 0 missing), no duplicate uid across game/assets, `.godot/imported/` holds 154 `.ctex` for the grid/faces_3x names, my run's boots printed no `Failed to load`.
- Nothing parented to the router host: no screen/router code touched.
- Handoff: five `## N. path:line` edits with `old:`/`new:` fenced blocks, four-space indented like the file; every `old:` block matches `git show HEAD:tests/unit/test_art_sources.gd` lines 20 / 24 / 113 / 124-125 / 135 verbatim. (Applied in the tree at 16:16 by someone other than the unit — §1.)
- Palette tokens: none added or renamed; no pure black/white in any generated file (0 of 117).
- Canon: docs/_source untouched by the unit (nothing under docs/ in its diff).
- Small nit: test_icons.gd:23 comments `CHIP_FILL := Color("020B12")` as "Palette.SURFACE_CHIP" — Palette.SURFACE_CHIP is `020A12`; the test pins the plan's number, one blue unit off the palette's, contrast unaffected. Minor.
- Reporting nit: the report says test_icons.gd has "10 tests"; it has 11.

## 6. Judgement calls vs §6

- J1 (new grid in `icons/grid/`, legacy names at the top level): a layout choice forced by test_art_sources.gd's top-level walk (not the unit's file); Icons.gd is the only file that knows the layout (§0.2 kept). Not a designer question. Fine.
- J2 (top-level 15x12 `class_warrior.png` hand crop left, shadowed by `grid/class_warrior.png`): pinned by SHADOWED_CROPS in the test. Fine.
- J3 (`item_whetstone_kit` = the sim SKU): recorded; the plan's spelling was shorthand. Fine.
- J4 (mech keys accept `m01` and `tank_swap`): recorded; both spellings resolve to the one top-level file RaidView loads. Fine.
- §0.6/Q18 warrior glyph: `Icons.WARRIOR_GLYPH := "swords"` with `class_warrior_shield.png` beside it — the plan's default, both files present. Not decided for the designer.
- Q18 "which sim states get a glyph beyond the eight named" — STATE_KEYS is exactly the eight. Nothing extra emitted.
- Q04 emote vocabulary mapping — no mapping made; ten glyphs only, consumers left to wave 2/3 (handoff note). Nothing decided.
- Q06 reputation chip gem vs sigil — untouched (the rank sigil on the DAY chip is the pre-existing KIT-16 placement the plan re-authors; the gem chip is unchanged).
- Mechanic plate tints by role group (tank steel / healer green / dps ember / support violet) and the rim colours varying per rank metal (greyish cream → pale gold) are aesthetic choices inside the plan's text ("1px cream rim… a device per rank"); the acceptance (≥ 3:1) is met by every rim. Not §6 material.

No decision reserved for the designer was taken.

## Verdict

**PASS.** Every acceptance line is met against the tree; the unit's 11 tests pass in isolation and inside the suite; `build_art.sh --check` is 0 DIFFERS / 0 MISSING; the shots show the re-authored sigil and nothing else moved (Guildhall: 70 px, all in chip 4; Town: chip 4 + the stage's bubble timer). `verify.sh --fast` is red on exactly one test in W1-STAGE's own in-progress file (`test_scene_stage.gd :: test_reduced_motion_holds_lights_and_bubbles`), which no W1-ICONS file touches.

Minors (no action required to merge; for the orchestrator's record):
1. `emote_exclaim` / `emote_question` / `emote_zzz` carry punctuation/letter pictograms — the plan names those assets in the A1-S8 style and also says "!" is always a Label; RULES-14 is a Q16 item. Record the choice; a strict ruling makes them bare bubbles + Label at wiring time.
2. `tests/unit/test_art_sources.gd` now carries the handoff's five edits (applied 16:16 by a party I cannot identify; the unit's report predates it by 2h15m). If the orchestrator did not apply it, find who did.
3. Nits: test comment says CHIP_FILL is Palette.SURFACE_CHIP (020A12 vs 020B12); report says 10 tests (11).

Unowned files touched by this unit: none found. False claims in the report: none found — every number I re-measured (114 rows, 71 grid PNGs, 73 sidecars, the six contrast ratios, the 70-px chip diff, the Town bubble patch, 0 DIFFERS) reproduced.
