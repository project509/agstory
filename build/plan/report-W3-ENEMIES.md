# Report — W3-ENEMIES (the boss moves and the townsfolk breathe)

Unit: 00-plan.md §4 "W3-ENEMIES". Owns `tools/aseprite/gen_boss_anims.lua` (new), `game/assets/enemies/**`,
`tools/art/gen_actors.py`, `game/assets/actors/**` (strips, .import, actors.json — not class_actors.json),
`game/ui/SceneStage.gd`, `tests/unit/test_scene_stage.gd`. Handoff: `build/plan/handoff-W3-ENEMIES.md`.
Written as each item lands (rule 3). (Rebuilt at 23:15 from the working notes (kept in build/shots/_w3en_report_full.md and copied over) after the file on disk lost
everything before its 23:05 entry between two appends — OneDrive or a concurrent writer; nothing below is
new, and `sed -i` is no longer used on it.)

## Acceptance (00-plan §W3-ENEMIES, as boxes)

- [x] A1 six boss strips (LUA, gen_boss_anims.lua from the W0-MANIFEST slices): idle 4 frames (1px breath + a 2-frame emissive eye/mouth layer at 1.4-1.8), hit 2 frames (flash + 2px recoil), death 6 frames (desaturate + 12° topple + fade); `enemies.json` gains `frame_w/fps/tags`
- [x] A2 `place_boss` becomes an AnimatedSprite2D built through `_strip_frames` with tags; the bob kept for single-texture fallbacks; NO integer upscale (marks.boss.scale stays 1, Q02)
- [x] A3 `test_a_boss_with_a_strip_has_idle_and_hit_tags`
- [x] A4 44 townsfolk strips regenerated by gen_actors.py: 4 ping-pong frames at 5fps for the `breath` entries, same keys, `frames` becomes 4; median-frame outlier rule + feet alignment (LESSONS:92-100)
- [x] A5 `test_no_actor_is_a_statue`, `test_the_pixels_back_the_measurement_up`, the geometry test (`frames*frame_w x frame_h`), `test_scale_flip_and_tint_are_honoured` green; every manifest entry `frames >= 4`
- [x] A6 `build_art.sh --check` prints 0 DIFFERS 0 MISSING (gen_boss_anims.lua picked up by the glob; gen_actors.py --check is a handoff edit to build_art.sh)
- [x] A7 RaidView shot shows bloom on the boss's eye (a > 1.0 pixel cluster inside the boss bbox) and, after `--advance=all`, the toppled boss or fallen figures
- [x] S1 shot `RaidView --fixture=raid --frames=1,20` (viewed)
- [x] S2 shot `RaidView --fixture=raid --advance=all` (viewed; the fallen party) + `--fixture=raid:clear --advance=all` (viewed; the toppled boss)
- [x] S3 shot `Town --fixture --frames=1,20` (breath, not a pop; viewed)
- [x] S4 `--set=reduced_motion=true` variants hold frame 0 (viewed)
- [x] G1 `place_boss` still returns a node with `BossShadow`; `apply_settings` holds every actor and the boss; `actor_for_class` mapping untouched; §0.5's SceneStage contract intact
- [x] G2 parse_check OK after every edit; `verify.sh --fast` green (summary pasted); `lint_motion.sh` MOTION LINT OK

## Judgement calls (rule 8)

- J1 The strips do NOT replace `game/assets/enemies/<rank>.png`: tests/unit/test_art_sources.gd (not mine) asserts each rank PNG is a byte copy of its export and that every PNG in that directory is a rank key (and `ranks.keys().size() == 6`), so the animated strips live in `game/assets/enemies/anim/<rank>.png` (+ `<rank>_glow.png`), which that test's non-recursive walk does not see; enemies.json's rank rows gain `strip`/`glow` paths beside `frame_w/fps/tags`. A handoff widens the hygiene walk to `anim/` so the strips are sourced by name after the wave.
- J2 `place_boss` returns `Node2D` (the note allows Node2D/AnimatedSprite2D): an AnimatedSprite2D named "Boss" when the texture's `resource_path` is a rank with a strip, else the Sprite2D + bob of today. RaidView.gd already declares `_boss_sprite: Node2D` (W3-RAIDVIEW2 widened it before this landed), so no type edit is needed there.
- J3 The glow layer is a SECOND strip (`<rank>_glow.png`, same canvas/frames/tags) played by a child AnimatedSprite2D "BossGlow" at modulate 1.6 (STAGE-08's 1.4-1.8): an 8-bit PNG cannot store > 1.0, and over-driving the whole body would bloom the whole body. Reduced effects returns it to 1.0 (CRITIC-G17's clipping note for the flames); reduced motion holds frame 0.
- J4 Tags carry their own rate: `tags: {name: [first, last, fps]}` — idle 5 (the townsfolk's cadence, STAGE-16), hit 11 (docs/12 §5.2's 90 ms), death 9 (110 ms). A reader taking `[0]`/`[1]` (W0-MANIFEST's shape) still works.
- J5 Emissive ramps per creature: the void ramp of spec 02 §9.1 for the colossus and the tentacle (core `#F8F0FF`, not `#FFFFFF` — no pure white), a red-hot ramp for the sludge maw's red eyes, fire for the crawler and the flame brute, a moss-green for the stone brute. STAGE-08 names the void ramp; painting magenta eyes on a fire creature would be wrong, so the ramp follows the creature. The lit eye is pushed to the ramp's HOT stop (saturated) rather than the pale core, so the creature's own hue blooms instead of a white block.
- J6 The hit/death verbs on a strip boss play the strip's tags (the recoil, the flash, the desaturate and the topple are baked) and skip the procedural recoil/flash/rotation; the dead tint is still written, so the existing verb tests keep their meaning and RaidView's own `modulate` writes compose.
- J7 Sliced sets shorter than 4 frames (warrior: 3 after the mis-cut drop) are ping-ponged to 4 (0,1,2,1) so "every manifest entry frames >= 4" holds without inventing a pose.
- J8 handoff-W2-RAIDVIEW's say_at observation is taken: ONE plate on the stage at a time — a new line retires the plate that is up, whoever said it (a test pins it).

## Log

- 21:50 Read 00-plan §0 + §4 (ownership, W3-ENEMIES), RULES §1 (test_scene_stage contracts), §2, §4, §5, STAGE-08, STAGE-16, PIPE-17, COMBAT-07, LESSONS (median-frame outlier rule, feet alignment, backslash trap, .import pattern), handoff-W2-RAIDVIEW (say_at observation), report/handoff-W2-STAGE2, SceneStage.gd (1955 lines, tabs) in full, test_scene_stage.gd (function list + the strip/actor/boss/verb tests), gen_actors.py, lib.lua, gen_fire.lua/gen_vfx.lua heads, build_art.sh, place_enemies.py, test_art_sources.gd (NOT mine — it pins the enemies dir).
- 21:58 Viewed build/shots/W3EN_boss_sheet.png (the six stills at 4x) and seven grid zooms (W3EN_z_*.png): sludge maw eyes at (53,20,3,4)/(90,18,4,4), the colossus core ring centred ~(75,28) r 7-8, the crawler's red eye at (45,30,4,4) + yellow crest tips (55,18,14,12), the stone brute's two pale eye clusters ~(24-26,28-30)/(40-43,24-26), the flame brute's dark sockets at (32,27)/(40,27) (2x2 each), the tentacle's bright magenta suckers (no eyes).
- 22:40 A1 landed on disk: tools/aseprite/gen_boss_anims.lua (2.3 s headless) writes 12 strips into game/assets/enemies/anim/ (`<rank>.png` body + `<rank>_glow.png`, 12 frames each: idle 0-3, hit 4-5, death 6-11), 12 .aseprite sources under art/src/enemies/, and enemies.json itself (frames/frame_w/frame_h/fps/tags [first,last,fps]/strip/glow/glow_px per rank, `glow_modulate` 1.6). Canvases: main 155x131, main_2 200x121, mini 149x105, mini_2 181x165, elite 123x91, trash 76x82 (the still + room for a 12° topple about the feet + 2px recoil, padded equally so the feet-centre stays [0.5, 1.0]). 12 `.import` sidecars written from boss_main's with fresh base-34 uids (no collision among 332) and md5(res path) dest names (build/shots/_w3en_imports.py). `./tools/build_art.sh --check`: `generators=7  runtime files: 197 agree  0 DIFFERS  0 MISSING` — the glob picked the new generator up.
  - Viewed build/shots/W3EN_strip_main.png (first cut, 3x): the 0.85 flash was a cream blob and the eye a white block — flash mix -> 0.6, the lit eye now pushed to the ramp's HOT stop (saturated, >= 215 in its hue channel so x1.6 clears the 1.3 threshold; lit mix 0.9 so the flame brute's dark sockets clear it too) instead of the pale core, dim frames sink to the ramp's second stop (< 160, no bloom; the fire ramp gained a darker second stop for it). Viewed W3EN_strips_all.png (all six, frames 0/2/4/5/8/11 with a crude bloom simulation): the maw's two red eyes, the colossus's magenta core ring, the crawler's eye + crest tips, the brute's two moss-green eye spots, the flame brute's lit sockets and the tentacle's suckers bloom on frame 0, not on the dim frame 2, again through the hit (flash frame keeps the creature legible, recoil 2px left in source space = away from the party once flipped), and the death frames desaturate, lean 12° and fade to 0.55 with the glow gone. Bloom-pixel counts per rank at frames [0,2,4,5,8,11]: main 28/0/28/28/0/0, main_2 99/0/99/99/0/0, mini 20/0/20/20/0/0, mini_2 10/0/10/10/0/0, elite 8/0/8/8/0/0, trash 27/0/27/27/0/0.
- 22:55 A4 landed: gen_actors.py rewritten — `breath_frames()` = pose, head+chest down 1 (`shift_bands`, alpha-aware so a stepping band lands ON the one below and never punches a hole), head down 1 with the chest back up (the shoulder shift), pose; FPS_BREATH 5.0; sliced sets shorter than 4 are ping-ponged (`pingpong_to`, warrior 3 -> 4 (0,1,2,1), flagged `pingpong_padded`); `--check` is now a byte gate (regenerate in memory, compare every strip + actors.json, `GEN_ACTORS CHECK OK (57 files)`), PNGs encoded `optimize=False` through one `encode_strip` in both modes. Ran the gate before regenerating: exactly 46 DIFFERS (44 breath + warrior + actors.json), the 11 other sliced strips byte-identical — the plant/trim path is unchanged. After `--gen`: CHECK OK; min frames 4; the 12 sliced entries 4-5 frames.
  - Viewed build/shots/W3EN_actors_sheet.png (r01_black, r05_ginger, r12_grey, r03_brown, warrior, knight at 6x): the four breath frames differ by the 1px head/chest step only, feet planted; the warrior's 4th frame is its 2nd (ping-pong); the knight's four sliced frames are the reference's own.
- 23:05 A2 code landed in SceneStage.gd (tabs, 0 space-indented lines; parse_check OK at 22:23 through the lock, 170 scripts): `ENEMIES`/`ENEMIES_TABLE`/`BOSS_GLOW_MODULATE` consts, `_bosses` list, `_strip_frames` gains an optional `tags` dictionary (one animation per tag, idle loops, per-tag fps), `place_boss -> Node2D` reads the rank off the still's `resource_path`, `_boss_row`, `_place_boss_strip` (AnimatedSprite2D "Boss" + child "BossGlow" mirroring its animation/frame through `frame_changed`, modulate 1.6 from the table, 1.0 under reduced effects, flip copied to the child because flip_h does not propagate), `boss_has_tag` (public), `_boss_play`, `_on_boss_anim_finished` (hit -> idle; death holds), `_mirror_boss_glow`; `hit()` and `act(…, "death")` play the tags first; `apply_settings` holds the strip boss on frame 0 (last frame when dead) and unlights the eyes under reduced effects; `say_at` keeps one plate on the stage. RaidView.gd already declares `_boss_sprite: Node2D` (W3-RAIDVIEW2 widened it before this landed), so the handoff carries no type edit.
- 23:06 Tests written (spaces): `test_a_boss_with_a_strip_has_idle_and_hit_tags`, `test_every_boss_strip_matches_its_table`, `test_a_strip_boss_hit_and_death_play_their_tags`, `test_reduced_effects_unlights_the_boss_eyes_and_keeps_it_breathing`, `test_a_new_speaker_retires_the_previous_plate`, `test_every_townsfolk_breathes_in_four_frames`; the bob tests and the bob-composition test now place a strip-less ImageTexture (`_still_only`) — the case the bob exists for; the shadow/head tests read the frame height. Handoff written (build_art.sh gen_actors gate; test_art_sources walk of enemies/anim; two observations) — `apply_handoff.py --dry-run` parses all three edits.
- 23:07 Batch 1 (import + parse + this unit's test file, one lock hold: build/shots/_w3en_batch1.sh) queued behind other agents' engines from 22:27.
- 23:02 Batch 1 (build/shots/W3EN_batch1.log): `--import` rc 0, 12 new .ctex under .godot/imported (the strips) and the 45 changed actor strips re-imported; parse_check reported two files NOT mine failing to compile (tests/unit/test_screens.gd, tests/unit/test_widgets_kit.gd — both clean in git, so a dependency of theirs is mid-edit by W3-KIT2; SceneStage.gd and this unit's test file parse). This unit's test file through the scratch runner (scratchpad/run_w3en.gd, untracked): 69 run, 1 failing — `test_every_townsfolk_breathes_in_four_frames` read the IMPORTED texture, whose `fix_alpha_border` bleeds colour across frame boundaries (frame 3 sits beside frame 2, frame 0 beside nothing); fixed to read the PNG file itself (`_raw`, test_art_sources.gd's rule) — the raw PNG has frame 3 == frame 0 and frame 1 differing only above the waist for all 44.
- 23:10 Batch 2 shots (build/shots/W3EN_batch2.log, all `SHOT OK`), each viewed:
  - S1 W3EN_RaidView.a/.b.png (`--fixture=raid --frames=1,20`): the fixture raid is E5 with a Main Boss, so the sludge maw stands on the right ledge at 1x facing the party (frame 155x131, feet on the mark). Crop W3EN_crop_boss_a.png (4x, viewed): BOTH eyes lit red-orange with a soft bloom halo around each — the eye pixels read (255,191,184)/(255,195,172) max over a 9x9 at screen (1206,420)/(1169,418) where the still's own eye is (104,31,47): the emissive layer over the 1.3 threshold, bloomed by the stage's glow (A7's "> 1.0 cluster inside the boss bbox"). Crop W3EN_crop_boss_b.png (frame 20, viewed): the eyes in their dim pulse state (no halo) and the head a pixel lower — the breath; 2046 px differ between a and b inside the maw's bbox.
  - S2 W3EN_RaidView_all.png (`--advance=all`, viewed): the fixture raid is a wipe, so the "fallen figures" half of A7: the twelve lie grey and toppled at the left with skull badges, "[R11] WIPE." in the log with W3-RAIDVIEW2's stamp/blot/seal; the maw stands at 7062/7150 with its eyes still lit. (The toppled boss needs a clearing seed — `--fixture=raid:clear --advance=all` is in batch 3.)
  - S4 W3EN_RaidView_rm.a/.b.png (`--set=reduced_motion=true --frames=1,20`): 0 px differ inside the boss bbox between frame 1 and 20 — the strip boss and its eyes hold frame 0 (crop W3EN_crop_boss_rm_a.png viewed: eyes lit, no pulse, no breath).
  - S3 W3EN_Town.a/.b.png (`--fixture --frames=1,20`, viewed): 26246 px differ in the scene band (mean abs 2.1) — the crowd, the fire, the shimmer. Crop W3EN_crop_town_pairs.png (three townsfolk at 4x, a beside b, viewed): in each pair the head and chest sit one art pixel (two screen pixels at 2x) lower in frame 20 while the feet, legs and the ground under them are identical — a breath, not a pop; the differing rows for r03_brown are 28..69 of a 112-row crop whose feet are at row 100.
  - S4 W3EN_Town_rm.a/.b.png (`--set=reduced_motion=true`): 0 px differ in the scene band — every figure holds frame 0.
  - W3EN_RaidView_adv10.png (`--advance=10`): taken for the record; W3-RAIDVIEW2's beats show over the party; the boss breathes and glows as in S1.
- 23:20 G1 grep over SceneStage.gd: each of the eight contract functions defined once (`from_data/load/place_party/place_boss/apply_settings/motion_held/party_state_style/actor_for_class`), `const DEFAULT_ARENA := "stage_arena_cave"`, the literal `func apply_settings(reduced_motion: bool, reduced_effects: bool)` at :2006, `create_tween` 0, focus 0, `Color("` only in a pre-existing comment, `class_name` 0, 20 MOUSE_FILTER_IGNORE sites; the boss node is still named "Boss" with a sibling "BossShadow" (and now a child "BossGlow"); class_actors.json and the scene JSONs untouched (`actor_for_class`: monk -> rogue_b, bard -> warrior_b, mage -> mage). `tools/lint_motion.sh`: `MOTION LINT OK` (rc 0).
- 23:21 Batch 3 (this unit's test file with the raw-PNG fix, `--fixture=raid:clear --advance=all` + reduced_motion + reduced_effects shots for the toppled boss, then `verify.sh --fast`) queued under the lock: build/shots/_w3en_batch3.sh -> W3EN_batch3.log.
- 23:16 Batch 3 (build/shots/W3EN_batch3.log): this unit's test file `W3EN TESTS 69 run, 0 failing` (the 63 existing test_scene_stage tests + the six new ones — A3 and A5 green: `test_a_boss_with_a_strip_has_idle_and_hit_tags`, `test_no_actor_is_a_statue`, `test_the_pixels_back_the_measurement_up`, `test_the_manifest_and_the_strips_agree`, `test_scale_flip_and_tint_are_honoured`, `test_every_townsfolk_breathes_in_four_frames` (every entry >= 4 frames)).
  - S2 (the boss half) W3EN_RaidView_clear_all.png (`--fixture=raid:clear --advance=all`, A0 seed 25, viewed): "Adventure 0 is defeated", 0 / 66 on the plate, and the tentacle on the right ledge is DEAD — crop W3EN_crop_RaidView_clear_all.png (4x, viewed): desaturated to a dark grey-violet, faded to about half, leaning right (away from the party, the strip's 12° topple flipped with the face), its suckers no longer lit. W3EN_RaidView_clear_all_rm.png (reduced motion) and _re.png (reduced effects) taken and `SHOT OK`; the rm one holds the death's last frame (the test pins the mechanism).
- 23:17 `GODOT_LOCK_WAIT=3000 tools/with_godot_lock.sh ./tools/verify.sh --fast` (inside batch 3; .verify.log):
  ```
  PASS  LINT OK  no cross-file class_name references in sim/ or game/
  PASS  MOTION LINT OK  tweens and morale glyphs each go through one door (docs/13 §12.2, §13)
  PASS  PARSE_CHECK scanned 173 script(s)
  PASS  16 generated file(s) agree with tools/gen_items.gd
  PASS  ART CHECK  generators=7  runtime files: 197 agree  0 DIFFERS  0 MISSING
  FAIL  unit tests   TESTS FAILED   7/1908 failing  [244876 ms]
        test_menu_lockup.gd :: test_hover_is_the_lighter_plate_lifted_one_pixel_with_no_layout_shift   (W3-MENU, in flight)
        test_options_layout.gd :: test_the_title_screen_side_prints_each_reason_once_per_row           (W3-OPTIONS, in flight)
        test_roster_layout.gd :: test_the_panel_leaves_room_for_two_full_rows_and_the_top_of_a_third   (W3-ROSTER, in flight)
        test_roster_layout.gd :: test_past_twelve_the_strip_says_how_many_more_and_where_they_stand    (W3-ROSTER, in flight)
        test_text_scale.gd :: test_every_screen_builds_its_theme_through_the_door  ("Guildhall.gd must not call Theme_.get_theme() itself" — W3-ROSTER's file)
        test_widgets_kit.gd :: test_a_placed_callout_is_kept_inside_the_scene                          (W3-KIT2, in flight)
        test_widgets_kit.gd :: test_the_callout_band_is_the_scene_rect_in_the_parents_space           (W3-KIT2, in flight)
  SKIP  4/8..7/8 (--fast)
  VERIFY FAILED
  ```
  The seven failures are all in files owned by other wave-3 units mid-edit (none names SceneStage, the actors, the enemies or this unit's test); every test that reads this unit's files is green in the same run: test_scene_stage (69), test_art_sources (the enemies walk: six ranks, byte copies, the typed slots), test_vfx_assets, test_raid_beats (W3-RAIDVIEW2's `place_boss` on an ImageTexture -> the bob), test_motion, test_icons. The two parse failures of batch 1 (test_screens.gd, test_widgets_kit.gd) are gone — parse is OK at 173 scripts. `tools/lint_motion.sh` run on its own: `MOTION LINT OK`; `tools/lint_no_global_classes.sh`: `LINT OK`. The scratch runner (scratchpad/run_w3en.gd) is deleted; the tree carries only this unit's files (`git status`: SceneStage.gd, test_scene_stage.gd, gen_actors.py, actors.json + 45 strips, enemies.json, gen_boss_anims.lua, game/assets/enemies/anim/ (24 files), art/src/enemies/ (12), the report and the handoff).

## What was left, and why

- Nothing in the unit's own text is unbuilt. Two things sit in the handoff because their files are nobody's this wave: the `gen_actors.py --check` line in build_art.sh (until it lands, the townsfolk strips are gated by this file's own `--check`, which is green) and the test_art_sources walk of `enemies/anim/` (until it lands, the twelve strips are sourced by enemies.json's `strip`/`glow` keys and byte-gated by build_art.sh --check, but not walked by the hygiene test).
- The fixture raid (E5, a wipe) shows the sludge maw alive after `--advance=all`; the toppled boss is shown on the clearing seed (`--fixture=raid:clear`), which W0-SHOT's flag makes one command.
- Q02 stands: every boss is at 1x (`marks.boss.scale` 1, `scale: 1.0` in enemies.json); no integer upscale anywhere.
